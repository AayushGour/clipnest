- 2026-08-10 [T35/T36/T37] Built + validated scripts/{build,sign,notarize,package_dmg}.sh (release pipeline chain) + project.yml Release signing settings (parameterized ${DEVELOPMENT_TEAM}/${CODE_SIGN_IDENTITY} via XcodeGen env expansion) + README Release section. Ran the full chain for real end-to-end in this sandbox: build.sh produces a genuine launchable universal Clipnest.app (both the DEVELOPMENT_TEAM-unset direct-copy path and the -set exportArchive path, the latter failing loudly with Apple's own "No signing certificate" error as expected with no real identity installed); sign.sh signs+verifies+spctl-assesses correctly with ad-hoc identity "-" (real Developer ID cert unavailable in this sandbox — 0 valid codesigning identities, confirmed via `security find-identity -v -p codesigning`); notarize.sh correctly ditto-zips + invokes `xcrun notarytool submit --keychain-profile --wait`, failing loudly with Apple's real "No Keychain password item found" error (no account/profile exists here — genuine external dependency per project-context.md, not a script bug); package_dmg.sh produces a real, correctly-structured Clipnest.dmg (Clipnest.app + Applications symlink) via both the create-dmg primary path (installed via brew, added --skip-jenkins after the Finder-prettifying AppleScript hung with no Automation permission in this shell) and the hdiutil fallback (tested via a restricted PATH), both verified by mounting and listing contents. All 4 scripts: bash -n clean, shellcheck clean (pasted evidence in report to senior-dev). Security self-pass: grepped scripts/ + project.yml for credential-shaped strings — zero hardcoded Apple ID/password/API key/Team ID; identity and profile name are always sourced via arg-or-env (`${1:-${VAR:-}}`), never literal. Gaps stated explicitly, not glossed over: real Developer ID codesign and real notarytool submission could not be run to completion (no Apple Developer account/cert/keychain profile in this sandbox) — scripts are correct and ready, account owner runs the real sign+notarize per notarize.sh's header. Added `build/` to .gitignore (pipeline's own generated output; small logged deviation from the task's file list). Decision recorded as D26 in project-context.md. → senior-dev for review/board update (T38 reviewer next).
- 2026-08-21 [T66] Fixed reviewer finding M-2 (install.sh/update.sh strip quarantine, never verified download integrity). release.yml now publishes `<dmg>.sha256` as its own release asset alongside the .dmg (plain `shasum -a 256` output, dual-use with manual `shasum -c`). install.sh/update.sh fetch+verify SHA-256 before `hdiutil attach` (never after); mismatch aborts loudly with a bordered SECURITY block, nothing mounted; missing checksum asset fails closed via one documented `REQUIRE_CHECKSUM=true` var (opt-out logged as NOT recommended). Also fixed the sibling bug found+verified this session: unauthenticated GitHub API 403s were opaque (`-f`+`-s` hid the body) — now captures real HTTP status via `-w`/`-D`/`-o`, prints the actual reason, and for 403-rate-limit specifically prints the `x-ratelimit-reset` time + honors optional `GITHUB_TOKEN` (never required/logged). Caught and fixed a second real bug while building the rate-limit fix: `${CURL_AUTH_ARGS[@]}` on an empty array throws `unbound variable` under `set -u` on macOS's actual `/bin/bash` 3.2.57 (confirmed live) — would have broken the installer for every GITHUB_TOKEN-unset user; fixed with the bash-3.2-safe `${CURL_AUTH_ARGS[@]+"${CURL_AUTH_ARGS[@]}"}` idiom, verified empirically. Verification (real, pasted in full to task-board.md T66): `bash -n` + `shellcheck 0.11.0` clean on both scripts; live 403 reproduced against the real GitHub API from this machine, script survived and printed the loud rate-limit message (no crash); local fixture (real hdiutil-built .dmg + real sha256 + byte-corrupted copy) + a curl test-double exercising each script's actual verification code (mount-truncated copies, not reimplementations) under real /bin/bash — 5 deterministic scenarios all passed as designed (good→verified+proceeds, corrupted→abort-before-mount, missing-checksum fail-closed→abort, missing-checksum opt-out→warn+proceed, simulated 403→loud reset-time message); repeated the good/corrupted cases against update.sh's logic with a scratch /tmp "installed app," confirmed its version stayed untouched after the corrupted-download abort. Never touched real /Applications (confirmed absent throughout) or GitHub. `AppUpdater.swift` confirmed unchanged — it only shells out to update.sh's own one-liner, inherits the fix with zero Swift changes. Consequence of fail-closed: all releases published before this fix have no checksum asset, so installs/updates against them now abort until a new release is cut or a maintainer runs `shasum -a 256 Clipnest-<version>.dmg > Clipnest-<version>.dmg.sha256 && gh release upload v<version> Clipnest-<version>.dmg.sha256` against the existing release. `git add` on exactly the 3 touched files (scripts/install.sh, scripts/update.sh, .github/workflows/release.yml); diff grepped clean for secrets (only the documented `ghp_xxx` help-text placeholder). No commit. External-I/O change (integrity rule 3 hard trigger) — capped at `status:test`, not self-certified `done`; flagged for reviewer/tester's mandatory security pass.
- 2026-08-21 [T68] Built `scripts/release-local.sh` — the local equivalent of `.github/workflows/release.yml`, since the user is out of Actions minutes. Orchestrates the existing chain (`build.sh` → `sign.sh` → `package_dmg.sh`) rather than reimplementing any of it; deliberately skips `notarize.sh` (the release cert is self-signed, not a Developer ID — Apple's notary service cannot accept it; documented in the header why that's fine for the curl-based install path, which never sets `com.apple.quarantine`). Reproduces every guarantee release.yml makes: signs with the stable "Clipnest Release" identity (never the dev cert) imported into a TEMPORARY keychain (never the login keychain, cleaned up via `trap` including keychain-search-list restore); asserts the identity is usable (test-signs a throwaway Mach-O) before the slow archive, and asserts after signing that the designated requirement has a stable `certificate root = H"..."` and NO `cdhash`, printing it; runs quality gates before building and fails the release on any violation — `swift format lint --strict` (Core + App, matching release.yml exactly, deliberately not linting `ClipnestApp/Tests`'s known pre-existing violation), `swift test`, and an intentional addition beyond release.yml — `xcodebuild test` for the App target, since a local release has no CI safety net (guards the documented "Executed 0 tests" XCTest-harness decoy line by asserting on the real Swift Testing summary line + `** TEST SUCCEEDED **`, not exit code alone); stages the release as a DRAFT, uploads the `.dmg` + its `.sha256` (exact `shasum -a 256`-shaped format T66's install.sh/update.sh already parse), confirms both are actually present on the draft via `gh release view`, and only then publishes; refuses an already-PUBLISHED `v$VERSION` outright (gated under `DRY_RUN==0`, matching the tool's own "--dry-run touches nothing remote" contract, including read calls); preflights a clean working tree + HEAD pushed to `origin/main` (via `git ls-remote`, unconditionally, since it's local-only) so the tag can never land on a commit GitHub doesn't have; never commits/tags/pushes itself (repo convention: the user commits). Options match the user's own `terminal-hub/scripts/release-local.sh` style: `--dry-run`, `--keep-draft`, `--reuse` (skips rebuild if `build/Clipnest-$VERSION.dmg` + `.sha256` already exist — safe by construction since the filename is version-scoped, still re-verifies the reused artifact's signature + checksum before trusting it), `-h/--help`. Found and fixed 3 real portability bugs while building + testing this, all confirmed live against the actual shipped `/bin/bash` 3.2.57 / BSD `sed`/`grep` on this machine (not assumed): (1) `\s` is a GNU-sed/PCRE escape, not POSIX ERE — macOS's `sed -E` silently does NOT treat it as whitespace (confirmed it inconsistently DOES work in macOS's own `grep -E` but not its `sed -E` — same OS, different tools, different behavior), which silently broke the `PRODUCT_BUNDLE_IDENTIFIER` extraction (left the key name concatenated onto the value); fixed by using `[[:space:]]` everywhere instead, in both tools. (2) `\|` alternation inside `\(...\)` (BRE) is also a GNU extension the shipped BSD `sed` doesn't support — silently no-opped the `--help` text's comment-marker stripping; fixed with the portable ERE form `-E 's/^#[[:space:]]?//'`. (3) The keychain search list is shared, global, per-user state, not scoped to a process tree — confirmed empirically that something running concurrently on this machine during the multi-minute archive build can reset it out from under a signing step that assumed it survived untouched (self-caused during testing, by my own two testing procedures both mutating it at once — not release.yml/CI's problem since CI has no other concurrent process — but real enough on a dev's actual machine that it's worth hardening against); added a defensive re-assertion of the search list + identity presence immediately before `sign.sh`, not just once before the build. Verification, real and pasted here (not described): `bash -n` + `shellcheck 0.11.0` — both clean, zero suppressions, after each fix. Since the disposable clone/worktree could not have `origin/main` genuinely contain this brand-new script, all live tests ran from a fully independent, disposable `/tmp` clone (own `.git`, own throwaway commits, own fake local-bare-repo "origin" for the `git ls-remote` check where needed) — never the real repo's working tree, never pushed anywhere, deleted afterward; the real repo's working tree and git state were confirmed untouched throughout (`git status --porcelain` before/after). Already-published guard: real (non-`--dry-run`) invocation against v0.8.0 (genuinely published, confirmed via `gh release view`) → died at the guard with the exact "bump MARKETING_VERSION" message, before any mutating `gh` call — reproduced twice (once before, once after all 3 fixes). Full `--dry-run` end-to-end: reproduced 3 times (once triggering bug #3 by accident via my own concurrent-testing race, twice clean afterward) — preflight → already-published guard correctly SKIPPED under `--dry-run` → both lint gates → `swift test` (183/14, this clone's pinned commit) → `xcodebuild test` (95/10) → cert import + usability assertion → notarization-skip explanation → `build.sh` → `sign.sh` → designated-requirement assertion (`identifier "com.clipnest.app" and certificate root = H"e51f292db8dbecd4536e1b5325f3bc5f283470c5"`, no cdhash, matches the real committed `release-cert/cert.pem`'s fingerprint) → `package_dmg.sh` → checksum in the exact `install.sh`/`update.sh`-parseable format → clean stop, confirmed via `gh release list`/`gh release view` that nothing on GitHub changed. `--reuse` also verified standalone (0.27s, correctly re-mounted + re-checked the just-built dmg's signature instead of rebuilding). Cert/designated-requirement mechanics also proven a second way, fully standalone (no script involved): imported `release-cert/{cert.pem,key.pem}` into a scratch temp keychain, confirmed empirically that `security find-identity -v` reports 0 valid for this self-signed cert (expected — no trust setting, deliberately never added, since `add-trusted-cert` would outlive the temp keychain) while `codesign --sign` still works fine once the keychain is in the search list (no `add-trusted-cert` needed — confirmed this is genuinely how release.yml's own CI step already works, not a new requirement); signed a real minimal Mach-O `.app` and printed its designated requirement, matching the assertion logic exactly. Login keychain and its search list confirmed restored to exactly their pre-test state (`security list-keychains -d user` → login-only; `security find-identity` → "Clipnest Release" absent) after every test, including after the mid-test race. Grepped the full staged diff for secrets — clean (only comment prose explaining why no p12 password needs to be stored). Did NOT create, upload, publish, or delete anything on GitHub, did not push, did not commit in the real repo, did not touch `/Applications`. `git add`ed `scripts/release-local.sh` only; no commit. Not verifiable in this session (stated plainly, not glossed over): the actual `gh release create`/`upload`/`edit --draft=false` path (draft creation, asset upload, publish) — explicitly out of scope per the task ("DO NOT create, upload, publish, or delete anything on GitHub"); `--keep-draft`'s stop-after-upload behavior is code-reviewed but untested for the same reason. Decision recorded as D41 in project-context.md.
- 2026-09-05 [P7-F] Debian packaging + Linux CI/release for the Ubuntu port. Built debian/** (control/changelog/rules/copyright/postinst/postrm/source-format-native/lintian-overrides/source-options), packaging/linux/** (schemas, D-Bus service, desktop file, polkit policy + clipnest-grant-input helper, udev rule, man pages, clipnest-ctl, vendored ONNX Runtime 1.28.1 amd64+arm64 with real SOURCE.md/fetch.sh provenance+checksums), .github/workflows/{ci.yml,release-linux.yml}. Real container verification (swift:6.0-jammy, not swiftlang/swift — that repo only has nightly-* tags on Docker Hub, corrected the task's own image name): full `dpkg-buildpackage -d` succeeded end-to-end producing real clipnest/clipnest-ocr/clipnest-ocr-data .debs (arm64, this host's arch) once the other agents' GTK build landed mid-session; lintian clean except 2 expected vendored-lib-naming warnings; signed PPA source upload (debuild -S -sa) verified with a throwaway GPG key, gpg --verify passed, dpkg-source -x round-tripped cleanly; glib-compile-schemas --dry-run clean on both schemas; shellcheck clean on every script (source tree AND extracted from the built .deb); actionlint clean on both workflows. Real bugs found+fixed via this verification: (1) libsqlite3-dev missing from Build-Depends (Package.swift's own CSQLite comment names it, but that target has no `providers:` array to surface it); (2) dh_dwz can't parse Swift 6's DWARF5 debug info on jammy — overridden as a no-op (debug-symbol-size optimization only, no functional effect); (3) SECURITY: native-format `dpkg-source -b .` swept the real, gitignored `release-cert/` private-key directory into the source tarball in an unpatched test run — never left the ephemeral --rm/read-only-mounted container, nothing committed/pushed, but fixed for real via debian/source/options (tar-ignore mirroring .gitignore) before any real build could hit it; (4) `dch --allow-lower-version` takes a Perl regex, not a shell glob — a bare `*` crashed it. Also corrected clipnest-ctl and the GSettings/D-Bus assumptions against the REAL D-Bus contract (Sources/ClipnestLinuxApp/DBus/ClipnestControlNames.swift) once it landed mid-session — app.clipnest.Control.{TogglePicker,ExpandSnippet,OpenSettings,Ping}, not a guessed org.freedesktop.Application.ActivateAction mapping (which would have silently mis-routed expand-snippet due to a real no-hyphen lowercasing quirk in the fallback path). Known, documented, NOT fixed here (cross-scope, flagged for architect): Sources/COnnxRuntime hard-links libonnxruntime.so.1, so clipnest is built WITHOUT -DCLIPNEST_HAS_ONNXRUNTIME for now to keep clipnest-ocr a genuine Recommends (not a forced Depends) — needs a dlopen-based fix in Sources (mirroring D59's uinput precedent) before OCR can actually ship in the binary; PP-OCRv5 models not vendored (packaging/linux/vendor/ppocr-models/README.md documents exactly what's needed, debian/rules fails loudly if absent rather than shipping wrong/empty data); no apt-installable Swift toolchain on jammy/noble blocks a real Launchpad PPA build today (dpkg-checkbuilddeps reproduced live) — release-linux.yml's dput path is fully wired and gated on a GPG secret regardless, ready once that's resolved. All work already committed to feat/linux-port by the user's own periodic commits during this session (verified via git log, not done by me); staged (git add, no commit) my last few fixes (debian/copyright, debian/source/{options,lintian-overrides}, packaging/linux/vendor/ppocr-models/README.md, .github/workflows/release-linux.yml). status:test — returning to orchestrator, not touching task-board.md per integrity rule 1.
- 2026-09-06 [infra] Fixed silent-failure self-hosted SearXNG (harness-searxng, :8081) behind mcp__web-search__*: root cause = only 5 "general"-category engines enabled by default (brave/duckduckgo/google cse/startpage/wikipedia), all suspended (429/CAPTCHA) within a few burst queries → merged "No results." every time. Fixed in /Users/aayushgour/Desktop/harness/tools/web_search/searxng/settings.template.yml (+ regenerated settings.yml, gitignored): enabled `bing` as an extra general engine, raised outgoing.request_timeout 3.0s→6.0s. Also bumped ensure_searxng.py's health-check timeout (5s→9s, _wait_json 30s→45s) since the raised engine timeout was making the MCP tool's own health probe falsely report "port-busy". Verified via direct curl to the JSON API across repeated bursts: non-empty results every time (previously empty). Residual, unresolved caveat documented in settings.template.yml: bing occasionally returns fully-formed but completely unrelated results (bing.com anti-scraping tar-pit behavior, not a config bug) — flagged for human awareness, not fixable via SearXNG config. Live MCP server subprocess for this session still runs old in-memory code (Python module caching) — fix will apply automatically on next session/reconnect. Ran `python3 -m unittest test_ensure_searxng` (11/11 pass).
- 2026-09-08 [T-LINT2] Made the swift-format lint gate reproducible across toolchains (CI's `swift:6.0-jammy`/`-noble` bundle a `main`-branch swift-format; this Mac's Apple toolchain reports `6.3.0` — verified they disagree on real code, which is how two reviewers reached opposite verdicts on the same SettingsWindow.swift line per T-LINT1). New `scripts/lint.sh`: checks `swift format --version` against the pinned image's known value (`main`); runs natively when it matches (the path CI's own job takes, zero overhead — verified inside both `swift:6.0-jammy` and `swift:6.0-noble` via `docker run ... scripts/lint.sh`, EXIT:0 both); otherwise re-execs itself inside `swift:6.0-jammy` via `docker run` so the verdict always comes from the literal binary CI trusts (verified on this Mac: mismatch detected, image pulled, re-exec'd, EXIT:0). Guard demonstrated firing on deliberate mismatch two ways: `CLIPNEST_LINT_REEXEC=1` forces the "drifted even inside the pinned image" path (exit 1, refuses to silence itself) and a docker-stripped `PATH` forces the "docker unavailable, cannot verify" path (exit 1) — neither silently passes. `.github/workflows/ci.yml`'s lint step now calls `scripts/lint.sh Sources Tests` instead of the raw command, so CI and any developer are certified by literally the same script and binary — single source of truth, no drift to maintain by hand. Rejected: a new SwiftPM swift-format dependency (would add a second third-party dependency against coding-standards.md's one-dependency policy, and still wouldn't match CI — the jammy image bundles a `main`-branch build, not a taggable release, so pinning a release tag would be a THIRD disagreeing version); a root `.swift-format` rules config (tested empirically — adding a default one changes zero verdicts on either toolchain, because the disputed behavior is the pretty-printer's line-wrap algorithm, not a configurable rule — `swift format dump-configuration` has no such knob); documenting a required toolchain version with no enforcement (doesn't work even in principle, since "main" is a moving branch build baked into a Docker tag, not something installable from swift.org). Did not touch `Sources/ClipnestGTK/Interop/.swift-format` (still honoured, still works) or any `.swift` file (`git diff -- '*.swift'` empty). Separately checked (not fixed, per the task's explicit "do not silently change triggers" instruction): confirmed via deepwiki against github/docs that `ci.yml`'s `on: pull_request` trigger WOULD run on a PR from this branch — a `pull_request`-triggered workflow is read from the PR's merge ref, not the base branch, so it does not need to pre-exist on `main` (only 58a6335, on `feat/linux-port`, has ever added it). The reason `gh run list --workflow=ci.yml`/`gh api .../actions/workflows` show nothing is that GitHub's workflow *listing* API only surfaces workflows that exist on the default branch or have already run once — a listing artifact, not a trigger defect — no PR from this branch has ever been opened to fire it. Also found, reporting only: `main` has zero branch protection rules (`gh api repos/.../branches/main/protection` → 404 "Branch not protected"), so even once ci.yml runs on a PR, nothing currently blocks a merge if it's red. Docs updated same-change: docs/architecture.md ss8 gained a `ci.yml`/`scripts/lint.sh` paragraph (previously undocumented entirely), README.md's Contributing section now points at `scripts/lint.sh`. `git add`ed only my 4 files (.github/workflows/ci.yml, scripts/lint.sh, docs/architecture.md, README.md) — left two other agents' in-flight, unstaged Sources/** changes (CGdkX11/shim.h, SettingsWindow.swift, 2 new GTKClipboardCrashNotice files) untouched. No commit. Diff grepped for secrets — clean. Not a security-trigger task (tooling/CI config only, no auth/secrets/PII/user-input/external-I/O surface touched) — no mandatory security pass owed. Returning status:test to whoever holds the board pen (spawned, single-writer rule) for T-LINT2 (currently `status:todo [architect]` on the board).
- 2026-09-08 [T-P10C/gnome-runtime] Answered the "can GNOME Shell run in Docker" question for real, correcting an earlier `docker run`-based negative. Privileged systemd-as-PID1 container (`--privileged --cgroupns=host -v /sys/fs/cgroup:/sys/fs/cgroup:rw`) gets a genuine `systemd-logind` (confirmed: `org.freedesktop.login1` on the system bus, real `seat0`); a plain `docker exec` still crashes gnome-shell (`loginManager.js`'s `getCurrentSessionProxy()`, `this._userProxy.Display is null` — the exact "needs logind and a real seat" failure, reproduced deliberately) until the user's session is registered via `systemd-run --uid=1000 -p PAMName=login -p 'Environment=XDG_SESSION_TYPE=x11' -p 'Environment=XDG_SESSION_CLASS=user' -p 'Environment=XDG_SESSION_DESKTOP=gnome'` — which makes logind promote it to the user's active/display session. From there real GNOME Shell 42.9 (Ubuntu 22.04 jammy, matches the `legacy` extension bundle) boots and renders for real (screenshotted: Activities/top-bar/dash). Installed the built `.deb` + the `legacy` Shell extension (manual `cp -r` into `~/.local/share/gnome-shell/extensions/` — `gnome-extensions install --force <dir>` fails on the real CLI, it wants a `.shell-extension.zip`, not a directory) — loads with zero errors (`GetExtensionInfo` state:ENABLED, `GetExtensionErrors` empty), exports `app.clipnest.ShellHelper`, advertises all 6 capabilities. All 11 D-Bus methods exercised for real (PyGObject probe scripts claiming `app.clipnest.Clipnest` themselves, since the extension's `_checkSender` requires it) and confirmed correct: GetPointer/GetMonitorWorkArea/GetClipboardMimeTypes/ReadClipboard/SetClipboard all returned real, verified-correct data (SetClipboard round-tripped through `xclip -o`); SendKeyChord real XTEST synthesis (true); PlaceWindow/FocusAndSendKeyChord degrade gracefully on bogus input (false/"target-lost"), no crash; GetFocusedApp returned `{}` (plausible, no window had real focus in this session, not confirmed either way). Real `<Alt><Super>v` via `xdotool` made Mutter's own keybinding grab fire and the extension emit a real `ShortcutActivated` signal with the exact live pointer coords (confirmed via `gdbus monitor`). TWO real, reproducible bugs found in the app's own D-Bus client (`Sources/ClipnestLinuxAppKit/`, not touched, not in the extension): (1) `HotkeyBackendResolver` never selects `.shellExtensionKeybinding` on a real first launch (5/5 runs) — `probeLiveDispatch()` races the extension's async `Gio.bus_watch_name` ownership resolution with no retry; proven by making the identical call succeed 100% of the time after an artificial 1.5s wait. (2) `ShellHelperClient.startWatching()` never adds a match rule for `ShortcutActivated`/`ClipboardChanged`/`CapabilitiesChanged` at all (only for `NameOwnerChanged` of the bus name) — `readLoop` has live parsing code for these signals that can never run; confirmed live: an independent `gdbus monitor` saw the real signal, the actual running `clipnest` process's log and picker window (`xwininfo`: `IsUnMapped`) showed zero evidence of it. Bug (2) means the Shell-extension hotkey path cannot deliver a hotkey to the app AT ALL even with (1) fixed — a second, more fundamental defect. Both reported with full reproduction steps, not fixed (out of devops scope; Sources/ untouched). Reusable environment + probe scripts saved to `packaging/linux/gnome-shell-test/` (Dockerfile, session-script.sh, README.md with full findings + repro steps, 4 PyGObject probe scripts) for the team to reuse. `git add`ed only the new `packaging/linux/gnome-shell-test/` files; no Sources/extension changes; diff grepped clean for secrets. No commit. Decision recorded as D90 in project-context.md; task-board T-P10C evidence updated, 2 new findings need a `senior-dev`/architect task line.
- 2026-09-08 [T-P10I/T-P10J verification] Proved out both fixes (commit 700c38b) against the real GNOME Shell 42.9 harness from the prior investigation — reused the coordinator's left-running `clipnest-gnome-hk` container (systemd+logind+Xvfb, extension already `cp -r`'d into place). **T-P10I (resolver race) — PASS, 15/15 trials.** Fresh `clipnest` launches with the extension enabled: `hotkey backend resolved: shellExtensionKeybinding` every time (10 trials via log-grep, 5 more with wall-clock timing via a small Python harness: 0.121s/0.143s/0.165s/0.163s/0.266s from process start to the resolved log line — a small bounded retry window, not a hang). With the extension `gnome-extensions disable`d (confirmed off the bus first): 5/5 trials resolved `gsettingsFloor`, 0.122-0.203s — same order of magnitude as the extension-present case, so the retry logic does not meaningfully slow the common "no extension" path either. **T-P10J (missing AddMatch) — PASS, real end-to-end.** Clean single-action repro: killed clipnest, moved the pointer to (300,600), relaunched (resolved `shellExtensionKeybinding`), screenshotted the plain desktop (no picker, confirmed via `xwininfo`: picker `IsUnMapped` at its default 0,0), sent ONE real `<Alt><Super>v` via `xdotool keydown/key/keyup` (not a synthetic D-Bus call), then re-checked: picker window now `IsViewable` at `560x420+300+600` — exactly the cursor position — and a second screenshot, read with the Read tool, shows the actual rendered Clipnest picker (search bar, type filter icons, History/Pinned/Snippets tabs, "No clipboard history yet") sitting at that exact position on the plain desktop. This is the first time in the port's history the real global hotkey has been shown opening the picker end-to-end through a real Mutter. One earlier attempt in this same session showed the mapped+positioned window NOT visibly appearing until `Escape` was pressed — turned out to be GNOME Shell's Activities Overview (the container's idle default state, not something the fix touches) sitting on top as an always-above Shell UI layer; irrelevant once verified from a plain-desktop starting state, not a defect in either fix. Two things noticed but NOT chased (out of scope for this verification, flagged for whoever owns picker UX next): (1) `PlaceWindow` does not clamp to the monitor's work area — the picker's height (420px) extends past the bottom edge when placed at y=600 on a 900px-tall screen; (2) a second hotkey press while the picker was still open (from the Overview-obscured test) did not toggle it closed (stayed `IsViewable`) — not reproduced from a clean state, so not confirmed as a real bug, just recorded honestly rather than glossed over. Root-caused the coordinator's stuck point (extension enabled per `gsettings` but never appeared to Shell/never got `app.clipnest.ShellHelper` on the bus): GNOME Shell only scans for a brand-new (never-before-seen) extension UUID during its OWN process startup, not via the live directory-watch path that handles ENABLING/DISABLING an already-known extension — `gnome-extensions enable` alone is not enough the very first time; the Shell process (`systemctl restart <session>.service`) must be restarted once after the extension directory first appears on disk. This was already one line in the README's install sequence but not called out as a distinct troubleshooting step, which is almost certainly why the coordinator missed it — added an explicit, symptom-matched "Troubleshooting" section to `packaging/linux/gnome-shell-test/README.md` so the next person recognizes the exact symptom (`enabled-extensions` correct, `GetExtensionInfo` empty, no journal error, `ShellHelper` absent from the bus) and the fix immediately. Two screenshots saved as evidence: scratchpad `clean-before.png` (empty desktop) / `clean-after.png` (real picker, cursor-anchored) — not committed to the repo (throwaway verification artifacts, not durable infra); the reusable Dockerfile/README/probe-scripts from the original investigation are what's kept. `git add`ed only `packaging/linux/gnome-shell-test/README.md`, `.claude/project-context.md`, `.claude/task-board.md`, `.claude/logs/devops.md`; no Sources/extension changes (this was verification only); diff grepped clean for secrets. No commit. Task board: T-P10I and T-P10J moved to `status:done` with this evidence (real-Mutter proof matches the bar this repo's other agents use for self-verified `done` without a separate tester hop — e.g. T-P10G, T-RT2).
- 2026-09-06 [T71] Jekyll scaffold for GitHub Pages (worktree clipnest-seo, branch feat/seo-aeo-macos): `docs/_config.yml` (url/baseurl/theme minima/plugins jekyll-sitemap+jekyll-seo-tag, no jekyll-feed, mandated 5-item exclude list intact), `docs/Gemfile` (`github-pages` gem, jekyll_plugins group, + `webrick`), `docs/robots.txt` (named-bot welcome list + wildcard catch-all + sitemap line + plan-§0 caveat comment), `docs/_layouts/default.html` (minima `default.html` override, full custom `<head>` incl. `{% seo %}` + `{% include seo-jsonld.html %}` + `{% include og-tags.html %}`, everything via `relative_url`), `docs/_includes/{seo-jsonld,og-tags}.html` (empty HTML-comment scaffolds for T76/T77), `docs/.gitignore` (`_site/`, `.jekyll-cache/`, `Gemfile.lock`, `.bundle/`, `vendor/`). System Ruby was 2.6.10/Bundler 1.17.2 (too old for the `github-pages` gem) — installed `ruby@3.3` via Homebrew (local dev toolchain only, no prod/infra change) and used it for all verification. Real build run, not assumed: `bundle install` → 97 gems incl. `github-pages 232`/`minima 2.5.1`; `bundle exec jekyll build --trace` → clean (also re-ran with `JEKYLL_ENV=production`); `bundle exec jekyll doctor` → "Everything looks fine." Found + fixed 2 real defects during verification, both flagged to architect: (1) first bundle attempt vendored gems inside `docs/vendor/` which Jekyll then tried to build as site content (crashed on a gem's own `.erb` template) — fixed by installing to the global gem path instead, kept `vendor/`/`.gitignore`'d anyway as defense-in-depth; (2) discovered `docs/superpowers/` (tracked, pre-existing internal SDD plans/specs — task-board.md's own "Full task detail" pointer target) is NOT in the plan's mandated exclude list and was rendering to public HTML + appearing in `sitemap.xml` in a real build — added `superpowers/` plus `Gemfile`/`Gemfile.lock`/`vendor/`/`.bundle/` as additional exclude entries (mandated 5 left untouched/verbatim), commented inline as a devops addition for architect to confirm. Confirmed via built output: every internal link/canonical/asset path in the rendered `usage.html` correctly carries the `/clipnest/` baseurl through `relative_url`/`absolute_url` (no literal paths), zero Linux/GTK/Wayland strings anywhere. Noted, not fixed (outside owned files): `jekyll-feed` is bundled inside the `github-pages` meta-gem and auto-activates via Bundler's `:jekyll_plugins` group regardless of the `plugins:` allowlist in `_config.yml` — same on the real Pages build server, which also just runs the meta-gem — so an inert, unlinked `/feed.xml` (zero entries, not in nav, not referenced by our layout) will exist on the live site; harmless but flagged for the record since the plan says "skip jekyll-feed." `git add` on exactly the 7 owned files; grepped diff for secret-shaped strings — clean. No commit, no Pages settings touched, no board edit (spawned — returning status to architect).
- 2026-09-13 [linux-migration dist rebuild + release-linux.yml tarball CI] Rebuilt both checked-in Linux tarballs at current HEAD (d1e9fc9, 4 commits past the stale 84d40e6 build: ba21a39/e0eac16/4314186/d1e9fc9) using the prepared incremental containers (clipnest-build-0.9.2:arm64's clipnest-fix-build, clipnest-amd64-done:1) — amd64 hit the documented bcm.cc slow-compile point under QEMU but progressed the whole way (single active clang/swift-frontend process, real CPU, never the reported two-idle-process deadlock), no DEB_BUILD_OPTIONS workaround needed this run. New packaging/linux/dist/build-tarball.sh is the ONE recipe assembling clipnest-$VERSION-linux-$ARCH.tar.gz from built .debs + this dir's install.sh/README.md + a generated SHA256SUMS — used for both the local rebuild and the new release-linux.yml `build-tarball` CI job (one recipe, no drift). Verified per spec: dpkg-deb --info both archs 0.9.2/correct Architecture; ELF e_machine read directly (arm64 b700, amd64 3e00 — QEMU did not silently mislabel); SHA256SUMS sha256sum -c clean on both; clean-container non-root-sudo install.sh run to completion on ubuntu:24.04 for both archs (clipnest --version → 0.9.2, all 3 packages `ii`); architecture-guard mismatch tested BOTH directions (arm64-on-amd64 in Docker, amd64-on-arm64 on the real arm64 UTM VM) — both correctly refused with exit 1. Tarballs built via GNU tar inside the Linux containers, so no macOS xattr contamination at all (not just suppressed via flags). Mid-task the coordinator redirected arm64 work to the UTM VM to relieve Docker/disk strain; arm64 Docker work was already complete+verified by then and was kept per instruction, only the amd64 leg (QEMU-only, unavoidable in Docker) continued there. Added release-linux.yml's `build-tarball` job (matrix amd64→ubuntu-latest, arm64→ubuntu-24.04-arm, both `container: swift:6.0-jammy`) plus wired its artifacts into `publish`; did not touch existing check/build/PPA logic. Verified arm64 runner choice against ground truth, not assumption: fetched actions/runner-images' live README (GA, no beta tag) and github/docs' supported-github-runners.md ("free and unlimited on public repositories") directly via `gh api`, and confirmed swift:6.0-jammy is a genuine multi-arch manifest (`docker buildx imagetools inspect` shows a real linux/arm64/v8 manifest) — deepwiki's summary of the same repo was stale/wrong on this exact question, cross-checked and overridden. Verified the merge-to-main activation question against github/docs directly: workflow_run and workflow_dispatch both require the *listening* workflow file to already exist on the default branch (workflow_dispatch additionally needs it there just to be discoverable via UI/API); plain push does not have that restriction. release-linux.yml is entirely new (verified: absent from main today), so before merge it cannot be triggered at all (not even via manual dispatch); merging linux-migration activates workflow_run automatically with zero further action, since the "Release" workflow (release.yml) it listens for is already on main. actionlint clean + `python3 -c yaml.safe_load` clean on the final YAML; shellcheck + bash -n clean on build-tarball.sh. Updated dist/README.md to point at the CI-produced Release going forward and to document the ~170 MB permanent history cost, per DONE gate item 5. Docker cleanup: removed every clipnest-* container I created (clipnest-amd64-rebuild, clipnest-arm64-install-test, clipnest-mismatch-test, clipnest-amd64-install-test2); did not remove clipnest-fix-build (pre-existing, disappeared on its own mid-session, not by my hand) or any of the ~30 other stale clipnest-* image tags already in `docker images` (built by other sessions, none created by me) — flagged those to the coordinator/user for pruning rather than touching them. `git add` on exactly the 5 owned files (release-linux.yml, dist/README.md, both tarballs, build-tarball.sh); grepped diff + tarball contents for secret-shaped strings — clean. No commit, no board edit (spawned — returning status to the orchestrator).
- 2026-09-13 [Linux test suite, commit 5b91d3e, UTM VM] First real execution of the Linux test targets — macOS structurally can't build `#if os(Linux)` targets and Docker's daemon was down, so ran on the arm64 UTM VM (Swift 6.3.3). Copied `git archive HEAD` (5b91d3e, `linux-migration`, dist/.git excluded, 126MB) to `~/clipnest-5b91d3e` on the VM. `swift build`: PASS ("Build complete!"). `swift test`: **1307 tests in 170 suites, 4 issues, all 4 in `GTKGlobalHotkeyAcceleratorTests.failsSafeWhenSchemaNotInstalled`** (2 keys × 2 assertions) — the pre-documented, VM-specific artifact (this VM has a real .deb installed so the GSettings schema IS present, contradicting the test's "no .deb ever installed" premise); confirmed by reading the test/production code, not assumed. Every other suite green, including `LinuxClipboardSelectionReplacerTests` (ran `fallbackReportsNoSelectionRatherThanAFalsePositiveWhenNothingWasCopied` in its real target for the first time ever; negative-controlled live by deleting the `sentinelReadBack` ternary branch in `LinuxClipboardSelectionReplacer.swift` — assertion flipped exactly as predicted, `.writeUnconfirmed` not `.noSelection` — then reverted and byte-diff-verified clean against the original archive), `AppShellHelperProtocolTests`, `LinuxPasteboardTests`. 1300-test baseline was an unconfirmed claim; real number is 1307. Fixed a real sysroot bug in the prior session's `~/build-sysroot` (dpkg-deb-extracted `libgtk-4-dev`/`libgraphene-1.0-dev`/`libvulkan-dev`, needed because this Ubuntu 24.04 image's own repos lack a compatible gtk4): its `.pc` files declared `prefix=/usr` (absolute), so pkg-config emitted real, nonexistent `-I/usr/include/gtk-4.0` paths instead of sysroot-relative ones. `PKG_CONFIG_SYSROOT_DIR` looked like the fix but would have wrongly sysroot-rewritten the ~13 other deps (pango/glib/cairo/harfbuzz/etc.) this VM already has as real, correctly installed system packages — used instead: edited `prefix=/usr` → `prefix=$SYSROOT/usr` directly in the sysroot's 9 `.pc` files (gtk4*/graphene*/vulkan), leaving `PKG_CONFIG_PATH` listing both sysroot and real paths and no `PKG_CONFIG_SYSROOT_DIR`. No new `.deb`s needed (X11/sqlite3/dbus headers and libs were already real system packages; `COnnxRuntime` is a self-contained dlopen shim, no headers needed). Full logs at `~/clipnest-test/run-5b91d3e/{build,test-final}.log` on the VM. No repo files changed (verification only, temporary negative-control edit reverted and diffed clean); nothing committed; not spawned — reporting directly to the user. D103 recorded in project-context.md.
- 2026-09-14 [0.9.2 -> 0.9.3 version bump + dist rebuild, HEAD c0cad01] Bumped to 0.9.3 since c0cad01 (IBus snippet-expansion Wayland fix) landed since the 0.9.2 tarballs were built (9be8d20). Grepped the whole tree for "0.9.2" and updated the three real version-of-record spots plus the one CI reads: `debian/changelog` (new 0.9.3 entry, user-facing changelog prose on the IBus/Mutter-modifier-tracking fix + the 8/8-vs-0/8 verification numbers, in the style of existing entries), `ClipnestApp/project.yml`'s `MARKETING_VERSION` (confirmed via release-linux.yml's `check` job and `LinuxAppEnvironment.swift`'s own doc comment that this is the single source `dpkg-buildpackage`'s changelog version and `build-tarball.sh`'s `<version>` arg both have to stay in lockstep with), `Sources/ClipnestLinuxAppKit/App/LinuxAppEnvironment.swift`'s `installedVersion` (the literal `clipnest --version` reads). Left `packaging/linux/dist/build-tarball.sh`'s `# e.g. 0.9.2` comment and `TESTING-LINUX.md`'s two "(0.9.2)"/"As of 0.9.2" historical mentions (both describe when a behavior was introduced, still true) untouched per the task's own instruction not to rewrite historical references — only bumped TESTING-LINUX.md's title and its install-flow tarball filenames, which are the currently-installable-version claims. `packaging/linux/dist/README.md` has no version strings (generic install doc, already current re: IBus/GTK/auto-paste); root `README.md` and `docs/` have none either — grepped and confirmed, no changes needed there.

  Built both `.deb` sets for real in `swift:6.0-jammy` containers (same recipe as release-linux.yml's `build` job: `dpkg-buildpackage -us -uc -b -d` after installing the same 8 build-deps), amd64 via Rosetta (`--platform linux/amd64`) and arm64 natively (`--platform linux/arm64`) in parallel — both exited 0, both produced `clipnest`/`clipnest-ocr`/`clipnest-ocr-data` at version 0.9.3. Assembled both tarballs with `packaging/linux/dist/build-tarball.sh` run *inside* the same `swift:6.0-jammy` containers (uname -s == Linux, so it took the plain GNU-tar branch, never macOS bsdtar) — confirmed by re-extracting both inside a fresh `ubuntu:24.04` container with zero xattr warnings and `sha256sum -c SHA256SUMS` → all 5 files OK on both.

  All 4 required verification categories run for real, not assumed:
  1. `dpkg-deb --info` on all 3 packages, both arches (via `ubuntu:24.04` container, since macOS has no `dpkg-deb`): amd64 → Version 0.9.3, Architecture amd64/amd64/all; arm64 → Version 0.9.3, Architecture arm64/arm64/all.
  2. ELF `e_machine` read directly via `od -An -tx1 -j 18 -N 2` on the extracted `usr/bin/clipnest` (not `file`/trusted labels): amd64 → `3e 00` (x86-64), arm64 → `b7 00` (AArch64) — exactly the expected, distinct values; QEMU did not mislabel either.
  3. Clean-container install test, non-root sudo user, `ubuntu:24.04`, both arches: `install.sh` ran to completion each time ("Setting up clipnest (0.9.3)" in apt's own output), `clipnest --version` → `0.9.3` on both.
  4. Architecture guard tested BOTH directions in fresh containers: arm64 tarball's `install.sh` on an amd64 container → "Architecture mismatch: these packages are arm64, this machine is amd64." exit 1; amd64 tarball's `install.sh` on an arm64 container → the mirrored message, exit 1.

  Replaced `dist/clipnest-0.9.2-linux-{amd64,arm64}.tar.gz` with the 0.9.3 pair (git detected as renames, ~87.7 MB combined — close to the prior pair's size); confirmed `.gitignore` still does not exclude `dist/` and the 0.9.2 pair was genuinely git-tracked (precedent from 9be8d20 holds). Updated `dist/README.md`'s version references and its own historical-cost accounting (now ~255-260 MB across three pairs' worth of git history, "fourth pair" instead of "third"). Did not touch `release-cert/` (doesn't exist on this machine), did not run `docker builder prune` (or any prune) — QEMU/binfmt platforms confirmed still listed in `docker buildx ls` (`linux/amd64`, `linux/arm64`) after the whole run. Grepped every staged text diff for secret-shaped strings — clean (only version literals + changelog prose + the pre-existing, already-public maintainer email already used identically in every prior changelog entry). `git add`ed exactly 7 paths: `debian/changelog`, `ClipnestApp/project.yml`, `Sources/ClipnestLinuxAppKit/App/LinuxAppEnvironment.swift`, `TESTING-LINUX.md`, `dist/README.md`, and the two renamed tarballs. No commit, no board/project-context edit (not spawned this time but the user's own instructions for this task said not to touch either file — reporting decisions back in the return message instead). One real gap I could not verify: I did not re-run `swift test` / the full CI suite against the version-bumped source before building the release `.deb`s (out of this task's stated scope, which was the version bump + tarball rebuild specifically, not a fresh test pass) — flagging rather than assuming green.
