- 2026-08-10 [T35/T36/T37] Built + validated scripts/{build,sign,notarize,package_dmg}.sh (release pipeline chain) + project.yml Release signing settings (parameterized ${DEVELOPMENT_TEAM}/${CODE_SIGN_IDENTITY} via XcodeGen env expansion) + README Release section. Ran the full chain for real end-to-end in this sandbox: build.sh produces a genuine launchable universal Clipnest.app (both the DEVELOPMENT_TEAM-unset direct-copy path and the -set exportArchive path, the latter failing loudly with Apple's own "No signing certificate" error as expected with no real identity installed); sign.sh signs+verifies+spctl-assesses correctly with ad-hoc identity "-" (real Developer ID cert unavailable in this sandbox — 0 valid codesigning identities, confirmed via `security find-identity -v -p codesigning`); notarize.sh correctly ditto-zips + invokes `xcrun notarytool submit --keychain-profile --wait`, failing loudly with Apple's real "No Keychain password item found" error (no account/profile exists here — genuine external dependency per project-context.md, not a script bug); package_dmg.sh produces a real, correctly-structured Clipnest.dmg (Clipnest.app + Applications symlink) via both the create-dmg primary path (installed via brew, added --skip-jenkins after the Finder-prettifying AppleScript hung with no Automation permission in this shell) and the hdiutil fallback (tested via a restricted PATH), both verified by mounting and listing contents. All 4 scripts: bash -n clean, shellcheck clean (pasted evidence in report to senior-dev). Security self-pass: grepped scripts/ + project.yml for credential-shaped strings — zero hardcoded Apple ID/password/API key/Team ID; identity and profile name are always sourced via arg-or-env (`${1:-${VAR:-}}`), never literal. Gaps stated explicitly, not glossed over: real Developer ID codesign and real notarytool submission could not be run to completion (no Apple Developer account/cert/keychain profile in this sandbox) — scripts are correct and ready, account owner runs the real sign+notarize per notarize.sh's header. Added `build/` to .gitignore (pipeline's own generated output; small logged deviation from the task's file list). Decision recorded as D26 in project-context.md. → senior-dev for review/board update (T38 reviewer next).
- 2026-08-21 [T66] Fixed reviewer finding M-2 (install.sh/update.sh strip quarantine, never verified download integrity). release.yml now publishes `<dmg>.sha256` as its own release asset alongside the .dmg (plain `shasum -a 256` output, dual-use with manual `shasum -c`). install.sh/update.sh fetch+verify SHA-256 before `hdiutil attach` (never after); mismatch aborts loudly with a bordered SECURITY block, nothing mounted; missing checksum asset fails closed via one documented `REQUIRE_CHECKSUM=true` var (opt-out logged as NOT recommended). Also fixed the sibling bug found+verified this session: unauthenticated GitHub API 403s were opaque (`-f`+`-s` hid the body) — now captures real HTTP status via `-w`/`-D`/`-o`, prints the actual reason, and for 403-rate-limit specifically prints the `x-ratelimit-reset` time + honors optional `GITHUB_TOKEN` (never required/logged). Caught and fixed a second real bug while building the rate-limit fix: `${CURL_AUTH_ARGS[@]}` on an empty array throws `unbound variable` under `set -u` on macOS's actual `/bin/bash` 3.2.57 (confirmed live) — would have broken the installer for every GITHUB_TOKEN-unset user; fixed with the bash-3.2-safe `${CURL_AUTH_ARGS[@]+"${CURL_AUTH_ARGS[@]}"}` idiom, verified empirically. Verification (real, pasted in full to task-board.md T66): `bash -n` + `shellcheck 0.11.0` clean on both scripts; live 403 reproduced against the real GitHub API from this machine, script survived and printed the loud rate-limit message (no crash); local fixture (real hdiutil-built .dmg + real sha256 + byte-corrupted copy) + a curl test-double exercising each script's actual verification code (mount-truncated copies, not reimplementations) under real /bin/bash — 5 deterministic scenarios all passed as designed (good→verified+proceeds, corrupted→abort-before-mount, missing-checksum fail-closed→abort, missing-checksum opt-out→warn+proceed, simulated 403→loud reset-time message); repeated the good/corrupted cases against update.sh's logic with a scratch /tmp "installed app," confirmed its version stayed untouched after the corrupted-download abort. Never touched real /Applications (confirmed absent throughout) or GitHub. `AppUpdater.swift` confirmed unchanged — it only shells out to update.sh's own one-liner, inherits the fix with zero Swift changes. Consequence of fail-closed: all releases published before this fix have no checksum asset, so installs/updates against them now abort until a new release is cut or a maintainer runs `shasum -a 256 Clipnest-<version>.dmg > Clipnest-<version>.dmg.sha256 && gh release upload v<version> Clipnest-<version>.dmg.sha256` against the existing release. `git add` on exactly the 3 touched files (scripts/install.sh, scripts/update.sh, .github/workflows/release.yml); diff grepped clean for secrets (only the documented `ghp_xxx` help-text placeholder). No commit. External-I/O change (integrity rule 3 hard trigger) — capped at `status:test`, not self-certified `done`; flagged for reviewer/tester's mandatory security pass.
- 2026-08-21 [T68] Built `scripts/release-local.sh` — the local equivalent of `.github/workflows/release.yml`, since the user is out of Actions minutes. Orchestrates the existing chain (`build.sh` → `sign.sh` → `package_dmg.sh`) rather than reimplementing any of it; deliberately skips `notarize.sh` (the release cert is self-signed, not a Developer ID — Apple's notary service cannot accept it; documented in the header why that's fine for the curl-based install path, which never sets `com.apple.quarantine`). Reproduces every guarantee release.yml makes: signs with the stable "Clipnest Release" identity (never the dev cert) imported into a TEMPORARY keychain (never the login keychain, cleaned up via `trap` including keychain-search-list restore); asserts the identity is usable (test-signs a throwaway Mach-O) before the slow archive, and asserts after signing that the designated requirement has a stable `certificate root = H"..."` and NO `cdhash`, printing it; runs quality gates before building and fails the release on any violation — `swift format lint --strict` (Core + App, matching release.yml exactly, deliberately not linting `ClipnestApp/Tests`'s known pre-existing violation), `swift test`, and an intentional addition beyond release.yml — `xcodebuild test` for the App target, since a local release has no CI safety net (guards the documented "Executed 0 tests" XCTest-harness decoy line by asserting on the real Swift Testing summary line + `** TEST SUCCEEDED **`, not exit code alone); stages the release as a DRAFT, uploads the `.dmg` + its `.sha256` (exact `shasum -a 256`-shaped format T66's install.sh/update.sh already parse), confirms both are actually present on the draft via `gh release view`, and only then publishes; refuses an already-PUBLISHED `v$VERSION` outright (gated under `DRY_RUN==0`, matching the tool's own "--dry-run touches nothing remote" contract, including read calls); preflights a clean working tree + HEAD pushed to `origin/main` (via `git ls-remote`, unconditionally, since it's local-only) so the tag can never land on a commit GitHub doesn't have; never commits/tags/pushes itself (repo convention: the user commits). Options match the user's own `terminal-hub/scripts/release-local.sh` style: `--dry-run`, `--keep-draft`, `--reuse` (skips rebuild if `build/Clipnest-$VERSION.dmg` + `.sha256` already exist — safe by construction since the filename is version-scoped, still re-verifies the reused artifact's signature + checksum before trusting it), `-h/--help`. Found and fixed 3 real portability bugs while building + testing this, all confirmed live against the actual shipped `/bin/bash` 3.2.57 / BSD `sed`/`grep` on this machine (not assumed): (1) `\s` is a GNU-sed/PCRE escape, not POSIX ERE — macOS's `sed -E` silently does NOT treat it as whitespace (confirmed it inconsistently DOES work in macOS's own `grep -E` but not its `sed -E` — same OS, different tools, different behavior), which silently broke the `PRODUCT_BUNDLE_IDENTIFIER` extraction (left the key name concatenated onto the value); fixed by using `[[:space:]]` everywhere instead, in both tools. (2) `\|` alternation inside `\(...\)` (BRE) is also a GNU extension the shipped BSD `sed` doesn't support — silently no-opped the `--help` text's comment-marker stripping; fixed with the portable ERE form `-E 's/^#[[:space:]]?//'`. (3) The keychain search list is shared, global, per-user state, not scoped to a process tree — confirmed empirically that something running concurrently on this machine during the multi-minute archive build can reset it out from under a signing step that assumed it survived untouched (self-caused during testing, by my own two testing procedures both mutating it at once — not release.yml/CI's problem since CI has no other concurrent process — but real enough on a dev's actual machine that it's worth hardening against); added a defensive re-assertion of the search list + identity presence immediately before `sign.sh`, not just once before the build. Verification, real and pasted here (not described): `bash -n` + `shellcheck 0.11.0` — both clean, zero suppressions, after each fix. Since the disposable clone/worktree could not have `origin/main` genuinely contain this brand-new script, all live tests ran from a fully independent, disposable `/tmp` clone (own `.git`, own throwaway commits, own fake local-bare-repo "origin" for the `git ls-remote` check where needed) — never the real repo's working tree, never pushed anywhere, deleted afterward; the real repo's working tree and git state were confirmed untouched throughout (`git status --porcelain` before/after). Already-published guard: real (non-`--dry-run`) invocation against v0.8.0 (genuinely published, confirmed via `gh release view`) → died at the guard with the exact "bump MARKETING_VERSION" message, before any mutating `gh` call — reproduced twice (once before, once after all 3 fixes). Full `--dry-run` end-to-end: reproduced 3 times (once triggering bug #3 by accident via my own concurrent-testing race, twice clean afterward) — preflight → already-published guard correctly SKIPPED under `--dry-run` → both lint gates → `swift test` (183/14, this clone's pinned commit) → `xcodebuild test` (95/10) → cert import + usability assertion → notarization-skip explanation → `build.sh` → `sign.sh` → designated-requirement assertion (`identifier "com.clipnest.app" and certificate root = H"e51f292db8dbecd4536e1b5325f3bc5f283470c5"`, no cdhash, matches the real committed `release-cert/cert.pem`'s fingerprint) → `package_dmg.sh` → checksum in the exact `install.sh`/`update.sh`-parseable format → clean stop, confirmed via `gh release list`/`gh release view` that nothing on GitHub changed. `--reuse` also verified standalone (0.27s, correctly re-mounted + re-checked the just-built dmg's signature instead of rebuilding). Cert/designated-requirement mechanics also proven a second way, fully standalone (no script involved): imported `release-cert/{cert.pem,key.pem}` into a scratch temp keychain, confirmed empirically that `security find-identity -v` reports 0 valid for this self-signed cert (expected — no trust setting, deliberately never added, since `add-trusted-cert` would outlive the temp keychain) while `codesign --sign` still works fine once the keychain is in the search list (no `add-trusted-cert` needed — confirmed this is genuinely how release.yml's own CI step already works, not a new requirement); signed a real minimal Mach-O `.app` and printed its designated requirement, matching the assertion logic exactly. Login keychain and its search list confirmed restored to exactly their pre-test state (`security list-keychains -d user` → login-only; `security find-identity` → "Clipnest Release" absent) after every test, including after the mid-test race. Grepped the full staged diff for secrets — clean (only comment prose explaining why no p12 password needs to be stored). Did NOT create, upload, publish, or delete anything on GitHub, did not push, did not commit in the real repo, did not touch `/Applications`. `git add`ed `scripts/release-local.sh` only; no commit. Not verifiable in this session (stated plainly, not glossed over): the actual `gh release create`/`upload`/`edit --draft=false` path (draft creation, asset upload, publish) — explicitly out of scope per the task ("DO NOT create, upload, publish, or delete anything on GitHub"); `--keep-draft`'s stop-after-upload behavior is code-reviewed but untested for the same reason. Decision recorded as D41 in project-context.md.
- 2026-09-05 [P7-F] Debian packaging + Linux CI/release for the Ubuntu port. Built debian/** (control/changelog/rules/copyright/postinst/postrm/source-format-native/lintian-overrides/source-options), packaging/linux/** (schemas, D-Bus service, desktop file, polkit policy + clipnest-grant-input helper, udev rule, man pages, clipnest-ctl, vendored ONNX Runtime 1.28.1 amd64+arm64 with real SOURCE.md/fetch.sh provenance+checksums), .github/workflows/{ci.yml,release-linux.yml}. Real container verification (swift:6.0-jammy, not swiftlang/swift — that repo only has nightly-* tags on Docker Hub, corrected the task's own image name): full `dpkg-buildpackage -d` succeeded end-to-end producing real clipnest/clipnest-ocr/clipnest-ocr-data .debs (arm64, this host's arch) once the other agents' GTK build landed mid-session; lintian clean except 2 expected vendored-lib-naming warnings; signed PPA source upload (debuild -S -sa) verified with a throwaway GPG key, gpg --verify passed, dpkg-source -x round-tripped cleanly; glib-compile-schemas --dry-run clean on both schemas; shellcheck clean on every script (source tree AND extracted from the built .deb); actionlint clean on both workflows. Real bugs found+fixed via this verification: (1) libsqlite3-dev missing from Build-Depends (Package.swift's own CSQLite comment names it, but that target has no `providers:` array to surface it); (2) dh_dwz can't parse Swift 6's DWARF5 debug info on jammy — overridden as a no-op (debug-symbol-size optimization only, no functional effect); (3) SECURITY: native-format `dpkg-source -b .` swept the real, gitignored `release-cert/` private-key directory into the source tarball in an unpatched test run — never left the ephemeral --rm/read-only-mounted container, nothing committed/pushed, but fixed for real via debian/source/options (tar-ignore mirroring .gitignore) before any real build could hit it; (4) `dch --allow-lower-version` takes a Perl regex, not a shell glob — a bare `*` crashed it. Also corrected clipnest-ctl and the GSettings/D-Bus assumptions against the REAL D-Bus contract (Sources/ClipnestLinuxApp/DBus/ClipnestControlNames.swift) once it landed mid-session — app.clipnest.Control.{TogglePicker,ExpandSnippet,OpenSettings,Ping}, not a guessed org.freedesktop.Application.ActivateAction mapping (which would have silently mis-routed expand-snippet due to a real no-hyphen lowercasing quirk in the fallback path). Known, documented, NOT fixed here (cross-scope, flagged for architect): Sources/COnnxRuntime hard-links libonnxruntime.so.1, so clipnest is built WITHOUT -DCLIPNEST_HAS_ONNXRUNTIME for now to keep clipnest-ocr a genuine Recommends (not a forced Depends) — needs a dlopen-based fix in Sources (mirroring D59's uinput precedent) before OCR can actually ship in the binary; PP-OCRv5 models not vendored (packaging/linux/vendor/ppocr-models/README.md documents exactly what's needed, debian/rules fails loudly if absent rather than shipping wrong/empty data); no apt-installable Swift toolchain on jammy/noble blocks a real Launchpad PPA build today (dpkg-checkbuilddeps reproduced live) — release-linux.yml's dput path is fully wired and gated on a GPG secret regardless, ready once that's resolved. All work already committed to feat/linux-port by the user's own periodic commits during this session (verified via git log, not done by me); staged (git add, no commit) my last few fixes (debian/copyright, debian/source/{options,lintian-overrides}, packaging/linux/vendor/ppocr-models/README.md, .github/workflows/release-linux.yml). status:test — returning to orchestrator, not touching task-board.md per integrity rule 1.
- 2026-09-06 [infra] Fixed silent-failure self-hosted SearXNG (harness-searxng, :8081) behind mcp__web-search__*: root cause = only 5 "general"-category engines enabled by default (brave/duckduckgo/google cse/startpage/wikipedia), all suspended (429/CAPTCHA) within a few burst queries → merged "No results." every time. Fixed in /Users/aayushgour/Desktop/harness/tools/web_search/searxng/settings.template.yml (+ regenerated settings.yml, gitignored): enabled `bing` as an extra general engine, raised outgoing.request_timeout 3.0s→6.0s. Also bumped ensure_searxng.py's health-check timeout (5s→9s, _wait_json 30s→45s) since the raised engine timeout was making the MCP tool's own health probe falsely report "port-busy". Verified via direct curl to the JSON API across repeated bursts: non-empty results every time (previously empty). Residual, unresolved caveat documented in settings.template.yml: bing occasionally returns fully-formed but completely unrelated results (bing.com anti-scraping tar-pit behavior, not a config bug) — flagged for human awareness, not fixable via SearXNG config. Live MCP server subprocess for this session still runs old in-memory code (Python module caching) — fix will apply automatically on next session/reconnect. Ran `python3 -m unittest test_ensure_searxng` (11/11 pass).
- 2026-09-08 [T-LINT2] Made the swift-format lint gate reproducible across toolchains (CI's `swift:6.0-jammy`/`-noble` bundle a `main`-branch swift-format; this Mac's Apple toolchain reports `6.3.0` — verified they disagree on real code, which is how two reviewers reached opposite verdicts on the same SettingsWindow.swift line per T-LINT1). New `scripts/lint.sh`: checks `swift format --version` against the pinned image's known value (`main`); runs natively when it matches (the path CI's own job takes, zero overhead — verified inside both `swift:6.0-jammy` and `swift:6.0-noble` via `docker run ... scripts/lint.sh`, EXIT:0 both); otherwise re-execs itself inside `swift:6.0-jammy` via `docker run` so the verdict always comes from the literal binary CI trusts (verified on this Mac: mismatch detected, image pulled, re-exec'd, EXIT:0). Guard demonstrated firing on deliberate mismatch two ways: `CLIPNEST_LINT_REEXEC=1` forces the "drifted even inside the pinned image" path (exit 1, refuses to silence itself) and a docker-stripped `PATH` forces the "docker unavailable, cannot verify" path (exit 1) — neither silently passes. `.github/workflows/ci.yml`'s lint step now calls `scripts/lint.sh Sources Tests` instead of the raw command, so CI and any developer are certified by literally the same script and binary — single source of truth, no drift to maintain by hand. Rejected: a new SwiftPM swift-format dependency (would add a second third-party dependency against coding-standards.md's one-dependency policy, and still wouldn't match CI — the jammy image bundles a `main`-branch build, not a taggable release, so pinning a release tag would be a THIRD disagreeing version); a root `.swift-format` rules config (tested empirically — adding a default one changes zero verdicts on either toolchain, because the disputed behavior is the pretty-printer's line-wrap algorithm, not a configurable rule — `swift format dump-configuration` has no such knob); documenting a required toolchain version with no enforcement (doesn't work even in principle, since "main" is a moving branch build baked into a Docker tag, not something installable from swift.org). Did not touch `Sources/ClipnestGTK/Interop/.swift-format` (still honoured, still works) or any `.swift` file (`git diff -- '*.swift'` empty). Separately checked (not fixed, per the task's explicit "do not silently change triggers" instruction): confirmed via deepwiki against github/docs that `ci.yml`'s `on: pull_request` trigger WOULD run on a PR from this branch — a `pull_request`-triggered workflow is read from the PR's merge ref, not the base branch, so it does not need to pre-exist on `main` (only 58a6335, on `feat/linux-port`, has ever added it). The reason `gh run list --workflow=ci.yml`/`gh api .../actions/workflows` show nothing is that GitHub's workflow *listing* API only surfaces workflows that exist on the default branch or have already run once — a listing artifact, not a trigger defect — no PR from this branch has ever been opened to fire it. Also found, reporting only: `main` has zero branch protection rules (`gh api repos/.../branches/main/protection` → 404 "Branch not protected"), so even once ci.yml runs on a PR, nothing currently blocks a merge if it's red. Docs updated same-change: docs/architecture.md ss8 gained a `ci.yml`/`scripts/lint.sh` paragraph (previously undocumented entirely), README.md's Contributing section now points at `scripts/lint.sh`. `git add`ed only my 4 files (.github/workflows/ci.yml, scripts/lint.sh, docs/architecture.md, README.md) — left two other agents' in-flight, unstaged Sources/** changes (CGdkX11/shim.h, SettingsWindow.swift, 2 new GTKClipboardCrashNotice files) untouched. No commit. Diff grepped for secrets — clean. Not a security-trigger task (tooling/CI config only, no auth/secrets/PII/user-input/external-I/O surface touched) — no mandatory security pass owed. Returning status:test to whoever holds the board pen (spawned, single-writer rule) for T-LINT2 (currently `status:todo [architect]` on the board).
