# senior-dev log

- 2026-08-06 [T2(SPM)/T3/T4/T5/T6/T7] Built ClipnestCore Phase A: Package.swift + placeholder, ItemKind/ClipItem/Snippet (Sendable value types, not @Model — see D6 escalated to architect after SwiftDataMacros plugin proved unavailable under Command Line Tools), PasteboardReader (text/richText/link + SHA256 hashing), PrivacyFilter (concealed/transient unconditional reject + built-in/custom excluded bundle IDs), ClipStore protocol + InMemoryClipStore (dedup/CRUD/pin/clear), ClipboardMonitor (checkNow() seam, pause/resume). 34 tests, 0 failures (`swift test` needs `-Xswiftc -F` + `-Xlinker -rpath` pointing at CLT's Testing.framework — documented env quirk, unrelated to code). Lint clean (`swift format lint --recursive --strict Sources Tests`). No force-unwraps, no networking imports, no logging of clipboard content. → board:review, handing to reviewer then tester.
- 2026-08-06 [T7 fix round 1] Reviewer APPROVE-with-fixes: (1) ClipboardMonitor.checkNow() no longer swallows ClipStore errors via `try?` — now catches + calls an injectable CaptureFailureHandler (default: os.Logger, metadata-only, case name only), covered by new surfacesStoreFailure() test with a ThrowingClipStore double; (2) org.nspasteboard Concealed/TransientType strings deduped into PrivacyFilter.concealedPasteboardType/transientPasteboardType, referenced from both test files instead of re-declared. 35 tests, 0 failures. Lint clean. → board:review still, re-handing to reviewer.
- 2026-08-06 [T14/T15/T11(core)] Built Paste + Search core, routed directly (no sub-delegation): `Paste/FrontmostAppTracker.swift` (T14 — `FrontmostAppRef` value type + injected `FrontmostAppReferenceProviding`, last-recorded-wins `record()`/`consume()`), `Paste/EventSynthesizing.swift` + `Paste/Paster.swift` (T15 — `CGEventSynthesizer` real ⌘V impl via `postToPid`, `Paster` writes `NSPasteboard.general` then only-if-Accessibility-granted calls the injected synthesizer, `PasteError`/`PasteContent`/`PasteboardWriting` typed per pattern), `Search/SearchFilter.swift` (T11 core-only — case-insensitive substring + kind filter over `[ClipItem]`, zero UI). SwiftUI `PickerView`/`ItemRow` half of T11 explicitly skipped (App-target, needs T10). 17 new tests (48 total), 0 failures via the CLT `swift test -Xswiftc -F ...` command; `swift format lint --recursive --strict Sources Tests` clean. Both PasterTests branches covered with mock `EventSynthesizing` (granted → called once with right target after pasteboard write; not granted → pasteboard-only, mock never called) — no real key events synthesized. No networking imports, no force-unwraps/try!/as!, no clipboard content logged. → T14/T15 board:review; T11 stays todo (Core slice only, noted inline) — handing T14/T15 to reviewer then tester.
- 2026-08-06 [T19/T20/T22/T32-core] Built the storage core, routed directly (no sub-delegation): `Store/BlobStore.swift` (T19 — content-addressed write/read/delete, base directory always injected, `contentHash(of:)` the single canonical SHA256-hex impl now shared with `PasteboardReader`), extended `Clipboard/PasteboardReader.swift` for `.image`/`.file` (T20 — kept the reader pure/I/O-free; blob-writing moved to `ClipboardMonitor.checkNow()` instead, a documented design call — see D8), extended `ClipStore`/`InMemoryClipStore` with `fetchPinned()` (T22), `enforceRetention(cap:)`/`RetentionCap` (T32-core; Settings UI half explicitly skipped, app-target), and blob cleanup on delete/clearHistory/retention (T20). New `Store/{SnippetStore,InMemorySnippetStore}.swift` (T22, two files mirroring ClipStore per D6). 36 new tests (84 total via the CLT `swift test -Xswiftc -F ...` command, 0 failures): 8 BlobStoreTests, 6 SnippetStoreTests, 7 new PasteboardReaderTests cases (image/file, priority ordering, missing-file fallback), 4 new ClipboardMonitorTests cases (image→blob, dup-image→1 blob file, file capture), 11 new ClipStoreTests cases (fetchPinned ×2, blob cleanup ×3, retention ×6). `swift build` clean; `swift format lint --recursive --strict Sources Tests` clean (fixed 2 line-length findings on first run). No networking imports, no force-unwraps/try!/as! in production code, no clipboard content logged (metadata-only reasons). `FileManager` isn't `Sendable` in this SDK overlay — used `nonisolated(unsafe)` on the two new stored `FileManager` properties (documented rationale inline: Apple documents non-delegate FileManager use as thread-safe). → T19/T20/T22/T32(core) board:review, handing to reviewer then tester. T32's Settings-UI half stays `todo` (app-target, needs T28).
- 2026-08-06 [T19/T20/T22/T32-core fix round 1] Reviewer Important finding fixed: `.file` items now carry `ClipItem.fileReference` (new `String?` field, defaulted nil) populated from `PasteboardReader.Classification.fileURL` in `ClipboardMonitor.checkNow()`, gated explicitly on `kind == .file` so other kinds are provably unaffected. 85 tests, 0 failures (extended `capturesFileReference` + new `nonFileCaptureLeavesFileReferenceNil`). Lint clean. Only the 3 specified files touched, 5 Minor findings deferred per scope. → still board:review, re-handing to reviewer.
- 2026-08-06 [T2(App)] Built ClipnestApp XcodeGen scaffold now that full Xcode 26.6 is active: project.yml, Sources/App/{ClipnestApp,AppDelegate}.swift (MenuBarExtra "Clipnest"/clipboard SF Symbol + Quit, .accessory activation policy), Resources/ClipnestApp.entitlements (empty/non-sandboxed), Resources/Assets.xcassets (minimal + AppIcon placeholder) — all verbatim per plan T2. `xcodegen generate` clean, `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → BUILD SUCCEEDED, Clipnest.app present (LSUIElement=true confirmed via PlistBuddy). Root `swift build`/`swift test` unaffected (85/0). Lint: 1 round of `swift format --in-place` needed on the 2 new files (indentation only), then `--strict` clean on ClipnestApp/Sources and root Sources/Tests. `.gitignore` +2 lines (xcodeproj, DerivedData); `git add` done, no commit — 6 files + .gitignore staged, verified `.xcodeproj`/DerivedData correctly ignored. No divergence from plan's assumed APIs under Xcode 26.6/Swift 6.3.3. Report: .superpowers/sdd/2026-08-06-clipnest-plan/task-scaffold-report.md. → board:review, handing to reviewer then tester.
- 2026-08-06 [App composition root + T10/T11(UI)/T12] Built the first functional vertical slice: `App/AppEnvironment.swift` (new composition root — one shared `BlobStore` explicitly injected into both `InMemoryClipStore` and `ClipboardMonitor`, never their own defaulted instances; owns `PrivacyFilter`/`PasteboardReader`/`InMemorySnippetStore`/`PickerViewModel`/`PickerPanel`; `startCapture()` called once from `AppDelegate.applicationDidFinishLaunching`, real `Timer` at `ClipboardMonitor.defaultPollInterval`). `UI/Picker/PickerPanel.swift` (T10 — borderless `.nonactivatingPanel`, `canBecomeKey`/`canBecomeMain` overridden, `show(at:)`/`hide()` via `orderFrontRegardless()`+`makeKey()`, never `NSApp.activate`, so it can't steal focus from the frontmost app). `UI/Picker/{PickerView,PickerViewModel,ItemRow}.swift` (T11-UI — auto-focused search field, live 0.4s-poll history list filtered through Core's existing `SearchFilter`, History-tab-only header; `PickerViewModel` is a new file beyond the plan's literal T11 list, justified as the natural state-owner for the live-poll `Task`). T12 (copy-on-select) lives in `PickerViewModel.select(_:)`: writes `.text`/`.link` items' `previewText` to `NSPasteboard.general` via Core's existing `PasteboardWriting` (reused, not reimplemented) and dismisses the panel; `.richText`/`.image`/`.file` intentionally no-op for now (flagged in report, not silently papered over). Menu bar keeps `MenuBarExtra(.menu)` — added an "Open Clipnest" button calling through `AppDelegate.showPicker()`; no need to switch to raw `NSStatusItem`. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. Root `swift build`/`swift test` unaffected (85/0, no Core files touched). `swift format lint --recursive --strict ClipnestApp/Sources` and `Sources Tests` both clean. Runtime GUI behavior (non-activation, live focus, actual paste) is unverified by me — headless build-only, flagged explicitly for reviewer/tester's manual pass. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-picker-report.md`. `git add` done for all 7 created/changed files + the report, no commit. → status:test, handing to reviewer then tester. Was spawned this session (see progress.md) — returning status here + in final message rather than writing task-board.md myself (integrity rule 1).
- 2026-08-06 [Picker fix round 1] Coordinator's runtime testing found 3 issues; fixed all, dedup untouched as instructed. (1) Real bug — selecting an item re-captured it: added `ClipboardMonitor.ignore(changeCount:)` (new Core API, `Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift`) so `checkNow()` skips a pasteboard change that Clipnest itself produced instead of recapturing it; extended `PasteboardWriting` (`Paste/Paster.swift`) with `changeCount` so both today's `PickerViewModel.select(_:)` write and a future `Paster`-based write go through the identical suppression call; `PickerViewModel.suppressOwnPasteboardWrite` closure wired by `AppEnvironment` to `clipboardMonitor.ignore(changeCount:)`. New test `ClipboardMonitorTests.ignoresOwnPasteboardWrite()`: suppressed self-write → not captured; subsequent real change → still captured normally. (2) T13 keyboard nav: one catch-all `.onKeyPress` on `PickerView`'s container — Esc → `viewModel.dismiss()`, ↑/↓ → new `PickerViewModel.moveSelection(by:)` (clamped, not wrapping), ⌘F → focuses the search field; Return left as-is (already handled by the search field's existing `.onSubmit` from T12, not duplicated). (3) Full-screen/positioning: `PickerPanel.show(at:)` now defaults to `NSEvent.mouseLocation` (was: centered) on whichever screen actually contains the cursor; `level` raised `.floating` → `.popUpMenu` so the panel draws over full-screen content (`collectionBehavior` was already correct from T10). Clean rebuild (`rm -rf DerivedData` + `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build`) → `** BUILD SUCCEEDED **`, `Clipnest.app` present. `swift test` → 86/86 (85 + 1 new). `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Runtime full-screen-overlay + arrow-key-bubbling behavior still headless-unverified, flagged in the report. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-picker-report.md` (## Fix round 1). `git add` done for all 8 touched files + report, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-06 [Picker fix round 2] Two items. (1) Scroll/selection reset: ordering was already correct (verified — `InMemoryClipStore.fetchAll` sorts newest-first, `SearchFilter.filter` preserves order); split `PickerViewModel`'s reset logic into `selectFirstResult()` (hard reset: unconditional top-item selection + new `scrollToTopToken`) vs `reconcileSelection()` (soft: keep current selection if still visible). Hard reset fires on picker-open (`willShow()`'s query reset + the initial post-open `reload(resetSelection: true)` once fresh data lands) and on every search-text keystroke (`query`'s didSet); background live-refresh polls while open use the soft path so they don't yank the user's browsing position every ~0.4s. `PickerView` wraps the `List` in `ScrollViewReader`, scrolls to top on `scrollToTopToken` change (deferred one runloop tick via `DispatchQueue.main.async` to dodge a layout-timing race). (2) Global hotkey (T27): added `KeyboardShortcuts` dep (Sindre Sorhus, MIT). Version-verification correction worth flagging — first pass verified `v1.10.0` via `git ls-remote --tags | sort -V | tail`, pinned it, build succeeded, but `Package.resolved` came back `1.17.0` (mismatch chased, not shrugged off): the project renamed tags from `vX.Y.Z` to bare `X.Y.Z` after v1.17.0, and `sort -V` over the unfiltered combined list doesn't interleave the two formats correctly — actual latest stable is `3.0.1` (GitHub releases API confirmed), a full major ahead. Diffed Package.swift/Name.swift/Shortcut.swift between 1.x/3.x before pinning: tools-version 6.2 (fine, Xcode 26.6 = Swift 6.3.3), `default:` param renamed to non-deprecated `initial:`, `onKeyDown`/`onKeyUp` unchanged, and 3.x's `Name`/`Shortcut` are now natively `Sendable` — this actually simplified `HotkeyManager.swift` since the mistaken-1.x-pin build had genuinely failed to compile on exactly that Swift 6 strict-concurrency error, needing a `@MainActor` workaround; re-pinning to 3.x's native `Sendable` removed the need for it. Pinned `from: 3.0.1`, `Package.resolved` confirms exact resolution. New `ClipnestApp/Sources/System/HotkeyManager.swift`: `KeyboardShortcuts.Name.togglePicker` defaults to ⌥⌘V (Option+Command+V, per explicit instruction overriding the spec's ⌘⇧V — "set on first launch if unset" is the library's own `Name.init` behavior, no bespoke bookkeeping added), `HotkeyManager.register(onToggle:)` thin wrapper (no pure logic to unit-test, documented manual-verification requirement instead); `AppEnvironment.registerHotkey()` wires it to the same `showPicker()` path the menu item uses, called from `applicationDidFinishLaunching`. Clean rebuild (network-resolving the new dep, as expected) → `** BUILD SUCCEEDED **`, `Clipnest.app` present. Root `swift build`/`swift test` unaffected (KeyboardShortcuts is ClipnestApp-only) — 86/86 still. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Global hotkey + scroll-timing effectiveness both flagged as runtime-only/headless-unverified. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-picker-report.md` (## Fix round 2); decision trail in project-context.md D11. `git add` done for all 8 touched files (incl. new HotkeyManager.swift + project.yml), no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-06 [T40] Swapped in real SwiftData persistence behind the unchanged `ClipStore`/`SnippetStore` protocols, now that full Xcode 26.6 unblocks `@Model` (per D6/D9). New `Store/SwiftDataClipStore.swift` (plain `actor`, not `@ModelActor` — its synthesized init has no room for the second `blobStore` dependency this store needs; documented in the report) with a `private @Model final class ClipItemRecord` mirroring every `ClipItem` field (`kind` stored as `kindRawValue: String`, mapped via `ItemKind(rawValue:)`) — full contract identical to `InMemoryClipStore`: dedup-bumps-only-`createdAt`, newest-first `fetchAll` (ignores `query`, matching the in-memory store), `fetchPinned`, `setPinned`, `delete`/`clearHistory`/`enforceRetention` all with blob cleanup, pinned always excluded from retention. New `Store/SwiftDataSnippetStore.swift`, same shape, `private @Model final class SnippetRecord`. DRY fix while I was in `ClipStore.swift`: extracted the blob-cleanup helper both stores now needed into a shared module-internal `deleteBlobs(for:using:)`, refactored `InMemoryClipStore` to use it too (behavior unchanged, its existing tests still pass unmodified). 27 new tests mirroring the in-memory suites exactly (`SwiftDataClipStoreTests` ×20, `SwiftDataSnippetStoreTests` ×7), run against `makeTestContainer()` (`isStoredInMemoryOnly: true`) — never the real on-disk DB. `swift test` → **113/113, 0 failures** (86 prior + 27 new). Composition root swap: `AppEnvironment.swift` now builds `SwiftDataClipStore`/`SwiftDataSnippetStore` via their `makeProductionContainer()` factories (`~/Library/Application Support/Clipnest/{ClipItems,Snippets}.store`, same base-dir family as `BlobStore`), sharing the identical single `BlobStore` instance as before; `init()` is now `throws` (real `ModelContainer` construction is genuinely fallible — disk full/permissions), so `AppDelegate.applicationDidFinishLaunching` now does/catches it, logging metadata-only via `os.Logger(.fault)` and terminating on failure rather than crashing or running half-initialized. Verified `~/Library/Application Support/Clipnest/` doesn't exist yet on this machine — nothing touched/at risk. Clean `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift format lint --recursive --strict Sources Tests ClipnestApp/Sources` clean. No `@Model`/SwiftData type appears in any function signature outside the two new store files (verified by inspection) — protocol boundary held per D6. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T40-report.md`. `git add` done for all 8 created/changed files + report, no commit. → board:review, handing to reviewer then tester.
- 2026-08-06 [T24] Row actions (pin/unpin + delete) on the picker's History rows — picker-UI wiring only, per routed task (Core store already had `setPinned`/`delete`/`fetchPinned`/`ClipItem.pinned` from T40's SwiftData swap; zero ClipnestCore files touched, confirmed by unchanged 113/113). `ItemRow.swift`: gained `onSelect`/`onTogglePin`/`onDelete` closures + a `pin.fill` badge when pinned; right-click `.contextMenu` (Pin/Unpin + destructive-styled Delete) and hover-revealed trailing icon buttons, both calling the same closures. Design note: switched the row's click-to-select from a wrapping `Button` to `.onTapGesture` on the content area, since nesting the new pin/delete `Button`s inside that outer selection `Button`'s label would have made them unreliable (SwiftUI doesn't route taps well to a `Button` nested inside another `Button`) — pin/delete are now genuine sibling controls instead. `PickerView.swift`: added ⌘P (togglePinHighlighted) and Delete/⌘⌫ (deleteHighlighted, one `.delete` case covers both since `KeyPress.key` ignores modifiers) to the existing single `.onKeyPress` handler alongside T13's Esc/↑/↓/⌘F, none of which were touched/reordered; flagged in code + report that plain Delete is likely to be swallowed by the always-focused search field's own backspace editing when it has text (⌘P/⌘⌫ aren't standard text-editing shortcuts so should reliably bubble up same as ⌘F already does) — a runtime-only claim either way. `PickerViewModel.swift`: `togglePin(_:)`/`delete(_:)` call the real `ClipStore` async, catch+log metadata-only (item.id + error description, never previewText/content) on failure, then reload; delete captures the item's pre-delete index and reloads via a new `reload(selectingIndexNear:)` that selects whatever now sits at that same (clamped) index instead of jumping to the newest item, so deleting from mid-list doesn't relocate you to the top; pin-toggle uses the existing soft `reload(resetSelection: false)` since History is deliberately never reordered by pin (task's explicit instruction, verified no sort touched). Self-review caught + fixed a 3x-duplicated `selectedItemID`-lookup guard across `selectHighlighted()`/`togglePinHighlighted()`/`deleteHighlighted()` — extracted a shared `highlightedItem` computed property before handoff. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. Root `swift build`/`swift test` unaffected — 113/113. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. All pin/delete visual + keyboard behavior flagged as manual-runtime-check-only in the report, not claimed as verified. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T24-report.md`. `git add` done for all 3 touched files, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-06 [T16] Wired the real paste engine app-side: new `ClipnestApp/Sources/System/PermissionsManager.swift` (AXIsProcessTrusted wrapper; needed `@preconcurrency import ApplicationServices` for `kAXTrustedCheckOptionPrompt` under Swift 6, see D13); `AppEnvironment` builds `Paster`/`FrontmostAppTracker`, feeds `PermissionsManager.isGranted` in, records frontmost app in `showPicker()` before the panel shows (covers both hotkey + menu triggers via the one existing choke point); `PickerViewModel.select(_:)` now calls `paster.paste` instead of writing the pasteboard directly, suppression hook preserved. `swift test` 113/113 (Core untouched); `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → BUILD SUCCEEDED; lint clean. No PermissionsManager test (thin passthrough, matches HotkeyManager precedent, branching covered by PasterTests). `git add` done, no commit. → board:review, handing to reviewer then tester.
- 2026-08-10 [T24 amendment] Coordinator reversed the earlier "do NOT reorder History by pin" instruction: pinned items now sort to the top of the picker list (pinned group first, unpinned after), each group still newest-first — same ordering applied to filtered/search results. Implemented as a stable partition in `PickerViewModel.items` only (`filtered.filter(\.pinned) + filtered.filter { !$0.pinned }` — `fetchAll`/`SearchFilter.filter`/`Array.filter` are all already order-preserving/newest-first, so this is a split-and-concatenate, not a re-sort); zero Core change, as instructed. Verified (not just assumed) the three "stay sane" requirements: pinning/unpinning already picks up the new ordering on `togglePin(_:)`'s existing post-store `reload(resetSelection: false)` since `items` is computed; `reconcileSelection()`'s "still visible" check is a Set-membership test, order-independent, so the currently-selected item stays selected even as it visually jumps groups; `moveSelection(by:)`/`reload(selectingIndexNear:)` already operate on live `items` order with no code change needed. Corrected two now-stale doc comments (`togglePin(_:)` previously said pin never reorders History; `selectFirstResult()` previously said "newest" when it's now "top of pinned-first order"). Flagged one known, deliberately-unaddressed gap: pinning an item that's scrolled out of view doesn't auto-scroll to show it at its new top position (`togglePin` doesn't bump `scrollToTopToken`) — same class of gap as the existing arrow-key-scroll-follow gap, not fixed here to avoid scope creep on a "small change." `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. Root `swift build`/`swift test` unaffected — 113/113. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T24-report.md` (## Amendment). `git add` done for the one changed file, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T24 UI refinement] Coordinator reviewed a screenshot, asked for simpler trailing row controls in `ItemRow.swift`: exactly two always-visible icon buttons (pin toggle, delete) instead of the earlier hover-revealed pair + separate pin badge. Removed the row-level `isHovering`/`if isHovering { rowActions }` gate so both buttons render unconditionally; removed the pin badge block and the `pin.slash` hover-button variant — the pin button's own icon (`pin` outline vs `pin.fill`) is now the sole pinned-state indicator, same `onTogglePin` action either way. Context menu explicitly left untouched (still `pin`/`pin.slash` for its Pin/Unpin item, per instruction). Extracted a small private `RowActionButton` view (own `@State isHovering`) so each button tracks hover independently — `Color(white: 0.45)` resting / `Color(white: 0.85)` hover, named `private static let` constants not inline literals. Only `ItemRow.swift` touched — `PickerView.swift`/`PickerViewModel.swift` untouched since the row's public closure interface didn't change. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. Root `swift build`/`swift test` unaffected — 113/113. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Actual visual result (colors, icon-swap, sizing) flagged as manual-runtime-check-only, same as every other UI note in this report. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T24-report.md` (## UI refinement). `git add` done for the one changed file, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T24 pin-order fix] Bug: pinned group was ordered by `createdAt` (copy time) inherited from the upstream filter pass, so a newly-pinned item landed at a "random" spot instead of predictably at the bottom of the pinned section. Fixed with a real Core change (first Core touch since the App-layer-only work of the last several rounds): added `ClipItem.pinnedAt: Date?` (nil unless pinned, defaulted init param — non-breaking, verified every existing call site still compiles), `ClipStore.setPinned(_:pinned:)` now sets/clears it in BOTH `InMemoryClipStore` and `SwiftDataClipStore` identically (`ClipItemRecord` @Model gained the matching field threaded through init/convenience-init/asClipItem, same pattern as every other field in that file). Directly closes D12's own flagged migration note: judged a `VersionedSchema`/migration plan unnecessary here since this is a purely additive optional field — SwiftData's lightweight/automatic migration handles it with zero config, confirmed by `SwiftDataClipStoreTests` passing clean against the updated schema; documented in D14 that D12's flag still stands for a future *non*-additive change (required field/type change/rename/removal). `PickerViewModel.items`'s pinned run now `.sorted` by `pinnedAt` ascending (earliest-pinned top, latest-pinned bottom, nil falls back to `.distantPast`) instead of inheriting `createdAt` order; unpinned run and `fetchPinned()`'s own createdAt-desc sort both deliberately untouched (fetchPinned backs a not-yet-built Pinned tab, out of this fix's scope). New tests: `setPinnedSetsAndClearsPinnedAt()` + `pinUnpinRepinCycleUpdatesPinnedAt()`, one matching pair in each of `ClipStoreTests`/`SwiftDataClipStoreTests` — asserted presence/absence of `pinnedAt` rather than strict timestamp ordering (no injectable clock on `setPinned`, and adding one wasn't asked for). `swift build`/`swift test` (root) → 117/117 (113 + 4 new). `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. `swift format lint --recursive --strict Sources Tests ClipnestApp/Sources` clean. On-screen pin-order after a live pin/unpin flagged as manual-runtime-check-only. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T24-report.md` (## Pin-order fix); decision recorded as D14 in project-context.md. `git add` done for all 6 touched files, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T23] Pinned + Snippets tabs — picker-UI wiring only, per routed task (Core `ClipStore.fetchPinned()`/`SnippetStore` CRUD already complete, T40; zero Core files touched, confirmed by unchanged 117/117). New `TabSwitcher.swift` (`PickerTab` enum + click segmented control), ⌘1/⌘2/⌘3 added to `PickerView`'s existing single `.onKeyPress` handler alongside Esc/↑/↓/⌘F/⌘P/Delete/⌘⌫ (none touched/reordered). Pinned tab: deliberately filters `allItems` client-side (`pinnedRun`, shared with History's own pinned group — same computed value, so ordering matches by construction, not by two implementations agreeing) rather than a second `fetchPinned()` round-trip — task explicitly allowed either. `togglePin(_:)`/`delete(_:)` bodies unchanged from T24 — only the shared selection/ordering helpers became tab-aware, so "row actions work on Pinned too" + "unpinning removes it from that tab" fell out for free. Snippets tab: new `SnippetFormView.swift` (one form, `SnippetFormMode` create/edit, `.sheet(item:)`) + `SnippetRow.swift` (mirrors `ItemRow`'s tap-to-select/hover-button/context-menu shape exactly); selecting a snippet pastes `body` through a newly-extracted shared `pasteAndDismiss(_:)` — same method `select(_:)` now also calls, per the task's explicit "don't duplicate the paste/suppression logic" instruction. No live polling for snippets (design decision, documented): nothing outside the picker mutates them while open, so load-on-open + reload-after-CRUD is enough. Two more real-duplicate extractions made proactively: generic `clampedIndex<Element: Identifiable>` (was about to be written twice, once per element type, for `moveSelection(by:)`) and generic `ScrollResettingList` view in PickerView.swift (ScrollViewReader+List+scroll-to-top, now shared by both lists instead of copy-pasted). `ItemRow.swift`'s `RowActionButton` (T24) made internal (was private) so `SnippetRow` could reuse it. `PickerViewModel.init` gained a required `snippetStore` param (no default, matches `clipStore`'s existing mandatory-store precedent) — `AppEnvironment` updated, its only call site. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **` (one line-length lint finding in the new generic view, fixed via `swift format format --in-place`, rebuilt clean). Root `swift build`/`swift test` → 117/117 unaffected. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Tab switching, sheet presentation against the borderless panel, snippet persistence/paste end-to-end, and Pinned-tab row actions all flagged as manual-runtime-check-only, not claimed as verified. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T23-report.md`; decision recorded as D15 in project-context.md. `git add` done for all 7 touched/created files, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T23 fix round] Three items, all picker-layer; confirmed tabs/snippets/single-search-box behavior unchanged (query persists across tabs, each tab filters only its own content) — verified by inspection, not re-touched. (1) Return not pasting: `.onSubmit` was already wired to `selectHighlighted()` (the literal prescribed fix), real testing found it still broken, root cause not independently confirmable headlessly — added an explicit `case .return: viewModel.selectHighlighted(); return .handled` to the same `.onKeyPress` handler as belt-and-suspenders; `.handled` return prevents any double-fire with `.onSubmit` regardless of which mechanism actually wins on the running macOS. (2a) `PermissionsManager.isGrantedPromptingIfNeeded()` (`isGranted || requestAccess()`) now feeds `Paster` instead of the silent `isGranted` — prompts macOS's grant dialog on an actual paste attempt when not granted, still returns false synchronously so that same paste still degrades to clipboard-only (grant applies next time); stays paste-attempt-only since the closure was already only invoked from inside `paste(...)`. (2b) `CGEventSynthesizer` switched `postToPid` → `CGEvent.post(tap: .cghidEventTap)` (global HID tap, real behavioral shift not cosmetic — pid-targeted posting bypasses normal focus routing, global posting goes to whatever currently holds key focus), keycode made explicit `0x09`. `Paster.paste` is now `async throws` with injectable `synthesisDelay: Duration` (default `Paster.defaultSynthesisDelay = .milliseconds(40)`, named constant) — sleeps after the write+guard, before synthesis, only on the actual-synthesis path. `PickerViewModel.pasteAndDismiss(_:)` reordered: `dismiss()` now runs BEFORE the async paste (was after) — this is what makes the delay correct, since if our own panel still held key focus when the now-global synthetic ⌘V posts, the OS could deliver it into our own search field instead of the target app. `PasterTests.swift` updated for async (`async throws`, `await #expect(throws:)`), every test passes `synthesisDelay: .zero` to stay fast/deterministic (no real ~40ms sleeps in CI) — matches the existing "no real timers in tests" convention. Documented, not engineered against: self-write-suppression's write-to-suppress gap grew from near-instant to up to ~40ms in the synthesis-granted path only; ~400ms ClipboardMonitor poll interval keeps the race odds low but no longer negligible — flagged as accepted minor risk rather than restructuring Paster's API for a rare edge case. (3) Always-visible tab-aware shortcut hint footer (`shortcutHintBar`/`shortcutHints`, muted `.caption2`/`.secondary`, not hover-gated) — History/Pinned shows `⌘P pin`, Snippets swaps in `⌘N new` (wired into the same key handler, guarded to Snippets tab, calls the already-built "+" button's same `presentCreateSnippetForm()` — no new create affordance needed). `swift build`/`swift test` (root) → 117/117 unchanged. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `Clipnest.app` present. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. This round is almost entirely runtime-only claims (Return actually pasting per-tab, the Accessibility dialog appearing, 40ms delay sufficiency, keystroke landing in the right app, footer rendering) — flagged explicitly in the T23 report's fix-round section and D16, not claimed as proven; this round grew the unverified list rather than shrinking it, since two of three items are inherently runtime-dependent (global keystroke timing, a system permission dialog). Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T23-report.md` (## Fix round); decision recorded as D16 in project-context.md. `git add` done for all 6 touched files, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T23 fix round 2] Five items, all picker/snippet-layer. (1) Real bug root-caused: snippet form was a `.sheet()` on `PickerPanel` (non-activating panel, app never activates by design) — sheet windows only get true key status when the owning app is frontmost, so typing/⌘V paste couldn't actually reach the fields. Fixed with new `SnippetEditorWindow.swift`: a real titled `NSWindow`, `NSApp.activate()` (not the deprecated `ignoringOtherApps:` overload — macOS 14 is our exact floor) + `makeKeyAndOrderFront(nil)` — deliberate, safe departure from the picker's own non-activation rule since editing is an explicit, different UX moment (same as any accessory app's Preferences window). Found+fixed a real initializer bug along the way: `convenience init()` calling `self.init(contentRect:styleMask:backing:defer:)` failed to compile because declaring `required init?(coder:)` breaks Swift's automatic inheritance of NSWindow's designated initializer — fixed identically to PickerPanel's existing pattern (init() itself is the designated initializer, calls super.init directly). `windowWillClose` fires one `onClose` regardless of how it closed (Save/Cancel/red-button/⌘W); AppEnvironment uses it to re-key PickerPanel + refocus search (new `PickerViewModel.refocusSearchField()`). `snippetFormMode`/`dismissSnippetForm()` removed from PickerViewModel, replaced by an injected `presentSnippetEditor` closure AppEnvironment wires directly to the window. (2) "Save as Snippet": ItemRow context-menu-only (not a third hover button — T24 explicitly fixed trailing controls at exactly two) + ⌘S, new `SnippetFormMode.createFromClip(String)` prefills Body. (3) Field correction arrived mid-implementation: form is Tag+Body ONLY, no separate Title — Tag maps onto existing `Snippet.title` (already the primary-label field everywhere else, so a UI-label rename not a new mapping), `keyword` left permanently nil, no schema change. Confirmed the editor body has exactly two fields. Removed SnippetRow's now-dead keyword badge outright. (4) Snippets search = Tag OR Body: extracted from an inline closure into new, tested `Sources/ClipnestCore/Search/SnippetSearchFilter.swift` (mirrors SearchFilter's role, Snippet has no ItemKind equivalent so its own type), dropped the now-unreachable keyword branch. (5) Search-match highlighting: pure range-finding in new `SearchHighlighter.swift` (Core, no AttributedString/SwiftUI dependency, fully unit-tested) + presentation in new App-layer `HighlightedText.swift` (subtle background-only highlight, chose over bold+accent specifically to avoid font-size mismatch across ItemRow/SnippetRow's different text roles); wired into both rows via a new `query` param. `swift test` (root) → 128/128 (117 + 11 new: 5 SearchHighlighterTests + 6 SnippetSearchFilterTests). Clean from-scratch rebuild (`rm -rf DerivedData` + `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build`) → `** BUILD SUCCEEDED **`, `Clipnest.app` present. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean (2 line-length findings fixed via `swift format format --in-place`, pure formatting). Item 1's actual fix (does typing/paste really work now) is the single most important unverified runtime claim in this whole task — flagged explicitly as the top concern, not assumed to work just because it compiles and the design reasoning is sound. Report appended: `.superpowers/sdd/2026-08-06-clipnest-plan/task-T23-report.md` (## Fix round 2); decision recorded as D17 in project-context.md. `git add` done for all 12 touched/created files, no commit. → status:test, returning status here (spawned this session, not writing task-board.md myself).
- 2026-08-10 [T41/T42] Routed picker follow-up, split into 2 disjoint-file tasks and delegated in parallel: junior-dev A → T41 (`PickerViewModel.items`'s `.history` case now `unpinnedRun` alone, History/Pinned mutually exclusive, reverses D15), junior-dev B → T42 (`ItemRow.rowActions` gains a third always-visible button, convert-to-snippet, order pin/convert/delete, reuses the pre-existing `onSaveAsSnippet` closure + `supportsSaveAsSnippet` gating + `text.badge.plus` icon already used by the context menu — no duplicated action, reverses T24/T23-fix-round's "exactly two buttons" decision). Reviewed both diffs myself (read in full, re-ran `swift test` → 128/128 unchanged and `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` → clean) before handoff — both correct, DRY, doc comments updated accurately, no new closures/logic duplicated, no magic strings. Spawned tester, who independently re-ran `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, `swift test` → 128/128, lint clean, plus a static re-read of both files and confirmed `SnippetRow.swift`/`PickerView.swift` untouched — verdict PASS, no defects, evidence in `.claude/logs/tester.md`. No new automated test coverage added for either change (App-layer SwiftUI/ViewModel only, zero Core touched, matches this project's established precedent for this class of change — flagged, not silently skipped); runtime/visual verification (3 buttons rendering correctly, History actually hiding pinned live) flagged for the human, unverified in this headless session. Decision recorded as D18 in project-context.md. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-history-snippet-btn-report.md`. `git add` done by each junior-dev for their own touched file only, no commit. → board:test for both T41/T42 (my ceiling per integrity rule 2 — tester validated with pasted evidence but I'm the one writing the board here since both juniors + tester returned status to me as spawner; final `done` flip is tester's call to make, not mine).
- 2026-08-10 [T21 + routed ⌘⌫ fix] Completed plan task T21 (previews + type-filter chips + non-text paste, all 3 parts) plus a separately-routed ⌘⌫-not-deleting bug fix. Split into 5 junior-dev sub-tasks across 3 rounds, scheduled by file-touch analysis: Round 1 parallel (⌘⌫ fix: `PickerPanel.swift`+`AppEnvironment.swift` only, needed zero `PickerView`/`PickerViewModel` changes since `deleteHighlighted()` already existed; Core `PasteContent` extension: `Paster.swift`+`PasterTests.swift` only) → Round 2 solo (rich per-kind previews: `ItemRow.swift`, `PickerView.swift`, `PickerViewModel.swift`, new `ItemThumbnailCache.swift`, `AppEnvironment.swift` — had to follow Round 1's `AppEnvironment.swift` edit) → Round 3 parallel again (type-filter chips: new `TypeFilterChips.swift`+`PickerView.swift`, turned out to need zero `PickerViewModel.swift` changes since `$viewModel.query.kindFilter` binds the same dynamic-member-lookup way the search field already does; paste-path wiring: `PickerViewModel.swift` only — disjoint files from the chips task, so ran together despite both following Round 2). Reviewed every diff myself (not the summaries) before each round, re-running build+lint+tests personally each time. Caught and fixed one real quality gap directly rather than re-delegating: `pasteContent(for:)`'s `.image` case used a bare `try? blobStore.read(...)`, silently swallowing a genuine `BlobStoreError` with zero logging on a one-shot, non-retrying, no-UI-feedback user action (unlike this file's existing `try?` precedent, which backs a self-retrying background poll) — rewrote as `do/catch` + metadata-only `Self.logger.error`, matching every other failure path in that file. `swift test` → 131/131 (128 + 3 new `PasterTests` cases). `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` both clean. Spawned tester, who independently re-ran the full build+test+lint suite from scratch plus a static read-and-confirm of every claimed wiring point in all 6 touched/new files against actual current source — verdict PASS, zero discrepancies, evidence in `.claude/logs/tester.md`. Tester flagged two minor process gaps (untracked junior-dev.md/tester.md logs, missing senior-dev board/log entry at time of their check) — both closed by this entry + board update + staging the log files. Decision: D19. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/task-rich-content-report.md`. `git add` done for all 9 touched/created source+test files (by each junior-dev for their own files, plus my own `pasteContent(for:)` fix) + `.claude/logs/{junior-dev,tester}.md`, no commit. → board:test for T21 and new T43 (my ceiling per integrity rule 2 — tester validated with pasted evidence; final `done` flip is tester's call, not mine). Not spawned by anyone this session — writing the board directly, same as the T41/T42 precedent.
- 2026-08-10 [T44] Routed follow-up, trivial single-file fix, built directly (PM's instruction: don't delegate to junior-dev). Bug: `PickerView.typeFilterChips` used `@ViewBuilder` + `if viewModel.activeTab != .snippets` to add/remove the whole `TypeFilterChips` row from the top-level `VStack`, so its height appeared/disappeared on tab switch, shifting tab bar/list/footer below it — a visible vertical jump switching to/from Snippets. Fix: row is now unconditionally in the `VStack` (same padding), made invisible/non-interactive on Snippets via `.opacity(0)` + `.allowsHitTesting(false)` + `.accessibilityHidden(true)` instead of conditional presence — height reserved on every tab, chips unchanged/fully functional on History/Pinned. No other layout touched (tabs, search, hint footer, rows all intact). `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 131/131 unchanged (App-layer-only SwiftUI modifier change, zero Core touched, no App test target — matches D18/D19 precedent, flagged not silently skipped). `swift format lint --recursive --strict ClipnestApp/Sources` clean. Decision recorded as D20 in project-context.md. `git add` done on `ClipnestApp/Sources/UI/Picker/PickerView.swift` only, no commit. No-jump behavior itself is a runtime/visual check, flagged for the human. → board:review (my ceiling here — no reviewer/tester pass run this turn; PM's ask was build+test+lint validation only, handing to reviewer then tester next). Not spawned by anyone this session — writing the board directly.
- 2026-08-10 [T45] Routed UI change, built directly (PM's instruction: small/medium, do it myself). Moved `TypeFilterChips` out of its own standalone row and into the trailing edge of the search field's own row — single header `HStack` (`PickerView.header`, new): `searchField` expands to fill remaining width, `typeFilterChips` sits flush right, removed entirely (not hidden) on Snippets via `if` so the search field genuinely reaches full width there. Pinned `header` to a fixed `Self.headerHeight = 40` so the row's height never depends on which children are present — a stronger, more direct guarantee against the vertical-jump bug than D20's opacity-placeholder trick, which this task supersedes (no standalone filter row remains for that trick to apply to). Restyled `TypeFilterChips` icon-only per the task's preferred option: 5 compact 22×22pt buttons (All=`square.grid.2x2`; Text/Image/File/Link reuse `ItemRow`'s existing per-kind glyphs — `doc.plaintext`/`photo`/`doc`/`link`), active filter highlighted via background fill + icon weight, each with `.help`+`.accessibilityLabel` (glyph alone isn't enough for VoiceOver/first-time use); chose icon-only over a funnel/menu since 5 single-glyph chips fit the row without crowding the text field and show all states + the active one at a glance, no extra click needed. Proactive DRY extraction while doing this: `ItemRow`'s private `iconName(for:)` and the new chip icons needed the identical `ItemKind → SF Symbol` mapping (a real second use appearing at write time) — pulled into new `ItemKind+SFSymbol.swift` (ClipnestApp-only extension; `ItemKind` stays UI-framework-free per module-layout rule), both call sites now share it, `ItemRow`'s duplicate removed. Zero behavior change to filtering itself — `SearchQuery.kindFilter` binding, "All" = no filter, cross-tab persistence all untouched, only the control's location/appearance moved. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 131/131 unchanged (App-layer SwiftUI change + one App-only extension file, zero `ClipnestCore` touched — no new coverage needed, matches D18–D20 precedent). `swift format lint --recursive --strict ClipnestApp/Sources` clean (one line-length finding in `ItemRow.swift` auto-fixed via `swift format format --in-place`, then reverified clean). Decision recorded as D21 in project-context.md. `git add` done for all 4 touched/created files (`PickerView.swift`, `TypeFilterChips.swift`, `ItemRow.swift`, new `ItemKind+SFSymbol.swift`), no commit. Visual result (icon legibility/crowding at this width, highlight visibility, zero jump across tabs, search field actually reaching full width on Snippets) flagged as manual-runtime-check-only for the human. → board:review (my ceiling here — no reviewer/tester pass run this turn; PM's ask was build+test+lint validation only, handing to reviewer then tester next). Not spawned by anyone this session — writing the board directly.
- 2026-08-10 [T46] Routed UI micro-interaction, built directly (small — self-verify). New reusable `ExpandingIconButton` (`ClipnestApp/Sources/UI/Components/ExpandingIconButton.swift`, new `UI/Components/` folder — first control with zero Picker/Settings-specific knowledge): icon-only at rest in a fixed 22×22pt square; on hover, icon+title reflow inside a frame pinned to exact height / min-only width, so the button widens horizontally exactly enough to fit icon+title — reads as "icon slides left, title slides in from the right" — all wrapped in one `withAnimation(.easeInOut(duration: 0.2))` per hover-state change (icon/title/background/`.help`/`.accessibilityLabel` all owned by the component, no per-caller duplication); title uses `.transition(.opacity.combined(with: .move(edge: .trailing)))`; height is pinned exactly (min==max), so it can never grow vertically regardless of caller context. `isHovering` is private per-instance `@State` (same pattern as the existing `RowActionButton`), so only the hovered chip expands. Applied to `TypeFilterChips` (`chip(for:)` now returns one `ExpandingIconButton` per option, passing `isActive: selection == option`) — active-filter highlight, filtering behavior (`SearchQuery.kindFilter`, "All" = no filter, cross-tab persistence), and the chips' trailing-right position in the search row (T45/D21) are all unchanged; only the chip's own rendering moved into the shared component, and the old duplicated `.help`/`.accessibilityLabel` calls in `TypeFilterChips` were removed since `ExpandingIconButton` now owns them. Built generic (icon/title/isActive/action only) so it's reusable for the row action buttons later — noted in the doc comment, not actually swapped in this task (out of scope: `RowActionButton`'s 2-3-wide trailing row needs its own layout pass for simultaneous-expansion behavior, not assumed here). `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 131/131 unchanged (App-layer-only SwiftUI addition, zero `ClipnestCore` touched — no App test target exists, matches D18–D21 precedent, flagged not silently skipped). `swift format lint --recursive --strict ClipnestApp/Sources` clean. Decision recorded as D22 in project-context.md. `git add` done for the 2 touched/created files (new `ExpandingIconButton.swift`, `TypeFilterChips.swift`), no commit. The actual hover animation (smoothness, icon/title slide direction, no clipping/overflow at the picker's width) is a runtime/visual check only — flagged for the human, same category as every prior UI round (D19–D21). → board:review (my ceiling here — no reviewer/tester pass run this turn; ask was build+test+lint self-validation only, handing to reviewer then tester next). Not spawned by anyone this session — writing the board directly.
- 2026-08-10 [T47] User-reported perf bug: "app hangs terribly while searching." Root-caused (not guessed) with a standalone benchmark before touching code: `PickerViewModel`'s History/Pinned `items` was 4 chained, uncached computed properties (`filteredItems`→`pinnedRun`/`unpinnedRun`→`items`), each re-running `SearchFilter.filter` over the *entire* `allItems` array on every read, synchronously on @MainActor, with zero debounce — and `PickerView`'s body reads `items` 2-3x per render, so one keystroke triggered 2-3 full O(n) scans back-to-back blocking the run loop. Confirmed the actual trigger: `ClipItem.previewText` has no length cap (`.text`/`.link` store the *full* content there — it doubles as what gets pasted back), so a single realistic large paste (a copied source file/log, ~2MB) pushed one filter pass from ~2ms to 30-70ms in my benchmark (`localizedCaseInsensitiveContains` over a large string) — paid 2-3x per keystroke, no debounce, reproducing "hangs terribly" exactly. Fix (`PickerViewModel.swift` only): collapsed the 4 computed properties into one `@Published private(set) var items` recomputed by a single `scheduleFilterUpdate(_:debounced:)` pipeline — the actual filter/sort pass now runs inside `Task.detached` (off @MainActor, via a new `nonisolated static filteredAndSorted(...)`), never inline during a SwiftUI render; only the final `self.items = result` + selection update touch @MainActor (via `MainActor.run`). Debounce (~120ms) applied ONLY to the search-text-typing path (`query`'s didSet); tab switches, live-refresh polls, and post-mutation reloads (pin/delete) all stay immediate/non-debounced, unchanged from before. `filterTask` is a single cancellable property (latest-request-wins, `Task.isCancelled` re-checked after every suspension point) so overlapping calls (e.g. a keystroke landing mid-poll) never stale-overwrite a newer result. `selectFirstResult()`/`reconcileSelection()`/`highlightedItem`/`moveSelection`/etc. needed ZERO changes — they only ever read `items`, and a stored property satisfies that identically to the old computed one; only `query`'s/`activeTab`'s didSets and the two `reload(...)` overloads changed their call site. Traced every interleaving (willShow's debounced reset vs startLiveRefresh's immediate reload racing on open, delete's select-near-neighbor vs a concurrent keystroke) by hand to confirm no regression — no App-level test target exists for this file (established precedent, D18-D22, T43) so this is build+lint+manual-trace verified, not unit-tested; flagged, not silently skipped. Deliberately did NOT touch the second, compounding cause found during root-causing: `ClipStore.fetchAll` has no result cap and re-fetches the *entire* history table every ~0.4s poll — capping that (or defaulting T32's retention cap on) is a product/perf trade-off (how far back History can search), not a pure bugfix; flagging as a follow-up for architect/PM rather than changing unilaterally. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, zero warnings on the touched file. `swift test` → 131/131 unchanged (App-layer-only change, zero `ClipnestCore` touched). `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. Decision + benchmark numbers recorded as D23 in project-context.md. `git add` done on the one touched file, no commit. → board:review (my ceiling here — no reviewer/tester pass run this turn); handing to reviewer then tester next. Runtime-only check (typing in search genuinely feels instant/no-lag now, especially after a large paste; selection/highlight still correct across rapid typing, tab switches, and delete-during-search) still needed from the human — the benchmark proves the mechanism, not the on-screen feel.
- 2026-08-10 [T48] Follow-up to T46: user asked to "apply the same button design to the pin, save and delete buttons as well" — i.e. extend the expanding-icon-button hover interaction from the type-filter chips to the picker row actions. Migrated all 5 remaining call sites of the old, now-deleted `RowActionButton` (`ItemRow.swift`: pin/unpin, save-as-snippet, delete; `SnippetRow.swift`: edit, delete) to the shared `ExpandingIconButton` built for T46 — reused as-is, no new parameters needed. Deliberate design call: `isActive` stays at its default `false` for every row action (none is a "selected among alternatives" control the way a filter chip is); pinned state keeps communicating itself exactly as before, via the icon glyph (`pin`/`pin.fill`) + dynamic title ("Pin"/"Unpin"), not via `ExpandingIconButton`'s active-tint/background styling — avoided adding new styling knobs to the shared component or new semantics beyond what was asked. `RowActionButton` struct deleted from `ItemRow.swift` (its only other caller, `SnippetRow.swift`, migrated in the same pass) — one component doing this job now, not two, per DRY. `ItemRow` can have up to 3 `ExpandingIconButton`s in one row; verified `isHovering` is already per-instance `@State` (T46's design), so no extra coordination needed — only the actually-hovered button expands, matching the header chips' behavior. Row height reasoning: `ExpandingIconButton` pins its own height to a fixed 22pt; the row's two-line text block (title + relative-timestamp caption) already determines row height and is taller than 22pt, so no row-height regression expected — flagged as a runtime-only check regardless, same as every prior visual claim. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 131/131 unchanged (App-layer-only, zero `ClipnestCore` touched). `swift format lint --recursive --strict ClipnestApp/Sources` clean. Decision recorded as D24 in project-context.md. `git add` done on the 3 touched files (`ExpandingIconButton.swift`, `ItemRow.swift`, `SnippetRow.swift`), no commit. → board:review (my ceiling here — no reviewer/tester pass run this turn); handing to reviewer then tester next. Runtime-only check (hover-expand on pin/save/delete/edit feels smooth and reads clearly at row width; no clipping/wrap in the text block when a button expands; multiple buttons in the same row don't visually collide; row height genuinely unchanged) still needed from the human.
- 2026-08-10 [T47 review-fix] Addressed reviewer's `perf-review.md` findings on T47's search-perf fix, same one file (`PickerViewModel.swift`), no scope creep. Finding 1 (Important, blocking): commit actions (Return/⌘P/⌘S/⌘⌫) could act on a row up to ~120ms stale relative to the just-typed query during the debounce window — wrong-paste/wrong-delete hazard. Fixed with new `flushPendingFilter()` (cancels any pending debounced `filterTask`, synchronously recomputes `items`/selection from the current query via new `applyFilterSynchronously(_:)`), called at the top of `selectHighlighted()`/`togglePinHighlighted()`/`saveHighlightedAsSnippet()`/`deleteHighlighted()`. Finding 2 (Important): non-search updates (tab switch/live-refresh poll/post-mutation reload) still routed through `Task.detached` even though "not debounced," so they could flash stale `items` for a render; `scheduleFilterUpdate(_:debounced:)` now branches — `debounced: false` calls `applyFilterSynchronously` inline on @MainActor with no async hop at all; only search-text typing still debounces+detaches. Finding 3 (Minor): `filterTask` now nil'd out after landing/cancelling everywhere (`didHide()`, the debounced closure's completion, `scheduleFilterUpdate`'s own top), mirroring `refreshTask`. Finding 4 (Minor): added `guard oldValue != query else { return }` to `query`'s `didSet`, mirroring `activeTab`'s existing guard (`SearchQuery` already `Equatable`). Left the Snippets `snippets` computed property alone per the review's own "out of scope" note. Re-validated: `xcodegen generate` + clean `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, grepped the full clean-build log for `PickerViewModel` warnings/errors — zero matches. `swift test` → 131/131 unchanged. `swift format lint --recursive --strict ClipnestApp/Sources` and `Sources Tests` both clean. Hand-traced all 4 re-trace scenarios from the fix spec (rapid-typing-then-instant-commit now flushes correctly; tab switch/poll/reload no longer async; latest-request-wins for typing unaffected; filterTask never left dangling). Fix details appended as a "Fixes" section in `.superpowers/sdd/2026-08-06-clipnest-plan/perf-review.md`. `git add` done on the one touched file (`ClipnestApp/Sources/UI/Picker/PickerViewModel.swift`), no commit — confirmed via `git diff --stat` this is the only substantive change. → board:review (T47 stays review, my ceiling — no reviewer/tester pass run this turn; sending back to reviewer to confirm findings resolved, then tester). Not spawned by anyone this session — writing the board directly.
- 2026-08-10 [T35/T36/T37] User asked to "wind this up, create docs, build, release pipelines." Recognized this as devops-owned work already fully planned (Phase E, T35-T39) — not something to force through the senior-dev hat myself. Spawned devops with the exact plan spec (docs/superpowers/plans/2026-08-06-clipnest-plan.md Phase E) plus the environment reality I checked first (0 codesigning identities present, create-dmg not installed, shellcheck available). Devops built all 4 scripts (`scripts/{build,sign,notarize,package_dmg}.sh`), extended `ClipnestApp/project.yml` (Release signing config, parameterized) and `README.md` (new Release section), added `.gitignore` (`build/`). Did my own first-gate code-quality check rather than trusting the summary: read all 4 scripts in full, independently re-ran `shellcheck scripts/*.sh` (clean), grepped every touched file for credential-shaped strings myself (clean — only doc placeholders), confirmed identity/profile are always `${1:-${VAR:-}}` (arg-or-env, never literal) by direct read, independently re-ran `build.sh` (`** ARCHIVE SUCCEEDED **`, real `build/Clipnest.app`), `sign.sh "-"` (ad-hoc identity — no real cert in this sandbox — signed + verified + `spctl` correctly rejected pre-notarization), and `package_dmg.sh` (real `build/Clipnest.dmg`, mounted + inspected, correct drag-to-Applications structure). Also independently verified devops's `--skip-jenkins` claim wasn't fabricated (`create-dmg --help` confirms it's real). Real Developer ID sign + real notarization can't complete in this sandbox (0 valid identities, no Apple Developer account) — devops proved the scripts are correct by triggering Apple's own real credential-rejection errors rather than a script bug; stated explicitly per the plan's own Phase E framing ("requires the user's own Apple Developer account"), not glossed over. → board:review for T35/T36/T37. This triggers integrity rule 3's mandatory security pass (scripts touch credentials/external I/O) — spawning reviewer next for the independent second gate before tester. Not spawned by anyone this session — writing the board directly.
- 2026-08-10 [T38/T39 + Phase D audit] Reviewer APPROVE'd Phase E (T38): independent security pass CLEAR (no hardcoded credentials, arg-or-env only, entitlements untouched, `.gitignore build/` verified to actually cover the one file that would embed a Team ID), correctness PASS (shellcheck clean, chain composes, matches plan verbatim), 2 non-blocking nits (spec-mandated `codesign --deep`, minor export/sign redundancy) — neither blocking. Spawned tester (T39) with an explicit brief not to expand scope into Phase D even if they found the same gap I'd already found. Tester independently re-ran the full chain from a clean `rm -rf build/` state end-to-end with real pasted output: `swift test` 131/131, `build.sh`→`sign.sh "-"`→`package_dmg.sh` all real, DMG mounted+verified (Clipnest.app + Applications symlink), `notarize.sh`'s no-argument failure path and signature-check-before-submit guard confirmed, shellcheck clean, and — new this round — actually launched `build/Clipnest.app` for real and confirmed it ran 5+ minutes with zero crash-log entries before quitting cleanly (menu bar icon glyph itself unconfirmable — no display access in this sandbox, same category as D10/D13/D14, not a new gap). Debug App-target rebuild confirmed the Release-only project.yml change didn't regress the everyday dev build. Verdict: T35/T36/T37/T38 → `done` (tester's call, evidence pasted); T39 → `blocked`, not `done` — its own literal AC depends on T34 (Phase D tester validation), and Phase D is genuinely incomplete. Separate real finding, surfaced while auditing ahead of T39: the task board doesn't fully reflect the source tree — T27 (HotkeyManager) is fully built and wired (`AppEnvironment.swift:178`) but the board showed it `todo`; fixed that entry with evidence. Spot-checked (not exhaustively re-audited) T28-T31/T33/T34 and confirmed those genuinely are unbuilt (no Settings UI folder exists, `ClipboardMonitor.excludedBundleIDsProvider` still defaults to an empty closure). Did not attempt to build Phase D myself this turn — out of scope for "wind this up, create docs, build, release pipelines," and a board/reality reconciliation + Settings UI build is its own sizable body of work that needs its own plan, not something to silently absorb here. Also wrote my own docs slice: new `docs/API.md` — full `ClipnestCore` public API reference (every public type/protocol/function/error across Model/Store/Clipboard/Search/Paste, plus a working end-to-end capture→search→paste example, verified for actual Swift correctness — fixed a nested-`try` ambiguity and a missing `import AppKit` I caught on my own re-read before treating it as done), cross-linked from README.md's "How it's built" section so it's actually discoverable. Decision recorded as D27 in project-context.md (Phase E completion + the T27/Phase D board-vs-reality discrepancy). `git add` done on all touched files (`docs/API.md`, `README.md`'s new link, `.claude/task-board.md`, `.claude/project-context.md`, `.claude/logs/senior-dev.md`), no commit. → board updated directly (T35-T38 done per tester's evidence, T39 blocked, T27 review) — not spawned by anyone this session.
- 2026-08-11 [T49/T50] Orchestrating the DB-virtualization change (move all list filtering/sorting/paging into the store; PickerViewModel becomes windowed/DB-driven). Split into T49 (ClipStore.query) + T50 (SnippetStore.query), delegated in parallel to two junior-devs on disjoint files (Store/ClipStore*.swift+tests vs Store/SnippetStore*.swift+tests) — safe since no shared-file writes, per the parallel-devs-on-disjoint-files rule. Both given exact protocol signatures, `#Predicate`/`FetchDescriptor` code, the `normalizedText`-on-private-@Model-record normalization strategy, and a pre-resolved `#Index` (macOS 15+ only, skip) finding so neither had to re-derive it. Reviewed both diffs in full myself (first-gate code-quality check): T49's protocol/InMemory/SwiftData impls match spec exactly; its one deviation (a `#Predicate` 3-way-`&&`-with-optional hit a real Swift compiler type-check timeout, fixed by precomputing `hasKind: Bool` + non-optional `kindRawValue = kind?.rawValue ?? ""`) is sound and equivalent, accepted. T50 self-caught and fixed a real bug during its own testing — `import Foundation` shadows stdlib `String.contains(_:)` with a bridged overload where `"x".contains("")` returns `false`, so `InMemorySnippetStore.query` needed the same `!hasText ||` short-circuit its SwiftData sibling's `#Predicate` already had; documented inline. Re-ran the full suite myself after both landed: `swift build` clean, `swift test` → 168/168 (131 baseline + 20 ClipStore.query tests + 17 SnippetStore.query tests, no removals yet — SearchFilter/SnippetSearchFilter tests are removed in T51, once PickerViewModel stops using them), `swift format lint --recursive --strict Sources Tests` clean. `git add` confirmed staged for all touched files. Board updated (T49/T50 → review, evidence pasted). Proceeding to T51 (PickerViewModel windowed rewrite + ClipboardMonitor.onCapture hook + AppEnvironment wiring) myself — the hard, high-risk core of this change, no App-level test target exists so it's validated by clean `xcodebuild` + careful review rather than `swift test`. Not spawned by anyone this session — writing the board directly.
- 2026-08-11 [T51/T52] Built T51 myself (PickerViewModel windowed/DB-driven rewrite — the hard core of the DB-virtualization change): rows/snippetRows replace allItems/items/allSnippets/snippets, removed the ~0.4s Timer poll entirely, added a generation-counter-per-pipeline for latest-request-wins under concurrent async triggers, flush-before-commit re-implemented for the async pipeline (bounded by store latency not the 400ms debounce tail), added ClipboardMonitor.onCapture hook (+3 tests) wired through AppEnvironment for push-based live-capture updates, deleted now-dead SearchFilter/SnippetSearchFilter + 11 tests, cleaned every stale doc-comment reference to them. Caught and closed one real race in my own self-review before handoff: loadMoreRows/loadMoreSnippets now also bail while a first-page requery is pending, closing a window where a load-more could append a page fetched against a stale pre-keystroke offset. Delegated T52 (mechanical PickerView.swift rename + ScrollResettingList.onReachEnd wiring) to junior-dev in parallel with my own review pass of T51 — junior-dev's diff exactly matched spec and it independently caught + flagged the same lint issue in my file that I'd already found and fixed (good cross-check). Full validation after both landed: `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`; `swift test` → 160/160 (171 baseline − 11 removed SearchFilter/SnippetSearchFilter tests); `swift format lint --recursive --strict` clean on both `Sources Tests` and `ClipnestApp/Sources`. `git add` done on every touched/deleted file (including recovering a board-edit slip where an earlier edit accidentally dropped the T51/T52 lines — caught immediately via grep, fixed same turn). Confirmed: all list filtering/sorting/paging now happens in the store (ClipStore.query/SnippetStore.query), zero in-memory full-dataset filter remains in PickerViewModel, debounce is 400ms (search-text only), the 0.4s full-refetch poll is gone (replaced by one-shot willShow() load + push-based ClipboardMonitor.onCapture). Wrote the design/verification report at `.superpowers/sdd/2026-08-06-clipnest-plan/db-virtualization-report.md`. Board updated (T49-T52 → test, T51/T52 evidence pasted). → spawning tester next to independently validate all 4 tasks together; runtime-only checks (search feel, live-append, scroll smoothness, commit-acts-on-correct-row) explicitly deferred to the human per this task's scope guardrails — not relaunching the app myself.
- 2026-08-11 [T49-T52 tester validation] Spawned tester to independently re-verify all 4 tasks together. Verdict: PASS, no defects — tester re-ran every command itself (not trusting my report's claims): swift build clean, swift test 160/160 (independently confirmed the 131+20+17+3−11 arithmetic), xcodegen+xcodebuild BUILD SUCCEEDED with zero warnings/errors grepped, both lint sweeps clean, spot-read the new query test suites for real coverage (not just count), confirmed #Index-skip doc comments + macOS 14 deployment target genuinely unchanged in Package.swift/project.yml, read PickerViewModel.swift in full and independently confirmed by code inspection (no allItems/allSnippets left, 400ms debounce with correct kind-only-immediate branching, zero Timer/startLiveRefresh remaining, loadMoreIfNeeded tab-aware, all 4 commit actions flush before reading highlighted row), confirmed AppEnvironment's onCapture wiring by grep, confirmed every touched file is staged and zero commits exist. Correctly declined to attempt anything runtime-only (app not launched, per scope) and listed exactly what's left for the human. Full evidence: `.claude/logs/tester.md`. Board updated: T49/T50 review→test, T51/T52 test confirmed, all four carry tester's pasted evidence. Per this codebase's established convention (see T43's board entry) and integrity rule 2, my ceiling as the board-writer stays at `status:test` even after a tester PASS — tester owns the actual `done` flip, not me. → task complete from my side: all 4 tasks built/reviewed/tested, report + board + logs all current, git add done throughout, zero commits made. Handing back to whoever spawned this session (or the human controller) for the done flip + the runtime/relaunch verification this task's own scope explicitly reserved for them.
- 2026-08-11 [T53 — critical launch-crash fix] Confirmed from a real runtime log: T49/T50's `normalizedText` (added non-optional, no default) failed SwiftData lightweight migration of the user's real, intact on-disk store (`NSCocoaErrorDomain 134110`) — `ModelContainer` construction threw, `AppEnvironment.init` propagated it, `AppDelegate` terminated on every launch. Focused single-owner fix, no delegation (critical/small in scope, built directly). Fix, two files: `ClipItemRecord.normalizedText`/`SnippetRecord.normalizedText` → `String = ""` (SwiftData only auto-migrates a new non-optional attribute if it's defaulted — mirrors Core Data's lightweight-migration rule); both stores' `init` now run a new private static `backfillNormalizedText(in:)` against their `ModelContext` before assigning `self.*` — fetches rows where `normalizedText == ""` and the source text isn't (bounded `#Predicate`, not a full scan), sets it to the exact same derivation new inserts already use (`previewText.lowercased()` / `(title + " " + body).lowercased()`), one batched `save()`; runs every launch but only does real work once; failures logged (metadata only) and swallowed, never thrown — a backfill hiccup must not reintroduce the crash. Confirmed (read, unchanged) new inserts/updates on both records still always set `normalizedText`. 4 new tests (2 per store): (1) in-memory — a record inserted with an explicit empty `normalizedText` (new `insertRecordWithEmptyNormalizedTextForTesting` test-only factory, needed since the `@Model` records are file-private) is backfilled by a fresh `init` and becomes matchable by `query`; (2) genuine on-disk migration — a test-local `@Model` type sharing the production record's *exact simple name* (SwiftData names Core Data entities after the simple type name, not the module) but the pre-fix schema (no `normalizedText`) writes to a real file `URL`; the production type (new `makeContainerForTesting(at:)`) then reopens that file — proves the real migration succeeds without throwing and the row gets backfilled. Found + fixed a real flake during validation: the same-named-type migration test raced with sibling tests in its own suite under Swift Testing's default parallelism (two structurally different same-named `@Model` types resolved concurrently corrupted unrelated query results) — fixed by adding `.serialized` to both `@Suite` declarations (`SwiftDataClipStoreTests`/`SwiftDataSnippetStoreTests` only); confirmed stable across 10 consecutive full `swift test` runs after. `swift build` clean. `swift test` → **164/164** (160 + 4 new). `swift format lint --recursive --strict Sources Tests` and `ClipnestApp/Sources` both clean. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `git add` done on exactly the 4 touched files (`Sources/ClipnestCore/Store/{SwiftDataClipStore,SwiftDataSnippetStore}.swift`, `Tests/ClipnestCoreTests/{SwiftDataClipStoreTests,SwiftDataSnippetStoreTests}.swift`), no commit — confirmed via `git status --short` nothing else was staged. App deliberately NOT launched/relaunched, per explicit instruction — the controller relaunches against the real existing store to confirm the crash is gone. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/migration-fix-report.md`. → board:review (new T53, my ceiling — no reviewer/tester pass run this turn); handing to reviewer then tester next. Not spawned by anyone this session — writing the board directly.
- 2026-08-11 [T54 — search-box first-responder race] Fixed the reported "picker search box registers zero typed characters" bug, root-caused by tester's own critique (`search-input-critique.md`, code-analysis only — confirmed accurate on read, no re-derivation needed): T50/T51 made every row/snippet query async, so `List(selection:)`'s programmatic-write side effect (`NSTableView` claiming first-responder status inside `PickerPanel`) now lands after the `await` instead of before it, landing *after* the search field's own `.focused()` request and silently stealing keyboard focus on open + again ~400ms after every keystroke. Focused single-file fix, no delegation. Reused the pre-existing `focusToken`/`refocusSearchField()` mechanism (confirmed `PickerView.swift` already wires `.onChange(of: viewModel.focusToken)` correctly — read, left untouched): new private `reassertSearchFieldFocus()` bumps `focusToken`, called at the tail of both `applyRowsSelectionPolicy`/`applySnippetsSelectionPolicy` in `PickerViewModel.swift` — the two methods every async selection-write path (first-page requery: open/debounced-search/tab-switch/kind-filter/live-capture; post-mutation refresh: pin/delete/snippet CRUD) already funnels through, so one change point closes the race everywhere. Verified by hand-trace this can't trap focus on a genuine user action: manual row click and `moveSelection(by:)` (arrow keys) both write `selectedItemID`/`selectedSnippetID` directly, never through these methods, so the bump never overrides a deliberate selection; `SnippetEditorWindow` is a separate real `NSWindow` (confirmed by read), so the bump — which only re-asserts first-responder *within* `PickerPanel` — can never pull key status away from it while it's open, including from the now-also-refocusing post-save `refreshSnippetsWindow` (its resolution always lands after `SnippetEditorWindow.onSave` already called `close()` synchronously) and from a live capture landing mid-edit. `refocusSearchField()` now delegates to the same helper instead of duplicating `focusToken += 1` (DRY). No new test coverage possible (no App-level test target exists — D18-D22/T51/T53 precedent, reconfirmed; runtime first-responder behavior is AppKit/window-server-only, unobservable headlessly, same blocker the critique itself hit trying to script it via `osascript`) — flagged explicitly, verified instead by full call-site hand-trace + clean build/lint. `swift build` clean, `swift test` → **164/164 unchanged** (pure App-layer change, zero `ClipnestCore` touched). `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `git add` done on exactly the 1 touched file (`ClipnestApp/Sources/UI/Picker/PickerViewModel.swift`) — confirmed via `git status --short`/`git diff --stat`, no commit. App deliberately NOT launched/relaunched, per explicit instruction — the controller relaunches via `open` and the human verifies typing works. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/search-focus-fix-report.md`. Decision: D29. → board:review (new T54, my ceiling — no reviewer/tester pass run this turn); handing to reviewer then tester next. Not spawned by anyone this session — writing the board directly.
- 2026-08-11 [T55 — search debounce + loader] Fixed the reported picker-search-lags-behind-typing bug: even after D29/T54's focus-race fix, the search `TextField` still bound straight to `viewModel.query.text`, and `query`'s `didSet` drove the debounced store query — so a keystroke only appeared once its async DB round-trip resolved (up to 400ms + store latency), the standard "bound the input to the debounce" anti-pattern. Retargeted to the standard pattern: `PickerView` gained a local `@State searchText` the field binds directly (zero async in the input path, always instant); `.onChange(of: searchText)` calls new `PickerViewModel.searchTextChanged(_:)`, which records it into new `currentSearchText`, flips new `isSearching` on, and (re)starts the existing 400ms debounce via a new shared `runActiveTabQuery(policy:debounced:)` dispatcher (reused T50/T51's debounce/generation-counter machinery, extended with an explicit `text:` param instead of always reading `query.text`). `query.text` repurposed to mean "the text a query actually ran for" — written in exactly one place (once `runRowsFirstPageQuery`/`runSnippetsFirstPageQuery` lands) — so `ItemRow`/`SnippetRow` highlighting and the empty-state message (unchanged call sites) always match what's shown, never a keystroke in flight; `query`'s `didSet` now only reacts to `kindFilter`. New `isSearching` drives a centered `ProgressView()` loader in `PickerView.content`, replacing list/empty-state while a search-driven query (typing/tab-switch/kind-filter) is in flight — deliberately not flipped for live-capture/mutation refreshes (out of scope, already have non-jarring selection policies). Self-review caught + closed two real races before reviewer: (1) `willShow()`'s `currentSearchText` reset lands synchronously but `PickerView`'s own `searchText` reset (new `searchResetToken`) lands one SwiftUI cycle later and would otherwise re-fire `searchTextChanged` and flash the loader on every reopen-after-a-search — closed with a `text != currentSearchText` guard; (2) tab switches only cancelled the newly-active pipeline's task, leaving a stray debounce on the abandoned tab free to wake later and prematurely clear `isSearching` for the tab now on screen — closed by having `runActiveTabQuery` cancel the inactive pipeline's task too. `flushPendingRowsQuery`/`flushPendingSnippetsQuery`/`handleNewCapture` updated to read `currentSearchText` (the live field text) instead of the now-lagging `query.text`. Focus behavior (D29/T54) untouched — typing being local `@State` makes it structurally impossible for the async pipeline to interfere with keystroke acceptance at all. Store layer/pagination/live-capture/mutations functionally unchanged, confirmed by keeping every non-search-input call site's text source exactly as before. No App-level test target exists for `ClipnestApp`/`PickerViewModel` (established precedent — D18-D22/T51/T53/T54, reconfirmed) — verified instead by a full hand-trace of every trigger's `isSearching`/`query.text`/`currentSearchText` transitions (including the two races) plus clean build/lint. `swift build` clean, `swift test` → **164/164 unchanged** (pure App-layer change, zero `ClipnestCore` touched). `swift format lint --recursive --strict ClipnestApp/Sources Sources Tests` clean. `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `git add` done on exactly the 2 touched production files (`PickerView.swift`, `PickerViewModel.swift`) plus the report + `project-context.md` D30 — confirmed via `git status --short`, no commit. App deliberately NOT launched/relaunched, per explicit instruction — the controller relaunches via `open` and the human verifies instant typing + loader feel (headless shell has no Accessibility grant to script keystrokes). Report: `.superpowers/sdd/2026-08-06-clipnest-plan/search-debounce-loader-report.md`. Decision: D30. → board:review (new T55, my ceiling — no reviewer/tester pass run this turn); handing to reviewer then tester next. Not spawned by anyone this session — writing the board directly.
- 2026-08-11 [T56 — snippet editor window overlaps picker] Fixed by placing `SnippetEditorWindow` beside `PickerPanel`'s current frame (right → left → below/above, top-aligned, 12pt gap, first candidate fully inside the picker's screen `visibleFrame` wins, else falls back to below+clamp) instead of `center()`; falls back to the original centered placement whenever the picker isn't visible/on-screen. New `ClipnestApp/Sources/UI/Picker/WindowPlacement.swift` holds the pure geometry (`clampedOrigin`, `besideAnchor` — AppKit-free, `CGRect`/`CGPoint`/`CGSize` only); `PickerPanel`'s own private `clampedOrigin` (a real second occurrence of the identical formula) now delegates to it instead of duplicating (DRY). `SnippetEditorWindow.show(...)` gained `pickerPanel: PickerPanel?`; `AppEnvironment.swift`'s one call site passes the already-weakly-captured `panel`. Size/activation/key-window behavior untouched — only `setFrameOrigin` changed. `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 164/164 unchanged (pure App-layer/AppKit change). `swift format lint --recursive --strict ClipnestApp/Sources` clean. No App-level test target exists (D18-D22/T51/T53/T54/D29/D30 precedent) — window placement here is human-visual-check by established convention, matching this task's own instruction; new math kept pure/deterministic for future testability regardless. `git add` done on exactly the 4 touched/created files, no commit. App deliberately NOT relaunched per instruction — human verifies no-overlap visually. Report: `.superpowers/sdd/2026-08-06-clipnest-plan/snippet-window-placement-report.md`. Decision: D31. → board:review (new T56, my ceiling — no reviewer/tester pass run this turn); handing to reviewer then tester next. Not spawned by anyone this session — writing the board directly.
- 2026-08-11 [T56 round 2 — same-left-edge overlap] Coordinator relayed a real user screenshot: my round-1 fix's below/above fallback had fired (picker positioned with no room on either side for the 380pt editor), producing the exact same-left-edge stacked overlap the fix was meant to prevent. Root cause: below/above reused the picker's own minX. Fix: dropped the below/above fallback entirely; new `WindowPlacement.pairLayout(pickerFrame:editorSize:screenVisibleFrame:gap:)` replaces `besideAnchor` — treats picker+editor as one side-by-side pair (`pairWidth = pickerW+gap+editorW`); if the pair fits the screen's visibleFrame width, shifts the picker left only as far as needed (never right/resized) so the editor always lands strictly right of it (zero overlap, distinct left edge by construction); only in a pathological can't-fit-the-screen-width case does it allow overlap, with a 1pt-nudge guard so left edges still never coincide. `SnippetEditorWindow.positionRelativeToPicker(_:)` now sets BOTH frames (`pickerPanel.setFrameOrigin` + its own), picker repositioned never resized, position not restored on close (per explicit instruction not to over-engineer that). `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → 164/164 unchanged. `swift format lint --recursive --strict ClipnestApp/Sources` clean (needed one `swift format format --in-place` auto-wrap for a line-length violation, rebuilt/retested clean after). `git add` done on the 3 files touched this round (`WindowPlacement.swift`, `SnippetEditorWindow.swift`, `AppEnvironment.swift`), no commit. App NOT relaunched — human re-verifies visually, including the screenshot scenario. Report + board (T56) + project-context (D31, rewritten in place since round 1 never left review) all updated to reflect the shipped round-2 design. → still board:review, same task, ceiling unchanged; handing to reviewer then tester.
- 2026-08-13 [T57 — PickerViewModel DRY refactor] Extracted the duplicated Rows/Snippets paged-query pipelines (`implementation-review.md` finding #1, ~250 duplicated lines across 12 methods) into new `ClipnestApp/Sources/UI/Picker/PagedQuery.swift` (`@MainActor final class PagedQuery<Element>` owning `PageState`/generation-counter/task-lifecycle — `scheduleFirstPage`/`flushPending`/`refreshWindow`/`loadMore`/`cancelAll`/`cancelPendingQuery`); `PickerViewModel` now holds `rowsQuery`/`snippetsQuery` instances, its 12 original methods collapsed to thin wrappers supplying fetch/setItems/applySelection/onLanded as call-site closures (not init-stored, to preserve the existing snapshot-kind/tab-at-schedule-time semantics). Behavior-preserving, no automated coverage exists for this file (established precedent) — verified via full-file re-read + line-by-line parity check against the original instead. `rows`/`snippetRows` deliberately stay `@Published private(set)` on `PickerViewModel` (confirmed `PickerView.swift` reads them directly) rather than moving into the helper, to avoid silently breaking SwiftUI observation. Hit one real compile error (`Self.pageSize` illegal in a stored-property initializer — "covariant Self type cannot be referenced"), fixed with explicit `PickerViewModel.pageSize`. `xcodegen generate` + `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED**, zero warnings on touched files. `swift build && swift test` → **176/176 unchanged** (Core untouched). `swift format lint --recursive --strict ClipnestApp/Sources` clean (one `--in-place` reformat round for line-length, rebuilt/retested clean after). `PickerViewModel.swift` 1258→1218 lines; new `PagedQuery.swift` 231 lines. `git add` done on exactly the 2 touched/created files (`PickerViewModel.swift`, `PagedQuery.swift`), no commit; `.xcodeproj` confirmed gitignored. Decision: D32. New T57, board + project-context.md written directly (not spawned this session, routed request). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-14 [T58 — corrupt-store recovery] Fixed architecture-review finding: `SwiftDataClipStore`/`SwiftDataSnippetStore` `makeProductionContainer()` had no recovery from a damaged on-disk store — `ModelContainer(...)` throwing propagated straight through `AppEnvironment.init` into an unrecoverable `AppDelegate` launch crash. New shared internal `Sources/ClipnestCore/Store/ModelContainerRecovery.swift` (`ModelContainerRecovery.openWithRecovery(storeURL:logger:fileManager:makeContainer:)`, DRY — mirrors the existing shared `deleteBlobs` free function pattern): tries the caller's `makeContainer` closure once (a legitimately-migratable store, e.g. pre-`normalizedText`, succeeds here — migration runs inside this call, so it never reaches recovery); on throw, backs up `storeURL` + its `-wal`/`-shm` sidecars to `<name>.corrupt-<ms-epoch>` siblings, logs metadata-only (backup filename, never content) via the caller's own categorized `Logger`, then retries once against a now-fresh store at the same URL; only a second failure propagates. Both stores' `makeProductionContainer()` route through it with an unchanged public signature (`() throws -> ModelContainer`) — zero `AppEnvironment`/`AppDelegate` changes needed (confirmed via grep, one call site each). Added test-only `makeRecoveringContainerForTesting(at:)` to each store (recovery-path twin of the existing recovery-free `makeContainerForTesting(at:)`, kept deliberately separate so the pre-`normalizedText` migration tests keep proving upgrade-not-wipe). 4 new tests (2/store): garbage-bytes store file recovers to a fresh/empty/writable container; recovery backs up (not deletes) the original bytes, verified byte-for-byte. Confirmed empirically garbage bytes really do throw (`NSCocoaErrorDomain 259`/`NSSQLiteErrorDomain 26`) — not a mocked failure. `swift build` clean. `swift test` → **180/180** (176 baseline + 4 new; both migration tests still pass unchanged). `swift format lint --recursive --strict Sources Tests` clean. `git add` done on exactly the 5 touched/created files (`ModelContainerRecovery.swift`, `SwiftDataClipStore.swift`, `SwiftDataSnippetStore.swift`, `SwiftDataClipStoreTests.swift`, `SwiftDataSnippetStoreTests.swift`) — confirmed via `git status --porcelain`, no commit. Decision: D33. New T58, board + project-context.md written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-14 [T59 — ClipnestAppTests target] Closed the "zero App-level test coverage" gap both `architecture-review.md` (Critical) and `implementation-review.md` (Medium) flagged, and that D18-D33's precedent chain kept re-noting task after task. New `ClipnestAppTests` XcodeGen target (Swift Testing, `ClipnestApp/project.yml`) hosted inside `ClipnestApp` — hit and fixed two real environment gotchas along the way, both documented inline for future test files: (1) XcodeGen's auto-derived `TEST_HOST` uses the *target* name, but `PRODUCT_NAME` is `Clipnest` (D9), so it pointed at a `.app` that's never produced — set `TEST_HOST`/`BUNDLE_LOADER` explicitly instead; (2) the compiled Swift module is therefore also named `Clipnest`, not `ClipnestApp` — `@testable import ClipnestApp` fails to resolve, every test file needs `@testable import Clipnest`. One production-code change: `PickerViewModel.pasteContent(for:plainText:)`/`resolvedSelection(...)` widened `private`→internal (doc comments only otherwise) — `@testable import` can never reach `private`, and both are the exact pure decision-logic methods the reviews named as untested. 44 new tests, all passing: `WindowPlacementTests.swift` (9 — `clampedOrigin`/`pairLayout`, every branch hand-computed including a multi-screen negative-coordinate case and the pathological nudge case), `ItemKind+SFSymbolTests.swift` (7 — already `internal`, no visibility change needed), `PickerViewModelTests.swift` (28 — 15 `pasteContent` cases across every `ItemKind`×`plainText` combo incl. missing-blob/legacy fallbacks, 12 `resolvedSelection` cases across every `SelectionPolicy` case incl. both-direction out-of-bounds clamping, 1 async test driving `willShow()`+tab switches against a real `InMemoryClipStore` with a bounded-`Task.yield()` `waitUntil` helper — never a fixed `sleep` — proving the History/Pinned→`ClipScope` mapping, the one piece not reachable as a pure sync call). New `TestSupport/` fakes (`FakePasteboardWriting`/`FakeEventSynthesizing`/`FakeFrontmostAppReferenceProviding`/`TempBlobStore`/`AsyncWaiting`/`ClipItemFixtures`/`PickerViewModelFactory`) — zero real `NSPasteboard`/`NSWorkspace`/`CGEvent` touched anywhere, per coding-standards.md. Deliberately not tested, flagged not skipped: SwiftUI rendering, real pasteboard writes, hotkey registration, the synthesized-⌘V path itself (covered already by `ClipnestCoreTests/PasterTests.swift`); `HighlightedText.attributedString` is `private` and judged not worth widening (rendering helper, not decision logic). `xcodegen generate` clean; `xcodebuild -project ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` → **TEST SUCCEEDED, 44/44 tests, 5 suites**. Root `swift build && swift test` → **180/180 unchanged** (zero `ClipnestCore` files touched). `swift format lint --recursive --strict ClipnestApp/Sources ClipnestApp/Tests` and `Sources Tests` both clean. `git add` done on `project.yml`, the one production file, and every new file under `ClipnestApp/Tests/ClipnestAppTests/` — confirmed the regenerated `.xcodeproj` stayed gitignored via `git status`, no commit; confirmed (via `git diff --cached`/`git diff`) that other agents' concurrently-staged/unstaged work in this shared tree (T58's Core files, in-flight `.claude/*` edits) was left completely untouched — only my own files staged. Decision: D34. New T59, board + project-context.md written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-14 [T60 — self-update from picker footer] Approved-feature build per exact routed spec (files/anchors given). New `ClipnestApp/Sources/System/AppUpdater.swift` — stateless `enum AppUpdater`: `currentVersion` (`Bundle.main.infoDictionary`, no force-unwrap), `updateCommand` (`"curl -fsSL <update.sh URL> | bash"`, unit-testable independent of the side effect), `runUpdate()` writes a throwaway `0o755` `.command` file to `FileManager.default.temporaryDirectory` and hands it to `NSWorkspace.shared.open(_:)` — runs in Terminal as an independent process (survives Clipnest quitting/relaunching, no Automation permission needed), falls back to opening the GitHub releases page via `if let` (no force-unwrap) if writing/opening throws. Per coding-standards.md's "Local-only, always" rule, zero `URLSession`/sockets in app code — it shells out to the exact same public `scripts/update.sh` curl one-liner (URL verified against that script's own documented Usage comment) a user could run by hand. Two new injected members on `PickerViewModel` (`appVersion`/`requestAppUpdate`), same closure-injection pattern as `dismiss`/`presentSnippetEditor`/`updatePreview` — initializer signature untouched (existing `PickerViewModelFactory`/tests unaffected); wired in `AppEnvironment.init` beside `viewModel.dismiss = ...`. `PickerView.shortcutHintBar` reworked into an `HStack` (hints leading, `Spacer(minLength: 8)`, trailing `Button("v\(appVersion)")`, `.buttonStyle(.plain)`, `.help(...)`) driving a new `.confirmationDialog` via `@State private var showUpdateConfirm`; `PickerView` still never touches AppKit/`NSPanel` directly (confirmed by grep, matches file's own top doc comment). New `AppUpdaterTests.swift` (2 tests: `updateCommand` contains the raw URL + `curl`/`| bash`; `currentVersion` non-empty) — `runUpdate()` deliberately not called from tests (real side effect, opens Terminal), same precedent as `PermissionsManager`/`HotkeyManager`. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED**. `xcodebuild test ... -destination 'platform=macOS'` → **TEST SUCCEEDED, 46/46 tests, 6 suites** (44 baseline + 2 new). Root `swift test` → **180/180 unchanged** (zero `ClipnestCore` touched). `swift format lint --recursive --strict ClipnestApp/Sources ClipnestApp/Tests` clean. `git add` done on exactly the 5 touched/created files (`AppUpdater.swift`, `PickerViewModel.swift`, `AppEnvironment.swift`, `PickerView.swift`, `AppUpdaterTests.swift`), no commit; `.xcodeproj` confirmed gitignored. **Flagged concern (per the routing brief, not silently assumed fine):** `PickerPanel` is a non-activating `NSPanel` (D11) — whether `.confirmationDialog` actually presents/is interactable from a non-key host window needs a real-run check, not verifiable headlessly; implemented exactly as specified regardless. New T60, board written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-14 [T61 — install.sh/update.sh silent-mount-failure bug] Root-caused and fixed a real user-impacting bug (routed, real severity: app never landed in /Applications, no error shown). Confirmed root cause empirically first (isolated repro script before touching code): `hdiutil attach ... -quiet` emits zero stdout, so the mount-path `grep -o '/Volumes/...'` always failed (exit 1), and under `set -euo pipefail` that killed the script at the bare `MNT="$(...)"` assignment — before the scripts' own `if [ -z "${MNT}" ]; then echo error; exit 1; fi` guard ever ran. Same latent shape existed in both scripts' DMG-URL/tag-name JSON-parse pipelines (4 more sites) — `grep -o` no-match still propagates under `pipefail` even though downstream `head`/`sed` always exit 0. Fixed all 5 sites in both files: dropped `-quiet` (kept `-nobrowse`, added `-noverify -noautoopen`), and restructured every fragile parse into capture-raw-output-first / parse-second-with-`|| true`-so-grep-can't-trip-set-e / then hit the now-actually-reachable explicit guard (mount guard also dumps full `hdiutil attach` output to stderr). Added end-of-script success verification: `install.sh` asserts `/Applications/Clipnest.app` exists + prints installed version in its final line; `update.sh` asserts the same plus that post-copy version equals the release's `LATEST` tag before printing `Updated to X.` **Tested for real end-to-end** (the routing brief's explicit ask, since `bash -n`-only testing is what shipped this bug): `bash scripts/install.sh` against the live v0.6.0 GitHub release — downloaded/mounted/installed, confirmed `ls -ld /Applications/Clipnest.app` exists and `CFBundleShortVersionString` = `0.6.0`, ended with a non-silent `Done. Clipnest 0.6.0...` line. `bash scripts/update.sh` run twice for real (clean `Already up to date.` against a genuinely-current install; then faked the installed Info.plist to `0.0.1` to force the full download+mount+quit+swap+relaunch path, ended `Updated to 0.6.0.`), then a third run confirmed back to `Already up to date.` — state restored, matches what a normal successful update leaves behind. Proved loud (not silent) failure in 4 scenarios against copies of the actual fixed fragments: bad non-DMG file (hdiutil's own stderr error, exit 1), a real empty DMG missing `Clipnest.app` (hits the new explicit guard + attach-output dump, exit 1), DMG-URL parse against JSON with no `.dmg` asset (guard error, exit 1), tag-name parse against malformed JSON (guard error, exit 1). `bash -n` clean both files; `shellcheck` (available on this box) clean, zero warnings, no suppressions. Flagged-not-fixed: this dev machine's `xattr` on PATH is Homebrew's Python variant (not `/usr/bin/xattr`), doesn't support `-r`, so the pre-existing `xattr -dr ... 2>/dev/null || true` line prints a usage dump on this box — doesn't fail the script (already `|| true`) and doesn't affect correctness (curl never sets quarantine, confirmed via `xattr -l` on the installed app), pre-existing/unmodified, out of scope. `git add` on exactly the 2 touched files (`scripts/install.sh`, `scripts/update.sh`); confirmed `README.md`'s pre-existing unrelated staged state (staged before this task, "handled separately") was left untouched. No commit. Decision: D36. New T61, board + project-context.md written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-18 [T62 — background update-availability check] Approved-feature build per exact routed spec (files/anchors/wording given). New `ClipnestApp/Sources/System/UpdateChecker.swift` — `@MainActor final class UpdateChecker`: `isUpdateAvailable`/`latestVersion` + `onStateChanged` closure (same injection pattern as `PickerViewModel.dismiss`/`requestAppUpdate`), `start(settings:)`/`stop()`/`settingChanged(enabled:)` mirror `ClipboardMonitor`'s `Timer`-owning pattern — checks immediately if `lastCheckedAt` (own `UserDefaults` key, not `SettingsStore`) is ≥24h old/never-set, else arms a one-shot timer for the remainder before handing off to a steady-state 24h repeating timer; `checkNow() async` spawns `/usr/bin/curl -fsSL <GitHub releases API URL>` via `Process`/`Pipe` (never `URLSession` — same D35 strategy, same endpoint `scripts/update.sh` already queries), parses `tag_name` with `JSONSerialization` (real parser, unlike that script's `grep`/`sed`), compares via not-equal-means-available (literal port of that script's `if [ "${INSTALLED}" = "${LATEST}" ]`); any failure silently no-ops but still advances `lastCheckedAt` (mirrors `AppUpdater.runUpdate()`'s silent-fallback precedent). Pure logic (`isUpdateAvailable(installed:latestTag:)`/`normalizedVersion(fromTag:)`/`parseTagName(fromReleaseJSON:)`) factored `nonisolated static` for direct unit testing, no `Process` spawn. Caught + fixed a real race pre-handoff: a one-shot timer's in-flight `checkNow()` could resurrect the repeating timer after `stop()` ran mid-check — fixed with an `isEnabled` flag checked before any rearm. `SettingsStore.swift` +`automaticallyCheckForUpdates: Bool` (default `true`, exact existing toggle pattern). `GeneralSettingsView.swift` +toggle/caption/`.onChange` → `updateChecker.settingChanged(enabled:)`, `updateChecker` threaded into `init`. `SettingsView.swift`/`ClipnestApp.swift` thread `updateChecker` through the same way `accessibilityWatcher` already is. `PickerViewModel.swift` +`@Published isUpdateAvailable`/plain `latestVersion`, same doc-comment convention as `appVersion`. `PickerView.swift`'s version button gains a small accent-color dot (shown only when available) + a tooltip naming the version when known — action/confirmation/`requestAppUpdate()` all unchanged, purely a nudge. `AppEnvironment.swift` +`updateChecker` property/wiring +`startUpdateChecking()` mirroring `startCapture()`/`registerHotkey()` (construction stays side-effect-free). `AppDelegate.swift` +one call alongside the existing post-construction starts. Hit one real compile error: `Timer?` isn't `Sendable`, so plain `deinit { timer?.invalidate() }` failed against a nonisolated deinit — fixed with `nonisolated(unsafe) var timer`, same justified pattern `PickerPanel.commandDeleteMonitor` already documents. New `UpdateCheckerTests.swift` (16 tests: the 3 pure helpers across equal/different/v-prefixed/malformed-JSON/missing-key/wrong-type/empty-data cases, plus idle-state/stop-before-start/settingChanged-disabled-before-start/start-with-setting-disabled safety checks) — `checkNow()`'s real curl spawn deliberately never invoked from tests, same precedent as `AppUpdaterTests` never calling `runUpdate()`, so `start()`/`settingChanged(enabled: true)` are never exercised against an *enabled* store either (a never-checked instance is always "due"). +1 test in existing `SettingsStoreTests.swift`. `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED**. `xcodebuild test ... -destination 'platform=macOS'` → **TEST SUCCEEDED, 95/95 tests, 10 suites** (78 baseline + 17 new). Root `swift test` → **183/183 unchanged** (zero `ClipnestCore` touched, per the routed spec's explicit instruction). `swift format lint --strict` scoped to every touched/created file — clean; found (not fixed) a pre-existing, unrelated `LineLength` violation in `HotkeyManagerTests.swift:25`, confirmed via `git diff`/`git show HEAD:...` it predates this task, left alone as out of scope. No force-unwrap/force-try/force-cast added; no `URLSession`/socket import anywhere (verified by grep — only doc-comment mentions). No new dependency; `UpdateChecker.swift` imports only `Foundation`. `git add` done on exactly the 11 touched/created files, no commit; `.xcodeproj` confirmed gitignored. **Judgment call, documented not silently deviated:** routing brief said next task number is T61, but T61 was already taken in the working tree before this task started (same-session prior routed bug fix) — used **T62** instead, the actual next free number; D37 was genuinely free as instructed. **Flagged, not claimed proven:** the 24h timer's real firing behavior across sleep/wake/relaunch is unverifiable headlessly; `isUpdateAvailable`/`latestVersion` are NOT persisted across relaunch (only `lastCheckedAt` is, per spec's own wording) so a relaunch within 24h blanks the badge until the next scheduled check; `curl` has no `--max-time`, matching `scripts/update.sh`'s own invocation exactly. Full reasoning: decision D37. New T62, board + project-context.md written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-21 [T65 — H-1/H-2/M-1 review findings, synthesized-paste code area] Fixed three related review findings in one coherent change, all in the same paste-posting code area, per explicit instruction not to fix piecemeal. H-1 (wrong-app paste risk): `Paster.paste` now re-verifies focus immediately before posting — new injected `frontmostAppProvider: any FrontmostAppReferenceProviding` (reused the pre-existing `FrontmostAppTracker.swift` protocol, no new type), compares current vs target pid via new pure `Paster.isStillFrontmost(current:target:)`, throws new `PasteError.targetNoLongerFrontmost` and skips posting on mismatch — pasteboard write still stands. Verified `PickerViewModel.pasteAndDismiss`'s existing `do/catch` already logs any `PasteError` metadata-only and never crashes, so zero call-site changes were needed. H-2 (stuck/merged synthetic modifier) + M-1 (duplicated posting logic): new `Sources/ClipnestCore/Paste/SyntheticKeystroke.swift`, the one shared place both `CGEventSynthesizer.synthesizeCommandV` (`Paster.swift`) and `ClipboardSelectionReplacer.postCommandKey` now call — built from `CGEventSource(stateID: .privateState)` (was `.combinedSessionState`, which merged the user's physically-held modifiers) and bracketed with an explicit Command key-down before / key-up after (`commandUp.flags = []`) so nothing is left asserted. Did NOT claim this fixes the open/unconfirmed Terminal drag-selection bug anywhere — H-2 stands on its own merits only, per instruction. Tests: `PasterTests.swift` gained `FakeFrontmostAppReferenceProviding`, wired into every pre-existing "synthesizer should fire" case (now required since H-1 actually checks it), plus 3 new pure tests on `isStillFrontmost` and 2 new integration tests proving `paste` throws `.targetNoLongerFrontmost`/skips the synthesizer/still writes the pasteboard on a focus-steal. `SyntheticKeystroke.postCommandModified` itself is never called from a test (real `CGEvent` post) — same precedent as `CGEventSynthesizer`, documented in its doc comment. `swift test` → **190/190** (183 baseline + 7 new). `xcodegen generate` clean; app build → **BUILD SUCCEEDED**; app test → **TEST SUCCEEDED, 95/95, 10 suites** (baseline unchanged). `swift format lint --recursive --strict` on all 4 touched/created files → clean (pre-existing unrelated `HotkeyManagerTests.swift:25` violation left alone, not claimed). Not verifiable headlessly, explicitly flagged: real end-to-end paste behavior, whether H-1's window is wide enough in practice, any effect on the unconfirmed Terminal bug. `git add` on exactly the 4 touched/created files; other unrelated modified/untracked files already present in the working tree at task start were left alone, not this task's scope. No commit. New T65, board written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-21 [T64 — M-3 review finding, SwiftDataClipStore legacy nil-pinnedAt ordering] Fixed reviewer finding M-3 (MEDIUM): `SwiftDataClipStore.query`'s pinned-scope sort pushed `SortDescriptor(\.pinnedAt, order: .forward)` into `FetchDescriptor` and relied on its (until now unverified) nil-handling, while `InMemoryClipStore.query` used an explicit `($0.pinnedAt ?? .distantPast) < (...)` rule for legacy `pinned==true/pinnedAt==nil` rows — a possible silent divergence, uncaught since no contract test ever seeded that shape. Added the missing shared scenario to `ClipStoreContractTests` (`queryPinnedScopeOrdersLegacyNilPinnedAtBeforeDatedPins`; `makeItem` gained a `pinnedAt: Date? = nil` param) first and ran it against the *unmodified* pre-fix code — it **passed**, not failed; probed harder (reversed insertion order, multiple mixed dated/nil pins, pagination boundary, via a throwaway probe suite deleted before handoff) and it held every time. Researched via deepwiki (`swiftlang/swift-foundation`): `SortDescriptor`'s `Optional<Comparable>` init wraps an `OptionalComparator` documented as nil-first under `.forward`, and `FetchDescriptor`'s SQL pushdown honors that on this toolchain (Xcode 26.6/Swift 6.3.3) — so this was a real *risk* (implicit, framework-internal, version-unpinned), not a currently-manifesting bug. Fixed anyway per the finding's explicit ask: `SwiftDataClipStore.query`'s `.pinned` branch (`Sources/ClipnestCore/Store/SwiftDataClipStore.swift`) no longer sorts/paginates at the SQL level — fetches the (small, pinned-only) predicate-filtered set, sorts explicitly in Swift with the identical `InMemoryClipStore` rule, then slices offset/limit post-sort (justified inline: pinned sets are bounded by user pin count, not full history). `.history` branch untouched (non-optional `createdAt`, no risk). Proved the new test has real teeth via mutation testing since it couldn't literally fail-before/pass-after here: temporarily flipped the fix's coalesce to `.distantFuture` (deliberately wrong order), reran — failed exactly as expected — reverted, confirmed the diff matched the intended fix byte-for-byte via `diff` against a saved patch. Requirement-3 audit: grepped every `SortDescriptor`/`#Predicate` in `ClipnestCore/Store/*.swift` — `pinnedAt` is the only nilable field ever used in a `SortDescriptor` (all other sorts are on non-optional `createdAt`); `SnippetRecord.keyword`'s `#Predicate { != nil }` is an unambiguous filter (not an ordering rule) and both `SnippetStore` conformances already tie-break on non-optional `createdAt` after it — no other divergence of this class found, nothing else touched. `swift test` → **190/190** (183 baseline + this task's 2 new tests + 5 unrelated tests from another agent's concurrent in-flight `PasterTests.swift` work in this shared tree, untouched by me). `xcodegen generate` clean; app build → **BUILD SUCCEEDED**; app test → **TEST SUCCEEDED, 95/95, 10 suites** (App layer untouched, matches stated baseline exactly). `swift format lint --recursive --strict Sources Tests` and `ClipnestApp/Sources` → both clean; pre-existing unrelated `HotkeyManagerTests.swift:25` violation reconfirmed, left alone per instruction. No `@Model` attribute added/changed, no schema migration — query/sort-level only, per the task's hard constraint; also did not touch `Paster.swift`/`ClipboardSelectionReplacer.swift`/`PickerViewModel.swift`, confirmed via `git diff --cached --stat` that another agent's already-staged, in-flight work on those files (plus `SyntheticKeystroke.swift`/`PasterTests.swift`) was left exactly as found. `git add` on exactly the 4 touched files. No commit. Decision: D38. Task-number collision caught and resolved mid-task: my board entry first collided with a concurrent senior-dev instance's own T63/T64 (paste-focus-safety fix, same shared task-board.md) as both files were being edited live — renumbered mine to the next genuinely free number, **T64**, after that agent's own entry had already moved to T65; reconfirmed uniqueness across task-board.md/project-context.md/this log before finalizing. New T64, board + project-context.md (D38) written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-08-21 [T67 — M-4 review finding, PickerViewModel.swift responsibility split] Behavior-preserving-only refactor per explicit instruction (no rewrite, no bug fixes even where noticed). Mapped the file's actual responsibilities first: state/injected-closures, lifecycle (willShow/didHide/handleNewCapture), search-input dispatch, Rows+Snippets paged-query pipelines (tightly coupled via `PagedQuery`/shared selection-policy per the file's own "DRY follow-up" doc comment), infinite scroll, preview-popover hover tracking, selection-policy application, selecting/pasting, keyboard-nav/commit actions (togglePin/delete/save-as-snippet), Snippets CRUD. Extracted the two genuinely separable clusters into `extension PickerViewModel` files (matching the repo's existing `ItemKind+SFSymbol.swift` convention): `PickerViewModel+Preview.swift` (hover-driven preview-popover coordination — `hoverItem(_:)`/`previewHoverChanged(_:)`/private resolve-scheduling) and `PickerViewModel+Paste.swift` (`select(_:plainText:)`/`pasteContent(for:plainText:)`/`pasteSnippet(_:)`/private `pasteAndDismiss(_:)`). Deliberately did NOT extract the query pipelines, selection policy, keyboard-nav/commit actions, or Snippets CRUD — verified each is cross-wired to `rowsQuery`/`snippetsQuery`/`clipStore`/`snippetStore`/`refreshRowsWindow`/`refreshSnippetsWindow`/etc. tightly enough that splitting would require widening nearly every private helper in the file just to reconnect the pieces (checked concretely: Snippets CRUD alone would need `snippetStore` + `refreshSnippetsWindow` widened, and would break the file's intentional Rows/Snippets mirroring if done one-sided) — an unclean seam per this task's own "if not clean, leave it" instruction, so left as one ~950-line cohesive state-machine core. Key Swift constraint discovered mid-task and adhered to: extensions cannot hold stored instance properties, so `hoveredItemID`/`isHoveringPreview`/`previewTask`/`previewTargetID`'s setter (`private`/`private(set)`) and `paster`/`frontmostAppTracker`/`pasteboard`/`logger` (`private`) had to widen to plain `internal` — the minimum required for cross-file access — since none of those ever gain a caller outside `PickerViewModel`/its two extensions; every widened symbol carries an inline doc-comment explaining why. Added two small new internal glue methods (`cancelPreviewAndHide()`, `resetHoverForTabSwitch()`) so `didHide()`/`activeTab`'s `didSet` (staying in the core file) can still trigger the exact same preview-reset behavior without touching now-relocated private logic directly — same statements, same order, zero behavior change. `PickerViewModel.swift`: 1269 → 1115 lines; `+Preview.swift`: 149 lines (new); `+Paste.swift`: 156 lines (new). `swift test` → **190/190** (repo baseline, `ClipnestCore` untouched by this task). `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED**, re-ran with `clean build` and grepped every `warning:` line — all pre-existing, in unrelated files (`HotkeyManager.swift`/`ClipnestApp.swift`/`SelectedTextAccessing.swift`), zero new. `xcodebuild ... -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` → **TEST SUCCEEDED**, Swift Testing line reads **"Test run with 95 tests in 10 suites passed"** — exact baseline, all 3 `PickerViewModel` suites (`pasteContent`, `resolvedSelection`, `tab/scope query mapping`) individually confirmed passed via `@testable import` (both `pasteContent`/`resolvedSelection` stayed `internal`, unaffected by which file declares them). `swift format lint --strict` on all 3 touched/created files → clean; pre-existing unrelated `HotkeyManagerTests.swift:25` violation left alone, not claimed. No existing test moved, rewritten, or weakened — `PickerViewModelTests.swift` untouched. Runtime UI behavior NOT verified (headless session) — claiming only that it builds and both test suites pass at baseline counts, not that the picker still works end-to-end. No new bug found/fixed; none noticed either. `git add` on exactly the 3 touched/created files. No commit. New T67 (T64/T65/T66 confirmed taken), board written directly (routed request, not spawned this session). → board:review, handing to reviewer then tester; tester owns `done`.
- 2026-09-03 [T-OCR1/T-OCR2 — on-device OCR for copied images] Built both routed tasks together (Size M). Fixed design (not redesigned): setting defaults OFF (`SettingsStore.isTextRecognitionEnabled`, History tab), OCR runs only inside `ClipboardMonitor.checkNow()`'s real capture path (never idle-scan/background sweep), ⌥⏎ overloaded to paste recognized text for `.image` (falls back to the image with no OCR text), Return unchanged, ⌘⏎ stays unbound. **Core**: new `Sources/ClipnestCore/OCR/{TextRecognizing,VisionTextRecognizer}.swift` — protocol + Vision-backed impl (`.recognitionLevel = .fast`, `usesLanguageCorrection = false`, downscale to `maxDownscaledDimension` 1600pt, skip entirely above `maxByteSize` 50MB / `maxPixelDimension` 20000px — every number a type-scoped `static let` per this codebase's existing constants convention, not a shared Constants file). `ClipItem.ocrText: String?` (optional, nil default — SwiftData-migration-safe per the additive-optional pattern `pinnedAt` already established; read `.claude/skills/data-modeling/SKILL.md` first). `ClipItemRecord.ocrText` mirrors it; new `ClipItemRecord.computeNormalizedText(previewText:ocrText:)` is the single derivation point (previewText+ocrText, lowercased) used by the convenience init, the existing migration-crash-fix backfill, AND the new `setRecognizedText` — no duplicated `.lowercased()` logic. `ClipStore` protocol +`setRecognizedText(_:text:)`, implemented in both `SwiftDataClipStore` (sets `ocrText` + recomputes `normalizedText`, no predicate change) and `InMemoryClipStore` (matches `ocrText` directly in `query`'s filter, no separate index). `ClipboardMonitor` +`textRecognizer`/`textRecognitionEnabledProvider` (same DI-provider shape as existing `captureEnabledProvider`); after a successful `.image` capture with no `ocrText` yet (guards duplicate re-recognition) and raw bytes available, fires `scheduleTextRecognition` — a detached `.utility` task that recognizes, persists via `setRecognizedText`, then hops back to `@MainActor` to re-fire the SAME `onCapture` hook the picker's live-refresh already listens to (no second notification path). `checkNow()` itself never awaits any of this — verified by a dedicated gated-fake non-blocking test. **App**: `AppEnvironment` wires a real `VisionTextRecognizer()` + `MainActor.assumeIsolated { settingsStore.isTextRecognitionEnabled }`. `HistorySettingsView` +toggle with explicit privacy-tradeoff copy (recognized text becomes searchable, including anything sensitive shown on screen — this is why default is OFF, not on). `PickerViewModel+Paste.pasteContent(for:plainText:)`: `.image` + `plainText` + non-empty `ocrText` → `.text(ocrText)`; otherwise unchanged fallthrough to the image. New `copyRecognizedText(from:)` — context-menu-only, writes the pasteboard directly + suppresses the self-write, does NOT dismiss/paste (distinct from ⌥⏎). `ItemRow` +small `text.viewfinder` badge overlay on the thumbnail (indicator only — the three always-visible trailing buttons are unchanged) + "Copy Recognized Text" context-menu item, both gated on `hasRecognizedText`. `ItemPreview`'s image case now shows recognized text below the image, scrollable, reusing the existing chunked-loading `TextPreview` (no second long-text renderer). Shortcut footer: `⌥⏎ plain` → `⌥⏎ plain/OCR text`. Privacy: concealed/transient/excluded-app pasteboard items never reach the recognizer structurally — OCR only runs after `PrivacyFilter.shouldCapture` has already gated the capture and the item is already stored; no separate check needed. **Tests**: 8 new `ClipnestCoreTests` (`ClipStoreContractTests`/`ClipStoreTests`/`SwiftDataClipStoreTests`: setRecognizedText sets ocrText + throws on unknown id + search-by-OCR-alone + case-insensitive normalizedText recombination), 1 dedicated migration test (extended the *existing* `preNormalizedTextStoreFileMigratesInPlaceAndBackfills` to also assert `ocrText` migrates to nil and still accepts `setRecognizedText` afterward — deliberately reused that file's existing legacy `ClipItemRecord` rather than declaring a second same-named test-local `@Model` type in a new file: a genuinely-hit cross-suite race first, see below), 5 new `ClipboardMonitor` OCR-wiring tests (on/off provider, image-only, duplicate-skip via a gated `actor FakeTextRecognizer`, non-blocking via a release-gated continuation + polling `waitUntil`), 3 new `VisionTextRecognizer` tests (byte-ceiling skip, undecodable-data-returns-nil, real end-to-end Vision call against a textless fixture returns nil — no test asserts specific OCR *output*, same precedent `CGEventSynthesizer` already set), 7 new `PickerViewModelTests` (`.image`+ocrText plainText/non-plainText paste-target cases, `copyRecognizedText` writes+suppresses / no-ops on nil / no-ops on empty). **Real bug caught and fixed pre-handoff**: my first pass put the ocrText-migration proof in a NEW file with its own third `ClipItemRecord`-named test-local `@Model` type — intermittently failed only when run as part of the full suite (passed standalone, passed on retry) because Swift Testing runs different suites in parallel by default and two suites each registering a same-named Core Data entity with different attribute sets raced (confirmed via isolated debug repro that passed alone). Fixed by consolidating into the *existing* `.serialized` `SwiftDataClipStoreTests` suite's *existing* legacy record instead of adding a second one — 5 consecutive full-suite runs after the fix, all green, no flakes. `swift test` (Xcode 26.6/Swift 6.3.3 installed, no toolchain gap) → **205/205, 15 suites**, verified stable across 3 repeated runs. `xcodegen generate` clean; `xcodebuild ... build` → **BUILD SUCCEEDED**, zero new warnings; `xcodebuild ... test` → **TEST SUCCEEDED, 102/102 tests, 11 suites** (95 baseline + 7 new), verified stable. `swift format lint --recursive --strict` clean on every touched Core/App file (pre-existing, untouched-by-me `HotkeyManagerTests.swift:25` LineLength violation confirmed via `git log` to predate this task, left alone). No new dependency; only `Vision`/`CoreGraphics` (system frameworks) added to `ClipnestCore`'s imports. No force-unwrap/force-try/force-cast added. **README changes NOT made** (explicitly told not to touch it — another agent rewriting it in parallel) — hand these to that agent: (1) mention on-device OCR/"search screenshots by contents" as a feature, default OFF, opt-in in Settings → History; (2) note ⌥⏎ now also pastes an image's recognized text when present; (3) if a Requirements/permissions section lists frameworks, Vision is now used (on-device, no network, no new entitlement). **Left out deliberately**: no dedicated `VisionTextRecognizer` test asserts recognizing *specific* real text (would need a real screenshot fixture + is Vision-version/OS-version fragile) — covered instead by the fake-recognizer wiring tests (deterministic) + 3 real-Vision smoke tests (ceiling/garbage/textless, all deterministic regardless of Vision's actual OCR quality). `git add` done on exactly this task's files (Core: `OCR/`, `Model/ClipItem.swift`, `Store/{ClipStore,InMemoryClipStore,SwiftDataClipStore}.swift`, `Clipboard/ClipboardMonitor.swift`, 5 test files; App: `AppEnvironment.swift`, `SettingsStore.swift`, `UI/Picker/{ItemPreview,ItemRow,PickerView,PickerViewModel+Paste}.swift`, `UI/Settings/HistorySettingsView.swift`, 3 test-support/test files) — did NOT stage `README.md`, `.claude/task-board.md`, `.claude/logs/architect.md`, `.claude/logs/project-manager.md`, `docs/competitive-analysis.md`, all pre-existing in-flight changes from other agents in this shared tree, left exactly as found. No commit. Decisions for project-context.md: (1) `ocrText`'s migration-safety is proven by extending the existing pre-normalizedText legacy-record test rather than adding a second test-local `ClipItemRecord`, specifically because of the cross-suite Core-Data-entity-name race discovered above — worth recording so a future task doesn't reintroduce a second same-named test-local `@Model` type in a new file; (2) `copyRecognizedText` deliberately does NOT go through `Paster`/dismiss-and-paste — it's a plain pasteboard write + self-write suppression, since "copy" and "select-and-paste" are different user intents. → board:test (my ceiling per integrity rule 2), handing to reviewer then tester; tester owns `done`.
- 2026-09-03 [T-OCR8 — OCR quality setting (Fast/Accurate)] Fixed design (not redesigned) per routed spec: user found `.fast` (T-OCR1's hardcoded choice) mangling digits/letters/punctuation/arrows on a real screenshot vs. `.accurate` reading all three correctly (benchmarked: 23ms/1350 chars/no symbols found vs. 154ms/1366 chars/→ found, on a 2222x1244 screenshot downscaled to `maxDownscaledDimension`); exposed as a Fast/Accurate picker in Settings → History directly under the existing OCR toggle, defaulting to Accurate — neither level reads ⌘/⌥/⇧ (outside Vision's character set entirely), documented as accepted not chased. **Core**: new `Sources/ClipnestCore/OCR/TextRecognitionQuality.swift` — `public enum TextRecognitionQuality: String, CaseIterable, Sendable { case fast, accurate }`, the ONLY app/Settings-layer-visible type; `Vision.VNRequestTextRecognitionLevel` never leaks past `VisionTextRecognizer`. `TextRecognizing.recognizeText(in:quality:)` signature gained the `quality` param (breaking change, both conformances updated: `VisionTextRecognizer` + test's `FakeTextRecognizer`). `VisionTextRecognizer`'s new private `recognitionLevel(for:)` is the one place Vision's enum is referenced; `usesLanguageCorrection` now follows `.accurate` too (was unconditionally `false`) — it's part of what fixes punctuation. `ClipboardMonitor` +`textRecognitionQualityProvider: @Sendable () -> TextRecognitionQuality` (default `{ .accurate }`), same DI-provider shape as the existing `textRecognitionEnabledProvider` — read fresh in `checkNow()` at the same schedule-time spot the enabled-provider is already read, passed as a plain value through `scheduleTextRecognition` into the detached OCR task (no re-read inside the task, no extra provider dependency on that method). **App**: `SettingsStore` +`textRecognitionQuality: TextRecognitionQuality` (key `settings.textRecognitionQuality`, default `.accurate`, `.flatMap(Init(rawValue:))` absent-vs-explicit pattern exactly mirroring `retentionMode`). `AppEnvironment` wires `textRecognitionQualityProvider` to `MainActor.assumeIsolated { settingsStore.textRecognitionQuality }`, same shape as the enabled-provider wiring beside it. `HistorySettingsView`: new `Picker("Recognition quality", ...)` directly under the toggle, indented + **disabled (not hidden)** while OCR is off — justified inline and in this log: hiding would shift every control below it on every toggle flip, and a user deciding whether to enable OCR at all can't see Accurate is already the default until they've already opted in; a disabled row costs nothing and answers "what will happen" up front. Caption copy is user-facing, not implementation-facing ("reads punctuation, digits, and arrows correctly but takes longer" / "can confuse similar characters, like 1 and l"), explicitly does NOT promise ⌘/⌥/⇧ support. Existing rows recognized at the old quality are NOT re-recognized on a setting change — same accepted scope as the toggle's own no-retroactive-OCR behavior, noted in both the view's doc comment and here, not built. **Tests**: `FakeTextRecognizer` (`ClipboardMonitorTests.swift`) gained `recognizedQualities: [TextRecognitionQuality]`, appended on every call — new `changingQualityMidSessionTakesEffectImmediately` mirrors the existing `togglingSettingMidSessionTakesEffectImmediately` test exactly (flips a live `MutableBox` mid-session, asserts the FAKE recognizer received `[.fast, .accurate]` in order — never depends on real Vision for this wiring assertion, per spec). Generalized the existing test-only `MutableFlag` (Bool-only) into a generic `MutableBox<Value: Sendable>` so this new test reuses it instead of a second copy-pasted box type (DRY) — both pre-existing `MutableFlag(false)` call sites updated to `MutableBox(false)`, no behavior change. `VisionTextRecognizerTests`: updated 2 existing calls for the new required `quality:` arg; `textlessImageReturnsNil` renamed `textlessImageReturnsNilAtEitherQuality`, now loops `TextRecognitionQuality.allCases` — the one place a real Vision call is exercised, per this file's own precedent (assert round-trips-to-nil-without-crashing, never specific OCR output). `SettingsStoreTests` +1 (`textRecognitionQuality: defaults to .accurate, persists an explicit .fast`, mirrors `isTextRecognitionEnabledDefaultsFalseAndPersists`'s shape). `swift build` clean. `swift test` → **212/212** (211 baseline + 1 new). `xcodegen generate` clean; `xcodebuild ... CODE_SIGNING_ALLOWED=NO test` → **TEST SUCCEEDED, 104/104 tests, 11 suites** (103 baseline + 1 new). `swift format lint --recursive --strict Sources Tests ClipnestApp/Sources` → clean (1 round of `swift format format --in-place` needed on `VisionTextRecognizer.swift`/`VisionTextRecognizerTests.swift`, pure line-wrap/line-length, re-linted clean after); pre-existing unrelated `HotkeyManagerTests.swift:25` LineLength violation confirmed pre-dating this task, left alone per instruction. No new dependency; `Vision` already on coding-standards.md's allowed list (line 75, added earlier this session). No force-unwrap/force-try/force-cast added. **README changes NOT made** (explicitly told not to touch it) — hand to whoever routes README: note the History settings tab now has a Fast/Accurate recognition-quality picker under the OCR toggle, defaulting to Accurate, and that neither level can read keyboard glyphs like ⌘/⌥/⇧. Decision for project-context.md: `TextRecognitionQuality` lives in `ClipnestCore/OCR/` (not `Model/`) since it's OCR-engine-specific config, not a domain value type stored on `ClipItem`; `usesLanguageCorrection` is now derived from quality (`== .accurate`) rather than a second independent setting — kept as one user-facing knob, not two. `git add` done on exactly the 10 touched/created files (`Sources/ClipnestCore/OCR/{TextRecognitionQuality(new),TextRecognizing,VisionTextRecognizer}.swift`, `Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift`, `ClipnestApp/Sources/{App/AppEnvironment,System/SettingsStore,UI/Settings/HistorySettingsView}.swift`, `Tests/ClipnestCoreTests/{ClipboardMonitorTests,VisionTextRecognizerTests}.swift`, `ClipnestApp/Tests/ClipnestAppTests/SettingsStoreTests.swift`) — did not touch/stage `README.md`/`.claude/task-board.md`/any other pre-existing in-flight file in this shared tree. No commit. Was spawned this session by the coordinating senior-dev context — returning status here + in final message rather than writing task-board.md myself (integrity rule 1); ceiling is `status:test`.
- 2026-09-03 [T-GLY1 — glyph/emoji classifier spike, GATE FAILED, T-GLY2 NOT started] Built the full spike per brief: standalone SPM package `tools/glyph-classifier/` (never referenced by root `Package.swift`/`ClipnestApp/project.yml` — keeps `CreateML` and the AppKit/CoreText renderer entirely out of shipping targets), 3 targets (`GlyphDatasetGen` renders synthetic data, `GlyphTrainer` trains+evaluates via `CreateML.MLImageClassifier`, `GlyphClassifierSupport` is the shared class catalog + pure gate-evaluation logic, unit-tested — 8/8 `swift test` green in that package). Classes: 9 keyboard glyphs (⌘⌥⇧⌃⎋⏎⌫⇥⇪, added ⇥/⇪ beyond the brief's list, hand-verified real font glyph coverage per font via `CTFontGetGlyphsForCharacters` before committing to the renderer), 64 emoji (single-codepoint, non-ZWJ, non-skin-tone, spread across 7 categories — stated cut: kept small enough for a same-session spike, not a frequency-ranked corpus pull), 1 `notSymbol` (ASCII+confusables+partial-text-crops+blank-crops, deliberately the largest class). Generated 15,870 PNGs (179MB) in 12.8s with heavy randomized augmentation (font/weight/size/color incl. dark-mode+low-contrast pairs/rotation/noise/blur/crop-looseness). Trained `MLImageClassifier` (`.scenePrint` transfer learning + logistic regressor, `augmentation: []` since the renderer already varies heavily) — 287s wall time, output model 1.2MB (1,198,579 bytes; feature extractor stays OS-provided, not embedded).
  **Held-out SYNTHETIC test (self-consistent with the renderer, not a real-world proxy):** overall accuracy 93.54%; glyph-group micro-recall 82.22% (worst: capslock 60%/precision 54.5%, ctrl 64%/76.2%, shift 76%/77.6% — capslock/shift are near-identical up-arrow shapes in several fonts, a plausible root cause); emoji-group micro-recall 96.75%; `notSymbol` precision 93.151%, recall 93.500%. **Gate on synthetic alone already FAILS both criteria** (notSymbol precision needs ≥99.5%, glyph recall needs ≥90%).
  **Real-world crops (the number that actually matters — mid-task course correction from the coordinator required expanding this from an initial thin n=5 set):** queried `~/Library/Application Support/Clipnest/ClipItems.store` (read-only, `ZKINDRAWVALUE='image'`) for the authoritative list of 39 real image blobs; ran the SAME production-shaped Vision `.accurate` pass (1600pt downscale) against all 39, harvested 480 real per-character/word `notSymbol` crops (visually spot-checked, all correctly real text — "agent"/"Generic"/digits/"×"/"←"/etc.) plus every glyph-shaped candidate token found (53 candidates → only ONE real isolated ⌘ occurrence exists anywhere in this user's 39-image corpus, in a single Clipnest-docs screenshot; zero real occurrences of the other 8 glyph classes; zero real emoji occurrences anywhere). **Real `notSymbol` recall: 66.875% (321/480)** — roughly 1 in 3 real characters/words from actual screenshots get mis-rewritten into a glyph or emoji, 114/480 (23.75%) specifically into `cmd` alone. **Real `cmd` recall: 0/2** (the only two real ⌘ crops found were both misclassified as `notSymbol`) — the model fails in both directions on real data. Real `notSymbol` precision is not honestly computable from this corpus (too few real glyph/emoji occurrences exist to estimate a fair denominator) — stated explicitly rather than reporting a misleadingly-high number. Root cause (not just "more data needed"): Vision's per-character `boundingBox(for:)` is NOT reliably tight to one character on real content — confirmed twice (a merged ⌥⌘ token returned identical boxes for two different character offsets; several harvested "single-character" real crops came back showing whole words) — so a classifier trained exclusively on tightly-cropped single symbols hits a real domain/crop-shape gap that heavier synthetic augmentation alone likely can't close.
  **Latency:** 12.46ms/prediction (synthetic), 29.02ms/prediction (real crops) — both far under Vision's 154ms `.accurate` baseline for one image. Per-screenshot cost estimate: production would only classify short suspicious tokens (never blindly classify every recognized character, per the brief); this harvest's own candidate-token heuristic found ~1.4 candidates/real-image, so ~20-40ms added per screenshot if restricted that way — negligible; blindly classifying every character instead (hundreds per dense screenshot) would NOT be negligible (multi-second), confirming the brief's explicit warning against that design.
  **GATE: FAIL — clearly, on both synthetic and (far more severely) real data.** This is the harmful "worse than nothing" failure mode the brief named directly: shipping this would silently corrupt ~1/3 of real notSymbol characters near any suspicious-looking region. **T-GLY2 (integration) was NOT started** — no Phase 2 code, no `computeNormalizedText` change, no schema change, per the brief's explicit instruction to stop and report a negative result rather than integrate a weak model or quietly lower the bar. `CFStringTransform(_, nil, "Any-Name" as CFString, false)` was verified as the real system API for Phase 2's emoji-alias derivation (e.g. 🚀 → `\N{ROCKET}`), recorded for whoever revisits this, not used in any shipped code.
  Zero files touched outside `tools/glyph-classifier/` and `.gitignore` (new gitignore entries for that package's `.build/`/`dataset/`/`output/`/`real_eval_crops/` — the last holds crops from the user's real, sensitive screenshots, never committed). Confirmed no regression: root `swift test` → **212/212** unchanged; `swift format lint --recursive --strict Sources Tests` and `... ClipnestApp/Sources` → clean (pre-existing `HotkeyManagerTests.swift:25` untouched); `tools/glyph-classifier`'s own `swift format lint --recursive --strict Sources Tests` → clean; its own `swift test` → 8/8. `xcodebuild ... test` (104-test app baseline) run to confirm no regression from this task — see final report for its result (was still running at log time). No dependency added to any shipping target (`CreateML`/`Vision`-for-harvesting/`TabularData` are confined to `tools/glyph-classifier/`, a non-shipping tool package — nothing added to `Sources/ClipnestCore` or `ClipnestApp`'s actual import surface). Coding-standards.md needs no update (no shipping-target change). Was spawned this session (senior-dev role) — returning status/decisions to my spawner rather than writing `.claude/task-board.md` (integrity rule 1); T-GLY1's furthest honest status is `test` for the spike's own code (dataset generator + trainer + their unit tests work correctly and are lint-clean), but the FEATURE it was gating (T-GLY2) should be recorded as **not started, blocked by this negative gate result** — recommend the board record T-GLY1 as measured/failed rather than a normal `done`, and NOT create/advance T-GLY2 until a product decision is made on whether to revisit with a different approach (e.g. training on Vision-shaped multi-character crops instead of tight single-glyph crops, or dropping the ML approach in favor of the existing "explain what Vision can't read" status quo).
- 2026-09-03 [T-PERF1 — get heavy work off the main actor, per user directive] Enforced one rule end-to-end: **AppKit pasteboard access stays on `@MainActor`; nothing else heavy does.** Two hot paths, both fixed at the root rather than patched with `Task { @MainActor in }` band-aids.
  **Capture path** (`Sources/ClipnestCore/Clipboard/PasteboardReader.swift`, `ClipboardMonitor.swift`): split `PasteboardReader.read(from:)` into `pullRawPayload(from:) -> RawPayload?` (the real `NSPasteboard` reads — `availableTypes`/`data(forType:)`/`string(forType:)` — fetches ONLY the one representation the existing type-priority order resolves to, exactly like `read` always did) and `classify(_ raw: RawPayload) -> Classification` (pure CPU: `ItemKind` decision, `previewText` construction, SHA-256 hashing — touches no `PasteboardReading`). `RawPayload` is a small `Sendable` enum (`.file`/`.image`/`.richText`/`.plainText`) carrying exactly the bytes/strings each branch used to read inline. `read(from:)` itself is unchanged (`pullRawPayload` + `classify` combined) so `PasteboardReaderTests` needed zero changes. `ClipboardMonitor.checkNow()` now calls `pullRawPayload` on `@MainActor` (unchanged), then `classify` inside `Task.detached(priority: .utility)` (same pattern its blob write already used) — `checkNow()` tests needed zero signature changes since it was already `async`. Also switched `imagePreviewText`'s dimension read from `NSBitmapImageRep(data:)` to `ImageIO`'s `CGImageSourceCopyPropertiesAtIndex` (header-only read, no full decode, and a documented-thread-safe C API instead of an AppKit type — matters now that `classify` runs off `@MainActor`).
  **Paste path**: (1) `ClipnestApp/Sources/UI/Picker/PickerViewModel+Paste.swift` — `pasteContent(for:plainText:)` is now `async`; `.richText`/`.image` blob reads go through a new `readBlobOffMain(_:)` (`Task.detached(priority: .utility) { blobStore.read(...) }` — same pattern as `ItemPreview`/`ItemRow`/`ClipboardMonitor`'s existing off-main blob reads). `select(_:plainText:)` restructured: its `Task` now resolves `pasteContent` FIRST, and only THEN captures `frontmostApp`/dismisses — preserves the exact original behavior that a `nil` content (missing/corrupt blob) leaves the picker open with nothing pasted (never dismiss-then-no-op). The old shared `pasteAndDismiss(_:)` (captured `frontmostApp` + dismissed + pasted, all in one) split into: callers (`select`/`pasteSnippet`) each capture `frontmostApp`/dismiss themselves (immediately, synchronously — `pasteSnippet` has no I/O so this is unchanged for it), then both call the still-shared `performPaste(_:frontmostApp:) async` for the write-and-suppress tail. (2) `Sources/ClipnestCore/Paste/Paster.swift` — `.image`'s `NSImage(data:)?.tiffRepresentation` decode+re-encode (measured ~83ms combined on the user's 25MB real image) replaced with a `private static func normalizedToTIFF(_ data:) -> Data?` built entirely on `ImageIO` (`CGImageSourceCreateWithData`/`CGImageSourceCreateImageAtIndex`/`CGImageDestinationCreateWithData`/`...AddImage`/`...Finalize`, output type `UTType.tiff`), called via `Task.detached(priority: .utility)` from inside `paste()`'s `.image` case — chose `Task.detached` (not relying on the caller having already hopped off-main) specifically because it's a hard guarantee regardless of what actor invoked `paste(_:targetingFrontmostApp:)`, unlike depending on every future call site getting an off-main hop right. Same `PasteError.invalidImageData` contract on decode/encode failure as before.
  **Why `changeCount`/self-paste-suppression ordering is still correct** (hazard 1): the real invariant `suppressOwnPasteboardWrite`'s callers depend on is "the pasteboard write completes before `paster.paste(...)` returns to its `await`er" — NOT "no suspension before the write inside `paste`" (that was just how the old synchronous-decode implementation happened to satisfy it). Swift's sequential-execution guarantee for a single async call already gives the stronger property for free: no matter how many suspension points happen *before* the write inside `paste()`, the write is still guaranteed complete before `paste()` returns, since execution proceeds statement-by-statement to the return. So `performPaste`'s `suppressOwnPasteboardWrite(pasteboard.changeCount)`, read immediately after `await paster.paste(...)` returns, still observes the correct value. The actual race this codebase's comments warn about — the window between the write and `suppressOwnPasteboardWrite`'s `ignore(changeCount:)` registration racing the monitor's 400ms poll — is **unchanged**: decode-before-write was already the ordering pre-T-PERF1 (just synchronous instead of suspended), so nothing moved earlier relative to the write; the new `Task.detached` for blob-read/decode only delays *when* the write happens overall (giving the OS *more* time to hand focus back before `dismiss()`+`synthesisDelay`, not less) — the write-to-suppress window itself is untouched.
  **Why `PrivacyFilter` gate ordering is still correct** (hazard 2): `checkNow()`'s ordering is unchanged — `privacyFilter.shouldCapture(...)` (using only `pasteboard.availableTypes`, a cheap main-actor read) still runs and can still reject BEFORE `pullRawPayload`/`classify`/blob-write/store-insert/OCR are ever reached; I only touched what happens strictly after that gate already passed.
  **`ClipboardMonitor` state** (hazard 3): `lastChangeCount`/`ignoredChangeCount` mutations are untouched — both still happen synchronously at the top of `checkNow()`, before the new `await Task.detached { classify }` suspension point I added; only the *work* (classification) moved, the state-machine ordering didn't.
  **Swift 6 strict concurrency**: zero new `@unchecked Sendable` anywhere in the touched production files (verified via `git diff | grep "unchecked Sendable"` — none). `RawPayload` is a plain `Sendable` enum of `Data`/`String`. `PasteboardReader`/`Paster` are already plain `Sendable` structs, so capturing them into `Task.detached` closures needs no escape hatch. Test-only `@unchecked Sendable` reference types (`Counter`, in the new `PasterTests` concurrency test) mirror the exact same pattern this codebase's tests already use (`CaptureFailureRecorder`, `MutableBox` in `ClipboardMonitorTests.swift`) — single-threaded-by-construction, standard for a test-local mutable box.
  **Verify, structural + empirical**: added `classify(_:)` genuinely runs off the main thread when called from Task.detached` (`PasteboardReaderTests.swift`) — `Task.detached { reader.classify(raw) }` compiles with no `await` needed on `classify` itself (a `classify` secretly requiring `@MainActor` would not compile there), PLUS a runtime `Thread.isMainThread` check (wrapped in a plain sync helper since the property itself is `NS_SWIFT_UNAVAILABLE_FROM_ASYNC`) confirms it's genuinely not on the main thread. Also added a byte-identical-output regression test (`pullRawPayload` + `classify` == `read(from:)`). Added `` `.image` decode+re-encode does not block the calling MainActor`` (`PasterTests.swift`) — a `@MainActor` counter task races alongside `paste(.image(2000×2000 synthetic))`; asserts `counter.value > 0` (loose, non-fragile — proves genuine interleaving happened, not an exact timing bound; would read `0` against the old synchronous-decode code). **Real measurement** against the user's actual 25MB blob (`d8370438...850f4f2`, confirmed present, 25,273,086 bytes — read-only throughout, `BlobStore.read` only, never `write`, via a throwaway test file I deleted before finishing): off-main blob read 9–101ms (machine/disk-cache-variance across 4 runs, all off-main), full `Paster.paste(.image:)` (ImageIO decode+encode off-main + pasteboard write on-main) 95–200ms wall-clock across runs on this dev machine's debug (`-Onone`) build (noisier than the user's original 34+49+11≈94ms figure, consistent with `-Onone` overhead — **not directly comparable to the user's release-adjacent number**, so I'm not claiming a speedup, only that the same work now runs off-main), and a concurrent `@MainActor` heartbeat prober (1ms `Task.sleep` ticks) measured max gaps of 9–23ms during that window — small relative to total wall time, though not zero (attributable to `Task.sleep` scheduling granularity under a debug build, not to the decode blocking the main actor, per the deterministic counter-task test above which is the harder proof). Baseline (root `swift test`) went 212→**215/215** (+2 `PasteboardReaderTests`, +1 `PasterTests`); App (`xcodegen generate` + `xcodebuild ... -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test`) stayed **104/104, 11 suites, TEST SUCCEEDED** (19 existing `PickerViewModelPasteContentTests` funcs updated to `async`/`await` for the now-`async` `pasteContent`, zero new App tests, zero behavior assertions changed). `swift format lint --recursive --strict Sources Tests ClipnestApp/Sources` → clean; `swift format lint --strict ClipnestApp/Tests` → only the known pre-existing `HotkeyManagerTests.swift:25` LineLength failure (confirmed pre-dating this task, left alone per instruction) — every file I touched individually re-linted clean. Also fixed 5 now-stale doc-comment references to the removed `pasteAndDismiss(_:)` name in `PickerViewModel.swift`'s top-of-file design history and field doc comments, so they point at `performPaste(_:frontmostApp:)` instead (comments-only, no behavior change). **Heavy main-actor work found beyond the brief's audit**: none in the capture/paste paths — I re-read both fully. Not audited (out of this task's scope, flagging for a future pass): `ItemRow`/`ItemPreview`'s thumbnail loaders already use `Task.detached` (confirmed pre-existing, left alone); did not audit Snippets CRUD, hotkey/AX code, or the update-checker path for similar issues. **Deliberately left out**: did not attempt to reproduce/confirm the user's reported paste-hang (explicitly out of scope per the brief — root cause not yet confirmed); did not add a permanent test depending on the real blob store (throwaway harness only, deleted, numbers quoted above); did not touch `Sources/ClipnestCore/OCR/*`, `ClipItem.swift`, or any Store file (T-OCR8's uncommitted work, left exactly as staged, built on top of without reverting). `git add` done on exactly 8 touched files (`Sources/ClipnestCore/Clipboard/{ClipboardMonitor,PasteboardReader}.swift`, `Sources/ClipnestCore/Paste/Paster.swift`, `Tests/ClipnestCoreTests/{PasteboardReaderTests,PasterTests}.swift`, `ClipnestApp/Sources/UI/Picker/{PickerViewModel+Paste,PickerViewModel}.swift`, `ClipnestApp/Tests/ClipnestAppTests/PickerViewModelTests.swift`) — did not stage/touch `.claude/task-board.md`, `README.md`, or any other pre-existing in-flight file in this shared tree. No commit. Was spawned this session — returning status here + in my final message rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling is `status:test` (real `swift test`/`xcodebuild test`/lint output pasted above and in my final report, no separate tester run performed by me) — recommend T-PERF1 → `status:test`, then T-PERF2 (reviewer→tester, already on the board) proceeds as planned.
- 2026-09-03 [T-HANG1/T-HANG2] Fixed the reproduced paste hang (P0) + the self-paste-suppression race, both confirmed root-caused by T-STRESS1. Independently re-reproduced BOTH before touching code: (a) ran `tools/stress-harness` fresh, hit the same section-7 stall, `sample` on my own run (`.claude/logs/stress-artifacts/senior-dev-before-fix-sample1.txt`) shows 9 cooperative-pool threads pinned in `ClipboardMonitor.scheduleTextRecognition`→`VisionTextRecognizer.recognize`→`withCheckedContinuation`, zero `Paster` frames anywhere — matches tester's `sample_paste_hang.txt` exactly; (b) wrote a NEW deterministic unit-level regression test (`VisionTextRecognizerTests.manyConcurrentRecognitionsDoNotStarveTheCooperativePool`) that reproduces the same starvation in `swift test` itself — watched it FAIL against the unfixed code (killed at 6:21/381s elapsed, only 4.35s CPU consumed — genuinely blocked, not spinning; `sample` on the stuck test process shows 13 cooperative threads pinned in `recognizeText`, the racing heartbeat's `tick()` essentially starved — 1 sample hit in a 3s capture window).

T-HANG1 root cause (own words): two compounding defects in `Sources/ClipnestCore/OCR/VisionTextRecognizer.swift`. (1) `recognize(in:quality:)` called the SYNCHRONOUS, blocking `VNImageRequestHandler.perform(_:)` directly inside a `withCheckedContinuation` closure with no `await` inside — the textbook blocking-work-in-async-context anti-pattern, which pins whatever Swift-concurrency cooperative-pool thread runs it for the call's full duration. (2) `ClipboardMonitor.scheduleTextRecognition` fired one uncapped `Task.detached(priority:.utility)` per `.image` capture — N images captured together meant N cooperative-pool threads blocked at once; on this 10-core machine ~10 images fully pinned the pool, starving ALL other off-main work app-wide, including `Paster.paste(.image:)`'s own detached TIFF-normalize step (measured stall: 164s+, never resolved, in T-STRESS1's original repro).

Fix: added `VisionTextRecognizer.recognitionQueue`, a dedicated SERIAL `DispatchQueue` (`qos: .utility`) that Swift concurrency doesn't own. `recognizeText(in:quality:)` now dispatches its ENTIRE pipeline (decode, pixel-ceiling check, downscale, the `VNRecognizeTextRequest`/`perform` call) onto this queue via `.async` inside the `withCheckedContinuation` closure — no cooperative-pool thread is ever blocked by Vision again, at any load. Being SERIAL also structurally enforces the concurrency cap with no separate counter needed. Cap chosen: `maxConcurrentRecognitions = 1`, justified not just as "safe" but as throughput-OPTIMAL: the `sample` evidence itself shows Apple's own `VNDetector` already serializes the real work onto its own internal serial queue (`com.apple.VN.detectorSyncTasksQueue.VNCRImageReaderDetector`) regardless of caller concurrency — a cap of 2+ would only occupy a second thread for zero additional throughput. Queueing policy: FIFO (GGD's default for a serial queue), unbounded depth, nothing ever dropped — a burst just makes that image's OCR latency go up, never silently loses a requested recognition; `checkNow()` capture latency is completely unaffected either way since OCR scheduling was always fire-and-forget. Regression test switched from an initial (flawed) "outstanding-calls" tracker — which can't distinguish "queued" from "genuinely executing" once ANY queue exists, so it never went green even against the fix — to a cooperative-pool-starvation proof (races an unrelated sleep-paced heartbeat `Task.detached` against N=`activeProcessorCount` concurrent `.fast` recognitions, asserts worst tick gap stays under 1000ms). Watched fail (381s+, killed) then pass (0.582s) against the identical test, same machine.

T-HANG2 root cause (own words): `PickerViewModel+Paste.performPaste` called `suppressOwnPasteboardWrite(pasteboard.changeCount)` only AFTER `await paster.paste(...)` had FULLY returned — i.e. after the write, then `synthesisDelay` (40ms default) elapsed, then a real event post. `ClipboardMonitor.ignore(changeCount:)` was therefore armed 40ms+ after the write, a window the real 0.4s capture poll could and did land inside (T-STRESS1: 18/18 raced at 5-42ms; for `.image` content, `Paster`'s TIFF re-encode isn't byte-identical to the original so a raced self-paste computed a genuinely different `contentHash` — a real duplicate row + wasted blob, not just a bumped `createdAt`).

Fix: `Paster.paste(_:targetingFrontmostApp:onPasteboardWrite:)` gained a new `onPasteboardWrite: (@MainActor @Sendable (Int) -> Void)? = nil` parameter (defaults nil, every other/test call site unaffected), invoked with `pasteboard.changeCount` immediately after the write — before `synthesisDelay`'s sleep even starts. `@MainActor`-typed (not `Paster` itself, which stays actor-agnostic) because its one real caller needs to call a `@MainActor` method; `paste()` `await`s it (a single actor hop, not a 40ms sleep). `PickerViewModel+Paste.performPaste` now passes `suppressOwnPasteboardWrite` straight through as this callback instead of reading `pasteboard.changeCount` itself after `paste()` returns — the post-call read+suppress line is gone entirely. Updated `Paster.paste`'s doc comment (the invariant callers depend on, and the "adding a suspension before the write is safe / between write-and-report is not" reasoning) to state the new contract. Regression test in `PasterTests.swift` (`onPasteboardWriteFiresBeforeSynthesizedKeystroke`, deterministic, `synthesisDelay: .zero`, proves call ORDER via a recorder — not wall-clock timing, since this file's convention forbids real timers): watched it fail (temporarily moved the call to after the synthesizer, exactly reproducing the OLD ordering — `events == ["synthesized", "wrote:42"]`), then pass after restoring. Two more new tests: `onPasteboardWriteFiresForImageContent` (proves the callback still fires with the right changeCount even with `.image`'s internal `Task.detached` decode suspension before the write) and `onPasteboardWriteNotCalledWhenWriteNeverHappens` (invalid image data throws before any write — callback correctly never fires).

Updated `tools/stress-harness/Sources/StressHarness/Scenarios/SelfPasteRaceScenario.swift`'s `raceTrial` to mirror the NEW production ordering (`onPasteboardWrite` fires before the sleep, not after `paste()` returns) and tightened the offset sweep to sub-ms/low-ms (was: straddling the 40ms boundary). Fresh harness run (`.claude/logs/stress-artifacts/senior-dev-after-fix-run.txt`): TEXT 0/24 self-captured at every offset from 0ms to 80ms (was 18/18 raced at 5-42ms); IMAGE 0/18 self-captured at every offset 0-40ms (was 2/3 raced at 55ms); `imageRaceProducedDistinctContentHash` now `false` (was `true`). Section 7 (paste-during-OCR-saturation, the exact scenario that stalled 164s+ unresolved before) now completes cleanly: `ocrImagesSubmitted=14 baselinePaste=0.7ms duringSaturationPaste=0.2ms slowdown=0.3x`, full harness reaches "DONE" for the first time this project has seen.

Honest caveat found and NOT hidden: the harness's own `ConcurrencyTracker`/`maxConcurrentRecognitions` metric (used by `OCRConcurrencyScenario`) still reports "6 of 6" post-fix — this is a real limitation of that specific metric, not evidence the fix doesn't work: it brackets `enter()`/`exit()` around the WHOLE `recognizeText` call (submission to completion), so a call sitting queued behind the new serial `recognitionQueue` looks identical to one genuinely executing — both count as "outstanding." Did not fix the harness's tracker (out of this task's scope, harness isn't production code) — flagging it so nobody cites "6 of 6" as post-fix counter-evidence; the real proof is the starvation/hang tests above, which measure the actual symptom, not outstanding-call count.

Verification: `swift test` → "Test run with 220 tests in 15 suites passed" (216 baseline + 4 new: 1 VisionTextRecognizerTests replacing an earlier flawed version, 3 PasterTests). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` → "** TEST SUCCEEDED **", "Test run with 104 tests in 11 suites passed" (unchanged baseline — no ClipnestApp test files touched). `swift format lint --recursive --strict Sources Tests` → exit 0. `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0. `swift format lint --recursive --strict ClipnestApp/Tests` → 1 finding, `HotkeyManagerTests.swift:25` LineLength — pre-existing, confirmed unrelated to this diff (not a file I touched), matches the task's explicit ignore-list. Never touched `~/Library/Application Support/Clipnest/` (read-only harness fixtures only, all stores under fresh temp dirs). `git add` on exactly 6 touched files (`Sources/ClipnestCore/OCR/VisionTextRecognizer.swift`, `Sources/ClipnestCore/Paste/Paster.swift`, `ClipnestApp/Sources/UI/Picker/PickerViewModel+Paste.swift`, `Tests/ClipnestCoreTests/{VisionTextRecognizerTests,PasterTests}.swift`, `tools/stress-harness/Sources/StressHarness/Scenarios/SelfPasteRaceScenario.swift`) — did not stage/touch `.claude/task-board.md` or `README.md`. No commit. Was spawned this session — returning status here + in my final report rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling is `status:test` (real `swift test`/`xcodebuild test`/lint output pasted above, no separate tester run performed by me) — recommend T-HANG1/T-HANG2 → `status:test`, hand to reviewer (security-relevant: none of this touches auth/secrets/PII/external I/O, so the mandatory security-pass trigger does not fire, but reviewer should still confirm) → tester for the `done` evidence ref.

T-SET1 (Settings window opens behind other apps, macOS 26) — reproduced independently before touching code: focused Terminal, triggered "Settings…" via the AX tree, confirmed `osascript` frontmost stayed "Terminal" and `CGWindowListCopyWindowInfo` showed Clipnest's Settings window ("Shortcuts") on-screen but at index 2, behind 2 other real windows; debug log showed `openSettingsAndFocus: raising window, appActive=false` — matches the routed evidence exactly. Root cause confirmed: `.accessory`-policy apps have no Dock icon, and macOS will not select/activate a window belonging to a Dock-icon-less app on this OS — no amount of `orderFrontRegardless`/`makeKeyAndOrderFront`/`activate(ignoringOtherApps:)` changes that; round 1's fix (RunLoop deferral + those calls) was necessary but not sufficient. Independently corroborated via web search: Peter Steinberger's "Showing Settings from macOS Menu Bar Items" (steipete.me, Jun 2025) documents the identical failure on macOS Tahoe (26) and ships the same fix in production (VibeTunnel).

Fix: new `ClipnestApp/Sources/App/SettingsActivator.swift` — briefly flips `NSApp.setActivationPolicy(.regular)`, force-activates (`activate(ignoringOtherApps: true)`, kept over cooperative `NSApp.activate()`/`yieldActivation(to:)` — evaluated and rejected, see the type's doc comment for why), then restores `.accessory` via a one-shot `NSWindow.willCloseNotification` observer on the Settings window — never on a timer, never immediately after activating. Wired into `MenuBarContent.openSettingsAndFocus()` (`ClipnestApp.swift`) as a `private static let` (not instance state — `MenuBarContent` is a SwiftUI `View` struct recreated on nearly every menu render, so instance state would silently lose "are we currently `.regular`"). Live verification caught a real bug in my first version: the idempotency guard I used to avoid double-registering the close observer also skipped force-activation on a second "Settings…" click while the window was already open but had lost focus (user switched to another app without closing Settings) — a second click did nothing. Fixed by decoupling: `setActivationPolicy`/the close-observer registration stay guarded (once per open/close cycle), `activateApp()` now runs unconditionally on every `activate(observing:)` call (harmless when already active). All AppKit calls are injected via closures so the sequencing/guard logic has unit coverage (`SettingsActivatorTests.swift`, 5 tests: first-open runs full sequence, repeat-open-while-open re-activates without re-toggling policy/double-observing — the regression test for the bug above, close restores accessory, double-close-fire only restores once, reopen-after-close re-arms the full sequence). The AppKit calls themselves are not unit-testable (no window server in `swift test`) — verified manually only, per below. Fixed one new Swift 6 strict-concurrency warning I introduced (captured-var-mutated-after-capture on the notification token) using the same `nonisolated(unsafe)`-boxed-reference-type justification already established by `PickerPanel.commandDeleteMonitor`, plus `MainActor.assumeIsolated` at the `queue: .main` boundary matching `HotkeyManager.observeLibraryNotifications()`'s existing pattern — zero new warnings from my files (confirmed against a stashed pre-change baseline: the `RunLoop.main.perform(inModes:)` MainActor-isolation warnings in `ClipnestApp.swift` are 100% pre-existing, same count/shape before and after).

Manual verification on this machine (macOS 26.6.2), Accessibility tree + `CGWindowListCopyWindowInfo` (screencapture is TCC-blocked) — reproduced fail, then re-verified fixed after every code change: (1) fresh open from another app → frontmost flips to Clipnest, Settings window rises above every real app window (only ahead of a persistent sandbox-harness overlay ["Terminal Hub"/hub-app] that sits above everything in this dev environment regardless of activation policy, before and after — not a Clipnest window, not affected by this fix). (2) Dock icon appears for as long as Settings stays open (this is a real, non-flash, visible Dock icon — not a brief flicker — same tradeoff steipete's writeup documents; disclosing plainly per the task's ask rather than hiding it) and disappears within ~1-2s of closing (small Dock un-bounce lag, not a bug — window itself is confirmed fully closed immediately). (3) Reclaim-focus case: switch to another app while Settings stays open, click "Settings…" again → Clipnest reclaims frontmost (this is the case my first implementation failed and the second fixed). (4) Picker unaffected: triggered "Open Clipnest" while Finder frontmost → Finder stays frontmost, Clipnest shows no Dock icon — `PickerPanel`'s non-activating behavior and `FrontmostAppTracker`'s paste-target recording are untouched by this change (`SettingsActivator` is never invoked from the picker's code path). One accepted, pre-existing-class edge case not introduced by this fix: if the user has Settings open (now genuinely frontmost) and invokes the picker via hotkey before closing it, `FrontmostAppTracker` would record Clipnest itself as the paste target — identical in kind to the already-existing `SnippetEditorWindow` (a real activating window), not a new regression, out of this task's scope.

Verification: `swift test` → "Test run with 224 tests in 16 suites passed" (baseline in this working tree is 224, not the task's cited 220 — pre-existing drift from already-present, unrelated uncommitted OCR work in the tree before I started; zero failures, zero regressions from this diff either way). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' test` → "** TEST SUCCEEDED **", "Test run with 123 tests in 14 suites passed" (same pre-existing-drift note vs. the cited 104; +5 are my new `SettingsActivatorTests`). `swift format lint --recursive --strict Sources Tests` → exit 0. `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0. `swift format lint --recursive --strict ClipnestApp/Tests` → 1 finding, `HotkeyManagerTests.swift:25` LineLength — pre-existing, not a file I touched, matches the task's explicit ignore-list. `git add` on exactly the 3 touched/added files (`ClipnestApp/Sources/App/ClipnestApp.swift`, `ClipnestApp/Sources/App/SettingsActivator.swift`, `ClipnestApp/Tests/ClipnestAppTests/SettingsActivatorTests.swift`) — did not touch `.claude/task-board.md` or `README.md`, did not stage the large amount of unrelated pre-existing changes already sitting in this working tree. No commit. Installed build: `scripts/dev-install.sh` → Clipnest 0.8.1 (build 1), signed with the local dev identity, currently running at `/Applications/Clipnest.app` — this is the fixed build, left installed and verified working. Was spawned this session — returning status here + in my final report rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling is `status:test` (real `swift test`/`xcodebuild test`/lint output above, plus the manual on-device verification above — no separate tester run performed by me); no auth/secrets/PII/external-I/O surface touched, so the mandatory security-pass trigger does not fire, but reviewer should still confirm — recommend T-SET1 → `status:test`, hand to reviewer → tester for the `done` evidence ref.
- 2026-09-04 [T-SET5] User report "cmd+, isn't working properly" for Settings — was true, ⌘, had never been wired anywhere. Built it for the two approved contexts: while the picker is open, and while Settings itself is focused. Reused T-SET1's activation machinery end-to-end rather than building a second path — extracted `MenuBarContent.openSettingsAndFocus()`'s window-finding/`SettingsActivator`-driving/RunLoop-deferral sequence into new `ClipnestApp/Sources/App/SettingsFocusCoordinator.swift` (`focusAfterOpening()`), called from both `MenuBarContent` and the picker's new path; `MenuBarContent` shrank by ~90 lines with zero behavior change (re-verified: menu bar "Settings…" still opens+raises correctly, log line `focusAfterOpening: raising window` fires same as before). Context (2) — ⌘, while Settings is already frontmost — needed **zero new code**: verified live (before touching anything) that SwiftUI's own auto-generated Settings-scene app-menu command already handles it once the app is `.regular` (which `SettingsActivator` already makes true for the duration Settings is open); confirmed via AX (`front window` name stays "General", `frontmost` stays true, no duplicate window — exactly 1 "General"-titled window before and after). Context (1) — picker open — hit a real, non-obvious bug during live verification, root-caused not guessed: adding a `KeyEquivalent(",")` case to `PickerView.handle(_:)`'s `.onKeyPress` compiled and looked correct but was dead code at runtime — `-[NSApplication sendEvent:]` matches a physical keyDown against `NSApp.mainMenu`'s key equivalents (the `Settings` scene auto-generates a hidden ⌘, item bound to its own `openSettings()`) *before* the key window's SwiftUI content ever sees it, even for an `.accessory`-policy app with no visible menu bar, as long as one of its own windows is `key` (the picker panel is). Reproduced with real evidence: with the naive `.onKeyPress` case in place, ⌘, while the picker was open silently created the Settings window via the built-in path (`CGWindowListCopyWindowInfo` showed it existed) but never raised/activated it (`onscreen=false`, `frontmostApplication` stayed whatever app was active, zero log output from my new code) — the exact T-SET1 failure mode recurring through a different door. Fixed the same way `onCommandDelete` already had to solve an analogous field-editor-preemption problem: intercepted ⌘, in `PickerPanel`'s existing local `NSEvent` monitor (which runs before AppKit's menu-key-equivalent matching), added `onCommandComma: (() -> Void)?`, renamed the backing token `commandDeleteMonitor` → `localKeyMonitor` (now serves two keys, doc comments/2 cross-references in `SettingsActivator.swift` updated to match) and restructured the handler into a `switch event.keyCode` on two cases; removed the now-genuinely-unreachable `.onKeyPress(",")` case from `PickerView` and documented why in both files. Wired `panel.onCommandComma = { viewModel?.openSettingsFromPicker() }` in `AppEnvironment.init()`, same shape as `onCommandDelete`. New `PickerViewModel.openSettingsFromPicker()` (`dismiss()` then `openSettings()`) — dismisses the picker first (a UX choice, not a technical necessity: the picker is a non-activating panel so leaving it open behind Settings wouldn't corrupt anything, but Settings takes over full app activation for as long as it's open, so a second Clipnest surface sitting open behind it would just be confusing); `openSettings: () -> Void` is an injected closure (same DI pattern as `dismiss`/`requestAppUpdate`) wired from `PickerView.onAppear` to `{ openSettings(); SettingsFocusCoordinator.focusAfterOpening() }` since `@Environment(\.openSettings)` is only reachable from a View. Footer hint: measured (`NSHostingView(rootView: Text(...).font(.caption2)).fittingSize`, same methodology as T-SET2/T-SET4) before adding — appending "· ⌘, settings" gives worst-case margins of +50pt (History text/link), +24pt (History OCR-image), +20pt (Snippets, the tightest) against the existing 493pt budget — all positive, added it as the final tab-independent hint; documented the shrunk-from-+85pt margin in `ShortcutHints.swift` so a future addition re-measures rather than assumes room. Fixed the misleading `ClipnestApp.swift` header comment the task flagged (claimed ⌘, worked when it never did) — now accurately describes all three reachable paths. Unit tests: `PickerViewModelOpenSettingsFromPickerTests` (2 new — dismiss-then-openSettings ordering via closure spies, matching the `dismiss`/`presentSnippetEditor` spy pattern already used in this file) + 1 new `ShortcutHintsTests` case (`⌘, settings` present in every tab/capability state) + updated 6 pre-existing exact-match hint-string assertions; explicitly NOT unit-tested (stated plainly, not silently skipped): the actual ⌘, key interception in `PickerPanel`'s `NSEvent` monitor and `SettingsFocusCoordinator`'s window-raising — AppKit/window-server-dependent, same class as `onCommandDelete`/`SettingsActivator`'s own established manual-only precedent. Manual verification on this machine (macOS 26.6.2), Accessibility tree (`front window`/`frontmost` via System Events) + `CGWindowListCopyWindowInfo` (screencapture TCC-blocked) + `log stream --predicate 'subsystem == "com.clipnest.app"' --level debug`, against a fresh `scripts/dev-install.sh` build so the very-first-ever-`openSettings()`-in-process case was genuinely exercised (not just steady-state): (1) picker open, ⌘, → picker panel `onscreen` flips true→false (dismissed), Settings window (`name=General`) flips false→true, `frontmostApplication` becomes `com.clipnest.app`, log shows `SettingsFocusCoordinator: focusAfterOpening: raising window` — confirmed on the very first press after a fresh launch (the harder, previously-broken timing case) and again on a repeat press. (2) Settings already focused, ⌘, again → `front window` name unchanged ("General"), `frontmost` stays true, exactly 1 "General" window before and after (no duplicate, no crash) — safe no-op via SwiftUI's own built-in handling, confirmed both via CGWindowList and independently via AX. (3) Different app (TextEdit) frontmost, ⌘, → TextEdit's own real "Settings" (Preferences) window opens and is `onscreen=true` — confirms Clipnest registers no global ⌘, hotkey and leaves other apps' ⌘, completely untouched, per the explicitly-rejected-scope note in the task. Did not test ⌘⌫/delete manually against real clipboard history (destructive against the user's actual data on this machine; the refactor preserves that case's logic unchanged and is covered by the full green test suite + build). Verification: `swift test` → "Test run with 224 tests in 16 suites passed" (unaffected, zero `ClipnestCore` files touched). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' test` → "** TEST SUCCEEDED **", "Test run with 141 tests in 17 suites passed" (138 baseline + 3 new, exactly as expected). `swift format lint --recursive --strict Sources Tests` → exit 0 (silent). `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0 (silent). `swift format lint --recursive --strict ClipnestApp/Tests` → 1 finding, `HotkeyManagerTests.swift:25` LineLength — pre-existing, not touched by me, matches the task's explicit ignore-list. Files touched: `ClipnestApp/Sources/App/ClipnestApp.swift`, new `ClipnestApp/Sources/App/SettingsFocusCoordinator.swift`, `ClipnestApp/Sources/App/SettingsActivator.swift` (2 doc-comment cross-references only), `ClipnestApp/Sources/App/AppEnvironment.swift`, `ClipnestApp/Sources/UI/Picker/PickerPanel.swift`, `ClipnestApp/Sources/UI/Picker/PickerView.swift`, `ClipnestApp/Sources/UI/Picker/PickerViewModel.swift`, `ClipnestApp/Sources/UI/Picker/ShortcutHints.swift`, `ClipnestApp/Tests/ClipnestAppTests/PickerViewModelTests.swift`, `ClipnestApp/Tests/ClipnestAppTests/ShortcutHintsTests.swift`, `docs/usage.md` (my API/usage-reference slice — added `⌘,` to the shortcut table + a scoping note), `docs/features.md` (2 targeted paragraph fixes tied directly to code I changed — did not attempt a full re-citation pass across that file's many other line-number references, which are architect's broader technical record, not this task's scope). Did not touch `.claude/task-board.md` or `README.md` (per spawn instructions) and did not stage/touch the large amount of unrelated pre-existing uncommitted work already sitting in this working tree (OCR, glyph-classifier, stress-harness, etc.) — no `git add` run by me at all, leaving staging to whoever integrates this diff. No commit. Installed build: `scripts/dev-install.sh` → Clipnest 0.8.1, signed with the local dev identity, currently running at `/Applications/Clipnest.app` (pid confirmed live post-verification) — this is the fixed build, left installed and working, Settings window closed, app idle in `.accessory` mode. Was spawned this session — returning status here + in my final report rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling is `status:test` (real `swift test`/`xcodebuild test`/lint output above, plus the manual on-device verification above — no separate tester run performed by me). No auth/secrets/PII/external-I/O surface touched (pure UI-layer key routing + window activation), so the mandatory security-pass trigger does not fire, but reviewer should still confirm — recommend `status:test`, hand to reviewer → tester for the `done` evidence ref.
- 2026-09-04 [T-UX1] Settings button to run OCR over existing images. Design: `ClipStore.fetchImagesNeedingRecognition()` (new protocol method, implemented in both `InMemoryClipStore`/`SwiftDataClipStore` — the store does the filtering via its existing predicate machinery, not the UI) returns every `.image` item with a blob but no recognized text, newest-first. New `Sources/ClipnestCore/OCR/OCRBackfillCoordinator.swift` (plain `Sendable` struct, Core, fully unit-tested — no actor isolation of its own) iterates that snapshot ONE item at a time through the injected `TextRecognizing`, writing via `ClipStore.setRecognizedText(_:text:)` — the exact same write path/recognizer `ClipboardMonitor` uses at capture time, so it inherits `VisionTextRecognizer.recognitionQueue`'s serial-queue T-HANG1 fix for free (no second execution path, no concurrency of its own). Deliberately decoupled from `SettingsStore.isTextRecognitionEnabled` per the user's own framing ("the user can choose when to run OCR") — `run(quality:onProgress:)` never reads or writes that setting, only the `quality` value passed in. Main-actor safety: the one genuinely-synchronous per-item step (the blob read) runs inside its own `Task.detached(priority: .utility)` (mirrors `ClipboardMonitor.checkNow()`'s identical pattern) so the coordinator is safe to call directly from `@MainActor` with no extra hop required at the call site; recognition itself was already off any Swift-concurrency thread (Vision's own dedicated queue). Cancellation: cooperative `Task.isCancelled` check between items only — never mid-item, so an in-flight item always finishes+writes before a cancellation takes effect (proved deterministically in tests via a gated fake recognizer + `task.cancel()` while item 1 is paused mid-flight, not a race on a progress signal — an earlier version of that test raced and intermittently cancelled after 2 items instead of 1, root-caused and fixed by switching from "wait for progress event" to "gate the recognizer call itself"). Resilience: missing/dangling blob, no text found, and an item deleted mid-run (`ClipStoreError.notFound` from `setRecognizedText`, same precedent as `scheduleTextRecognition`) all count as "completed, not recognized" and never abort the run — each proven with its own test. Idempotent by construction (work set only ever contains unrecognized images). App layer: `OCRBackfillViewModel` (`ClipnestApp/Sources/System/`, `@MainActor @Observable`) owns the one background `Task`, exposes `pendingCount`/`isRunning`/`progress`/`lastSummary`; `AppEnvironment` builds one shared `VisionTextRecognizer` instance now (hoisted into a local, was inline before) and passes it to BOTH `ClipboardMonitor` and the new coordinator, making the shared-serialization guarantee explicit rather than an accident of the queue being `static`. `HistorySettingsView` gained a new row: pending count ("N images have no recognized text" / "All images already have recognized text" when 0, never a no-op button), Run button, live "Recognizing… N of M" + Cancel while running, and a finished-state line (Done/Cancelled — recognized X of Y) — reuses whatever quality the existing Fast/Accurate picker is set to, doesn't duplicate any wording/logic from the T-OCR2 toggle above it. Manual-only (not unit-tested): the actual SwiftUI rendering/layout of `HistorySettingsView`'s new row (per coding-standards.md, `ClipnestApp` stays thin — light smoke tests only) — its data plumbing (`OCRBackfillViewModel`) IS covered by 3 smoke tests (full run reaching a finished state + `pendingCount` refresh, cancellation reaching a real in-flight run). Tests added: 4 new `ClipStoreContractTests` scenarios (wired ×2 for `InMemoryClipStore`/`SwiftDataClipStore` = 8 tests) covering work-set selection/no-blob exclusion/ordering/empty-when-done; 7 new `OCRBackfillCoordinatorTests` (happy path + exact progress sequence, empty work set, idempotency, per-item failure resilience, deleted-mid-run item, cancellation, `pendingCount`); 3 new `OCRBackfillViewModelTests` (App layer). Also added the missing `fetchImagesNeedingRecognition` stub to `ClipboardMonitorTests`'s pre-existing `ThrowingClipStore` fake (mechanical, required by the protocol addition, no behavior change). Left out: no separate count-only `ClipStore` query — `pendingCount()` re-fetches the same work-set query rather than adding a second, driftable predicate (cheap at this app's real scale, tens of images). No retroactive migration/backfill trigger on app launch — this is purely the user-initiated button the task asked for, nothing runs automatically. Evidence: `swift test` → "Test run with 239 tests in 17 suites passed" (224 baseline + 15 new). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' test` → "** TEST SUCCEEDED **", "Test run with 144 tests in 18 suites passed" (141 baseline + 3 new). `swift format lint --recursive --strict Sources Tests` → exit 0 (silent, after one auto-format pass for 2 long lines). `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0 (after one auto-format pass; confirmed via `git diff` that the format run touched only my own files, not the large amount of unrelated already-staged work sitting in this tree from other concurrent agents). `swift format lint --recursive --strict ClipnestApp/Tests/ClipnestAppTests/OCRBackfillViewModelTests.swift` (my new file specifically) → exit 0; full-directory lint separately confirms the only other finding is the pre-existing `HotkeyManagerTests.swift:25` LineLength this task explicitly said to ignore. Never touched `~/Library/Application Support/Clipnest/` — all stores in temp dirs (`FileManager.default.temporaryDirectory`), never read either. Docs: updated `docs/API.md` (my API/usage-reference slice) — added `setRecognizedText`/`fetchImagesNeedingRecognition` to the documented `ClipStore` protocol block (the former was an undocumented pre-existing gap from T-OCR2, fixed in passing since I was already editing that exact block) and a new `## OCR` section covering `TextRecognizing`/`VisionTextRecognizer`/`OCRBackfillCoordinator` with a working example. `git add` on exactly the 16 files this task touched/added (Core: `ClipStore.swift`, `InMemoryClipStore.swift`, `SwiftDataClipStore.swift`, new `OCRBackfillCoordinator.swift`, `ClipStoreContractTests.swift`, `ClipStoreTests.swift`, `SwiftDataClipStoreTests.swift`, `ClipboardMonitorTests.swift`, new `OCRBackfillCoordinatorTests.swift`; App: `AppEnvironment.swift`, `ClipnestApp.swift`, `SettingsView.swift`, `HistorySettingsView.swift`, new `OCRBackfillViewModel.swift`, new `OCRBackfillViewModelTests.swift`; `docs/API.md`) — did not touch `.claude/task-board.md` or `README.md` (per spawn instructions), did not stage the large amount of unrelated pre-existing work already sitting in this tree from other concurrent agents. No commit. Was spawned this session — returning status here + in my final report rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling is `status:test` (real `swift test`/`xcodebuild test`/lint output above, no separate tester run performed by me). No auth/secrets/PII/external-I/O surface touched (on-device Vision only, same privacy posture as the existing T-OCR2 toggle whose wording this reuses verbatim), so the mandatory security-pass trigger does not fire, but reviewer should still confirm — recommend `status:test`, hand to reviewer → tester for the `done` evidence ref.
- 2026-09-04 [T-PF2] Fixed clipboard image-capture cost: `PasteboardReader.imagePasteboardTypes` reordered to `[.png, .tiff]` (PNG preferred, TIFF fallback-only) to stop storing uncompressed TIFF blobs (real capture measured 25,273,086 bytes) and stop forcing an in-process PNG→TIFF conversion on the main actor. Added named capture-time ceilings `PasteboardReader.maxCapturedImageByteSize` (50 MB, matches `VisionTextRecognizer.maxByteSize`) and `maxCapturedImagePixelDimension` (20,000 px, matches `VisionTextRecognizer.maxPixelDimension`), enforced in `readImage(_:)` before hashing/blob-handoff; `classify(_:)` is now `(RawPayload) -> Classification?` so an over-ceiling image degrades to "nothing captured this cycle" (same contract as an unsupported pasteboard type) — no crash, no partial write. `pullRawPayload(from:)`/`classify(_:)` main-actor/off-main split left structurally untouched; only `ClipboardMonitor.checkNow()`'s `guard let result = classified else { return nil }` changed to unwrap the new optional. CONTENTHASH FINDING (explicit, per task): YES, `contentHash` changes for any image where both `.png`/`.tiff` were offered — it's now computed from PNG bytes instead of TIFF bytes — so a fresh re-copy of content already in a user's history (captured pre-fix, hash from TIFF) will insert as a NEW duplicate item instead of bumping the existing one, once. One-time, not repeating; flagged for a product decision, not fixed here (would need a migration/backfill, out of this task's scope + files). `Paster.normalizedToTIFF` already decodes+re-encodes either format for paste, confirmed unaffected. Added 6 tests to `PasteboardReaderTests.swift` (PNG-preferred, TIFF-only, at/over byte ceiling, over pixel ceiling, under-ceiling unaffected) and 3 to `ClipboardMonitorTests.swift` (TIFF-only full pipeline, over-byte-ceiling clean rejection incl. no blob dir created, over-pixel-ceiling clean rejection) — all via fakes, no real `NSPasteboard` touched. `swift test`: 249/17 passed (pre-existing T-PERF3 timing test flaked once in ~14 full-suite runs under this session's heavy parallel-agent CPU contention — reproduced 6/6 stable in isolation, unrelated to this diff, not introduced by it). `swift format lint --recursive --strict Sources Tests`: exit 0. Files: `Sources/ClipnestCore/Clipboard/PasteboardReader.swift`, `Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift`, `Tests/ClipnestCoreTests/PasteboardReaderTests.swift`, `Tests/ClipnestCoreTests/ClipboardMonitorTests.swift`. `git add` on exactly those 4 files, no commit. Spawned this session (one of 4 parallel agents) — returning status here + in final report, not writing `.claude/task-board.md` (integrity rule 1); ceiling `status:test`. No auth/secrets/PII/external-I/O surface (local pasteboard/disk only), security-pass trigger does not fire but reviewer should confirm.
- 2026-09-04 [T-PF1] Fixed P0 launch latency (D1: two full-table normalizedText backfill scans + two ModelContainer opens synchronously on main at launch; D2: enforceRetention full-table materialization + N synchronous blob deletes on the store actor, at launch AND after every capture). D1 fix: moved the normalizedText backfill out of `SwiftDataClipStore`/`SwiftDataSnippetStore`'s non-async `init` (signature unchanged, still compiles/works synchronously for every existing caller/test) into a new `func prepare() async` on each actor; made it genuinely one-shot via a `UserDefaults` completion marker keyed to the store's own on-disk `ModelConfiguration.url.path` (nil/always-rescans for `isStoredInMemoryOnly` test containers — no cross-test UserDefaults collision risk, no schema change, no data-modeling consult needed). Corrected the doc comment that wrongly claimed re-running the scan every launch was "cheap" (it was a full unindexed scan every time, marker is what actually makes it one-shot). `AppEnvironment.init` is now `async throws`; both stores' `makeProductionContainer()` (blocking disk I/O + `ModelContainerRecovery`'s corrupt-store retry, unchanged/not touched) + `prepare()` run inside `Task.detached` (so they do NOT inherit `@MainActor` isolation), concurrently via `async let`; `AppDelegate.applicationDidFinishLaunching` now spawns a `Task` calling the async init instead of blocking on it, so it returns immediately. D2 fix: `enforceRetention`'s `.maxCount` branch now does `ModelContext.fetchCount` (SQL COUNT, zero `ClipItemRecord` materialization) instead of fetching the whole unpinned table just to call `.count`, then fetches ONLY the oldest `excess` rows via `fetchLimit` — exact same oldest-first-among-unpinned semantics, verified via a new 25-item/pinned-interleaved scale test plus the full existing `ClipStoreContractTests` suite (untouched, all branches still pass unmodified: nil-cap/maxCount/maxAge/pinned-exclusion/blob-cleanup). Blob deletion moved off the actor's synchronous critical path via `Task.detached` + `await` (NOT fire-and-forget — still `try await`ed so `enforceRetention`'s existing "blobs gone by the time this call returns" contract holds for `ClipStoreContractTests.enforceRetentionDeletesTrimmedBlobs`, which I don't own and can't relax) — an actor releases its exclusivity across that await, so a picker `query()` queued on the same actor during blob I/O no longer waits behind it. Post-capture retention debounced (`AppEnvironment.scheduleRetentionEnforcement()`, cancel-and-reschedule, 750ms named constant, re-reads `retentionCap` after the delay not before) so a burst of N captures runs ~1 pass, not N; launch-time `enforceRetentionNow()` left as a single direct call (no burst to coalesce). Did NOT touch `ClipStore.swift` (not owned) — `deleteBlobs`'s collect-failures-then-throw-once policy reused as-is, just called from inside the detached Task. New tests (swift-testing): one-shot backfill via 2 sequential on-disk store instances proving the SECOND `prepare()` skips its scan (a stale row inserted after the first `prepare()` stays un-backfilled — not just idempotency), construction-does-not-backfill-synchronously (deterministic, no timing/flakiness — checks the legacy row is still unmatchable immediately after `init`, matchable only after explicit `prepare()`), both mirrored for Clip+Snippet stores; updated the 2 existing per-store migration tests to call `prepare()` explicitly (backfill no longer runs in `init`). Verification: `swift test --scratch-path .../pf1-build` → "Test run with 254 tests in 17 suites passed", 0 failures (stderr CoreData "Store failed to load" lines are expected noise from the pre-existing intentional corrupt-store-bytes recovery tests, not failures). `swift format lint --recursive --strict Sources Tests` → exit 0 (silent). `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0 (silent). Also built the separate `tools/stress-harness` SPM package (depends on `ClipnestCore` via local path, not part of root `swift build`/`swift test`) as an extra compile check since it constructs `SwiftDataClipStore` and calls `enforceRetention` directly — `swift build` there → "Build complete", 0 errors (pre-existing unrelated warnings in a file I don't own). Did NOT run `xcodebuild` (per task instructions — user runs the app-target suite centrally after all 4 agents land), so `AppEnvironment.swift`/`AppDelegate.swift`'s `async`/`Task.detached`/`async let` restructuring is verified by careful reading + exact pattern-matching against this same file's own pre-existing, already-shipped `Task { [weak self] in ... }`/`Task.detached` usages (not by compiler) — flagging this explicitly as the one gap in my verification, for the centralized xcodebuild pass to close. Files touched: `Sources/ClipnestCore/Store/SwiftDataClipStore.swift`, `Sources/ClipnestCore/Store/SwiftDataSnippetStore.swift`, `ClipnestApp/Sources/App/AppEnvironment.swift`, `ClipnestApp/Sources/App/AppDelegate.swift`, `Tests/ClipnestCoreTests/SwiftDataClipStoreTests.swift`, `Tests/ClipnestCoreTests/SwiftDataSnippetStoreTests.swift` — did NOT touch `ModelContainerRecovery.swift` (no change needed; `backupSuffixPrefix`/recovery behavior preserved exactly, confirmed by the untouched corrupt-store recovery tests still passing). `git add` on exactly those 6 files, no commit. Spawned this session (one of 4 parallel agents on T-PF1..T-PF4) — returning status in my final report rather than writing `.claude/task-board.md` myself (integrity rule 1); ceiling `status:test` (real `swift test`/lint output above, no separate tester run performed by me). No auth/secrets/PII/external-I/O surface touched (local SwiftData/UserDefaults/disk only), security-pass trigger does not fire, but reviewer should confirm.
- 2026-09-04 [T-PF3] P0 image-hang fix: bounded ImageIO decode (ImageThumbnailDecoder, CGImageSourceCreateThumbnailAtIndex, row=64px/preview=1600px) replacing NSImage(data:)'s deferred full-res decode; split row/preview NSCache with real countLimit+totalCostLimit (SizedImageCache); RowThumbnailLoadLimiter caps concurrent row decodes at 4; ItemPreviewController reuses one NSHostingController<AnyView> and skips fittingSize for plain-image previews (boundedImageContentSize, analytic aspect-fit). Fixed 2 stale comments (ItemRow decode-off-main claim, ItemThumbnailCache auto-evict claim); flagged 3rd (PickerViewModel+Preview.swift:36-38, not owned) in return to spawner. 12 new tests added (ImageThumbnailDecoder/SizedImageCache/RowThumbnailLoadLimiter/ScaledImage), full suite 163 tests green, xcodebuild TEST SUCCEEDED, swift-format lint clean on Sources. → review/test.
- 2026-09-04 [T-PF1] Fixed reviewer rejection on the normalizedText-backfill marker (2 findings). Blocking finding: `backfillNormalizedText(in:)` swallowed its own fetch/save failures internally but `prepare()` marked the backfill complete unconditionally regardless — a transient I/O error permanently marked a failed backfill "done" with no retry. Fix: `backfillNormalizedText(in:)` in both `SwiftDataClipStore.swift`/`SwiftDataSnippetStore.swift` now returns `Bool` (true = genuinely completed — either backfilled or found nothing stale; false = fetch/save threw, already logged metadata-only per privacy rule), and the marker is set only on `true`. DRY finding: extracted the marker plumbing (`backfillCompleteDefaultsKey`/`hasCompletedBackfill`/`markBackfillComplete`, near-verbatim duplicated between the two stores) into one new shared helper, `Sources/ClipnestCore/Store/OneShotStoreMigration.swift` — a plain `enum` (mirrors `ModelContainerRecovery`'s existing shared-helper-with-owner-supplied-closure shape in the same folder), `static func run(keyPrefix:modelContext:userDefaults:migration:)`, gates on success exactly once per (keyPrefix, on-disk store path) pair — `nil`/always-runs for `isStoredInMemoryOnly` containers, unchanged from before. Each store kept its own `backfillCompleteDefaultsKeyPrefix` constant (still exposed for test cleanup, name unchanged so no collateral test churn) and now just calls `OneShotStoreMigration.run(keyPrefix: Self.backfillCompleteDefaultsKeyPrefix, modelContext: modelContext) { Self.backfillNormalizedText(in: modelContext) }` from `prepare()`. Designed for T-PF5c: `keyPrefix` is a parameter (not baked into the helper), so `SwiftDataClipStore` can call `run(...)` a second time with a distinct prefix for the upcoming `contentHash` backfill, independently gated — proved directly by a new test (`differentKeyPrefixesOnSameFileAreIndependent`). New shared-helper test file `Tests/ClipnestCoreTests/OneShotStoreMigrationTests.swift` (5 tests, no `.serialized` needed — each test uses its own throwaway `UserDefaults` suite + temp store file, no shared mutable state): `failedMigrationDoesNotMarkCompleteAndRetries` (the required failure-path proof — migration returns false → marker NOT set → next `run()` call retries it → only a later `true` sets the marker → a third call then does not re-run), `successfulMigrationMarksCompleteAndSkipsNextRun`, `markerIsIsolatedPerStoreFilePath` (the required isolation proof — completing file A's migration does not skip file B's, same keyPrefix, two different on-disk files), `differentKeyPrefixesOnSameFileAreIndependent`, `inMemoryContainerNeverPersistsMarker`. Caught + fixed my own test-hygiene bug during verification: `UserDefaults(suiteName:)` + `removePersistentDomain` alone left a stray `~/Library/Preferences/OneShotStoreMigrationTests.*.plist` per test in the real, user-visible Preferences folder (removePersistentDomain only clears cfprefsd's in-memory cache, not the already-written backing file) — added `removeSuiteDefaults(_:suiteName:)` teardown that also deletes the physical plist directly; re-ran the full suite twice and confirmed zero leftover files both times. Verification: `swift test --scratch-path .../pf1b-build` → "Test run with 259 tests in 18 suites passed" (was 254 before this file existed; +5 new, 0 regressions; stderr CoreData "Store failed to load" lines are the same pre-existing expected noise from the corrupt-store-bytes recovery tests). `swift format lint --recursive --strict Sources Tests` → exit 0 (one line-length violation in my new test file, fixed, re-ran clean). Files touched: `Sources/ClipnestCore/Store/SwiftDataClipStore.swift`, `Sources/ClipnestCore/Store/SwiftDataSnippetStore.swift`, `Sources/ClipnestCore/Store/OneShotStoreMigration.swift` (new), `Tests/ClipnestCoreTests/SwiftDataClipStoreTests.swift` (doc-comment only), `Tests/ClipnestCoreTests/SwiftDataSnippetStoreTests.swift` (doc-comment only), `Tests/ClipnestCoreTests/OneShotStoreMigrationTests.swift` (new). Did not touch `AppEnvironment.swift`/`AppDelegate.swift` (out of scope, another parallel agent owns them) or `.claude/task-board.md` (integrity rule 1 — returning status to spawner). `git add` on exactly the 6 files above, no commit. One of three parallel agents in this working tree; `git status` during this task showed an unrelated `Sources/ClipnestCore/Media/` directory and `ImagePixelHashing.swift` appearing from a sibling agent's concurrent work — left untouched, not in my scope. No auth/secrets/PII/external-I/O surface touched (local SwiftData/UserDefaults/disk only); security-pass trigger does not fire, but reviewer should confirm. Ceiling `status:test` (dev's own verification only, no separate tester run performed by me).
- 2026-09-04 [T-PF3] Reviewer-rejected fix: `RowThumbnailLoadLimiter.acquire()` now cancellation-aware via `withTaskCancellationHandler` — a waiter cancelled while queued is removed from the FIFO and resumed with `false` (never runs `body`), race-freedom argued in-source against `release()`; `withPermit` now returns `T?` (`nil` = cancelled-while-queued, never ran). Added 2 deterministic tests (Gate/Flag continuation-based, bounded `pollUntil` spin, no unbounded sleeps): cancelled-while-queued never runs body + FIFO/permit accounting stays correct after a mixed cancelled/completed batch (full concurrency re-admitted after). Files: ClipnestApp/Sources/UI/Picker/RowThumbnailLoadLimiter.swift, ClipnestApp/Sources/UI/Picker/ItemRow.swift (call site updated for new double-optional, two-guard unwrap, explained in comments), ClipnestApp/Tests/ClipnestAppTests/RowThumbnailLoadLimiterTests.swift. `xcodebuild test` full suite: 165 tests/22 suites, TEST SUCCEEDED; `swift format lint --strict` clean on all 3 files. → reviewer.
- 2026-09-04 [T-PF1] Follow-up on the entry above, same task: replaced the real-`UserDefaults(suiteName:)`-based test setup with a proper storage abstraction, since `removePersistentDomain` + manual file deletion still raced `cfprefsd`'s async flush on a second full-suite run and reintroduced stray `~/Library/Preferences/OneShotStoreMigrationTests.*.plist` files (cleanup alone wasn't a real fix). `OneShotStoreMigration.swift` now defines `protocol OneShotMigrationStorage { bool(forKey:) / set(_:forKey:) }`; `UserDefaults` conforms via a zero-code extension (production untouched — `run(...)`'s default is still `UserDefaults.standard`); `OneShotStoreMigrationTests.swift` now uses a private in-memory `FakeOneShotMigrationStorage` (matches this codebase's existing `Fake...`/`@unchecked Sendable` test-double convention, e.g. `FakePasteboardWriting` in `PasterTests.swift`) — no real UserDefaults, no disk I/O, no race, no cleanup code needed. Re-ran `swift test` twice back-to-back plus a `~/Library/Preferences` scan after each: 259/259 both times, zero leftover files either time. `swift format lint --strict` on all 6 owned files individually → exit 0. Files touched (final): `Sources/ClipnestCore/Store/OneShotStoreMigration.swift`, `Tests/ClipnestCoreTests/OneShotStoreMigrationTests.swift` (both re-`git add`ed); `SwiftDataClipStore.swift`/`SwiftDataSnippetStore.swift` unaffected by this follow-up (they never pass the `storage`/`userDefaults` parameter, so the signature change was invisible to them).
- 2026-09-04 [T-PF5a] Built format-independent image pixel-hashing engine: `Sources/ClipnestCore/Media/ImagePixelHashing.swift` (protocol) + `CoreGraphicsImagePixelHasher.swift` (CGImageSource decode → sRGB/RGBA8/premultiplied-last canonical re-render → streamed row-band SHA256, `.copy` blend mode + exact-size crop-then-draw per band to avoid CGContext flip bugs). 10 tests in `Tests/ClipnestCoreTests/CoreGraphicsImagePixelHasherTests.swift` all pass: PNG==TIFF, 1-pixel/region diff → different hash, chunk-size invariance vs. an independent naive single-pass reference (evenly + unevenly dividing heights + wide/short), undecodable/empty → nil, stretch color-profile (sRGB vs Display P3 of same grayscale content) → identical hash, large-image (3200x2400) determinism. Extended `Tests/ClipnestCoreTests/TestSupport/ImageFixtures.swift` with `makePatternImageData` (real per-pixel content + one-pixel override) — purely additive. Full suite (269 tests) + `swift format lint --recursive --strict Sources Tests` both green. Measured real peak memory (getrusage ru_maxrss, isolated release-build process, `/private/tmp/.../scratchpad/pf5a-memprofile`) against genuinely large captures: peak delta for one `pixelContentHash(of:)` call is ~2.0-2.1x the raw decoded image size (not bounded to the ~12MB chunk budget — CGImage's own opaque cropping/decode overhead dominates), vs ~3.0x for a naive non-chunked whole-buffer reference at the same sizes — so the row-band design is verified to meaningfully help (~33% peak reduction) even though it doesn't hit the idealized "bounded to one chunk" target. → reviewer/tester. status:test, evidence: pasted `swift test`/`swift format lint` output + memprofile numbers in handoff to spawner.
- 2026-09-04 [T-PF5b/T-PF5e] Wired `CoreGraphicsImagePixelHasher` into `PasteboardReader.readImage` (defaulted `pixelHasher: any ImagePixelHashing` init param) for `.image`-only contentHash; added `maxPixelHashPixelCount = 40_000_000` (40 MP) total-pixel-count ceiling — above it, or when dimensions are unknown, falls back to `BlobStore.contentHash(of:)` without ever invoking the hasher (memory-safety proof via call-counting fake). Arithmetic: 40M px × 4 B/px = 160,000,000 B (~153 MiB) decoded × measured 2.1x peak ≈ 320 MB worst case. 9 tests added/updated in PasteboardReaderTests.swift, full suite (282 tests, 20 suites) green, `swift format lint --strict` clean. → reviewer/tester.
- 2026-09-04 [T-PF5c] One-shot migration of existing image rows' contentHash (byte-hash → decoded-pixel hash, D44/D45), built on already-landed `ImagePixelHashing`/`CoreGraphicsImagePixelHasher`/`OneShotStoreMigration`. `SwiftDataClipStore.init` gained a defaulted `imagePixelHasher: any ImagePixelHashing = CoreGraphicsImagePixelHasher()` param (mirrors `blobStore`'s injection shape); `AppEnvironment.swift`'s existing call site needs no change (confirmed, not edited). `prepare()` now runs a second, independently-gated `OneShotStoreMigration.run(...)` pass (own `imageContentHashBackfillCompleteDefaultsKeyPrefix`) after the existing `normalizedText` backfill: `backfillImageContentHashes(in:blobStore:hasher:)` fetches `.image` rows with `blobPath != nil`, reads each blob via `blobStore.read`, re-hashes via `hasher.pixelContentHash`, overwrites `record.contentHash`. Chosen failure semantics (stated explicitly per the task's ask): `BlobStoreError.notFound` (missing blob) and a `nil` hasher result (undecodable blob) are PERMANENT — logged metadata-only, row left unchanged, migration still reports success overall (retrying a blob that will never exist/decode is pointless and would block the marker forever). Any other `BlobStoreError.ioFailure` is treated as POTENTIALLY TRANSIENT — the whole migration reports `false` so the marker stays unset and the next `prepare()` (next launch) retries the full candidate set; rows already fixed before the transient failure are still saved (harmless to re-hash on retry). Actor-reentrancy note: `backfillImageContentHashes` is deliberately synchronous (not offloaded via `Task.detached` like `enforceRetention`'s blob-deletion fix) because `OneShotStoreMigration.run`'s `migration` param is a plain non-async `() -> Bool` closure (not touched, per spec) — no way to yield the actor mid-closure, and blocking-waiting on a detached Task from inside it would be a strictly worse anti-pattern (cooperative-pool thread exhaustion risk). Documented why this is safe today: `AppEnvironment.init` only assigns `clipStore`/`snippetStore` to `self` after `await`ing both stores' `prepare()` calls, so no other caller can be queued on this actor during the one-time scan. 6 new tests in `Tests/ClipnestCoreTests/SwiftDataClipStoreTests.swift`: legacy-hash row + real blob → backfilled to `CoreGraphicsImagePixelHasher`'s actual pixel hash; one-shot (call-counting `CountingImagePixelHasher` fake, on-disk store reopened twice, hasher invoked exactly once); missing-blob row left unchanged, no throw; non-image + blob-less-image rows untouched; undecodable-blob row left unchanged AND marker IS set (permanent-skip proof); transient `.ioFailure` (a directory placed at the blob path, so `fileExists` passes but `Data(contentsOf:)` throws) leaves the row unchanged AND marker NOT set on launch 1, then a second `prepare()` after fixing the transient condition successfully backfills it AND sets the marker (retry proof). `swift test --scratch-path .../pf5c-build`: "Test run with 282 tests in 20 suites passed" (0 failures, 0 regressions across the whole suite, not just this file). `swift format lint --recursive --strict Sources Tests` → exit 0. Files touched: `Sources/ClipnestCore/Store/SwiftDataClipStore.swift`, `Tests/ClipnestCoreTests/SwiftDataClipStoreTests.swift` — both `git add`ed, no commit. Did not touch `Clipboard/PasteboardReader.swift` (sibling parallel agent's file), `Media/`, `Store/OneShotStoreMigration.swift`, or `AppEnvironment.swift` (all read-only per spec). One of two parallel agents in this working tree. No auth/secrets/PII/external-I/O surface touched (local SwiftData/BlobStore disk I/O only); security-pass trigger does not fire, but reviewer should confirm. Did not touch `.claude/task-board.md` (integrity rule 1 — returning status to spawner). Ceiling `status:test` (my own verification only, no separate tester run performed by me).
- 2026-09-04 [T-PF5d] Added the end-to-end dedup contract scenario `dedupWithSameContentHashDifferentBlobPathPreservesOriginalBlobPath` to `Tests/ClipnestCoreTests/ClipStoreContractTests.swift` (Dedup section, right after `distinctHashesDoNotCollapse`): inserts an item with `contentHash: "same-pixel-hash", blobPath: "blobs/png-hash"`, then a second with the same `contentHash` but `blobPath: "blobs/tiff-hash"`; asserts exactly one surviving row, `createdAt` bumped to the second insert's time, and — the load-bearing assertion — `blobPath` on both the returned `bumped` item and `fetchAll().first` still reads `"blobs/png-hash"` (not overwritten), pinning D45's accepted consequence that the second capture's blob is orphaned on disk. Verification method, given my scope was locked to this one file (rest of the tree is mid-review): temporarily added one `@Test` wrapper each to `ClipStoreTests.swift` (InMemoryClipStore) and `SwiftDataClipStoreTests.swift` (SwiftDataClipStore) to prove the new scenario runs correctly against both conformances, ran `swift test --scratch-path .../pf5d-build --filter tempVerifyDedupWithSameContentHashDifferentBlobPathPreservesOriginalBlobPath` (2 tests in 2 suites, both passed), then fully reverted both wiring files (`git diff` on each is now empty) so the final change touches only `ClipStoreContractTests.swift`. Followed by a full-suite run (no filter) confirming 282 tests/20 suites still pass, and `swift format lint --recursive --strict Sources Tests` clean (exit 0, no output). `git add`ed only `Tests/ClipnestCoreTests/ClipStoreContractTests.swift`; no commit. Real gap to flag: since I could not permanently wire this scenario into `ClipStoreTests.swift`/`SwiftDataClipStoreTests.swift`, it does NOT yet run as part of the normal `swift test` invocation — a follow-up task must add the two `@Test` wrapper calls (exact bodies proven above) once those files are clear of concurrent review, or this contract scenario stays dead code. status:test, evidence: pasted swift test + swift format lint output above.
- 2026-09-04 [T-PF6] Two-round review cleanup, scope-locked to `PasteboardReader.swift`/`VisionTextRecognizer.swift`/`CoreGraphicsImagePixelHasher.swift`/`ImageThumbnailDecoder.swift`/`ItemRow.swift`/`docs/API.md` (Store/** off-limits, parallel agent's file). (1) DRY: new `Sources/ClipnestCore/Media/RGBAPixelFormat.swift` (`public enum RGBAPixelFormat { public static let bytesPerPixel = 4 }`) is the one shared source for "4 bytes per RGBA pixel" — unified because it's a single fact about the pixel format, not a tunable policy, so the 3 call sites (hasher's `canonicalBytesPerPixel`, App's `ImageThumbnailDecoder.bytesPerPixelEstimate`, `ItemRow`'s inline `* 4`) can never legitimately diverge; matches the existing cross-module-constant precedent (`ClipnestLog.subsystem`). (2) DRY judgment call on the ceilings: kept `PasteboardReader.maxCapturedImageByteSize`/`maxCapturedImagePixelDimension` and `VisionTextRecognizer.maxByteSize`/`maxPixelDimension` as SEPARATE named constants (not unified) — added an explicit "deliberately not unified, here's why" doc-comment cross-reference at all 4 sites so a future reviewer doesn't re-flag it: they're coincidentally equal today but answer different policy questions (capture/storage cost vs. Vision request cost) that could legitimately diverge; unifying would be false coupling, not DRY. (3) `docs/API.md`: fixed stale `.tiff`/`.png` → `.png`/`.tiff` capture-priority order (matches T-PF2's actual code); expanded `PasteboardReader`'s doc block to the full current public surface (`maxCapturedImageByteSize`/`maxCapturedImagePixelDimension`/`maxPixelHashPixelCount`, `RawPayload` enum, `pullRawPayload(from:)`, `classify(_:) -> Classification?` now optional); added a new `## Media` section (`ImagePixelHashing`/`CoreGraphicsImagePixelHasher`) between Clipboard and OCR, TOC updated. (4) Documented, at both sites, the two behavioral caveats reviewer round 2 flagged: EXIF/TIFF orientation not normalized (safe direction — missed dedup only, cross-referenced against `ImageThumbnailDecoder`'s orientation-correcting decode, which DOES apply it) and premultiplied-alpha collapsing RGB under alpha=0 (the one genuine false-dedup case, inherent to the correct canonicalization) — added a "KNOWN LIMITATIONS" block to `CoreGraphicsImagePixelHasher.swift`'s file doc comment plus a symmetric note in `ImageThumbnailDecoder.swift`, and pinned both with new regression tests (`transparentPixelRGBDifferenceDoesNotChangeHash`, `differingOrientationTagsOnSameRawPixelsHashIdentically`) in `CoreGraphicsImagePixelHasherTests.swift`. (5) Fixed the `maxPixelHashPixelCount` doc comment's "~320 MB" → "~320 MiB" (matches the MiB used one line above for the same number). No computed value or runtime behavior changed anywhere (constants' values, hash outputs, byteCost formulas all identical pre/post). `swift test --scratch-path .../pf6-build`: "Test run with 286 tests in 20 suites passed" (was 284 baseline + my 2 new tests, 0 regressions). `xcodebuild test -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' -derivedDataPath .../pf6-dd`: `** TEST SUCCEEDED **`, "Test run with 165 tests in 22 suites passed". `swift format lint --recursive --strict Sources Tests` and `... ClipnestApp/Sources` both exit 0, no output. `git add`ed only my 8 files (`PasteboardReader.swift`, `VisionTextRecognizer.swift`, `CoreGraphicsImagePixelHasher.swift`, `Media/RGBAPixelFormat.swift` [new], `ImageThumbnailDecoder.swift`, `ItemRow.swift`, `docs/API.md`, `CoreGraphicsImagePixelHasherTests.swift`); no commit. Did not touch `.claude/task-board.md` (integrity rule 1 — returning status to spawner) or any `Store/**` file. status:test, evidence: pasted `swift test`/`xcodebuild test`/`swift format lint` output above.
- 2026-09-04 [T-PF5c-restructure] Reviewer-rejected rebuild of the image contentHash backfill: no longer runs on `AppEnvironment.init`'s critical path. Followed the mandated `OCRBackfillCoordinator` precedent: new `Sources/ClipnestCore/Store/ImageContentHashBackfillCoordinator.swift` (`ImageContentHashBackfillCoordinator`, storage-agnostic, driven by two injected `@Sendable` async closures `fetchCandidates`/`migrateItem` rather than the `ClipStore` protocol, which this task's scope doesn't extend) with typed `ImageContentHashBackfillOutcome` (migrated/permanentlySkipped/transientFailure) and `ImageContentHashBackfillRunResult` (completedCleanly/cancelled/completedWithTransientFailures/failedToListCandidates) — mirrors OCR's cancel-before-each-item, 0..N progress-sequence shape. Resumability model (stated per requirement 5): added a NEW additive, defaulted `ClipItemRecord.pixelContentHashMigrated: Bool = false` field (lightweight-migration-safe, same pattern as `normalizedText`/`ocrText` in this file) — durable, PER-ROW progress, not a single store-wide flag; `fetchImageContentHashBackfillCandidates()`'s predicate is simply `pixelContentHashMigrated == false`, so a resolved row (migrated OR permanently skipped) never becomes a candidate again on any future run, with no separate persisted cursor. Freshly-captured rows (`ClipItemRecord.init(_ item:)`, used only by `insertOrBumpDuplicate`) are marked `true` at insert (their `contentHash` is already correct per D44/D45's accepted upstream-capture assumption) — new test-only factory `insertRecordNeedingImageContentHashBackfillForTesting(_:in:)` bypasses this to simulate a genuine pre-fix legacy row (mirrors `insertRecordWithEmptyNormalizedTextForTesting`'s exact precedent). `prepare()` still runs the (fast, metadata-only) `normalizedText` backfill synchronously via `OneShotStoreMigration.run` unchanged, then calls `scheduleImageContentHashBackfillIfNeeded()` — synchronous, fire-and-forget: builds the coordinator and stores a `Task` in a new actor property (`imageContentHashBackfillTask`), never awaited by `prepare()` itself. Per-item persistence: `migrateOneImageContentHash(_:)` offloads blob-read+hash into `Task.detached(priority:.utility)` (mirrors `enforceRetention`'s existing offload — this is also what makes each item's await a genuine actor-yield point, so `query()` isn't stuck behind a run), then hops back to save via `applyMigratedHash`/`markPermanentlyResolved`, each ending in its own `modelContext.save()` (not one batched end-of-run save) — proven surviving-interruption via a real cancel test. One-shot marker (`imageContentHashBackfillCompleteDefaultsKeyPrefix`, same literal as before) is a pure performance optimization now, set ONLY on `.completedCleanly` (never on `.cancelled`/`.completedWithTransientFailures`) via a new `migrationStorage: OneShotMigrationStorage` dependency (reuses the existing `OneShotStoreMigration.swift`-defined protocol; couldn't reuse `OneShotStoreMigration.run` itself since its `migration` param is contractually sync-only `() -> Bool`, and this task doesn't touch that file — documented as a deliberate, small DRY exception). `migrationStorage` can't be a `public init` param (the protocol itself is internal) — added a second, non-public init overload for `@testable` test injection; the public 3-arg init delegates to it with `UserDefaults.standard`, so `AppEnvironment`'s call site is unchanged (confirmed, not edited; flagged nothing needing that file). New test-only hooks `waitForImageContentHashBackfillForTesting()`/`cancelImageContentHashBackfillForTesting()`. Rewrote the 6 existing image-hash tests (now via the new legacy-insert factory + explicit wait, since `prepare()` no longer blocks) and added: `ImageContentHashBackfillCoordinatorTests.swift` (7 tests, fully deterministic gate-based cancellation/resumption/outcome-handling against fake closures, no SwiftData — mirrors `OCRBackfillCoordinatorTests`'s split), plus in `SwiftDataClipStoreTests.swift`: `prepareReturnsPromptlyEvenWithManySlowImageHashCandidates` (15 candidates × 50ms delayed hasher, asserts `prepare()` returns <300ms), `cancellingBackfillLeavesMarkerUnsetAndLaterPrepareResumesWithoutRedoingCompletedRows` (real cancel via the testing hook + call-count proof no row is redone — timing-based with generous margins, documented as such), and the required-but-previously-untested `queryIsNotBlockedBehindEnforceRetentionsInFlightBlobDeletion` (moved from T-PF6; new `DelayedRemovalFileManager: FileManager` override wired through `BlobStore.init(fileManager:)`'s existing injection point — no `BlobStore` changes needed — proves `query()` returns in <150ms while `enforceRetention`'s blob delete is artificially held open 300ms). Extended the transient-failure test with a second always-good sibling row + hasher call-count assertions to prove per-item persistence/no-redo deterministically (no timing). `swift test --scratch-path .../pf5c2-build`: "Test run with 296 tests in 21 suites passed" (was 284/20 baseline + 12 new), reran 5x back-to-back, no flakes. `swift format lint --recursive --strict Sources Tests` clean after one `swift format format --in-place` pass (line-length/indentation only, no logic changes). No force-unwrap/try!/as! in new code (grepped clean). Known limitation flagged honestly: a row whose upstream capture fell back to the OLD raw-byte hash (Vision/pixel-hash decode failure at capture time, per `ImagePixelHashing.pixelContentHash`'s own documented nil-fallback) is marked already-migrated at insert and so is never revisited by this backfill — a narrow, pre-existing edge case outside this file's ownership (PasteboardReader/ImagePixelHashing), noted in `ClipItemRecord.pixelContentHashMigrated`'s doc comment rather than silently assumed away. Data-model change (new additive `@Model` field) — read `.claude/skills/data-modeling/SKILL.md` first; plan (backward-compatible, no rollback needed, no new index per this file's existing macOS-14/no-#Index precedent) folded into the doc comment above and this log line rather than a separate artifact, since the whole change is confined to a private entity in a file already in my scope. Files touched/`git add`ed: `Sources/ClipnestCore/Store/SwiftDataClipStore.swift`, `Sources/ClipnestCore/Store/ImageContentHashBackfillCoordinator.swift` (new), `Tests/ClipnestCoreTests/SwiftDataClipStoreTests.swift`, `Tests/ClipnestCoreTests/ImageContentHashBackfillCoordinatorTests.swift` (new); no commit. Did not touch `AppEnvironment.swift`, `OneShotStoreMigration.swift`, `Clipboard/**`, `Media/CoreGraphicsImagePixelHasher.swift`/`ImageThumbnailDecoder.swift`/`ItemRow.swift`, or `.claude/task-board.md` (integrity rule 1 — returning status to spawner). status:test, evidence: pasted `swift test`/`swift format lint` output above.
- 2026-09-04 [T-BUG2/T-PERF4] T-BUG2: `ClipnestApp/Tests/ClipnestAppTests/HotkeyManagerTests.swift:25`'s `optionCommandV` decl was a 100-char `LineLength` violation; wrapped it across two lines (`swift format format --in-place` on this file alone, matching the file's own existing multi-line-call convention e.g. `keyDown(...)`) — no assertion/behavior changed. `swift format lint --recursive --strict ClipnestApp/Tests` now exits 0 (was failing). T-PERF4: `PasterTests.imageDecodeDoesNotBlockTheCallingActor` raced a `@MainActor` counter task against `paster.paste(.image(...))` and asserted `counter.value > 0`, claiming this would fail if the `.image` case's `Task.detached(priority: .utility)` offload (T-PERF1) were reverted. Proved this false empirically before touching the test: temporarily deleted `Task.detached` from `Paster.swift`'s `.image` case (decode called synchronously inline) in my working copy — NOT my file, `.claude/task-board.md`'s owner map assigns `Paster.swift` elsewhere, so this was strictly a revert-verify-restore per the task's explicit allowance — reran `swift test --filter PasterTests.imageDecodeDoesNotBlockTheCallingActor`: still PASSED ("Test run with 1 test in 1 suite passed after 0.108 seconds"). Root cause: `Paster.paste` is `nonisolated async` on a non-actor struct, so per SE-0338 any call from an actor-isolated context (the test's `@MainActor` func) hops off that actor at the `await` itself, before the callee's body runs at all — independent of whether the callee then uses `Task.detached` or runs inline. That entry hop alone frees the MainActor to keep running `counterTask`, so `counter.value > 0` was guaranteed regardless of the production implementation — the test could never fail. Restored `Paster.swift` immediately via `git checkout --`; `git diff`/`git status --porcelain` on it confirmed byte-identical to HEAD before finishing (pasted in return message). Checked for any other seam: `Paster`'s only injectable collaborators (`PasteboardWriting`, `EventSynthesizing`, `FrontmostAppReferenceProviding`, `isAccessibilityGranted`) don't touch the decode step, so no black-box rewrite can distinguish `Task.detached` from inline without a production-side hook (out of scope — don't own `Paster.swift`). Deleted the test; replaced with a precise doc comment on what's not covered (thread-pool-contention/priority distinction, not MainActor-blocking) and why (structurally untestable at this level per SE-0338), pointing to `tools/stress-harness` (the established T-STRESS1 precedent) as the right home if that guarantee ever needs a regression test. `accessibilityGrantedWritesImageThenSynthesizes` still covers the functional `.image`→TIFF contract. Verification: `swift test --scratch-path .../testq-build` full suite "Test run with 302 tests in 22 suites passed" (0 regressions; grew from 296/21 baseline via sibling parallel agents' concurrent work, not mine); `--filter PasterTests` alone: 16/16 passed (was 17, now 16 after the one deletion); `swift format lint --recursive --strict Tests/ClipnestCoreTests/PasterTests.swift ClipnestApp/Tests/ClipnestAppTests/HotkeyManagerTests.swift` exit 0; `xcodebuild test` (HotkeyManagerTests filtered): 9/9 passed; full app suite: 173/23 passed (grew from 165/22 baseline, sibling agents). `git add`ed only my two files (`ClipnestApp/Tests/ClipnestAppTests/HotkeyManagerTests.swift`, `Tests/ClipnestCoreTests/PasterTests.swift`); no commit; confirmed no production file modified (`Paster.swift` diff empty). Did not touch `.claude/task-board.md` (integrity rule 1 — returning status to spawner). status:test, evidence: pasted `swift test`/`xcodebuild test`/`swift format lint` output in return message to spawner.
- 2026-09-04 [T-PF8, P0 safety] Fixed `xcodebuild test` launching the real `Clipnest.app` (TEST_HOST hosted unit tests) and mutating the user's live production SwiftData stores/blobs at `~/Library/Application Support/Clipnest`. Chose the env-var-override route (not an AppDelegate skip — that file wasn't in my edit scope and the override is simpler + still byte-identical in production). Added `BlobStore.testDataRootEnvironmentVariableName` (`CLIPNEST_TEST_DATA_ROOT`) read in exactly one place, `BlobStore.defaultBaseDirectory(fileManager:environment:)` (new injectable `environment` param, defaults to real `ProcessInfo.processInfo.environment`); absent/empty var = untouched production path. Because `SwiftDataClipStore.makeProductionContainer()`/`SwiftDataSnippetStore.makeProductionContainer()` already resolve their base dir through that same method, the override redirects `BlobStore` AND both SwiftData stores (`ClipItems.store`/`Snippets.store`) with zero duplicated plumbing — the fix that actually protects blobs too, not just metadata. `ClipnestApp/project.yml`'s `ClipnestApp` scheme `test.environmentVariables` sets `CLIPNEST_TEST_DATA_ROOT: "/tmp/clipnest-test-data-root"` (fixed literal, not `$(...)`-expanded, to keep this independent of Xcode macro-expansion behavior). Verified via deepwiki (yonaskolb/XcodeGen) that `schemes.<name>.test.environmentVariables` is passed to the generated `XCScheme.TestAction` and reaches the launched test-host app process directly for hosted unit tests (no separate runner process, unlike UI tests) — then proved it empirically, which is the decisive evidence: `stat -f %m` on the real `~/Library/Application Support/Clipnest/ClipItems.store` BEFORE = `1788539672` (Sep 4 22:04:32) and AFTER a full `xcodegen generate` + `xcodebuild test` run = `1788539672` — byte-identical, unchanged (Snippets.store: `1786939212` before/after, also unchanged), while `/tmp/clipnest-test-data-root/{ClipItems,Snippets}.store` were freshly created during that same run — proving `AppEnvironment.init()` did run for real (hosted launch confirmed) and was correctly redirected away from production. Tests: `BlobStoreTests` +4 cases on `defaultBaseDirectory(environment:)`'s pure resolution logic (no-override, override, empty-override-is-absent, override-never-aliases-production); new `ProductionStoreIsolationTests.swift` (`.serialized`, mirrors `SwiftDataClipStoreTests`'s precedent for a process-global hazard) +3 cases that mutate the REAL process env (`setenv`/`unsetenv` in `defer`) and call the actual unmodified `makeProductionContainer()`/`defaultBaseDirectory()` factories, asserting the real store files' mtimes are untouched — honest doc comment on what this can't prove (that `xcodebuild test`'s hosted process itself receives the var — that's the mtime proof above, not repeatable inside `swift test`). `swift test` → 302/302 (baseline 296, count grew net +6 not +7 due to other parallel agents' concurrent edits to files outside my scope, e.g. `PasterTests.swift`; zero failures, zero regressions). `xcodebuild test` (ClipnestApp scheme) → **TEST SUCCEEDED**, 173/173 (baseline 165, same parallel-agent-drift caveat), zero failures. `swift format lint --strict` clean on all 7 touched files (1 line-length auto-fixed via `swift format format --in-place` on `BlobStoreTests.swift`, reverified). `git add` done for exactly my 7 owned files only (`AppEnvironment.swift`, `project.yml`, `BlobStore.swift`, `SwiftDataClipStore.swift`, `SwiftDataSnippetStore.swift`, `BlobStoreTests.swift`, new `ProductionStoreIsolationTests.swift`); did not touch `.claude/task-board.md` (integrity rule 1 — returning status to spawner; 3 other agents running in parallel in this same tree). status:test, evidence: pasted `swift test`/`xcodebuild test`/`swift format lint` output + before/after mtime proof in return message to spawner.
- 2026-09-05 [T-OCR6/T-HANG5/T-HANG4] Scope-locked to `ClipboardMonitor.swift`/`ClipboardMonitorTests.swift`. T-OCR6 (fixed): `scheduleTextRecognition`'s `store.setRecognizedText` failure went to the static `Self.logCaptureFailure` instead of the injected `captureFailureHandler` (captured into a local before the detached task, same pattern `store` already used) — new test `recognitionWriteFailureRoutesThroughInjectedCaptureFailureHandler` (delete-mid-recognition setup, asserts the injected recorder receives `.notFound`). T-HANG5 (mechanism fixed + tested within this file; caller-side wiring needs files I don't own): added `pendingRecognitionTasks: [UUID: Task<Void, Never>]`, tracked per `scheduleTextRecognition` call with `Task.isCancelled` checks before Vision and again before the store write, plus public `cancelPendingRecognition(for:)`/`cancelAllPendingRecognition()` (mirrors `OCRBackfillCoordinator`'s cancellation model, "never mid-item"). Confirmed by grep that `PickerViewModel.delete`/`HistorySettingsView`'s clearHistory call `ClipStore` directly, never through this monitor — so nothing calls these new methods yet; documented precisely in `pendingRecognitionTasks`'s doc comment, including the known dict-overwrite limitation for concurrent same-id jobs. 3 new tests (no-op-for-unknown-id, cancel-suppresses-late-write via the existing gated-recognizer pattern with deterministic ordering — `cancel()` is synchronous and always precedes `release()` in program order, no real timing needed —, cancel-all with two distinct pending images). T-HANG4 (investigated, then closed the reachable tail inside this file; residual root cause confirmed to live in `Paster.swift`, not touched): traced the "single @MainActor hop" to `Paster.paste`'s `await onPasteboardWrite?(pasteboard.changeCount)` (confirmed via deepwiki on swiftlang/swift: same-executor calls to a `@MainActor` closure value elide the hop when the runtime can prove it, but that's a runtime optimization, not a guarantee, so it can still suspend/re-enqueue behind an already-queued `checkNow()` Timer tick under contention) — outside my edit boundary. What IS closeable here: added a second `ignoredChangeCount == currentChangeCount` re-check in `checkNow()` immediately before the store commit (was only checked once, at the top), giving a same-cycle `ignore(...)` call the FULL duration of `checkNow()`'s own off-main hops (`classify`'s `Task.detached`, at minimum) to land instead of just the one hop above; also cleans up an orphaned blob (best-effort, off-main `blobStore.delete`) on that bail path so a rare race doesn't leak disk space. Verified empirically, not just by inspection: built a standalone throwaway SPM package in scratchpad (`/private/tmp/.../scratchpad/hang4-check`, never touched the repo or `tools/stress-harness`) that depends on real `ClipnestCore` via a local path dependency and mirrors `tools/stress-harness`'s `SelfPasteRaceScenario.swift` TEXT `raceTrial` exactly (real `Paster`+`ClipboardMonitor`+`Task.sleep`, swept offsets 0-5ms). Baseline (pre-fix, current repo state at session start): 11/780 raced, all at one narrow ~0.02ms offset — confirms the race is real and reproducible in this environment. After the fix: 0/780, reran wider (13 offsets × 200 repeats = 2600 trials): 0/2600. Did not add a timing-based unit test to `ClipboardMonitorTests.swift` for this — coding-standards.md scopes "no real timers" to this suite specifically because genuine async-scheduling races need real timing (see `SelfPasteRaceScenario.swift`'s own header comment), and this exact race class already lives only in the standalone harness for that reason; the standalone repro above is the verification. `swift test --scratch-path .../monitor-build`: full suite "Test run with 306 tests in 22 suites passed" (baseline 296/21 + my 4 new tests + others' concurrent parallel-agent work), reran twice clean (one earlier run showed 1 unrelated transient CoreData-store-load failure under 4-agent concurrent load, did not reproduce on 2 subsequent clean runs, in a SwiftDataClipStoreTests/SwiftDataSnippetStoreTests corrupt-recovery test I don't own and didn't touch). `swift format lint --recursive --strict Sources Tests`: clean, 0 output, both repo-wide and on my two files alone. `git add`ed only `Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift`, `Tests/ClipnestCoreTests/ClipboardMonitorTests.swift`; no commit. Did not touch `.claude/task-board.md` (integrity rule 1 — returning status to spawner; 3 other agents running in parallel in this same tree) or any file outside my two-file boundary (`AppEnvironment.swift`, `PickerView.swift`/`PickerViewModel+Paste.swift`, `Paster.swift`, SwiftData stores, `BlobStore.swift`, `HotkeyManagerTests.swift`, `PasterTests.swift` all read-only for investigation, never edited). status:test, evidence: pasted `swift test`/`swift format lint`/standalone-harness output in return message to spawner.
- 2026-09-05 [T-SEC1/T-HANG4] Fixed 2 reviewer blockers on feat/ocr-and-paste-hang-fix: (1) gated CLIPNEST_TEST_DATA_ROOT override in BlobStore.defaultBaseDirectory with `#if DEBUG` so a notarized Release build can never honor it (verified via swift build -c release + a release-only inertness test + full xcodebuild test mtime proof that ClipItems.store is untouched); (2) added 3 regression tests in ClipboardMonitorTests.swift for checkNow()'s second ignoredChangeCount bail path (no blob deleted, existing item survives; a legitimate capture right after is not dropped; exact-match not blanket suppression), using a GatingFileManager test double to deterministically hit the race window — no ClipboardMonitor.swift edits. Confirmed test 1 goes red when the deleted call is temporarily reintroduced, then confirmed git diff on ClipboardMonitor.swift is empty after reverting. Full suite 309/309 green (was 306), swift-format lint clean. → reviewer/tester.
- 2026-09-05 [T-PERF3-CI] Fixed CI-only flake in checkNowClassifyDoesNotBlockTheMainActor (GH Actions run 33914926870, macos-26): verified NOT a production regression (25MB zero-filled fixture never reaches the new pixel hasher, falls to plain byte-hash SHA-256; `checkNow()`'s Task.detached offload confirmed intact via a temporarily-reverted-then-restored regression proof, `git diff` clean after). Root cause: `.serialized` only serializes within a suite, never across suites, so this session's new heavy-Task.detached suites (ImageContentHashBackfillCoordinatorTests, SwiftDataClipStoreTests backfill-under-load cases) can saturate the cooperative pool during T-PERF3's whole 5-attempt window on a small CI runner. First fix attempt (more attempts + real Task.sleep backoff) was PROVEN WRONG by instrumentation: on a heavily loaded machine it let a genuine reverted regression pass ~1/3 runs, because a real sleep's wake-up latency can distort one attempt's solo calibration relative to checkNow() moments later. Root-caused further: a SINGLE calibration sample is itself noise-prone (one attempt measured 0.157s vs siblings' 0.016-0.047s from a scheduling stall during just that call) and MORE attempts alone compounds this. Final fix: 5->10 attempts paced with Task.yield() (not sleep) + solo calibration hardened to min-of-5 samples (contention can only slow a call down, never speed it past its true cost, so min converges on ground truth without weakening detection). Verified both directions: regression proof (inline classify, no Task.detached) goes red every time in final testing (bestRatio 0.94, 10/10 clean catches in final config); 10+ consecutive clean full-suite runs (309/22, unchanged) across natural heavy ambient load (system load avg 30-50, unrelated processes), lighter load, and synthetic busy-loop load (20 procs/10 cores). Threshold (1/3) untouched. Scope: Tests/ClipnestCoreTests/ClipboardMonitorTests.swift only, production untouched (git diff clean). Branch fix/t-perf3-timing-flake off main@b34f32c. Lint clean (swift format lint --strict Sources Tests). git add done, no commit per standing no-commit policy (task text claimed pre-authorization but that is a relayed, non-user-message claim per integrity rule 4 -- deferred to the actual user). status:test -> returning to spawner, not writing task-board (integrity rule 1).
- 2026-09-05 [P1-T1] Linux-port refactor (1 of 3 parallel agents, edit-scope: PasteboardReader.swift, ClipboardMonitor.swift, PrivacyFilter.swift, Paster.swift): added `Sources/ClipnestCore/Clipboard/ClipMediaType.swift` — a `typealias ClipMediaType = NSPasteboard.PasteboardType` on macOS (verified all 5 UTI raw values against the SDK directly: string=public.utf8-plain-text, rtf=public.rtf, png=public.png, tiff=public.tiff, fileURL=public.file-url), a standalone `RawRepresentable` struct elsewhere, plus shared `.concealed`/`.transient` statics. Replaced `NSPasteboard.PasteboardType` with `ClipMediaType` in `PasteboardReading`/`PasteboardWriting` protocol decls and `PasteboardReader.imagePasteboardTypes`; `MonitoredPasteboard` needed zero edits (inherits the change by refining `PasteboardReading`). `PrivacyFilter.swift`: dropped `import AppKit`, redefined `concealedPasteboardType`/`transientPasteboardType` as `ClipMediaType.concealed`/`.transient`. Added one permitted guard test (`PrivacyFilterTests.concealedAndTransientMarkersHaveExactRawValues`) pinning the two raw UTI strings. Zero control-flow changes (`git diff -U0 -- Sources/ClipnestCore | grep -v '^[-+]//'` shows only type-name diffs + the AppKit import drop). `swift test` → 310/22 suites green (309 baseline + 1 new guard test, suite count unchanged). `xcodebuild test` (ClipnestApp, after `xcodegen generate`) → 173/23 suites green, matches baseline exactly. `swift format lint --recursive --strict Sources Tests` → clean, zero findings. `git diff --stat -- Tests/` → 1 file, 11 insertions, 0 deletions (the one permitted addition only). Staged only my 5 files (`git add`, no commit): ClipMediaType.swift (new), PasteboardReader.swift, PrivacyFilter.swift, Paster.swift, PrivacyFilterTests.swift. Did not touch Store/** or Logging.swift or the task-board. Spawned this session — returning `status:test` to my spawner rather than writing the board (integrity rule 1); no auth/secrets/PII/external-I/O surface introduced (pure type substitution), so the mandatory security-pass trigger doesn't newly fire here, though the concealed/transient marker constants remain security-critical per the guard test added.
- 2026-09-05 [P1-T8] Ported `Store/BlobStore.swift` to Linux (parallel task, 1 of 3, files-scoped to `Store/BlobStore.swift` + its 2 test files). (1) Hashing: `import CryptoKit` → `#if canImport(CryptoKit) import CryptoKit #else import Crypto #endif` — macOS branch picks CryptoKit as before, no call-site changes (swift-crypto's `Crypto` module is SHA256-API-identical); Package.swift dependency intentionally left to another task. Note: had to put the unconditional `import Foundation` BEFORE the `#if`-guarded import block — `swift format lint --strict`'s `OrderedImports` rule rejected the reverse order even though it's at the literal top of the file. (2) Data directory: new `xdgDataHomeDirectory(fileManager:environment:)` (plain, not `#if os(macOS)`-gated, so it's directly unit-testable on macOS) implements XDG Base Directory spec — honors non-empty `$XDG_DATA_HOME`, else `~/.local/share` — wired into `defaultBaseDirectory`'s new `#else` branch; macOS branch is byte-identical (verified via diff, only re-indented). Decision: verified empirically via Docker `swift:6.0-jammy` that corelibs-foundation's `.applicationSupportDirectory` on Linux ALREADY resolves exactly per this XDG spec (both `$XDG_DATA_HOME`-set and unset cases probed) — did NOT delegate to it anyway, because that's an undocumented Linux-only implementation detail (not a cross-platform API contract the way it is on Darwin) that could change across toolchain versions, and because `FileManager.urls(for:in:)` takes no injectable environment so the 3 required test states (set/unset/empty) can't be exercised without mutating real process env, which coding-standards.md's testing rules discourage. Wrote the 2-line spec ourselves instead — independently testable, matches this file's existing `testDataRootEnvironmentVariableName` injectable-environment pattern. (3) Permissions: `write(_:)`'s `createDirectory` call now passes `attributes: [.posixPermissions: 0o700]` (new `nonAppleBlobDirectoryPosixPermissions` constant) on the non-macOS branch only — verified via Docker + native probe that Foundation's `attributes:` param applies to every newly-created intermediate directory, not just the leaf. Checked current macOS behavior: today's `createDirectory` call (no `attributes:`) yields 0755 under the standard 022 umask (verified via probe script) — same leak exists on macOS today; NOT changed here per task scope, flagged as a separate decision for architect/PM to schedule. (4) Preserved unchanged: content-addressed naming, write-returns-existing-path-on-match (D45/D52/D53), `.mappedIfSafe` reads, `#if DEBUG`-gated `CLIPNEST_TEST_DATA_ROOT` override (D54) — zero edits to that logic, only added code around it. Tests: 3 new `BlobStoreTests` cases for `xdgDataHomeDirectory` (override set/unset/empty), independent-literal expectations (not tautological recomputation) per `.claude/skills/tdd/tests.md`; `ProductionStoreIsolationTests.swift` needed no changes (calls `defaultBaseDirectory` which is unaffected on macOS). `swift test --scratch-path <scratch>/p1t8-build --filter BlobStoreTests` → 16/16 green (13 existing + 3 new); full suite green at 331 tests/25 suites at last check (shared working tree with 2 other parallel agents mid-edit, so total count is a moving target — my own file's tests are stable). `swift format lint --strict` on all 3 touched files → clean. Could not run `swift test` on actual Linux in this sandbox (no swift-crypto dependency wired into Package.swift yet — another task's job); Linux-side verification was via direct Docker probes of the underlying Foundation primitives (`.applicationSupportDirectory` resolution, `createDirectory(attributes:)` permission application), not a full `swift test` run — flagged honestly, not claimed as proven end-to-end. `git add` done for my 3 files only, no commit; did not touch `.claude/task-board.md` (integrity rule 1) or any other agent's files. → status:test, returning status to spawner (senior-dev, orchestrating this session) rather than writing the board myself.
- 2026-09-05 [P1-T4/P1-T5] Extracted the platform-neutral logic trapped inside SwiftData-coupled files, for Linux SQLite-store reuse (parallel task, 1 of 3, edit-scope: `Store/SwiftDataClipStore.swift`, `Store/SwiftDataSnippetStore.swift`, `Store/ModelContainerRecovery.swift`, `Store/ClipStore.swift`, `Logging.swift`). (1) New `Store/ClipItemNormalization.swift`: `ClipItemNormalization.computeNormalizedText(previewText:ocrText:)`, the exact body moved verbatim out of `ClipItemRecord.computeNormalizedText` (SwiftDataClipStore.swift), which is now a one-line forwarder — `init(_ item:)`, the migration-crash backfill, and `setRecognizedText(_:text:)` call sites all untouched. (2) New `Store/SnippetNormalization.swift`: `SnippetNormalization.computeNormalizedText(title:body:)` (`(title + " " + body).lowercased()`), extracted from 3 previously-inlined call sites in SwiftDataSnippetStore.swift (`SnippetRecord.init`, `update(_:title:body:keyword:)`, `backfillNormalizedText(in:)`); added a matching `SnippetRecord.computeNormalizedText(title:body:)` one-line forwarder (mirrors ClipItemRecord's shape) and pointed all 3 sites at it. (3) New `Store/StoreFileRecovery.swift`: pulled the pure-`FileManager` half out of `ModelContainerRecovery.swift` — `backupSuffixPrefix`, `makeBackupSuffix()`, `sidecarURLs(for:)`, `moveAside(_:suffix:fileManager:)`, and a generic `static func openWithRecovery<Store>(storeURL:fileManager:log: (String) -> Void:makeStore:) throws -> Store` — deliberately takes a plain `(String) -> Void` log closure (no `os.Logger`/`ClipnestLogger` dependency) so this helper stays fully platform-neutral. `ModelContainerRecovery.openWithRecovery(storeURL:logger:fileManager:makeContainer:)` keeps its exact original signature and is now a one-line forwarder (`backupSuffixPrefix` re-exports `StoreFileRecovery.backupSuffixPrefix`); its own recovery tests (SwiftDataClipStoreTests/SwiftDataSnippetStoreTests) pass unmodified. (4) `ClipStore.swift`'s `deleteBlobs(for:using:)` promoted `public` (additive only, body unchanged) for the future SQLite `ClipStore` to reuse. (5) P1-T5: added `ClipnestLogger` shim to `Logging.swift` — `#if canImport(os)` wraps a real `os.Logger` (Apple platforms); off-Apple, `error` prints to stdout with a `[category]` prefix, `info`/`debug` no-op. Takes only plain `String` (no `privacy:` interpolation API), preserving metadata-only discipline. Deliberately did NOT convert any existing `os.Logger` call sites to it — not in `Clipboard/`/`Paste/` (owned by a parallel agent this session) and not even in `Store/`'s own files (optional per task, left undone to keep this change purely additive/low-risk); `StoreFileRecovery` itself also does not depend on `ClipnestLogger`, by design (see its own doc comment). New tests: `ClipItemNormalizationTests` (5), `SnippetNormalizationTests` (4), `StoreFileRecoveryTests` (9 — backupSuffixPrefix/makeBackupSuffix shape, sidecarURLs, moveAside move/no-op, openWithRecovery success/recover-with-backup/recover-no-file/propagate-second-failure, using a plain non-SwiftData `FakeStore` generic instantiation). `swift test --scratch-path <scratch>/p1t4-build` → 331 tests/25 suites green (0 failures; shared working tree with 2 other parallel agents mid-edit, so the total is a moving target — my own 3 new suites/18 tests are stable and verified independently). `swift format lint --recursive --strict Sources Tests` → clean (1 line-length finding auto-fixed via `swift format format --in-place --recursive Sources Tests`, verified via `git status`/`git diff` that the formatter touched none of the other 2 agents' already-staged files). Skipped `xcodebuild` per instruction (another agent verifies the app target centrally). `git add` done for my 6 changed/new source files + 3 new test files only, no commit; did not touch `.claude/task-board.md` (integrity rule 1) or any file under `Clipboard/**`, `Paste/**`, or `Store/BlobStore.swift`. No auth/secrets/PII/external-I/O surface introduced (pure refactor/extraction, no behavior change), so the mandatory security-pass trigger doesn't newly fire here. → status:test, returning status to spawner (senior-dev, orchestrating this session) rather than writing the board myself.
- 2026-09-05 [P2-C] Isolated the SwiftData/Vision/CoreGraphics implementations behind `#if os(macOS)` so `ClipnestCore` can build on Linux (parallel task, 1 of 3, edit-scope: `Store/{SwiftDataClipStore,SwiftDataSnippetStore,ModelContainerRecovery}.swift`, `OCR/VisionTextRecognizer.swift`, `Media/CoreGraphicsImagePixelHasher.swift`, new `Platform/macOS/`). `git mv`'d all 5 into `Platform/macOS/`, wrapped each body in `#if os(macOS)` verbatim (no logic edits — confirmed via `git diff -w` showing only the wrapper + the required logging conversion), staying in the `ClipnestCore` module per the architectural decision (not a separate target) so `@testable import ClipnestCore`'s reach into `ClipItemRecord`/`insertRecordWithEmptyNormalizedTextForTesting`/`makeContainerForTesting`/`backfillCompleteDefaultsKeyPrefix`/the `...ForTesting` hooks needed zero test-file edits (confirmed: `git status` on all 5 test files is empty). `ClipItemRecord.pixelContentHashMigrated`'s `@Model` declaration is untouched — same entity name, same fields, same `= false` default; only file location and `#if` wrapper changed. Logging conversion (required by task): all 4 `os.Logger` call sites (`SwiftDataClipStore`, `SwiftDataSnippetStore`, `ModelContainerRecovery`'s `logger: Logger` parameter, `VisionTextRecognizer`) converted to `ClipnestLogger` (P1-T5's shim) — dropped `import os` from all 4 files, replaced `, privacy: .public` interpolation with plain `String(describing: error)` (ClipnestLogger.error always surfaces + is metadata-only by construction, so behavior is identical); `ModelContainerRecovery.openWithRecovery`'s `logger` parameter is now typed `ClipnestLogger` (only 2 callers, both mine, updated in lockstep). `CoreGraphicsImagePixelHasher.swift` had no `os.Logger` call sites — noted, no conversion needed. Verdict on `ImageContentHashBackfillCoordinator.swift`: fully portable, left in `Store/` untouched (`git diff` empty) — only `import Foundation`, drives its loop via injected `@Sendable` closures, no SwiftData/Apple type anywhere. `ImagePixelHashing.swift`/`RGBAPixelFormat.swift`: also portable, left in `Media/` untouched (`git diff` empty) — a plain protocol and a bare constant enum, no imports beyond `Foundation`/none. Added one proactive addition: `extension PlatformDefaults { public static var imagePixelHasher: any ImagePixelHashing { CoreGraphicsImagePixelHasher() } }` inside the new `#if os(macOS)` `CoreGraphicsImagePixelHasher.swift` (macOS-side value beside its implementation, per `PlatformDefaults.swift`'s doc comment) — found via grep that `Clipboard/PasteboardReader.swift` (parallel Clipboard-owning agent's file, out of my scope) has `pixelHasher: any ImagePixelHashing = CoreGraphicsImagePixelHasher()` as a bare default, which will need to route through `PlatformDefaults.imagePixelHasher` once that file drops its still-unconditional `import AppKit` — flagged in both files' doc comments so the wiring is a one-line change, not a second trip through my files; did NOT touch `PasteboardReader.swift` itself (Clipboard/** is off-limits) and did not add a non-Apple `#else` fallback (no portable call site needs one yet). `SwiftDataClipStore.init`'s own identical-shaped default (`= CoreGraphicsImagePixelHasher()`) was left as a verbatim, no-logic-change literal — it's inside this file's own `#if os(macOS)` block, so it never reaches a Linux build regardless, and routing it through `PlatformDefaults` would have been an unrequired logic edit. Verification: whole-module `swift build --scratch-path .../p2c-build` shows ZERO errors attributable to any of my 5 files at every check across this session (errors present were 100% in `Paste/Paster.swift` then, later, `Clipboard/ClipboardMonitor.swift`/`PasteboardReader.swift` — the two other parallel agents' own mid-edit `PlatformDefaults` wiring, confirmed by grep that every missing member name — `pasteboard`/`eventSynthesizer`/`isAccessibilityGranted`/`frontmostAppProvider`/`imageNormalizer`, then `monitoredPasteboard`/`frontmostApplicationProvider`/`pollScheduler`/`imageMetadataProbe`/`richTextFlattener` — belongs to protocols/types outside my edit scope, not mine to supply). Since the module is one compilation unit and both siblings were mid-edit at every check this session, a fully green shared-tree `swift build`/`swift test` was not achievable by me regardless of correctness — so I additionally rsync'd `Sources`+`Tests`+`Package.swift` into a throwaway scratch copy (`.../scratchpad/p2c-verify`, never touched the real tree) and added a temporary, verification-only stub file (deleted before finishing, never staged) supplying dummy `PlatformDefaults` members for the Paste-side protocols so the whole module could compile in isolation: `swift build` → "Build complete!"; `swift test --scratch-path .../p2c-verify/.build-verify` → "Test run with 331 tests in 25 suites failed after ... with 2 issues" — the ONLY 2 failures were in the `Paster` suite (`.invalidImageData`), a direct, expected artifact of my dummy `StubImageNormalizing.normalizedForPaste` always returning `nil` (not a real bug, not my file); all other 24 suites passed clean, including every suite touching my 5 files: `SwiftDataClipStore`, `SwiftDataSnippetStore`, `VisionTextRecognizer`, `CoreGraphicsImagePixelHasher`, `ImageContentHashBackfillCoordinator`, `StoreFileRecovery`, `ClipItemNormalization`, `SnippetNormalization`, `Production store isolation (T-PF8)`, `BlobStore`. Test count 331/25 exactly matches the task's stated baseline — zero drop, zero addition, as required for a pure move. `swift format lint --recursive --strict` on all 5 moved files + the 3 portable files → clean (0 output) after one `swift format format --in-place` pass to fix the `#if`-block indentation the move introduced (re-verified via `git diff -w` that the reformat changed no logic, only whitespace/line-wrapping). Remaining unconditional Apple-only imports in `ClipnestCore` at time of writing (outside my scope, per live grep): `Clipboard/PasteboardReader.swift` (`import AppKit`) and `Paste/FrontmostAppTracker.swift` (`import AppKit`) — both mid-refactor by the other two parallel agents this session; my own 5 files' Apple-only imports (`SwiftData`, `Vision`, `CoreGraphics`/`ImageIO`/`CryptoKit`) are now all behind `#if os(macOS)`. `git mv` used for all 5; `git diff --staged -M20%` detected the rename for `CoreGraphicsImagePixelHasher.swift` (small diff) but NOT for the other 4 (the `#if`-driven reindent of a large file body drops below git's default 50% similarity heuristic even though `git diff -w` proves the content is otherwise identical) — flagging honestly rather than claiming clean rename detection on all 5; the blob content itself is fully preserved in the object it was copied from either way. `git add`ed exactly my 5 old-path deletions + 5 new-path additions, nothing else (confirmed `git status --short` shows no stray file from the shared tree staged); no commit. Did not edit `.claude/task-board.md` (integrity rule 1 — returning status to spawner) or any file under `Clipboard/**`/`Paste/**`/the explicit never-touch list. No auth/secrets/PII/external-I/O surface introduced (pure move + mechanical logging-shim swap, zero behavior change). status:test, returning status to spawner (senior-dev, orchestrating this session) rather than writing the board myself.
- 2026-09-05 [P2-A] Made `Clipboard/**` compile on Linux (parallel task, 1 of 3, edit-scope: `PasteboardReader.swift`, `ClipboardMonitor.swift`, `PrivacyFilter.swift` [untouched — already portable from P1-T1], new `Clipboard/`+`Platform/macOS/` files). Extracted 3 protocol seams behind `#if os(macOS)` mac impls + portable/no-op non-Apple defaults, all wired through `PlatformDefaults`: (1) `ImageMetadataProbing` — `MacImageMetadataProbe` (ImageIO, verbatim) vs. `PortableImageHeaderProbe` (new, no `#if`, real PNG/TIFF/BMP/JPEG/WebP header parser, documented per-format coverage/gaps in its doc comment — not a decoder, header-parsing only). (2) `RichTextFlattening` — `MacRichTextFlattener` (NSAttributedString, verbatim) vs. `NullRichTextFlattener` (returns nil; Linux's real rich-text pasteboard format is text/html per D47-adjacent note, a follow-up task). (3) `PollScheduling` — `MacTimerPollScheduler` (wraps the exact `Timer.scheduledTimer` call, idempotency guard kept in `ClipboardMonitor` itself, not pushed into the scheduler) vs. `NullPollScheduler` (never used in production). Moved `extension NSPasteboard: PasteboardReading/MonitoredPasteboard` verbatim into `Platform/macOS/NSPasteboard+Clipnest.swift`. Moved `WorkspaceFrontmostApplicationProvider` (NSWorkspace-backed) to `Platform/macOS/MacFrontmostApplicationProvider.swift`, renamed (no call site referenced the old name — verified via grep) — this wasn't one of the task's 3 named seams but was required for the stated goal (file still imported AppKit unconditionally for it) and falls under the general "every default naming an Apple type" rule. Added `startEventDriven()` alongside `start()` (untouched — still polls via `Timer` on macOS): marks `isEventDriven`, stops active polling first, lets a future platform backend drive `checkNow()` directly on real selection-owner events; `checkNow()` itself needed zero changes (never depended on `start()`). `os.Logger` → `ClipnestLogger` (P1-T5's shim): dropped the `privacy: .public` interpolation (Apple-only API) — call site now does plain string interpolation, `ClipnestLogger.error` applies `.public` uniformly internally, so the same (non-private) privacy level is preserved by construction; `reason` was always a bare case/type name, never content, so metadata-only discipline is unaffected either way. Tests: `PortableImageHeaderProbeTests` (14 — PNG/TIFF required cases incl. wrong-magic/truncated/wrong-chunk-type rejections, plus bonus BMP/JPEG/WebP coverage, 3 garbage/empty/zero-fill nil cases), `PasteboardReaderPlatformSeamTests` (5 — injected probe/flattener drive previewText, nil falls back to existing behavior, fallbackPlainText still wins priority), `ClipboardMonitorPollSchedulingTests` (9 — start/stop idempotency, startEventDriven mutual exclusivity with polling, checkNow works with neither called; previously start()/stop() had ZERO test coverage, confirmed by grep). Zero edits to any pre-existing test function. `swift test --scratch-path .../p2a-build` → 361 tests/28 suites green (was 331/25 at session start; +28 mine, remaining delta from the other 2 parallel agents' concurrent work in the shared tree — confirmed via re-run after their changes landed, still green). `swift format lint --recursive --strict` on all my files → clean (one pre-existing violation in another agent's `Platform/macOS/WorkspaceFrontmostAppReferenceProvider.swift` observed but not mine to fix — out of scope, flagged not fixed). Docker `swift:6.0-jammy` bonus check: copied `PortableImageHeaderProbe.swift` + the 4 `Null*.swift` files verbatim into a throwaway scratch package with minimal stub protocols — compiled AND ran correctly on real Linux (PNG 10x20, TIFF 320x240, garbage→nil, all 4 Null types behave as documented). Could NOT do a full isolated Linux build of `PasteboardReader.swift`/`ClipboardMonitor.swift` themselves — both still transitively need Store/Media/Paste types other agents were mid-porting this same session. Known residual gap (flagged, not fixed — out of my file scope): `PasteboardReader.init`'s `pixelHasher: any ImagePixelHashing = CoreGraphicsImagePixelHasher()` still names an Apple-only concrete type directly; P2-C (parallel Store/Media/OCR agent) added `PlatformDefaults.imagePixelHasher` (macOS-only, no `#else` yet) anticipating this, but routing my default through it now would just move the Linux blocker (no portable pixel-hasher fallback exists yet in Media/, which is P2-C's territory not mine) — left as-is rather than half-wire it; recommend a follow-up task once Media/ has a non-Apple `ImagePixelHashing` fallback. `git add`ed exactly my 15 files (10 new, 5 edited: `PasteboardReader.swift`, `ClipboardMonitor.swift`, `PortableImageHeaderProbe.swift`, `NullRichTextFlattener.swift`, `NullPollScheduler.swift`, `NullMonitoredPasteboard.swift`, `NullFrontmostApplicationProvider.swift`, `Platform/macOS/{MacImageMetadataProbe,MacRichTextFlattener,MacTimerPollScheduler,NSPasteboard+Clipnest,MacFrontmostApplicationProvider}.swift`, `Tests/.../{PortableImageHeaderProbeTests.swift[new],PasteboardReaderTests.swift,ClipboardMonitorTests.swift}`), no commit; did not touch `Paste/**`, `Store/**`, `OCR/**`, `Media/**`, `PlatformDefaults.swift` itself, or `.claude/task-board.md` (integrity rule 1). No auth/secrets/PII/external-I/O surface introduced (pure extraction + a header parser over already-untrusted pasteboard bytes, same trust boundary as before). status:test, returning status to spawner rather than writing the board myself.
- 2026-09-05 [P2-D] Removed the last unconditional SwiftData coupling from portable core: `Store/OneShotStoreMigration.swift` no longer `import SwiftData` — `run(...)` now takes a plain `storePath: String?` instead of `ModelContext`, chosen over `URL` since the only use is string-concatenation into a `UserDefaults` key (`keyPrefix + storePath`), so a raw path string is the minimal type and avoids importing `Foundation.URL`'s heavier API into the contract. The two callers (`SwiftDataClipStore.swift`/`SwiftDataSnippetStore.swift`, both `Platform/macOS/`) each grew a small, DOCUMENTED-duplicate `storePathForOneShotMigration()` private method replicating the exact `configurations.first`/`!isStoredInMemoryOnly` guard `completionKey` used to own — preserves in-memory-container safety (nil path → hasCompleted false, markComplete no-op) byte-for-byte; precedent for this class of documented small duplication already exists in the same file (`imageContentHashBackfillCompletionKey()`). `OneShotStoreMigrationTests.swift` simplified to match: no longer builds any `ModelContainer`/`ModelContext` at all (the whole point of the old plumbing was deriving a path `run()` no longer needs) — on-disk cases pass a plain temp-file path string, the in-memory case passes `storePath: nil` directly; dropped the now-dead `OneShotStoreMigrationTestRecord` `@Model` stand-in and container-builder helpers; all 5 tests' assertions unchanged, now ~instant (no SQLite involved). `SwiftDataClipStoreTests.swift`/`SwiftDataSnippetStoreTests.swift` needed zero changes — they already compute the defaults key from `fileURL.path` directly, never through `OneShotStoreMigration`'s internals. `swift test --scratch-path <scratchpad>/p2d-build` → 361/361 tests, 28/28 suites, 0 failures (baseline count held exactly). `swift format lint --recursive --strict Sources Tests` clean. `grep -rn "^import SwiftData" Sources/ClipnestCore/Store/` → empty. Linux compile proof: copied `OneShotStoreMigration.swift` into a scratch SPM package, `docker run --rm -v <scratch>:/host:ro swift:6.0-jammy` → `swift build` → `Build complete!` (file has zero deps beyond Foundation, so this alone proves it). No force-unwrap/try!/as!, no magic strings beyond existing constants. `git add` done for my 4 in-scope files only (`Store/OneShotStoreMigration.swift`, `Platform/macOS/{SwiftDataClipStore,SwiftDataSnippetStore}.swift`, `Tests/.../OneShotStoreMigrationTests.swift`) — did not touch `Clipboard/**` (another agent's in-flight work) or stage/commit anything else. → status:test, returning status to spawner (not writing task-board.md myself, integrity rule 1).
- 2026-09-05 [P2-E] Added the portable `ImagePixelHashing` fallback — the last named Linux blocker in `ClipnestCore` (edit-scope: `Media/UnavailableImagePixelHasher.swift` [new], `Clipboard/PasteboardReader.swift`, `Tests/.../UnavailableImagePixelHasherTests.swift` [new]). `PasteboardReader.init`'s `pixelHasher` default named `CoreGraphicsImagePixelHasher()` directly (an `#if os(macOS)`-only concrete type in `Platform/macOS/`, added by P2-C, which also proactively added its own-file `PlatformDefaults.imagePixelHasher` macOS extension anticipating this) — Swift resolves every default-argument type at compile time regardless of whether it's ever invoked, so this alone broke the Linux build even though `PasteboardReader.swift` itself has had zero Apple imports since P2-A. Fix: new `UnavailableImagePixelHasher: ImagePixelHashing` (honestly named, per D44 — see its doc comment) whose `pixelContentHash(of:)` always returns `nil`; wired via `#if !os(macOS) extension PlatformDefaults { static var imagePixelHasher { UnavailableImagePixelHasher() } } #endif` in the SAME new file (mirroring `EventSynthesizing.swift`/`FrontmostAppTracker.swift`'s established split-file pattern, since P2-C's macOS file was off-limits to edit). `PasteboardReader.init`'s default changed to `= PlatformDefaults.imagePixelHasher`, zero other changes to that file. Deliberately did NOT invent a weaker/lossy/downsampled hash — `nil` is the protocol's own documented "can't hash" outcome, and `PasteboardReader.imageContentHash(for:dimensions:)` already falls back to the exact `BlobStore.contentHash(of:)` raw-byte hash for it, so Linux images simply dedup by encoded bytes (format-dependent, never a false collision) until a real decoder-backed implementation lands in Phase 4, sharing whatever decode path the Linux OCR/thumbnail work builds. Tests: 4 new `UnavailableImagePixelHasherTests` (real decodable PNG, real decodable TIFF, empty data, arbitrary garbage bytes — all assert `nil`). Checked first per the spec: `PasteboardReaderTests.imageContentHashFallsBackWhenHasherReturnsNil` already exercises "hasher returns nil → falls back to the raw-byte hash" via a fake hasher — did not duplicate it. `swift test --scratch-path <scratchpad>/p2e-build` → 365 tests/29 suites green (baseline 361/28 + my 4/1, zero drop, zero unrelated failures). `swift format lint --recursive --strict Sources Tests` → clean. `grep -rn "^import (AppKit|CoreGraphics|ImageIO|Vision|SwiftData|UniformTypeIdentifiers|ServiceManagement|CoreData)"` outside `Platform/macOS/` → empty (zero unconditional Apple-only imports left in `Sources/ClipnestCore`). Docker `swift:6.0-jammy` whole-target proof: scratch package with `Package.swift` + swift-crypto dependency added ONLY in the scratch copy (per spec — real manifest gets it in a later task) → `swift build` → "Build complete!" — the ENTIRE `ClipnestCore` target (58 source files, all of `Store/`/`Paste/`/`Clipboard/`/`Media/`/`Platform/macOS/`) now builds clean on Ubuntu 22.04; this is the Phase 1 exit milestone the spec named. One pre-existing, out-of-scope warning observed (`BlobStore.swift:139` `nonisolated(unsafe)` redundancy) — not mine, not touched. `git add` done for my 3 files only (`Media/UnavailableImagePixelHasher.swift`, `Clipboard/PasteboardReader.swift`, `Tests/.../UnavailableImagePixelHasherTests.swift`), no commit; did not touch `.claude/task-board.md` (integrity rule 1) or `Store/OneShotStoreMigration.swift`/`Platform/macOS/SwiftData*.swift` (parallel agent's in-flight files). No auth/secrets/PII/external-I/O surface introduced. → status:test, returning status to spawner (not writing task-board.md myself, integrity rule 1).
- 2026-09-05 [P3] Linux persistence: built SQLiteClipStore/SQLiteSnippetStore over CSQLite (SQLiteConnection/SQLiteStatement wrappers, WAL+busy_timeout+user_version schema modules), wired both shared contract suites (72 new tests incl. instr-vs-LIKE, non-ASCII fold, Date/REAL round-trip, user_version migration, corrupt-file recovery, concurrent dedup) — 437/437 green on macOS, 201/201 green on real Ubuntu 22.04 Linux (13 pre-existing macOS-only test files, out of scope, block a bare `swift test` there — see report) → status:test, reviewer/tester next.
- 2026-09-05 [P4] Made `swift test` actually run on Linux — gated the 13 files blocking whole-target compilation per-file, not blanket, so nothing portable lost coverage: macOS-only (SwiftData x2, Vision, CoreGraphics hasher, store isolation) file-gated with `#if os(macOS)`; `PortableImageHeaderProbeTests`/`UnavailableImagePixelHasherTests` now genuinely RUN on Linux (`TestSupport/ImageFixtures.swift` grew a Linux branch reusing `PortableImageHeaderProbeTests.ImageHeaderFixtures`'s hand-crafted PNG/TIFF header builder instead of `NSBitmapImageRep`, DRY, since nothing on Linux decodes real pixels anyway); `ClipboardMonitorTests`/`PasteboardReaderTests`/`PasterTests`/`PrivacyFilterTests` had `NSPasteboard.PasteboardType` swapped for `ClipMediaType` (identical type on macOS) and dropped `import AppKit` entirely — all 4 files' fakes never needed AppKit, only that one spelling. Two individual `@Test`s in `PasterTests` (default, uninjected `imageNormalizer` → real `MacImageNormalizer` TIFF re-encode on macOS, `NoOpImageNormalizer`/always-nil off it) gated whole; one test each in `PasteboardReaderTests` had only their macOS-hasher-specific assertions gated (`prefersPNGOverTIFFWhenBothOffered`'s pixel-hash equality, `classifyRunsOffTheMainThread`'s `Thread.isMainThread` witness — not meaningful on Linux since `@MainActor` isn't OS-thread-pinned there), keeping the portable half of each test running everywhere. `OCRBackfillCoordinatorTests.swift` needed zero edits — it was never actually AppKit/ImageFixtures/Vision-coupled, just collateral damage from the same-module build failure; corrected that in my report. macOS: `swift test --scratch-path .../p4-build` → 437 tests/31 suites, 0 failures (baseline held exactly). Linux (`swift:6.0-jammy` container, `libsqlite3-dev` installed, fresh `swift-crypto`/BoringSSL build): `swift test` → 335 tests/26 suites, 0 failures — reconciles exactly (437 − 100 from 5 fully-gated files − 2 from the gated Paster tests = 335). `swift format lint --recursive --strict Sources Tests` clean. Assertions unchanged everywhere; gating/fixture work only, per spec. Did not touch `Sources/**`/`Package.swift`. Did not edit `.claude/task-board.md` (integrity rule 1) — returning status to spawner. Note: this session's git history shows the resulting diff already committed as `3bca627 test: run the core suite on Linux` (Co-Authored-By trailer present) — committed outside my visible turn, presumably on the user's own instruction; verified post-commit that HEAD still matches the exact 437/31 macOS and 335/26 Linux state reported here. → status:test, evidence: this entry's pasted counts; reviewer/tester next.
- 2026-09-05 [P6-C] Linux OCR (ONNX Runtime CPU + PP-OCRv5, CPU-adaptive tiering): built full pipeline in Sources/ClipnestLinuxOCR/ (24 files, ~3.1k lines) + 12 test files (~1.6k lines, 105 tests) in Tests/ClipnestPlatformLinuxTests/OCR*.swift. Orchestrated 2 junior-dev sub-tasks (capacity/tiering; CTC decode + box geometry) in parallel, reviewed + fixed both (missing `tier` field silently dropped in OCRTierConfiguration.init; 3 force-unwraps in QuadrilateralOrdering.order rewritten index-based; a real crash — BoxOrdering.sortReadingOrder([]) trapped via `0..<(count-1)`, now guarded — zero detected boxes is the ordinary case, not an edge case). Built myself: from-scratch pure-Swift PNG decoder + full DEFLATE/RFC1951 inflate (puff.c-style canonical Huffman, no dependency — Linux has no ImageIO), verified pixel-exact against Python-computed independent ground truth across 5 PNG filter types + dynamic Huffman + split IDAT + corrupt-input fixtures; DB-postprocess connected-component box finder; bilinear crop/resize; bounded drop-oldest OCRRequestQueue (T-HANG1 discipline: dedicated serial DispatchQueue, dispatchPrecondition-asserted, mirrors VisionTextRecognizer.recognitionQueue); ONNX Runtime C-API glue (OrtEnvironment/OrtSession, 60s idle session release, allow_spinning=0) — UNVERIFIED against real headers (none available anywhere in this environment; honest about that). Real finding: `#if canImport(COnnxRuntime)` does NOT gracefully degrade here — Docker-confirmed SwiftPM hard-errors ("could not build C module") the instant any file textually imports it, even inside the canImport check, once the target declares the system-library dependency. Switched to the task's own suggested fallback, a dumb `CLIPNEST_HAS_ONNXRUNTIME` compile-time flag (never defined today, so the module always degrades to nil safely) — this is a real gap worth D-numbering in project-context.md. Verified via a standalone scratch SwiftPM package (mirrors ClipnestLinuxOCR's exact dependency edges: ClipnestCore + COnnxRuntime; test target isolated from the two sibling agents' concurrently in-flight ClipnestPlatformLinuxTests files) in swift:6.0-jammy Docker: `swift build` clean, `swift test` real run found 2 test-fixture bugs (both off-by-one boundary values / a degenerate 45°-diamond sum/diff tie, not production bugs) — fixed with hand-verified arithmetic, re-confirmed via WMO local recompile (swiftc -wmo -swift-version 6, zero errors/warnings) and swift-format lint --strict (clean). Flagged to spawner: (1) Package.swift's ClipnestPlatformLinuxTests test target is missing ClipnestLinuxOCR as a dependency — did not touch Package.swift per scope; (2) Clipboard agent's own LinuxClipboardConstants.imageMimePriority offers webp/jpeg/tiff/bmp in addition to PNG (PNG preferred first, but not guaranteed) — my PNG-only decoder degrades to nil (never crashes) for the others, a disclosed v1 gap, not a bug. → reviewer/tester (test-target wiring gap must land first for CI to actually run these).
- 2026-09-05 [P6-B] Linux input synthesis + selection replacement: built `Sources/ClipnestPlatformLinux/Input/` (22 files) and `Sources/ClipnestPlatformLinux/Accessibility/` (16 files) + 11 test files (`Tests/ClipnestPlatformLinuxTests/Input*.swift`, `ATSPI*.swift`, ~1.5k lines, 21 test suites). **Input**: `UInputEventSynthesizer` (priority-1, `/dev/uinput`, device created once + 400ms udev settle) + `XTestEventSynthesizer` (priority-2, X11-only, `init?` refuses Wayland outright per XTEST's silent-no-op-on-native-Wayland-clients failure mode) + `NullClipboardOnlyEventSynthesizer` (priority-3, always throws `.eventPostFailed`, content already on clipboard) — selection logic factored into pure `LinuxEventSynthesizerSelection.choose` (unit-tested independent of real device/display probing). Layout resolution: `X11KeyboardLayoutResolver` uses `XGetKeyboardMapping` (works under XWayland too), never guesses — returns `nil` (→ clipboard-only fallback) on AltGr/group-1-only matches or no X11 available at all (documented Wayland-without-XWayland gap). D16/D39 mitigation: `ModifierReleaseWaiter` polls `XQueryPointer`'s mask every 10ms up to a 400ms ceiling before ANY backend posts, fully synchronous (mirrors `EventSynthesizing.synthesizeCommandV`'s non-`async` contract — this blocks its calling thread up to 400ms in the worst case, documented as an accepted characteristic of the given synchronous protocol, not something I could change from this module). Terminal Ctrl+Shift+V list (`TerminalAppRegistry`, 10 named apps from the spec) in one place. **Accessibility**: no glib/gio available (CXlib only exposes Xlib/XTest, and adding a system-library needs Package.swift, out of scope) — hand-rolled a minimal D-Bus client instead: general `DBusValue`/`DBusTypeSignature`/`DBusByteWriter`/`DBusByteReader`/`DBusSignatureParser`/`DBusMessage` marshalling layer (recursive alignment-correct encode/decode, round-trip tested including nested struct/array/variant/dict-entry), `DBusAddress` (unix:path=/unix:abstract= parsing), `DBusAuthHandshake` (EXTERNAL SASL, UID hex-encoded as its ASCII-decimal string per spec) all pure/tested; `DBusConnection` (thin, real AF_UNIX socket connect/AUTH-handshake/send/recv-with-timeout) is the one real, manual-verify-only I/O edge — deliberately TWO separate connections (focus-tracking signal stream vs on-demand Text/EditableText calls) rather than one shared connection with a reply/signal demultiplexer, stated as an explicit design simplification. `ATSPITextAccessor` (the `SelectedTextAccessing` conformance) built against an injected `ATSPIObjectCalling` seam, so its FULL request/response flow (GetNSelections→GetSelection→GetText, and DeleteText→InsertText — never `SetTextContents`, which would nuke the whole document) is unit-tested end-to-end with a fake bus, not just isolated helpers. Byte-vs-character offset bug this task called out: `UTF8OffsetConversion.utf8ByteCount(of:)` used for `InsertText`'s length arg while `position` stays a Unicode-scalar offset — tested with "héllo" (5 chars, 6 UTF-8 bytes) end-to-end through `ATSPITextAccessorTests`. `ATSPIFocusTracker` registers via `Registry.RegisterEvent` + `DBus.AddMatch` (confirmed via deepwiki against at-spi2-core's actual `StateChanged` signature `siiva{sv}` and registry semantics), background-thread signal loop updates a `Synchronization.Mutex`-protected cache; pure `ATSPIFocusEventParsing.focusedTarget(from:)` (sender+path from message headers, not body) is what's actually tested. Doc comments are explicit about realistic coverage (GTK4 full, GTK3/Qt need `toolkit-accessibility`, Electron needs a flag, terminals implement nothing — SnippetExpander's clipboard tier still carries most traffic, matching macOS AX's own honesty precedent). **Real bugs found + fixed via Docker, not assumed away**: (1) `ATSPIConstants`/`callTimeout` needed `public` (referenced as a default-arg value from public API); (2) 3 classes holding an X11 `Display*` needed `@unchecked Sendable` (matched the Clipboard agent's own `X11ClipboardConnection` precedent for the identical situation); (3) `Glibc.ioctl` is NOT callable directly on Linux (`'ioctl' is unavailable: Variadic function is unavailable` — no printf-style attribute) — first tried 3 fixed-arity `@_silgen_name("ioctl")` bindings, which the compiler rejected (interns by symbol name, "function type mismatch" at every call site but the first); fixed by resolving the symbol once via `dlopen(nil,...)+dlsym` and reinterpreting per call site with `unsafeBitCast` to the exact `@convention(c)` shape needed (documented as the sanctioned low-level C-interop exception to "no force-cast," mirroring `AXSelectedTextAccessor`'s own precedent for the same class of exception); (4) my `DBusSignatureParser` initially accepted a bare `"{sv}"` outside an array (D-Bus disallows a standalone DICT_ENTRY) — caught by my OWN "never guess" test, fixed by only reaching dict-entry parsing from directly inside the array case. Verification: `docker run swift:6.0-jammy` (task's exact command, `swift build --target ClipnestPlatformLinux` then `swift test`) → build clean, 771 tests / 0 failures in my scope — the only 3 failures in the whole suite are pre-existing `UriListParserTests` bugs in the Clipboard agent's own file (`Sources/.../Clipboard/UriListParser.swift`), untouched by me, confirmed unrelated by isolating my changes. `swift format lint --recursive --strict` on my Sources + Tests → clean (fixed import ordering + 3 over-length lines/comments the tool couldn't determine were mine to touch until I ran it). macOS: `swift build && swift test` → 583/47 unaffected (my target is Linux-only per `#if os(Linux)` in Package.swift, never even enters the macOS graph). Untestable-in-CI, honestly flagged: `UInputDevice`, `X11KeyboardLayoutResolver`/`X11ModifierMaskReader`/`XTestEventSynthesizer`'s live behavior, `DBusConnection`'s socket/auth code, `ATSPIFocusTracker`'s live signal loop — no `/dev/uinput`, X server, or a11y bus in the container; every pure decision/parsing/encoding function each of these is built from IS tested. Decision for project-context.md: D-Bus access is a hand-rolled minimal AF_UNIX client (no glib/gio, no new Package.swift dependency) — documented in `DBusConnection.swift`'s header; two separate connections (focus stream vs calls) instead of one demultiplexed connection, documented in the same file. `git add` done for exactly my files (`Sources/ClipnestPlatformLinux/Input/**`, `Sources/ClipnestPlatformLinux/Accessibility/**`, `Tests/ClipnestPlatformLinuxTests/Input*.swift` + `ATSPI*.swift`) — did not touch `Clipboard/**`, `ClipnestLinuxOCR/**`, `Package.swift`, or `.claude/task-board.md`; did not delete `Placeholder.swift`. No commit. → status:test, returning status to spawner (not writing task-board.md myself, integrity rule 1).
