- 2026-08-10 [T41/T42] PASS — xcodegen generate clean, xcodebuild Debug BUILD SUCCEEDED, swift test → "Test run with 128 tests in 14 suites passed" (no new tests, expected — App-target UI-only change per spec), swift format lint --recursive --strict clean (exit 0). Static check: PickerViewModel.items `.history`→unpinnedRun / `.pinned`→pinnedRun confirmed; ItemRow rowActions renders pin→conditional convert-to-snippet (text.badge.plus, reuses onSaveAsSnippet/supportsSaveAsSnippet)→delete confirmed; SnippetRow.swift unmodified (2 buttons: edit/delete only) confirmed. Single call site (PickerView.swift) for both items/ItemRow, no other regression surface. GUI-rendering + runtime tab-filtering behavior flagged as manual-only, not verifiable headless. → done.
- 2026-08-10 [⌘⌫ delete / rich previews / type filter chips / image+file paste] PASS (build/test/lint/spot-check) — from-scratch build: `xcodegen generate` clean, `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`. `swift test` → "Test run with 131 tests in 14 suites passed" (0 failures; 128 baseline + 3 new PasterTests: accessibilityGrantedWritesImageThenSynthesizes, invalidImageDataThrowsBeforeAnyWrite, accessibilityGrantedWritesFileThenSynthesizes). `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0, no output. `swift format lint --recursive --strict Sources Tests` → exit 0, no output. Spot-check of real current source (not diffs) confirms all 4 claims genuinely wired, no discrepancies: PickerPanel.swift installs a local NSEvent.addLocalMonitorForEvents(matching:.keyDown) in configure() matching deleteKeyCode(51)+.command, fires onCommandDelete, removed in deinit; AppEnvironment.swift:125 `panel.onCommandDelete = { [weak viewModel] in viewModel?.deleteHighlighted() }` and :103 `blobStore: blobStore` into PickerViewModel(...) both present; ItemRow.swift leadingContent switches item.kind, routes .image/.file through ItemIconThumbnail(blobStore:) with real BlobStore.read/NSWorkspace.icon(forFile:) and graceful nil-fallback (no throw/crash), .link gets .accentColor+.underline; PickerView.swift body includes typeFilterChips (hidden on .snippets) and ItemRow(...) call site passes blobStore: viewModel.blobStore; PickerViewModel.select(_:) calls pasteContent(for:) (not old text/link-only guard), .image case is a genuine do/catch (not bare try?) that logs metadata-only via Self.logger.error on a real blobStore.read throw before returning nil; Paster.swift PasteContent has .text/.image/.file, paste(_:targetingFrontmostApp:) handles all three, .image path uses guard-let-else-throw (no force-unwrap) to decode via NSImage before writing TIFF. No production-code discrepancies found. git status --short: all 9 expected files staged as `A` (PickerPanel.swift, AppEnvironment.swift, ItemRow.swift, ItemThumbnailCache.swift [new], PickerView.swift, TypeFilterChips.swift [new], PickerViewModel.swift, Sources/ClipnestCore/Paste/Paster.swift, Tests/ClipnestCoreTests/PasterTests.swift) — confirmed via `git diff --cached --stat`. Gaps noted (not blocking, reported to routing agent): (1) `.claude/logs/junior-dev.md` is untracked/not `git add`-ed despite containing this batch's work log; (2) no senior-dev review-log entry exists yet for this batch (task-board.md T21 still shows status:todo, unrelated to this untracked routed batch) — code itself passed independent verification regardless. Runtime-only GUI/paste-into-other-app checks (⌘⌫ row delete vs Backspace text-edit, real thumbnail/file-icon rendering + graceful fallback, link styling, chip filtering/persistence-on-tab-switch/reset-on-reopen/hidden-on-snippets, actual image/file paste into Preview/Mail/Finder, text/link regression) cannot be exercised in this headless environment — flagged to the routing agent as "runtime-only, needs a human," not claimed as verified. Evidence pasted in full to routing agent's response.
- 2026-08-10 [T39] PASS (Phase E scope) — full regression `swift test` → "Test run with 131 tests in 14 suites passed" (matches AC, 0 failures). Clean-state script chain from `rm -rf build/`: `scripts/build.sh` → xcodegen generate OK, `xcodebuild archive` Release (CODE_SIGNING_ALLOWED/_REQUIRED=NO) → `** ARCHIVE SUCCEEDED **`, real `build/Clipnest.app` produced. `security find-identity -v -p codesigning` → "0 valid identities found" (confirms no real cert in sandbox, matches known constraint). `scripts/sign.sh "-"` (ad-hoc) → signed, `codesign --verify --deep --strict --verbose=2` → "valid on disk"/"satisfies its Designated Requirement", `spctl --assess` → "rejected" (expected pre-notarization behavior, not a bug). `scripts/notarize.sh` with no arg → fails loudly: "[notarize.sh] ERROR: no keychain profile name provided. Usage: ..." exit code 1, no hang/crash — guard path verified. Read-confirmed notarize.sh runs `codesign --verify --deep --strict "$APP_PATH"` before `xcrun notarytool submit` — signature check precedes submission as required. Real notarytool submission correctly skipped (no keychain profile exists here, already demonstrated failing once during T36 review — not re-demonstrated). `scripts/package_dmg.sh` → create-dmg path this run (installed in this sandbox, unlike T37's original hdiutil-fallback run) → real `build/Clipnest.dmg` produced, `spctl --assess` on the dmg → "rejected" (expected, app inside isn't notarized). Mounted via `hdiutil attach build/Clipnest.dmg -readonly -nobrowse` → confirmed `Clipnest.app` + `Applications -> /Applications` symlink present (`readlink` → `/Applications`), detached cleanly ("disk4" ejected). `shellcheck scripts/*.sh` → exit 0, no output, clean. `open build/Clipnest.app` → real launch, PID confirmed via `ps`/`lsappinfo` (`type="UIElement"`, matches `LSUIElement=true` — correct menu-bar-only registration, checked in with window server, launch-to-checkin 0.34s), stayed alive 5+ min with no crash report in ~/Library/Logs/DiagnosticReports and no crash/fault/terminate entries in `log show`; quit cleanly via `osascript -e 'tell application "Clipnest" to quit'`, process gone after. Could NOT visually confirm the menu bar icon glyph on screen — `screencapture -x` fails ("could not create image from display") and System Events AppleEvent automation times out (-1712); this session has no active console/display access, the same headless-GUI constraint already logged repeatedly in project-context.md (D10/D13/D14) — not a new defect, a pre-existing environment limit. Debug App-target regression: `cd ClipnestApp && xcodegen generate` clean → `xcodebuild ... -configuration Debug ... CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, no errors/warnings besides the benign "multiple matching destinations" xcodebuild notice — confirms Phase E's project.yml Release-signing-config addition did not break the everyday Debug build. Fraction of the release pipeline actually exercised here: build → sign(ad-hoc) → package → mount-verify is 100% real and passing; real Developer ID signing + real notarization cannot complete in this sandbox (no Apple Developer account/cert/keychain profile — genuine external dependency, D26/T35-38, not a script defect) — that final ~30% (real cert sign + real notarize.sh submission + Gatekeeper-pass on a genuinely notarized artifact) is gated on the account owner's own machine and was not and could not be exercised here. README.md's existing "Release" section (T37) verified accurate against this actual run — no correction needed. VERDICT: T35/T36/T37/T38 → done (Phase E fully validated within what's exercisable in this environment). T39 itself: Phase-E-scoped AC + full regression PASS, but T39's own literal AC ("only after this does overall Clipnest v1 count as done") is NOT met — T39 depends on T34 (Phase D tester validation) and Phase D (T28-T31,T33,T34: Settings UI, quick menu, excluded-apps wiring, pause-capture UI) is genuinely unbuilt per senior-dev's audit — T39 cannot be set `done` as "v1 complete"; recommend T39 stays open/blocked on Phase D, with a separate explicit note that Phase E itself is proven end-to-end. Returned to senior-dev, not writing task-board.md myself (single-writer rule).
- 2026-08-11 [T49/T50/T51/T52 — DB-virtualization] PASS (independent re-verification, evidence pasted in full to spawner) — `swift build` → "Build complete!" 0 errors/warnings. `swift test` → "Test run with 160 tests in 12 suites passed" (0 failures) — arithmetic confirmed exactly: 131 baseline + 20 (T49 ClipStore.query) + 17 (T50 SnippetStore.query) + 3 (T51 ClipboardMonitor.onCapture) = 171, − 11 removed SearchFilter/SnippetSearchFilter tests = 160, +29 vs baseline as claimed. Spot-checked ClipStoreTests.swift/SwiftDataClipStoreTests.swift `// MARK: - query (T49)` (10 tests each, identical bodies): confirmed case-insensitive substring match, empty-text-matches-all, kind filter, text+kind AND-combine, `.history`(unpinned/createdAt desc)/`.pinned`(pinned/pinnedAt asc) scope split, 3-page pagination asserted via id-sequence concatenation (no gap/dup), offset-beyond-count→empty, limit-cap, >100k-char previewText substring match. Spot-checked SnippetStoreTests.swift (8 query tests, no MARK comment but present)/SwiftDataSnippetStoreTests.swift (same 8 + 1 SwiftData-only update-resyncs-normalizedText test = 9): confirmed empty-text-returns-all, title/body OR case-insensitive match, keyword-never-checked, full-set pagination via while-loop with Set-equality dedup check, offset-beyond-count, limit-cap. `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -configuration Debug -derivedDataPath ClipnestApp/DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **`, zero warning/error lines grepped from full output. `swift format lint --recursive --strict Sources Tests` → exit 0. `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0. Code-read confirmations (item 7-9 of spawner's list): #Index/#Unique skip rationale IS documented in both `ClipItemRecord` (SwiftDataClipStore.swift:248-253) and `SnippetRecord` (SwiftDataSnippetStore.swift:148-152) doc comments (macOS 15+ requirement, deployment target not raised); `Package.swift` still `.macOS(.v14)`, `project.yml` still `"14.0"` — confirmed. Read PickerViewModel.swift in full (883 lines): no `allItems`/`allSnippets` property anywhere (only appear in a historical doc-comment describing the OLD deleted design) — every rows/snippetRows write path traced through `clipStore.query`/`snippetStore.query` (runRowsFirstPageQuery/refreshRowsWindow/loadMoreRows and snippets mirrors); `Self.searchDebounce = Duration.milliseconds(400)` confirmed line 101; `query`'s didSet computes `textChanged = oldValue.text != query.text` and passes `debounced: textChanged` — a kind-filter-only change (text unchanged) correctly calls `scheduleRowsQuery`/`scheduleSnippetsQuery` with `debounced: false`, confirmed by reading the didSet body; grepped `Timer`/`startLiveRefresh`/`stopLiveRefresh` — zero matches in actual code (only 1 historical doc-comment mention); `loadMoreIfNeeded()` exists (line 513) and is tab-aware (switches history/pinned→loadMoreRows, snippets→loadMoreSnippets); `selectHighlighted`/`togglePinHighlighted`/`saveHighlightedAsSnippet`/`deleteHighlighted` (lines 680,753,773,803) each `await self.flushActiveTabPendingQuery()` before reading `highlightedItem`/`highlightedSnippet` — confirmed for all 4. `AppEnvironment.swift:173` confirmed `monitor.onCapture = { [weak viewModel] _ in viewModel?.handleNewCapture() }`. Dead code removal confirmed: `find` for SearchFilter.swift/SnippetSearchFilter.swift/SearchTests.swift/SnippetSearchFilterTests.swift → no hits; grep for `SearchFilter`/`SnippetSearchFilter` type names across Sources/Tests/ClipnestApp → zero live-code references (only historical doc-comment mentions in PickerViewModel.swift and one pre-existing unrelated comment in SearchQuery.swift). PickerView.swift confirmed renamed `viewModel.items`→`rows`/`viewModel.snippets`→`snippetRows` (zero old-name references left) and `onReachEnd: { viewModel.loadMoreIfNeeded() }` wired on both list/snippetsList. `git status --short`: all 16 files named in the report's "Files touched" list confirmed staged `A ` (none untracked); zero SearchFilter/SnippetSearchFilter entries (consistent with delete-before-ever-committed); `git log` → "fatal: your current branch 'master' does not have any commits yet" confirms no commits made. 3 untracked logo assets (clipnest-logo-clean.png/clipnest-logo.png/clipnest-nest.svg) and one `AM` progress.md are pre-existing/unrelated to T49-T52, not a regression. NOT verified here (runtime-only, needs human — matches report's own "not verified" list): search/scroll feel at 400ms debounce, infinite-scroll stutter-free loading, live-append while picker open, large-history (thousands of items) smoothness, commit-actions-on-correct-row under rapid typing, tab-switch async-gap perceptibility. → PASS, all AC met with pasted evidence above; returned to spawner (senior-dev), not writing task-board.md myself (single-writer rule — spawner's own message confirmed it owns task-board.md/logs/senior-dev.md this round).
- 2026-08-11 [T51-regression] Adversarial critique: picker search box registers zero typed characters. REJECT. Runtime repro blocked — `osascript`'s controlling process lacks Accessibility TCC grant (`System Events` UI elements enabled = false; both `keystroke` send (-1002) and read-only AX tree access to Clipnest (-1728) refused) — documented exact commands/errors, no workaround attempted. Root-caused via full code trace instead: `PickerView.swift`'s TextField/FocusState code is byte-identical pre/post rewrite (T52's diff was 4 mechanical changes only, confirmed against `db-virtualization-report.md`) — the regression is a first-responder race in `PickerViewModel.swift` (T51): initial/every-keystroke row-selection (`applyRowsSelectionPolicy(.hardReset,...)`, PickerViewModel.swift:371→551-555) now lands *after* an `await clipStore.query(...)` (PickerViewModel.swift:366-367) instead of synchronously as before, so `List(selection: $viewModel.selectedItemID)` (PickerView.swift:270/387) now claims key/first-responder status *after* the search field's own `.focused()` request instead of before it, silently stealing focus off the field on open and again ~400ms after every keystroke. Zero test coverage exists for this class of bug (no test references `isSearchFieldFocused`/`focusToken`/`makeFirstResponder`), explaining why 160/160 green tests + prior review passes missed it. Snippets tab flagged as likely equally broken (identical pattern, PickerView.swift:295 + PickerViewModel.swift:557-561) — not yet independently confirmed. Full findings + minimal fix (not applied, report-only per task): `.superpowers/sdd/2026-08-06-clipnest-plan/search-input-critique.md`. Blocks done on T51 (and by extension the search flow of T49/T50/T52) until fixed and re-verified live with real AX access.
- 2026-09-03 [T-OCR3] PASS — on-device OCR for copied images (validation half). Full re-run + gap-closing this session, not a rubber-stamp of reviewer's earlier PASS. `swift test` → "Test run with 211 tests in 15 suites passed" (0 failures; 205 baseline + 6 new adversarial ClipboardMonitorTests + 1 new SettingsStoreTests, both written by me, test-code only). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` → "** TEST SUCCEEDED **", "Test run with 103 tests in 11 suites passed" (102 baseline + 1 new). `swift format lint --recursive --strict Sources Tests` → exit 0, clean. `swift format lint --recursive --strict ClipnestApp/Sources ClipnestApp/Tests` → exit 1, ONE finding: `HotkeyManagerTests.swift:25` LineLength — confirmed via `git diff --cached --stat` this file isn't part of the OCR diff at all (predates it, `git log` shows commit 84a62aa), pre-existing/out-of-scope, matches reviewer's own note. Every OCR-touched file lints clean when scoped explicitly.

AC→test mapping: (1) toggle exists/OFF-on-fresh-install → `HistorySettingsView.swift` Toggle(isOn: $settings.isTextRecognitionEnabled); default-false was UNTESTED before this session (gap, closed) — added `SettingsStoreTests.swift:isTextRecognitionEnabledDefaultsFalseAndPersists` (defaults false, persists explicit true) — also independently confirmed live via Accessibility-tree UI walk + `defaults read com.clipnest.app settings.isTextRecognitionEnabled` returning "does not exist" pre-toggle on a real launch. (2) OFF→zero recognition/no ocrText → `ClipboardMonitorTests.swift:recognitionDoesNotRunWhenProviderDisabled`; live-confirmed via real sqlite3 read of the production on-disk store (`ZOCRTEXT IS NULL` after copying an image with the toggle off). (3) ON→stores+searchable via normal search box → `ClipStoreContractTests.queryFindsItemByRecognizedTextAlone` (+InMemory/SwiftData instantiations) + `SwiftDataClipStoreTests.setRecognizedTextUpdatesNormalizedTextCaseInsensitively`; live-confirmed end-to-end: toggled ON via real UI, copied a real screenshot containing "QUIBBLEFONT7731 receipt total", real on-device Vision recognized it verbatim (sqlite3 ZOCRTEXT), opened the real picker panel via the real ⌥⌘V hotkey, typed "quibblefont" into the actual search field — exactly 1 row returned, the correct image. (4) Return pastes image / ⌥Return pastes OCR text / ⌥Return-no-text falls back to image → `PickerViewModelTests.swift` `imageWithRecognizedTextNonPlainPastesImage`/`imageWithRecognizedTextPlainTextPastesOcrText`/`imageWithoutRecognizedTextPlainTextFallsBackToImage`/`imageWithEmptyRecognizedTextPlainTextFallsBackToImage`; ALL FOUR live-confirmed via real ⏎/⌥⏎ keystrokes into the real picker + real pasteboard inspection (⌥⏎ on the OCR'd row → clipboard text == "QUIBBLEFONT7731 receipt total" exactly; plain ⏎ on that same row → clipboard holds image data, not text; ⌥⏎ on a non-OCR'd image row → clipboard still holds image data, unregressed). (5) ⌘⏎ unbound/no new behavior → `PickerView.swift`'s `handle(_:)` key switch is untouched by this diff (confirmed via `git diff --cached`); live-confirmed ⌘⏎ on the OCR'd row still pastes the image (identical to plain ⏎, a PRE-EXISTING fallthrough since `case .return:` has no modifier guard and predates this feature — flagging as a pre-existing quirk for the record, not an OCR regression, out of this diff's blast radius). (6) non-image kinds unchanged incl. ⌥⏎ → `PickerViewModelTests.swift`'s .text/.link/.richText/.file plainText cases — diff is PURE ADDITION to this file (confirmed via `git diff --cached --stat`: 116 insertions, 0 deletions), so every pre-existing case proving today's behavior is byte-for-byte untouched. (7) checkNow() never awaits recognition → `ClipboardMonitorTests.recognitionDoesNotBlockCheckNow` (gated fake recognizer proves checkNow() returns before recognition resolves). (8) excluded-app/concealed images never recognized → structurally proven (PrivacyFilter gate runs before store insert, OCR trigger is strictly downstream of a successful insert) AND now test-proven — both were GAPS (no test existed) — closed with new `ClipboardMonitorTests.recognitionNeverRunsForConcealedImage` / `recognitionNeverRunsForExcludedAppImage`.

Adversarial cases (task's explicit list): two rapid duplicate copies before the first Vision pass completes — reviewer flagged "idempotent-but-untested"; GAP, closed with `recognitionHandlesConcurrentDuplicateCopiesBeforeFirstPassCompletes` (rewrote the test-local `FakeTextRecognizer` gate from a single `CheckedContinuation?` to a FIFO queue so two genuinely-concurrent suspended calls can be proven in flight at once via `pendingGateCount`, then released independently — proves exactly 1 row, no corruption, no duplicate). Item deleted / `clearHistory()` racing the OCR write — GAP, closed with `recognitionWriteAfterDeleteDoesNotResurrectItem` / `recognitionWriteAfterClearHistoryDoesNotResurrectAnyItem` (both prove no resurrection; noted for the record that the failure path calls the hardcoded `Self.logCaptureFailure` rather than the injected `captureFailureHandler` — a minor, non-blocking observation, not a correctness bug, flagged not fixed since I can't touch production code). Pre-`ocrText` on-disk store migrating in — verified via the REAL production store on this machine, not just the unit test: `~/Library/Application Support/Clipnest/ClipItems.store` (238 real rows, created before this feature existed) opened cleanly under the new schema on real app launch, `sqlite3` confirms a `ZOCRTEXT` column now exists and all 238 pre-existing rows migrated with `ZOCRTEXT IS NULL` (correct terminal state) and content otherwise intact — stronger evidence than an in-memory container, and the exact crash class this repo has hit before did NOT recur. Image with genuinely no text / corrupt-garbage image — unit-tested (`VisionTextRecognizerTests.textlessImageReturnsNil`/`undecodableDataReturnsNil`) AND live-confirmed (copied a real blank white PNG through the real app with the toggle on — `ZOCRTEXT` stayed NULL, app stayed alive, no crash). Toggling the setting mid-session (off→on→off) — GAP (no test, no live proof existed), closed with `ClipboardMonitorTests.togglingSettingMidSessionTakesEffectImmediately` (same `ClipboardMonitor` instance, provider closure reads a mutable flag fresh per call, both directions proven) AND live-confirmed: toggled ON via the real Settings checkbox without restarting the already-running app, next image copy WAS recognized (real Vision output).

Real-app verification (not headless-only): built+launched a fresh Debug `Clipnest.app` (stale `/Applications/Clipnest.app` + 2 stale DerivedData dirs removed first per this project's known "remove old copies before testing" gotcha). `screencapture` confirmed still TCC-blocked in this environment (not attempted, per prior-session note) — used the Accessibility tree via `osascript`/System Events instead (this process does have AX Accessibility access, unlike an earlier logged session's tester). Verified via real AX-tree walk + real keystrokes + real pasteboard reads: Settings→History toggle exists and is unchecked on a fresh key (no `settings.isTextRecognitionEnabled` in `defaults read` before first touch); copying with it off captures the image but leaves `ocrText`/`ZOCRTEXT` null; flipping it on live (no restart) makes the very next image capture run real on-device Vision and store the exact recognized text; the real picker's real search box (⌥⌘V hotkey, real `NSEvent` keystrokes) finds that image by a distinctive OCR'd word alone; ⏎ pastes the image, ⌥⏎ pastes the recognized text, ⌥⏎ on a non-OCR'd image still pastes the image. Cleaned up after: toggle restored to OFF, test app quit, temp fixture PNGs removed. Did NOT touch/delete the user's 238 pre-existing real history rows — only added 4 new test rows via real copies (harmless, same as any other real capture).

Production code: NOT touched (integrity rule respected) — only `Tests/ClipnestCoreTests/ClipboardMonitorTests.swift` (+6 tests, `FakeTextRecognizer` gate reworked to a FIFO queue) and `ClipnestApp/Tests/ClipnestAppTests/SettingsStoreTests.swift` (+1 test) changed, both `git add`-ed, no commit. Verdict: PASS — all 8 AC verified with both automated tests (new + pre-existing) and live, real-app evidence; every gap the task called out by name is now closed with a real test; no defect found that blocks shipping. → tester owns this `done`, evidence: logs/tester.md#T-OCR3 (this entry).
- 2026-09-03 [T-PERF2/T-PERF3] PASS — main-actor offload (capture classify + image paste), validation half + closing the checkNow()-level test gap. `swift test` → "Test run with 216 tests in 15 suites passed" (215 baseline + 1 new: `ClipboardMonitorTests.checkNowClassifyDoesNotBlockTheMainActor`). `xcodegen generate` clean; `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO test` → "** TEST SUCCEEDED **", "Test run with 104 tests in 11 suites passed" (matches baseline). `swift format lint --recursive --strict Sources Tests` → exit 0 clean. `swift format lint --recursive --strict ClipnestApp/Sources` → exit 0 clean (didn't re-check `ClipnestApp/Tests` — not in coding-standards.md's pinned lint command; task told me to ignore the known pre-existing `HotkeyManagerTests.swift:25` finding there anyway).

T-PERF3 (new test, `Tests/ClipnestCoreTests/ClipboardMonitorTests.swift`, `checkNowClassifyDoesNotBlockTheMainActor` + helper `measureClassifyOffloadRatio`): drives `checkNow()` itself (not `classify` directly) with a real 25MB zero-filled payload, races a `@MainActor` heartbeat, and asserts the heartbeat's worst inter-tick gap stays under 1/3 of a self-calibrated solo `classify()` baseline. Watched it FAIL for real before shipping it: reverted `checkNow()` to `let result = reader.classify(rawPayload)` (no `Task.detached`) and reran — failed 6/6 times (solo AND inside the full 218-test suite), with exact numbers, e.g. `heartbeat.maxGap → 0.0077 s` vs threshold `0.0038 s`; restored the real code (`diff` confirmed byte-identical to the staged version) and reran — passed 13/13 times (solo and full-suite). Two real gotchas found and fixed while building this, both counterintuitive enough to flag: (1) a plain `#expect(tickCount > 0)` — the shape reviewer cited `PasterTests.imageDecodeDoesNotBlockTheCallingActor` as "genuine, non-vacuous" for — does NOT actually prove what it claims for `checkNow()`, because `checkNow()` has other genuine off-main hops after `classify` (blob write, `store.insertOrBumpDuplicate`) that keep tick counts in the thousands regardless of whether `classify` itself is inline; had to switch to worst-GAP-vs-self-calibrated-baseline instead. (2) the default `frontmostApplicationProvider` (real `WorkspaceFrontmostApplicationProvider`/`NSWorkspace`) pollutes a timing-sensitive measurement and had to be swapped for `FakeFrontmostApplicationProvider` — not used by any other test in this file since none of them measure timing. (3) discovered — but did NOT fix, since it's outside T-PERF3's scope and I don't edit production code — that `PasterTests.imageDecodeDoesNotBlockTheCallingActor` itself is ALSO structurally vacuous, for a different, deeper reason than the one reviewer flagged for `classifyRunsOffTheMainThread`: `Paster.paste` is a plain non-actor `struct`'s nonisolated `async` method, so per SE-0338 any `await`-call to it from `@MainActor` code hops off the actor before running REGARDLESS of the callee's internals — confirmed empirically (throwaway diagnostic, not checked in, deleted before finishing): a plain nonisolated struct method doing a 200ms synchronous inline busy-spin (zero internal offload) still let a racing `@MainActor` counter tick 3700+ times, while the identical busy-spin behind a genuinely-`@MainActor`-isolated method (like `ClipboardMonitor`/`checkNow()`) recorded exactly 0 ticks. This means `imageDecodeDoesNotBlockTheCallingActor` would currently pass even if `Paster.paste`'s `.image` case were reverted to fully inline, no-`Task.detached` work — reviewer's sign-off on that test was based on an incorrect belief about what it proves. Flagging this back up the chain (senior-dev/reviewer), not fixing it myself. Also discovered the retry-worthy fact that under full-218-test-suite parallel execution, `Task.yield()`-based heartbeats can see huge spurious gaps (observed once: `maxGap → 0.401s`) purely from shared-cooperative-thread-pool contention from ~200 unrelated concurrent tests, NOT from `checkNow()` itself blocking — the shipped test uses a "best of 5 attempts, stop at first clean one" design to absorb that noise; verified a genuine regression still fails 100% of attempts (3/3 full-suite reruns) since the actor block is structural, not a scheduling artifact a retry could get lucky past.

Main-actor stall measurement (item 2 of the task): built a throwaway SwiftPM bench (`PerfBench`, path-dependency on this repo's `ClipnestCore`, never committed) that calls the REAL production `PasteboardReader.classify` and `Paster.paste(.image:)` against the user's real 25MB blob (`~/Library/Application Support/Clipnest/blobs/d8370...` — read-only, `Data(contentsOf:)` only, confirmed via `xxd`/`file` it's a real big-endian TIFF, never wrote to it or its directory), with a `@MainActor` heartbeat + max-gap harness. Sanity-checked the harness itself first with a deliberate 0.2s busy-spin control (had to fix a real bug along the way: a `@Sendable` non-`@MainActor`-typed closure parameter silently hops off the actor per SE-0338, same gotcha as above — same fix, annotate the closure param `@MainActor`). Results, CAPTURE (`classify`, real 25MB blob), several runs debug+release: BEFORE (simulated inline, no `Task.detached`) maxGap ≈ full elapsed (8-40ms, fully frozen — heartbeat ticks 0-2 total); AFTER (real code) maxGap consistently 30-700µs on a quiet machine (occasionally low-single-digit-ms under this machine's heavy concurrent background load — `uptime` showed load avg 16+ from unrelated rustc/chromium/node processes this session; never anywhere near the BEFORE numbers even then). PASTE (`Paster.paste(.image:)`, real 25MB blob, real `BlobStore.read` + real decode/re-encode chain): BEFORE (simulated `NSImage(data:)+tiffRepresentation` inline, old code no longer in the tree) 9-40ms fully frozen; AFTER (real code, `Task.detached` ImageIO) maxGap 30µs-6ms depending on machine load, always a clean order-of-magnitude-plus better than BEFORE in every single run. Could not reproduce this task's cited calibration numbers exactly (~103ms/blob read 9ms/decode 34ms/tiff 49ms/write 11ms) — my NSImage+tiffRepresentation reimplementation measured ~9-12ms combined on this same real blob, not ~83ms; flagging as an honest discrepancy (possibly a different blob, a different machine, or Instruments-profiling overhead baked into the original numbers) rather than papering over it — the *offload* (BEFORE-vs-AFTER, same machine, same blob, same run) is the number that actually matters for the directive and is unambiguous either way.

Behavioral regressions hunted (item 3): (a) self-paste suppression — verified via `git diff --staged -- Sources/ClipnestCore/Paste/Paster.swift`: the post-write ordering (`pasteboard.writeData` → guard → `Task.sleep(synthesisDelay)` → re-verify frontmost → synthesize) is byte-for-byte UNCHANGED by this diff; only the PRE-write decode step moved to `Task.detached`. The `synthesisDelay`-vs-0.4s-poll race window is provably the same width as before T-PERF1, not introduced or widened by it — didn't write a new real-timer stress test for this (`.claude/coding-standards.md` explicitly forbids real timers in `ClipnestCoreTests`; a genuine end-to-end race repro would need to run outside that suite, which I didn't build given time budget — noting as unverified-by-me rather than claiming it). (b) ImageIO swap — `PasterTests.invalidImageDataThrowsBeforeAnyWrite` (pre-existing, still passing) confirms the `PasteError.invalidImageData` failure path. Wrote and ran (then deleted, not checked in) a throwaway test pasting a real 2-frame animated GIF built via `CGImageDestination`: produces a valid single-frame TIFF using frame 0 (sampled pixel confirmed red, not the frame-1 blue), no crash — consistent/expected, matches "one static frame only" behavior a paste target has always gotten. (c) `imagePreviewText` swap — pre-existing `PasteboardReaderTests.classifiesImage` (untouched by this diff, so now transparently exercises the new ImageIO-based dimension read) still passes with correct `"2×3"`-style dimension text; separately confirmed (throwaway PerfBench check, not checked in) the undecodable-bytes fallback path still produces `"Image (6 bytes)"` for garbage input, matching the documented byte-count fallback. (d) dismiss ordering — verified via `git diff --staged -- ClipnestApp/Sources/UI/Picker/PickerViewModel+Paste.swift`: `select(_:)`'s content-then-dismiss ordering is real (a `nil` `pasteContent` result returns before `dismiss()` runs); pre-existing `PickerViewModelTests` (`.image with no stored blobPath returns nil`, `.image whose blob is missing on disk returns nil`) still cover the underlying nil-return cases this depends on. Did not write a new `select()`-level integration test proving "paste lands in the previously-frontmost app, not Clipnest's own search field" specifically — existing coverage is at the `pasteContent`/ordering-by-construction level, not an end-to-end proof; flagging as unverified-by-a-new-test (verified only by code reading), for honesty.

Real-app check (item 4): installed `/Applications/Clipnest.app` (PID 5781) confirmed alive/responsive throughout — `ps` showed 0% CPU/idle between checks, never hung. `screencapture` confirmed TCC-blocked as expected; used the Accessibility tree instead (`osascript`/System Events) per this project's known gotcha. Confirmed via real AX-tree walks + a real ⌥⌘V hotkey + real Escape: menu bar icon present; the picker panel genuinely opens (confirmed independently via `CGWindowListCopyWindowInfo`, a 560×420 panel owned by PID 5781) and closes cleanly; its full UI tree is present and correct (search field, tab buttons, scroll area with 76 real outline rows from the user's real history, the keyboard-shortcut hint bar); no freeze observed opening/closing it repeatedly. IMPORTANT DISCLOSURE: while trying to exercise a real end-to-end image paste into a controlled TextEdit target, a synthesized paste landed in "Terminal Hub" (`hub-app`, an unrelated running app on this machine) instead, because this is a busy shared dev machine with many background processes (`rustc`, chromium, node dev servers) contending for frontmost-app status between my automation steps — not a Clipnest bug, a limitation of doing blackbox AX automation on a live, busy desktop. `hub-app` remained alive afterward (not crashed), but I did not/could not verify what, if anything, visibly changed in that app's window, and stopped further live-paste automation immediately once I noticed this to avoid compounding the risk. Flagging this plainly so the user can check that app's state themselves — I did not investigate further myself (out of scope, risk of doing more harm poking at an app I don't own/understand).

Hang (item 5): did NOT reproduce. No crash reports found; `sample`-based monitoring around a real paste attempt (6s window) captured normally with no anomaly; the app stayed responsive across every open/close/paste cycle exercised in this session (~15+ hotkey cycles). Per the task's explicit instruction, NOT claiming this work fixed the reported hang — only that I could not trigger it in the time/scenarios I tried (short, interactive AX-driven sessions; did not attempt a long-running idle-hours reproduction or the user's exact original repro steps, which were never recorded).

Production code: NOT touched (integrity rule respected) — `Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift` was temporarily reverted+restored 3x for T-PERF3 verification, confirmed byte-identical to the staged version via `diff` before finishing every time. Only `Tests/ClipnestCoreTests/ClipboardMonitorTests.swift` changed (+1 test, +1 helper), `git add`-ed, no commit. Verdict: PASS on both T-PERF2 (validated: tests/lint green, offload genuinely measured and effective, no regression found in the areas hunted) and T-PERF3 (gap closed, non-vacuous, watched fail then pass with pasted real numbers) — with the findings above (1 real gap in `PasterTests` reviewer should know about, 1 unverified self-paste-race-under-real-timers item, 1 unverified `select()`-level dismiss-target item, 1 live-app paste-mistargeting incident) surfaced honestly rather than glossed over. → evidence: logs/tester.md#T-PERF2.
- 2026-09-03 [T-STRESS1] IN PROGRESS (resumed after an infra ENOTFOUND drop, restarted from scratch per coordinator confirmation nothing had been written). Read CLAUDE.md, coding-standards.md, project-context.md (D6-D41, esp. D16/D19 self-paste-suppression risk, D23/D25 search-hang root cause precedent) and task-board.md/logs grep for prior OCR/hang work (T-OCR1/2/8, T-PERF1 main-actor offload, tester's earlier `sample`-based hang check = not reproduced). Read production source: ClipboardMonitor.swift, VisionTextRecognizer.swift, PasteboardReader.swift, ClipStore.swift/SwiftDataClipStore.swift, BlobStore.swift, Paster.swift, PickerViewModel+Paste.swift (App layer), PrivacyFilter.swift, ClipItem.swift, TextRecognizing.swift/TextRecognitionQuality.swift, FrontmostAppTracker.swift, EventSynthesizing.swift. CONFIRMED BY CODE READING (not yet empirically measured): (1) `ClipboardMonitor.scheduleTextRecognition` fires one `Task.detached(priority:.utility)` per eligible `.image` capture with NO semaphore/queue/cap anywhere in the OCR path — nothing bounds concurrent Vision passes. (2) The self-paste-suppression window devs flagged as an "accepted minor risk" in D16 is real by inspection: `Paster.paste` writes the pasteboard, THEN sleeps `synthesisDelay` (40ms default) + CGEvent post, and only AFTER `paste()` fully returns does `PickerViewModel+Paste.performPaste` call `suppressOwnPasteboardWrite` -> `monitor.ignore(changeCount:)` — so the real 0.4s poll Timer has a ~40ms+ window per paste where it could self-capture. Surveyed user's real store (READ-ONLY, never touched/mutated): `~/Library/Application Support/Clipnest/blobs/` has 48 files, 265MB — 41 real TIFF images (16KB-25MB, the named 25MB `d8370438...` confirmed present) + 7 RTF blobs; `ClipItems.store`/`Snippets.store` left untouched throughout. Built standalone SPM harness `tools/stress-harness/` (local path dep on root ClipnestCore, mirrors `tools/glyph-classifier/`'s isolation precedent — never referenced by shipping targets): drives real `ClipboardMonitor`/`PasteboardReader`/`SwiftDataClipStore` (on-disk container via `makeContainerForTesting(at:)`, always under a fresh `NSTemporaryDirectory()` subdir, never the real store)/`BlobStore`/`VisionTextRecognizer`/`Paster` against fakes only for pasteboard+event-synthesis+frontmost-app (never touches the real NSPasteboard or posts real CGEvents — other agents' work may be in the foreground on this machine). 6 scenarios: throughput+heartbeat, concurrent-OCR (real Vision, `ConcurrencyTracker` measuring actual max-simultaneous recognition calls), self-paste race (real Task.sleep timing sweep around the 40ms boundary, reproducing production's exact write-then-arm ordering), soak (RSS+disk over time, retention-reclaim proof), failure injection (corrupt/truncated image bytes, missing blob at read, item deleted mid-OCR, blob-dir permission failure). `swift build` in tools/stress-harness/ -> clean, 0 errors (2 harmless "unreachable catch" warnings in FailureInjectionScenario.swift, non-blocking). Next: run it, capture unified-log corroboration for OCR/notFound paths, then run baseline swift test / xcodebuild test / swift format lint and write the final PASS/REJECT verdict with pasted numbers.
- 2026-09-03 [T-STRESS1] CRITICAL FINDING captured via `sample` on the real production `VisionTextRecognizer`/`ClipboardMonitor.scheduleTextRecognition` path (harness run 1, pool=10 real images up to 10MB, quality=.fast then would-be .accurate). Machine state at run start: `vm.swapusage` 7138/8192MB used (87%), ~94-500MB free physical (fluctuating), load avg 6.3 — other agents' processes (multiple `claude`, node/vite dev servers, Playwright chromium headless renderers for a different project) confirmed active via `ps aux` sorted by %MEM, none dominant individually. After ~4.5-6.5 minutes wall clock with the harness process itself using only ~2s of total CPU time and sitting at ~4-5% instantaneous CPU / ~82MB RSS (i.e. NOT spinning hot, NOT obviously crashed — `ps` state `SN`), captured 3 `sample <pid> 3-5` snapshots (saved: `.claude/logs/stress-artifacts/sample1.txt`, `sample2.txt`, `sample3.txt`). All 3 show the IDENTICAL pattern, persisting across a 2+ minute span with no forward progress: exactly 10 threads (matching `hw.ncpu: 10` on this machine) simultaneously parked at `CountingTextRecognizer.recognizeText(in:quality:)` → `VisionTextRecognizer.recognizeText` → `withCheckedContinuation`, i.e. 10 concurrently in-flight `Task.detached` OCR calls from `ClipboardMonitor.scheduleTextRecognition` (ClipboardMonitor.swift:386) all genuinely running/blocked at once — 9 of them on `DispatchQueue_12: com.apple.root.utility-qos.cooperative (concurrent)` worker threads (Swift's cooperative thread pool — EVERY core's worker thread claimed), the 10th actually executing inside Apple's OWN internal `-[VNRecognizeTextRequest internalPerformRevision:...]` on a SEPARATE, SERIAL, Vision-owned queue (`com.apple.VN.detectorSyncTasksQueue.VNCRImageReaderDetector`). This is the empirical proof of the structural finding from code reading: nothing in `ClipboardMonitor`/`VisionTextRecognizer` bounds concurrent OCR tasks, and on this 10-core machine it took as few as ~10 images to fully saturate the ENTIRE Swift-concurrency cooperative thread pool with threads blocked awaiting Vision's own internal (serial, one-at-a-time) recognizer — meaning ANY other off-main `Task.detached` work in the app (the exact same pool `checkNow()`'s own classify/blob-write detached tasks and `PickerViewModel`'s `readBlobOffMain`/image-TIFF-normalization detached tasks use) would have to queue BEHIND the OCR backlog. This is a highly plausible mechanism for the user's reported paste hang: if several images were recently captured with OCR enabled, the cooperative pool can be fully pinned by pending Vision calls for an extended period (observed here: still stuck after 6+ minutes on a loaded machine), during which a paste's own off-main blob-read/image-normalize `Task.detached` would have to wait its turn. Killed the run (`kill -QUIT` then `-9`, clean exit, verified) after capturing 3 consistent samples, rather than let it run indefinitely on a machine other agents are actively using — swap usage held steady (~7130-7138MB) through the kill, not worsening. Reduced main.swift's OCR pool for a completing run (limit 6 images, <=4MB total, drain 3s/5s) + unbuffered stdout (`setvbuf`) for live progress visibility, plus trimmed throughput/soak sizes, to get a full end-to-end pass with real numbers without further loading this already-strained shared machine. `swift build` clean after edits.
- 2026-09-03 [T-STRESS1] Full harness runs completed (2 runs, run2/run3, small pools post-scale-down: throughputPool 19 real images/14.76MB, ocrPool 6 real images/502KB, named 25MB blob loaded separately for the self-paste-race image variant). Fixed a real design bug in the self-paste-race scenario mid-session (the "unarmed" trials never called `ignore()` at any offset, so late offsets trivially showed "raced" even though production would have armed suppression by then — not a real finding, a test bug) — rewrote to run ONE continuous background task mirroring production's exact ordering (write -> sleep(synthesisDelay) -> verify/synthesize -> paste() returns -> ignore(changeCount)) with `checkNow()` polling concurrently at swept offsets; run3 is the corrected/canonical data. KEY NUMBERS (pasted below, full output `.claude/logs/stress-artifacts/run3.txt`):
  THROUGHPUT: 200 mixed-kind items (text/link/file/richText/image incl. real blobs) in 0.155s driving loop = 1286 items/sec; main-actor heartbeat during the loop: n=82 mean=1.89ms p99=2.08ms max=2.09ms (baseline-idle calibration same run: mean=1.99ms p99=2.14ms max=5.84ms) — throughput heartbeat gaps are NOT worse than idle baseline; no main-actor stall from the capture path itself at this rate.
  OCR CONCURRENCY (6 distinct real images, small pool): maxConcurrentRecognitions=6 of 6 EVERY time (both .fast and .accurate, both harness runs) — i.e. 100% of submitted images ran their real Vision recognition truly simultaneously, confirming zero throttling empirically, not just by code reading. Durations .fast: min=27.5ms p50=33.1ms max=37.2ms (doc baseline ~23ms — plausibly-higher under this machine's load); .accurate: min=140.8ms p50=173.9ms max=255.8ms (doc baseline ~154ms). RSS before/afterSubmit/afterDrain: .fast 248->250->267MB, .accurate 267->270->307MB — modest growth per pass, not alarming at this small scale. deletedMidFlightSurvivedAsGhost=false both runs (item deleted 4 captures in while its OCR task was plausibly still in flight never resurrected). heartbeat during OCR: mean~2.0ms p99~2.14ms max~2.3-3.1ms — still no main-actor stall at 6-image scale.
  OCR CONCURRENCY (10 distinct real images up to 10MB, first run before scale-down): did NOT drain within 6.5+ minutes wall clock under this machine's real contention (swap 87% full, other agents' processes active) — `sample` (3 separate captures, 2+ min apart, IDENTICAL pattern each time — evidence in `.claude/logs/stress-artifacts/sample1.txt/sample2.txt/sample3.txt`) shows exactly 10 threads (== `hw.ncpu`) simultaneously parked inside `CountingTextRecognizer.recognizeText` -> `VisionTextRecognizer.recognizeText` -> `withCheckedContinuation`: 9 on Swift's `com.apple.root.utility-qos.cooperative` pool (every core's worker thread claimed) + 1 actually executing inside Apple's own SERIAL `com.apple.VN.detectorSyncTasksQueue.VNCRImageReaderDetector` queue. Process itself: ~82MB RSS, ~2s total CPU consumed over the whole 6.5 min (not spinning — genuinely blocked/starved, `ps` state SN). Killed cleanly (QUIT then -9) rather than let it run indefinitely on a shared machine.
  SELF-PASTE RACE (corrected, run3): TEXT paste — offset 0ms: no capture (3/3); offsets 5,10,20,30,38,42ms: SELF-CAPTURED every trial (18/18); offsets 55,80ms: no capture (6/6). Race window empirically closes between 42ms and 55ms — matches the 40ms `synthesisDelay` + small overhead almost exactly, confirming D16's "accepted minor risk" is real and its size is close to the nominal delay, not wildly larger. IMAGE paste — offsets 0,10ms: no capture (6/6); offset 55ms: 2/3 self-captured, 1/3 not (image path's extra `Task.detached` TIFF-normalize suspension before the write adds jitter, widening/shifting the window vs. text — a new, more-precise finding than D16's text-only framing). imageRaceProducedDistinctContentHash=true (both runs): a raced image self-paste writes a NEW blob with a DIFFERENT contentHash than the original (Paster's decode/re-encode-to-TIFF isn't byte-identical) — confirms the coordinator's "TIFF-hash row right after a paste" concern is real, not hypothetical: a raced image paste is a genuine duplicate ROW + wasted blob on disk, not just a bumped createdAt.
  SOAK (15s, small pool): storeRows grew 0->3578->6513->9019 by ~11s; `enforceRetention(cap:.maxCount(15))` at ~11s cut rows 9586->15 AND blobBytes 14.08MB->2.69MB, blobFiles 19->4 — retention demonstrably reclaims disk, not just DB rows. RSS bounced 141-322MB non-monotonically (down after the retention pass) — no runaway growth observed at this (short) duration; flagged as NOT a multi-hour claim (see caveat below).
  FAILURE INJECTION: all 6 cases true — garbage image bytes, truncated-TIFF bytes, missing blob at read (-> BlobStoreError.notFound), item deleted before setRecognizedText (-> ClipStoreError.notFound, confirmed NOT resurrected in fetchAll()), blob-dir permission failure (-> captureFailureHandler invoked, not a crash) — every one degraded gracefully, `allCasesCompletedWithoutCrash=true`.
  Both completing runs (run2 pre-fix, run3 post-fix) agree closely on every non-race number (throughput/OCR/soak/failure-injection) — good reproducibility.
- 2026-09-03 [T-STRESS1] DIRECT REPRODUCTION of a paste-hang mechanism (follow-up experiment #7, `PasteDuringOCRSaturationScenario.swift`, added mid-session after the section-3 `sample` evidence). Design: submit N distinct real images through the REAL `ClipboardMonitor`+`VisionTextRecognizer` capture path with OCR enabled (exactly production's path), then IMMEDIATELY attempt one real `Paster.paste(.image:)` call (fake event synthesizer only — never a real keystroke) and time it, comparing to a pre-load baseline. First pass (8 tiny images, <1.2MB total, system load had eased to ~1.3): baseline=0.5ms, during=0.2ms — no effect, negative result (pool wasn't saturated long enough at that size/load to catch it — reported honestly as inconclusive at that scale, not "disproven"). Second pass (~20 images, <6MB total, system load back up to ~3.4): the paste call NEVER RETURNED within 60s (`timeout 60` killed it, exit 124). Re-ran a third time in the foreground (no timeout) specifically to `sample` it live: reached section 7 at t=33s, still not resolved at t=69s, t=164s (2:44) — killed cleanly at that point (`kill -QUIT` then `-9`, confirmed dead, real store re-verified untouched: still 48 blobs). Captured `sample <pid> 3` at t≈69s (`.claude/logs/stress-artifacts/sample_paste_hang.txt`) — THE decisive artifact: 9 threads on Swift's cooperative pool (`com.apple.root.utility-qos.cooperative`), 9 distinct `com.apple.VNRecognizeTextRequestRevision3` concurrent Vision dispatch queues, 6 `ANEServicesThread`s (Apple Neural Engine) all simultaneously active — and critically, **zero threads anywhere in the process show a frame inside `Paster.paste`/`normalizedToTIFF`/`PasteDuringOCRSaturationScenario`**: the paste's own `Task.detached(priority: .utility)` TIFF-normalize step (the exact off-main path a real `.image` paste depends on in production `Paster.swift`) had not even been DISPATCHED to a thread yet — it was enqueued and starved, not merely running slowly. Main thread confirmed idle/parked in the run loop the whole time (a genuine actor suspension, not a literal main-thread block) — so from a systems-purity view "the main thread never blocks," but from the USER's view the paste action simply never completes, which is indistinguishable from a hang. Root mechanism, now fully explained: `VisionTextRecognizer.recognize`'s `withCheckedContinuation` closure calls `VNImageRequestHandler.perform(_:)` SYNCHRONOUSLY (no `await` inside that call) — Apple's own textbook Swift-Concurrency anti-pattern of blocking, non-suspending work inside a `Task.detached` body. `ClipboardMonitor.scheduleTextRecognition` (ClipboardMonitor.swift:380-404) fires one such detached task per eligible `.image` capture with NO semaphore/queue/cap, so N images captured together = N cooperative-pool threads blocked simultaneously; once N >= the pool's effective capacity, ANY other off-main `Task.detached` work in the whole app — not just OCR's own backlog — is starved, including `Paster.paste(.image:)`'s TIFF-normalize step and `PickerViewModel+Paste.readBlobOffMain` (image/richText paste content loading). This is a fully plausible, now empirically demonstrated, root cause consistent with the user's reported "hangs while pasting" symptom.
- 2026-09-03 [T-STRESS1] VERDICT: REJECT on pipeline stability under load — one Critical finding (unbounded OCR concurrency -> cooperative-thread-pool starvation, directly reproduces a paste-stalls-for-minutes mechanism) and one Medium finding (self-paste-suppression race, quantified, image variant confirmed to create a genuine duplicate row) block a clean PASS; everything else tested (throughput, main-actor responsiveness at normal load, failure injection, retention/soak reclaim) held up with zero crashes/corruption/resurrection. Baseline regression check (unaffected by harness-only changes, all in `Sources/ClipnestCore`/`ClipnestApp` untouched by this task): `swift test` -> 216 tests, 15 suites, all passed (pasted output showed the intentional corrupt-store CoreData error logs from `ModelContainerRecovery`/`SwiftDataClipStoreTests`/`SwiftDataSnippetStoreTests` — expected, those tests assert recovery from exactly that error). `xcodegen generate` clean; `xcodebuild test -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` -> 104 tests, 11 suites, `** TEST SUCCEEDED **`. `swift format lint --recursive --strict Sources Tests ClipnestApp/Sources` -> clean, exit 0, zero output (matches baseline; `HotkeyManagerTests.swift:25` lives under `ClipnestApp/Tests`, outside this exact command, per the known/accepted pre-existing exception). No regression vs. the 216/104 baseline.

DEFECTS (ranked):
1. [CRITICAL] Unbounded concurrent OCR recognition tasks starve the shared Swift-concurrency cooperative thread pool, which can stall ANY unrelated off-main work app-wide — including a real paste — for minutes. `ClipboardMonitor.scheduleTextRecognition` (Sources/ClipnestCore/Clipboard/ClipboardMonitor.swift:380-404) fires one `Task.detached(priority: .utility)` per eligible `.image` capture with no semaphore/queue/cap; `VisionTextRecognizer.recognize` (Sources/ClipnestCore/OCR/VisionTextRecognizer.swift:97-126) wraps a SYNCHRONOUS, blocking `VNImageRequestHandler.perform(_:)` call inside `withCheckedContinuation` — the textbook Swift-Concurrency anti-pattern of blocking work with no `await` inside a `Task.detached` body, which occupies (not merely slows) a cooperative-pool worker thread for the call's full duration. Minimal repro (harness): with "Recognize text in copied images" effectively enabled (`textRecognitionEnabledProvider: { true }`), capture ~15-20 real images in quick succession (`tools/stress-harness`'s `PasteDuringOCRSaturationScenario`, ~20 images <6MB total, `.accurate` quality — the shipped default), then attempt any off-main paste (`Paster.paste(.image:)`) — did not return within 164s in this session's live repro; `sample` at t≈69s (`.claude/logs/stress-artifacts/sample_paste_hang.txt`) shows 9 cooperative-pool threads + 9 concurrent `VNRecognizeTextRequestRevision3` Vision queues + 6 ANE service threads all live, and ZERO threads anywhere in the process yet inside `Paster.paste`/`normalizedToTIFF` — the paste's own detached task was starved, not merely slow. This is offered as the most plausible reproduced mechanism for the user's reported paste hang, CONDITIONAL on OCR ("Recognize text in copied images") being enabled — it is off by default (T-OCR1), so this only manifests for a user who opted in and then captured a burst of images. Suggested fix direction (not implemented — dev's call): cap concurrent Vision recognition (e.g. a serial/small-width queue or semaphore around `scheduleTextRecognition`'s dispatch), and/or ensure the synchronous `handler.perform` call can never share a thread pool with the rest of the app's off-main work.
2. [MEDIUM] Self-paste-suppression race is real, not just theoretical (project-context.md D16's "accepted minor risk"), and now quantified: a poll landing 5-42ms after a paste's pasteboard write self-recaptures the just-pasted content (window closes between 42-55ms, matching the 40ms `synthesisDelay` + small overhead almost exactly — harness: `SelfPasteRaceScenario`, 18/18 raced at 5-42ms, 0/9 raced at 55-80ms, run3.txt). For a TEXT paste this only bumps/reorders the existing row (dedup by identical contentHash) — low impact. For an IMAGE paste it is worse: `Paster`'s TIFF decode/re-encode (`normalizedToTIFF`, Paste/Paster.swift) is not byte-identical to the original capture, so a raced image self-paste writes a NEW blob with a DIFFERENT contentHash and inserts a genuine duplicate `ClipItem` row — confirmed empirically (`imageRaceProducedDistinctContentHash=true`, both runs). At the real 0.4s poll interval this is roughly a low-single-digit-percent chance per paste (window/period ≈ 45ms/400ms ≈ 11%, upper bound assuming uniform arrival), not negligible over a session. Suggested fix direction: arm `ClipboardMonitor.ignore(changeCount:)` right after the pasteboard write itself (before `synthesisDelay`), not after the full `paste()` call returns.
3. [LOW/informational] Real-world Vision recognition latency is highly load-sensitive: doc-quoted baselines (~23ms `.fast`, ~154ms `.accurate`) roughly held at small scale on this session's machine (27-48ms `.fast`, 141-285ms `.accurate` for 6 real images) but degraded far worse than linearly once concurrency + system contention combined (10-image/10MB batch did not drain in 6.5+ minutes under heavy load). Not a code defect per se — context for why #1 compounds badly on a loaded machine, which is realistic (this IS the user's daily-driver machine).

HANG — DIRECT ANSWER: Did I reproduce THE user's exact original paste-hang with the exact original repro steps? No — those steps were never recorded (per the prior tester's log entry) and I did not drive a real synthesized keystroke into a real foreground app (deliberately, to avoid disrupting the user's session). But I DID reproduce, with real production code and `sample` evidence, a concrete, unbounded-duration stall (164s and still not resolved when killed) affecting the exact off-main code path a real image paste depends on (`Paster.paste(.image:)`'s `Task.detached` TIFF-normalize step), triggered by OCR's unbounded concurrency (finding #1). This is a strong, evidence-backed candidate mechanism for the reported symptom, conditional on OCR being enabled. RULED OUT by this session (no stall/hang observed, real production code, real measurements): the plain capture path itself (throughput scenario, up to ~1290 items/sec, main-actor heartbeat unaffected vs. idle baseline); OCR at small scale (<=6 images) under lighter load; retention/clearHistory/delete racing OCR completion (handled gracefully, confirmed via `deletedMidFlightSurvivedAsGhost=false` and `deletedRowSetRecognizedTextThrowsNotFound=true`, no resurrection); all 5 failure-injection cases (corrupt/truncated image bytes, missing blob, IO failure) — none hang or crash.

Evidence anchors: `.claude/logs/stress-artifacts/{run2,run3,run4,run6}.txt` (full harness stdout, run3/run6 canonical), `.claude/logs/stress-artifacts/{sample1,sample2,sample3}.txt` (10-image/10MB OCR pileup, 3 snapshots 2+ min apart, identical), `.claude/logs/stress-artifacts/sample_paste_hang.txt` (direct paste-starvation repro, t≈69s into a 164s+ stall). Harness source staged (not committed) under `tools/stress-harness/` (`git add`, verified via `git status`). Real `~/Library/Application Support/Clipnest` store re-verified untouched throughout (still 48 blobs / same file timestamps on `ClipItems.store` as session start) — hard constraint honored.
- 2026-09-03 [T-HANG3] PASS (validation of T-HANG1/T-HANG2, reviewer already PASSed). HEADLINE: my original T-STRESS1 repro (164s+ stall, paste starved behind unbounded OCR concurrency) does NOT reproduce — confirmed NO twice, on two independent full harness runs, re-running the exact scenario-7 experiment that produced it: `ocrImagesSubmitted=14 baselinePaste=3.4ms duringSaturationPaste=1.4ms slowdown=0.4x` (run1) and `baselinePaste=0.5ms duringSaturationPaste=0.3ms slowdown=0.5x` (run2) — paste is now AS FAST OR FASTER during OCR saturation, not 164s+. Root cause (`VisionTextRecognizer.recognitionQueue`, a dedicated serial `DispatchQueue`) verified fixed by direct code read + empirical timing, matching reviewer's independent trace.

NEW: pushed to ~100 real images (task's explicit ask, beyond the ~14-25 used before). Real store only has ~41 real TIFF blobs total, not 263 — used all 20 available under a 20MB budget as-is (unmutated) plus 80 salted mutated variants (unique trailing-byte suffix per index -> distinct SHA-256 contentHash, so none dedup-skip OCR scheduling) built on top of those same real images; verified empirically (not assumed) that appending trailing bytes never breaks TIFF decode: `mutatedStillDecodable=80/80` (100%), and real Vision cost is genuinely paid per image (durations up to 5.5s under this session's heavy load, not near-zero decode-failures). New harness scenario: `tools/stress-harness/Sources/StressHarness/Scenarios/LargeScaleOCRBacklogScenario.swift`, wired as section 8 of `main.swift`. Caught and fixed my OWN instrumentation bug before trusting results: first run showed `backlogFullyDrained=true drainWaitSeconds=0.0`, which was a false positive — my drain check used `ConcurrencyTracker.totalCalls` (incremented on dispatch-start, same "enter fires before real work" quirk reviewer already flagged for `maxConcurrentRecognitions`), not actual completions (`durationsMs.count`, incremented only on genuine finish). Fixed + re-ran (2 corrected runs, one isolated section-8-only re-run + one full 8-section re-run):
  - Backlog DOES fully drain, no deadlock: 100/100 completed both corrected runs (drainWaitSeconds=14.7s under heavy load, 6.1s under lighter load — this machine had 2-14x latency swings across the session from other agents' concurrent work, confirmed via `vm.swapusage`/`uptime` at session start: 87% swap, load avg 4.4-6.3).
  - Memory does NOT grow unboundedly: run2 (clean/comparable baseline) `RSS before=407.78MB afterSubmit=477.69MB peak=607.42MB afterDrain=515.41MB` — peaks mid-drain then SETTLES DOWN by ~92MB once fully drained, does not climb monotonically. Some residual growth (~38-70MB submit-to-drain across runs) is partly explained by the scenario intentionally holding its whole 94.70MB image pool in memory for the run's duration (a harness design choice, not proven representative of real one-at-a-time user pasting) — flagging as a real but bounded, non-blocking cost, not a P1 (drained/settled every run, no runaway).
  - Paste stays responsive throughout, at every depth: baseline/25-queued/60-queued/immediately-after-100/mid-drain paste latencies all sub-4ms in both runs (run2: 0.23-0.36ms across all 5 checkpoints) — zero correlation with backlog depth, confirming T-HANG1 holds at 5x the previously-tested N.
  - Cancellation semantics at 100: mid-flight delete (index 20, ~50+ items still queued behind it) and `clearHistory()` at index 70 (wiping ~70 rows with many OCR jobs still in flight) both produced `midFlightDeleteGhostSurvived=false`, `clearHistoryGhostCount=0`, no crash, in both corrected runs — the existing `ClipStoreError.notFound`-thrown-and-swallowed path (no cancellation exists, none needed) holds at scale. `finalStoreCount=29` not 30 in both runs is explained (own harness off-by-one: index 70's item is captured as "post-clear" the same loop iteration `clearHistory()` immediately wipes it) — not a product bug, reproduced identically both runs so deterministic, not flaky.

RACE SWEEP (widened per explicit ask): `SelfPasteRaceScenario.swift` offsets widened from 8/6 points to 18/17 (finer near-zero resolution, 0-80ms), repeatsPerOffset raised 3->20 (700 trials/run: 360 TEXT + 340 IMAGE). Run2 (lighter load): 0/700 raced. Run1 (heavier load): 1/700 raced (TEXT, at the tightest ~0ms offset) — a genuine, disclosed finding, not swept under the rug: T-HANG2 does not provide an absolute guarantee, it shrinks the write-to-arm window from a always-losing 40ms+ (pre-fix: 18/18) down to a single `@MainActor`-hop scale that is now RARE (0.14% in the one run it appeared, 0% in the other, both far higher trial density than production's 0.4s poll would ever land in) but not proven impossible under scheduler contention. Severity: low (a raced TEXT paste creates one duplicate history row, no data loss/security impact, matches the doc comment's own honest framing "essentially a single @MainActor hop instead of 40ms+" — not "eliminated"). Not filed as a new P0/P1; recommend a note in project-context.md if the team wants that residual tracked.

Real-app check: `/Applications/Clipnest.app` (fresh binary, `strings` confirms `com.clipnest.visiontextrecognizer.recognitionQueue` present = the fix is in what's installed). OCR already enabled in real Settings (`settings.isTextRecognitionEnabled=1, quality=accurate` via `defaults read`). Verified via Accessibility-tree + real ⌥⌘V hotkey (System Events AX walk showed 0 windows for the LSUIElement panel — a known AX-reporting gap, not a bug; ground-truth via `CGWindowListCopyWindowInfo` confirmed the real 504x379 panel genuinely opened, responsively, PID 60409) — no hang opening the picker. DISCLOSURE per this task's explicit ask: did NOT attempt a live synthesized-⌘V-into-another-app test this time — "Terminal Hub" (`hub-app`, the exact same unrelated app from the prior session's incident) was independently confirmed still running/frontmost-contending on this shared machine at check time, so repeating that risk was judged not worth it given the harness already exercises the identical `Paster.paste` production code path (fake event-synthesizer only) end-to-end with strong evidence above. Cleaned up: quit + relaunched Clipnest cleanly (`osascript quit` + `open -a`) to leave a fresh, panel-closed process (new PID 88615, confirming "PID may change" as expected) — no crash reports in `~/Library/Logs/DiagnosticReports`. Real `~/Library/Application Support/Clipnest/` blobs directory untouched (harness never writes there; read-only `Data(contentsOf:)` only, verified by design in `RealBlobFixtures.swift`, spot-checked file count unchanged).

REGRESSION BASELINE (re-run myself, not trusted from prior agents): `swift test` -> "Test run with 220 tests in 15 suites passed" (matches senior-dev/reviewer's claim, own independent run). `xcodegen generate` clean; `xcodebuild test -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO` -> "Test run with 104 tests in 11 suites passed", `** TEST SUCCEEDED **`. `swift format lint --recursive --strict Sources Tests` -> exit 0. `swift format lint --recursive --strict ClipnestApp/Sources` -> exit 0. `swift format lint --recursive --strict ClipnestApp/Tests` -> exactly the known pre-existing `HotkeyManagerTests.swift:25` LineLength finding, nothing else (confirmed unrelated to this diff, matches task's explicit ignore-list).

Could not verify: the user's *exact original* repro steps (never recorded, per T-STRESS1's own log) — this task validates against the STRESS1-reproduced mechanism, not a literal user-session replay. Did not attempt a real live-app synthesized paste into another foreground app (see disclosure above) — mitigated via equivalent fake-synthesizer harness evidence covering the identical production code path.

Evidence: `.claude/logs/stress-artifacts/t-hang3-run1.txt` (first full 8-section run, section 8's drain numbers invalidated by the instrumentation bug — everything else in it valid), `.claude/logs/stress-artifacts/t-hang3-section8-rerun.txt` (isolated section-8 re-run, first corrected numbers), `.claude/logs/stress-artifacts/t-hang3-run2-final.txt` (full 8-section re-run, all corrected, canonical). New/changed harness source: `tools/stress-harness/Sources/StressHarness/Scenarios/LargeScaleOCRBacklogScenario.swift` (new), `tools/stress-harness/Sources/StressHarness/Scenarios/SelfPasteRaceScenario.swift` (widened sweep), `tools/stress-harness/Sources/StressHarness/main.swift` (wired section 8 + widened race call). VERDICT: PASS — T-HANG1 (paste-hang fix) and T-HANG2 (self-paste race fix) both hold at 5x prior scale under real load; one low-severity residual self-paste race disclosed above, not blocking. → evidence:logs/tester.md#T-HANG3
- 2026-09-06 [T83] FINAL GATE — REJECT. All 10 mechanical checks PASS (evidence below), but truth-bar spot check against actual shipped source (per this task's explicit charter, "Keyboard shortcuts and retention defaults, against HotkeyManager.swift / SettingsStore.swift") found TWO unambiguous false claims in `docs/usage.md` that contradict shipped, wired-up code AND contradict sibling pages on the SAME site — exactly the class of defect this gate exists to catch.

CHECK 1 (clean build): `cd docs && rm -rf _site .jekyll-cache && bundle exec jekyll build` -> "Configuration file: .../docs/_config.yml ... Generating... Jekyll Feed: Generating feed for posts done in 0.242 seconds. Auto-regeneration: disabled." No errors. PASS.
CHECK 2 (7 pages @ correct permalinks): `find _site -name "*.html"` -> compare/index.html, download/index.html, faq/index.html, features/index.html, index.html, privacy/index.html, usage/index.html — exactly 7, all matching required permalinks (/, /features/, /download/, /usage/, /privacy/, /compare/, /faq/). PASS.
CHECK 3 (JSON-LD): extracted all `<script type="application/ld+json">` blocks (8 total: 1 per page + homepage's extra) via python3 regex, validated every one with `python3 -m json.tool` -> all 8 VALID. Types found: WebPage x6 (features/download/faq/compare/privacy/usage), WebSite+SoftwareApplication on index.html only. `grep -il -E "aggregateRating|\"review\"|codeRepository|SearchAction|FAQPage|HowTo"` across all 8 extracted files -> zero matches (exit 1). PASS.
CHECK 4 (twitter:card): grep count per page -> all 7 pages: count=1, value="summary_large_image". PASS.
CHECK 5 (sitemap/robots): `_site/sitemap.xml` (770 bytes, 7 <url> entries, no feed.xml) and `_site/robots.txt` (1787 bytes) both present. robots.txt `Sitemap: https://aayushgour.github.io/clipnest/sitemap.xml` (matches baseurl). `head -3`/`grep '^---$'` on built robots.txt -> no front-matter leakage. PASS.
CHECK 6 (excluded docs absent): full `find _site -type f` listing shows only the 7 html pages + feed.xml + sitemap.xml + robots.txt + assets/ — no API.md/architecture.md/competitive-analysis.md/review//superpowers/ anywhere. Confirmed the one "features" hit is `_site/features/` (built from `docs/pages/features.md`, the small 5812-byte page, title "Features — Clipnest Clipboard Manager | Clipnest") — NOT the excluded 75600-byte `docs/features.md` (basename trap, correctly avoided). `grep -ril "competitive.analysis|internal strategy"` in `_site/` -> only a false-positive substring hit inside compare.md's own disclaimer sentence ("not copied from an internal roadmap doc"), no actual leaked content. PASS.
CHECK 7 (zero Linux/GTK/Wayland, case-insensitive): `grep -rli` across README.md, docs/pages/*.md, docs/usage.md, docs/_layouts, docs/_includes, docs/_config.yml, docs/robots.txt, .claude/submission-drafts.md, and built `_site/` -> zero real matches. Only two innocuous hits, both non-leaks: (a) `docs/_includes/seo-jsonld.html` line 29-30, inside a `{%- comment -%}` Liquid block (stripped at build, confirmed absent from `_site` output) stating the design constraint itself ("operatingSystem is macOS-only... zero Linux/GTK/Wayland strings anywhere in this file"); (b) `.claude/submission-drafts.md` line 175, meta-commentary confirming absence ("...macOS-only, no Linux mention"). Pre-existing, build-excluded internal docs (`docs/competitive-analysis.md`, `docs/superpowers/specs/...`) mention Linux only when describing THIRD-PARTY competitors or as an explicit product non-goal — confirmed absent from `_site/` per Check 6, not part of the public site. PASS.
CHECK 8 (link check): python3 script parsed every `href="..."` out of all 7 built pages, normalized `/clipnest/...`-prefixed paths against the real built-URL set -> "NO BROKEN INTERNAL LINKS". Explicitly grepped for hrefs into excluded-doc basenames (API.md, architecture.md, competitive-analysis.md, /review/, /superpowers/, features.md) -> only one hit, `_site/usage/index.html` -> `href="https://github.com/AayushGour/clipnest/blob/main/docs/API.md"`, which is a legitimate external github.com blob link (API.md genuinely exists in the repo, just not built as a Jekyll page) not a same-site relative link. PASS.
CHECK 9 (no hand-written absolute URLs): grepped docs/pages/, usage.md, _layouts/, _includes/, _config.yml, robots.txt, README.md, .claude/submission-drafts.md for `"/clipnest/`, `href="/clipnest`, `aayushgour.github.io` -> zero hand-written occurrences in content; the only literal occurrences are `_config.yml`'s `url:` config value (required, not a content link) and explanatory `#`-comments in robots.txt's own header (prose, not a rendered link) — confirmed the actual `Sitemap:` line is `{{ '/sitemap.xml' | absolute_url }}` (Liquid, not hand-written) and every in-page nav link uses `{{ "/x/" | relative_url }}` (spot-checked across all pages/*.md). PASS.
CHECK 10 (asset integrity): `sips -g pixelWidth -g pixelHeight` -> og-image.png = 1200x630 (92,614 bytes); github-social-preview.png = 1280x640 (95,587 bytes, ~93KB, well under the 1MB GitHub limit). `og:image` meta on index.html resolves to `.../assets/og-image.png`. PASS.

TRUTH-BAR SPOT CHECK:
- Headline differentiator (Accessibility survives updates): read `scripts/release-cert.sh` in full — confirms the mechanism exactly as described on the site: one long-lived self-signed cert -> stable designated-code-requirement (no cdhash) -> Accessibility grant survives updates; script's own header explicitly states "NOT a Developer ID and does NOT enable notarization." Site copy (index.md L12-14, faq.md L18/L22, usage.md L46-50) matches this mechanism precisely, no overstatement. VERIFIED ACCURATE.
- Self-signed/not notarized: confirmed nowhere (README.md, submission-drafts.md, all 7 pages) does copy imply notarization or a paid Developer ID; submission-drafts.md leads with the caveat per its own internal truth-bar check (L175) and explicitly states "Self-signed .dmg, not notarized, no Apple Developer account" (L5). VERIFIED ACCURATE.
- Keyboard shortcuts vs HotkeyManager.swift: source confirms defaults `.togglePicker` = ⌥⌘V (`.init(.v, modifiers: [.command, .option])`, L77) and `.expandSnippet` = ⌥⌘E (L85) — site's ⌥⌘V/⌥⌘E claims (download.md, features.md, faq.md, usage.md, index.md) all VERIFIED ACCURATE.
- **DEFECT 1 (retention default, docs/usage.md:129):** "There's currently no automatic limit or expiration ... Configurable retention (e.g. 'keep the last 500 items' or 'keep 30 days') is planned but not available yet." FALSE. `SettingsStore.swift` L29-30/128-132: `defaultItemCount = 1000`, `defaultDays = 30`, `retentionMode` defaults to `.items` (i.e. retention IS on by default, capped at 1000 items) when no UserDefaults value exists. `HistorySettingsView.swift` L56-81 ships a real, wired "Keep history for" Picker (Everything / Most recent items / A number of days) + Steppers — a real, shipped Settings UI for exactly the feature usage.md says doesn't exist. Directly contradicted by three OTHER pages on the same site, correctly worded: features.md:47 ("default is the most recent 1,000 items; switch to a day-based cap (30 days by default) or Everything"), faq.md:50 (same), compare.md:42 ("1,000 items by default; switch to a day-based cap or unlimited") — and corroborated by project-context.md D50 ("at the default 1000-item retention cap"). usage.md is the stale/wrong one.
- **DEFECT 2 (shortcut rebinding, docs/usage.md:218 and :228):** L218 "These shortcuts aren't yet customizable from within the app — a Settings window for rebinding them is planned but not shipped." L228 (troubleshooting, under heading "⌥⌘V (or ⌥⌘E) doesn't do anything"): "There's currently no in-app way to rebind Clipnest's own shortcuts, so the fix has to come from the other app." FALSE for the two global hotkeys. `ShortcutsSettingsView.swift` (full file read) ships `KeyboardShortcuts.Recorder("Open Clipnest", name: .togglePicker)` and `KeyboardShortcuts.Recorder("Expand snippet", name: .expandSnippet)` — a real, wired Settings tab (`SettingsView.swift` L33-43: `TabView` with a "Shortcuts" tab, `systemImage: "command"`) that lets a user rebind exactly ⌥⌘V and ⌥⌘E, which is precisely the scenario L228's troubleshooting entry claims has no fix. Directly contradicted by features.md:49 ("Shortcuts — rebind both global hotkeys (open the picker, expand a snippet) to whatever key combination you want") and features.md:54 ("Both global hotkeys (⌥⌘V and ⌥⌘E) are rebindable — see [Usage] for the full shortcut reference") — which sends readers to usage.md expecting confirmation, where they instead find the opposite claim. usage.md never documents the Settings window's General/History/Shortcuts/Permissions tabs anywhere (`grep -n '^## \|^### '` shows no such section) despite being titled "the full shortcut reference" (section 8 heading) and being this project's canonical usage guide — a completeness gap on top of the two outright false statements.
- Maccy pinning: `curl` of Maccy's own README (p0deje/Maccy, raw.githubusercontent.com) line 62: "To pin the history item ... press OPTION (⌥) + P. The item will be moved to the top with a random but permanent keyboard shortcut." Matches compare.md exactly ("Yes, via ⌥P (moves item to top, assigns a shortcut)"). VERIFIED ACCURATE.
- Deck license: `gh api repos/yuzeguitarist/Deck` + raw LICENSE file confirms: `deckclip/` subdir = AGPL-3.0-only, everything else = source-available/All-Rights-Reserved (not unlicensed). Matches compare.md L30/L39 exactly. Deck's own README (raw, grep -i notariz) confirms "not yet notarized" / "not notarized via Apple's paid Developer Program" — matches compare.md's claim about Deck. Star counts verified via `gh api`: Maccy 21,490 (site says "~21,500"), Deck 1,423 created 2025-12-06 i.e. ~9 months old as of 2026-09-06 (site says "~1,400... ~9 months old"). VERIFIED ACCURATE.
- macOS 26 Tahoe: compare.md's Sources section (L55) attributes the 8h-default/30min-7day-configurable retention specifics to Cult of Mac + MacMost (third-party), and only the opt-in/search/clear behavior to Apple's own support doc — matches this task's explicit instruction that retention specifics must be third-party-attributed, not claimed as sourced from Apple. Corroborated by project-context.md D64 (documents this exact sourcing decision). VERIFIED ACCURATE / correctly sourced.
- feed.xml (T85 known-accepted): confirmed genuinely orphaned — absent from sitemap.xml (`grep -c feed.xml sitemap.xml` = 0), absent from every built page's hrefs, content is the generic empty Jekyll Feed template. Not re-litigating cause per instructions.
- Nothing else was found unverifiable — every sampled claim resolved to either VERIFIED ACCURATE or a confirmed DEFECT above.

VERDICT: REJECT. Repro for the owner: open `docs/usage.md`, sections "5. Clipboard history > How much history does Clipnest keep?" (line 129) and "8. Keyboard shortcuts reference" + "9. Troubleshooting" (lines 218, 228). Expected: usage.md should state retention defaults to 1,000 most-recent items (configurable to a day-based cap or Everything, matching features.md/faq.md/compare.md) and should state both global hotkeys (⌥⌘V, ⌥⌘E) ARE rebindable from Settings > Shortcuts (matching features.md), ideally with usage.md gaining a "Settings window" section describing all 4 tabs (General/History/Shortcuts/Permissions) since it's the site's canonical usage reference. Actual: usage.md flatly states neither feature exists ("planned but not available/shipped yet"), contradicting shipped source code (SettingsStore.swift, HistorySettingsView.swift, ShortcutsSettingsView.swift, SettingsView.swift) and contradicting 3 sibling pages on the same site. This blocks `done` per the DONE gate's truth-bar requirement for an SEO/AEO initiative whose entire purpose is to be quoted accurately by AI answer engines — shipping a self-contradictory site is worse than not shipping. All 10 mechanical build/output checks otherwise PASS with pasted evidence above. Owner: fix docs/usage.md lines 129, 218, 228 (senior-dev/architect, not a tester fix) then re-request tester validation.
- 2026-09-06 [T83-REVALIDATION] FINAL GATE — PASS. Re-ran all 10 mechanical checks from scratch (not trusted from the prior REJECT run) plus an independent truth-bar re-check of all 5 usage.md fixes T89 reported, and the T90 HotkeyManager-vs-PermissionsSettingsView contradiction (report-only, app UI out of scope).

CHECK 1 (clean build): `cd docs && rm -rf _site .jekyll-cache && bundle exec jekyll build` -> "Generating... Jekyll Feed: Generating feed for posts done in 0.216 seconds. Auto-regeneration: disabled." No errors. PASS.
CHECK 2 (7 pages @ permalinks): `find _site -name "*.html"` -> compare/index.html, download/index.html, faq/index.html, features/index.html, index.html, privacy/index.html, usage/index.html — exactly 7. PASS.
CHECK 3 (JSON-LD): python3 regex-extracted every `<script type="application/ld+json">` from all 7 pages (8 blocks total) and `json.loads` each -> all 8 valid: WebPage x6 (compare/download/faq/features/privacy/usage), WebSite+SoftwareApplication on index.html only (SoftwareApplication home-only confirmed). `grep -ril -E "aggregateRating|\"review\"|codeRepository|SearchAction|FAQPage|HowTo"` across _site/ -> zero matches. PASS.
CHECK 4 (twitter:card): grep count per page -> all 7 pages: count=1, content="summary_large_image". PASS.
CHECK 5 (sitemap/robots): `_site/sitemap.xml` (770 bytes, 7 `<url>` entries, feed.xml absent) and `_site/robots.txt` (1787 bytes) both present; `Sitemap: https://aayushgour.github.io/clipnest/sitemap.xml` correct; `head -3`/front-matter check on built robots.txt clean (no leaked `---`). PASS.
CHECK 6 (excluded docs absent): full `find _site -type f` listing -> only 7 html pages + feed.xml + sitemap.xml + robots.txt + assets/ (og-image.png, github-social-preview.png, favicons, css, screenshot). Zero API.md/architecture.md/competitive-analysis.md/review//superpowers/ anywhere (the one substring hit on a path-based grep was `github-social-preview.png`, a legitimate asset, not the excluded `review/` dir — confirmed false-positive). PASS.
CHECK 7 (zero Linux/GTK/Wayland, case-insensitive): `grep -rli` across README.md, docs/pages/*.md, docs/usage.md, docs/_layouts, docs/_includes, docs/_config.yml, docs/robots.txt, and built `_site/` -> only hit is `docs/_includes/seo-jsonld.html` lines 29-30, inside a `{%- comment -%}` Liquid block describing the design constraint itself ("zero Linux/GTK/Wayland strings anywhere in this file") — confirmed stripped from built `_site/` (re-grepped `_site/` alone -> zero matches). PASS.
CHECK 8 (link + anchor check — highest regression risk this round, given the renumbering): wrote a python3 script (html.parser-based) that (a) collects every real built file path under `_site/` (html + assets), (b) for every `href` in all 7 built pages resolves relative/baseurl-prefixed/absolute-site paths against that real set, (c) for every same-page `#anchor` and every cross-page `path#anchor` resolves the fragment against the target page's actual `id="..."` attributes. Result: "Total html pages scanned: 7", "BROKEN INTERNAL LINKS: NONE", "BROKEN ANCHORS: NONE". Specifically spot-checked the exact renumbered anchors the task flagged: `grep -oE 'id="[^"]*"' _site/usage/index.html` shows `id="8-settings-window"`, `id="9-keyboard-shortcuts-reference"`, `id="10-troubleshooting"`, `id="11-uninstall"` — all match usage.md's own `[Settings window](#8-settings-window)` / `[Uninstall](#11-uninstall)` references exactly (kramdown auto-generated the ids T89 predicted). PASS.
CHECK 9 (no hand-written absolute URLs): grepped docs/pages/, usage.md, _layouts/, _includes/, _config.yml, robots.txt, README.md for `"/clipnest/`, `href="/clipnest`, `aayushgour.github.io` -> only hits are `_config.yml`'s required `url:` config value and robots.txt's own explanatory header comment (prose, not a rendered link) — confirmed the real `Sitemap:` line is Liquid (`{{ '/sitemap.xml' | absolute_url }}`). PASS.
CHECK 10 (asset integrity): `sips -g pixelWidth -g pixelHeight` -> og-image.png = 1200x630 (92,614 bytes); github-social-preview.png = 1280x640 (95,587 bytes, well under 1MB). PASS.

Regression note: `git diff HEAD --stat` confirms this change set is docs/README-only (docs/, README.md, .claude/*) — zero files under ClipnestApp/, Sources/, Tests/ touched, so no Swift build/test re-run was needed or performed for this docs-only re-validation.

TRUTH-BAR RE-CHECK (independent, read production Swift source directly, not trusted from T89's report):
- FIX #1 (retention, usage.md:143 now, was :129): read `SettingsStore.swift` L29-30 (`defaultItemCount = 1000`, `defaultDays = 30`), L128-132 (retentionMode defaults `.items` when absent from UserDefaults), and `HistorySettingsView.swift` L58-81 (real wired Picker: Everything/Most recent items/A number of days + Steppers). usage.md now says exactly this (1,000-item default, day-cap/Everything alternatives, pinned always kept, Clear All History button) and matches features.md:47/faq.md:50/compare.md:42 verbatim in substance. VERIFIED ACCURATE, no remaining contradiction.
- FIX #2 (hotkey rebinding, usage.md:213/246/256 now, was :218/:228): read `ShortcutsSettingsView.swift` (ships `KeyboardShortcuts.Recorder` for both `.togglePicker`/`.expandSnippet`) and `SettingsView.swift` L42-43 (real "Shortcuts" tab). usage.md now correctly states both global hotkeys are rebindable from Settings -> Shortcuts, takes effect immediately, and correctly scopes the limitation to only the in-picker shortcuts (search/pin/delete/tabs) staying fixed — matches features.md:49/54. VERIFIED ACCURATE.
- FIX #3 (network exception, usage.md:18/190 — the one flagged as needing extra scrutiny): read `UpdateChecker.swift` in full. Confirmed line-by-line: endpoint is GitHub's public Releases API (`https://api.github.com/repos/AayushGour/clipnest/releases/latest`, L47) — matches "GitHub's public Releases API" claim exactly. Frequency: `checkInterval = 24 * 60 * 60` (L39) — matches "once a day"/"once-a-day". Default-on: `SettingsStore.swift` L137-138, `automaticallyCheckForUpdates = defaults.object(...) as? Bool ?? true` — matches "on by default" exactly. Never auto-installs: `checkNow()` (L228-239) only fetches/parses/compares/sets state + fires `onStateChanged`; the actual download/install stays behind the separate, unchanged, user-click `AppUpdater.runUpdate()` — matches "never downloads or installs anything on its own". What's transmitted: `fetchLatestReleaseJSON()` (L304-330) spawns `curl --max-time 10 --connect-timeout 5 -fsSL <url>` — a plain GET with curl's stock User-Agent, no custom headers, no device ID, no auth token, no clipboard content of any kind — matches "sends nothing about you or your clipboard, just an anonymous request for the latest release tag" (an IP address is unavoidably visible to GitHub for any network request, same as any anonymous web request; the claim is about what Clipnest itself transmits, which is accurate). How to disable: `GeneralSettingsView.swift` L55-64, a real wired Toggle bound to `settings.automaticallyCheckForUpdates` that calls `updateChecker.settingChanged(enabled:)` live — matches "can be turned off in Settings -> General". Cross-checked wording is identical in substance across usage.md/faq.md/privacy.md (all three: GitHub Releases API, ~once/day, on by default, Settings->General toggle, no clipboard data, anonymous tag request) — no contradiction between sibling pages. VERDICT: FIX #3 is accurate AND complete, not just less-wrong.
- FIX #4 (Accessibility auto-prompt, usage.md:69/73 now, was :62/:69): read `AppEnvironment.swift` L455-472 (`requestAccessibilityOnceIfNeeded()`: guards on silent `PermissionsManager.isGranted` then persisted `SettingsStore.hasRequestedAccessibility`, sets the flag, calls the prompting `requestAccess()`) and `AppDelegate.swift:65` (called once, unconditionally, from `applicationDidFinishLaunching`) — confirms usage.md's "the very first time you launch... and only that once, ever, per install, it automatically shows the dialog" is correct. Grepped every call site of `requestAccess()`/`requestAccessibilityOnceIfNeeded()` across `ClipnestApp/Sources/` (`PermissionsManager.swift:60`, `AppEnvironment.swift:471`, `AppDelegate.swift:65`, `PermissionsSettingsView.swift:48`) — confirmed exactly 2 call sites of the prompting variant exist: the one-shot launch nudge and the Permissions tab's explicit Grant button; nothing in `showPicker()`/`HotkeyManager`/the paste path calls it — confirms usage.md's "neither opening the picker nor pressing ⌥⌘E triggers it either" and "a paste attempt... falls back to clipboard-only... instead of re-prompting" are both correct (`Paster.swift:316` / `AppEnvironment.swift:242` feed `Paster` only the silent `PermissionsManager.isGranted`, never the prompting call). VERIFIED ACCURATE.
- FIX #5 (downstream Troubleshooting entry, usage.md:259): now reads "Granting a permission never takes effect for a paste that's already in progress — press ⌘V once yourself to finish that one, and every paste after that should work automatically" — no longer references the debunked "paste attempt triggers the dialog" mechanic. Consistent with FIX #4's corrected mechanism. VERIFIED ACCURATE.
- New "## 8. Settings window" section: read `SettingsView.swift` (TabView: General/History/Shortcuts/Apps/Permissions, in that order) and each tab's view file (`GeneralSettingsView.swift`, `HistorySettingsView.swift`, `AppsSettingsView.swift`, `PermissionsSettingsView.swift`, `ShortcutsSettingsView.swift` seen earlier). Every cell of usage.md's 5-row table matches the real, wired UI: General (launch-at-login toggle, pause-capture toggle, update-check toggle — all 3 present); History (retention Picker+Stepper, OCR toggle+Fast/Accurate Picker, Clear All History button — all present); Shortcuts (KeyboardShortcuts.Recorder x2); Apps (NSOpenPanel add + minus.circle.fill remove, built-in password-manager list shown locked with lock.fill, matches "always on, can't be removed"); Permissions (Granted/Not-granted label, Grant Accessibility button, Open System Settings button, and the stale-grant callout text — usage.md's wording is a close paraphrase of `PermissionsSettingsView.swift`'s own copy, "remove it with the − button and add it again... toggling the switch off and on does not fix it" — verbatim match). New OCR subsection cross-checked against `ItemRow.swift:153` ("Copy Recognized Text" context-menu button, exact string match) and `HistorySettingsView.swift:202` ("Recognize Text in Existing Images" button, exact string match). VERIFIED ACCURATE.
- Sweep for a possible 10th stale claim: grepped all 7 pages + usage.md for planned/not-yet/currently-no/coming-soon/no-way-to/isn't-customizable phrasing. Found only 2 hits, both true-and-intentional, not stale: faq.md:22 ("a fully notarized... build... on the roadmap but not shipped yet" — true, app genuinely isn't notarized, re-confirmed no page anywhere implies notarization) and compare.md:16 (describes a *competitor*, Spotlight's clipboard history, lacking pinning/snippets — true, not a Clipnest limitation). Cross-checked every other "never/always/no way/can't" sentence across all pages against source (dedup-by-bump behavior, password-manager denylist immutability, Apps-tab add/remove mechanics, snippet Accessibility-then-clipboard-fallback two-tier expansion, AppUpdater's Terminal/`.command`-file update flow) — all matched real code (`ClipStore` dedup precedent from earlier sessions, `PrivacyFilter.builtInExcludedBundleIDs`, `AppsSettingsView.swift`, `HotkeyManager.swift`/expansion fallback docs, `AppUpdater.swift`). No 10th false claim found in this pass.
- Notarization re-check: swept README.md + all 7 pages + usage.md for "notariz" — every occurrence explicitly states self-signed/NOT notarized (download.md:52-56, faq.md:22, compare.md:8/30/47, README.md:236-263, usage.md:46). Nothing implies the shipped app is notarized. VERIFIED CLEAN.
- feed.xml (T85, known/accepted): re-confirmed still genuinely orphaned this build — `grep -rl "feed.xml" _site/*.html _site/*/index.html _site/sitemap.xml` returns nothing but the file itself; content is still the generic empty Jekyll Feed template (0 entries). Not re-litigating cause, matches WONT-FIX.

T90 (report-only, NOT blocking this validation, app UI code out of scope): read `HotkeyManager.swift`'s full header comment (L1-52) and `PermissionsSettingsView.swift:40`. CONFIRMED: `HotkeyManager.swift` is the TRUE one — its documented behavior ("NOT trusted -> Carbon hotkey. Works immediately, no permission. Consumes ⌥⌘V...") is real and matches `KeyboardShortcuts`' library-level Carbon registration path; the picker genuinely DOES open via the global hotkey without Accessibility granted, it just also consumes the keystroke (so Finder's own ⌥⌘V "Move Item Here" doesn't fire in that state, trusted mode releases it). `PermissionsSettingsView.swift:40`'s untrusted-state copy ("Without Accessibility, the global shortcut will not open the picker...") is the STALE/WRONG one — it should say the shortcut still opens the picker (just consumes the chord) and it's only the paste-into-target-app step that falls back to clipboard-only. Separately confirmed usage.md itself does NOT repeat this false claim anywhere (usage.md:83 unconditionally says ⌥⌘V opens the picker "from anywhere," with no Accessibility caveat) — so this discrepancy is fully contained to the in-app Settings UI string T90 already targets, does not leak into the SEO docs site, and does not block this validation.

VERDICT: PASS. All 10 mechanical checks green with pasted evidence above. All 5 usage.md fixes independently re-verified accurate against the cited production source (FIX #3's network-exception wording confirmed both correct AND complete: right endpoint, right frequency, right default-on state, right non-auto-install guarantee, right disable path, right "what's transmitted" claim). No remaining false/stale claim found anywhere in the change set after a full sweep. T90 resolved as a reporting matter only (HotkeyManager.swift is correct; PermissionsSettingsView.swift is the stale string) — correctly out of scope for this gate, does not block. Link/anchor regression risk (the renumbering hazard this round) came back completely clean. → tester authorizes `done` for T83/T89; evidence: logs/tester.md#T83-REVALIDATION (this entry).
