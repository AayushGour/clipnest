- 2026-09-06 [T72] Built docs/assets/ brand pack: og-image.png (1200x630, 90K), github-social-preview.png (1280x640, 93K, distinct asset from og-image per GitHub spec), screenshot-picker.png (1600x1000 placeholder, clearly badged, branded), favicon.svg (verbatim copy of clipnest-mono.svg) + favicon.ico (16/32/48/64 multi-res) + favicon-16x16.png/favicon-32x32.png + apple-touch-icon.png (180x180) — all geometry programmatically extracted from assets/clipnest-icons/clipnest-color.svg / clipnest-app-icon.svg's own <path> elements (zero redrawing). Also wrote docs/assets/css/style.scss (minima override), compiled clean against the real installed minima 2.5.1 gem (verified via a scratch `jekyll build`, no errors, overrides present in compiled CSS) — fixed one real AA contrast gap found in the reference preview.html palette (muted #7C7061 on paper #F6F3EC = 4.36:1, fails 4.5:1 body-text floor) by adding a darker --muted-strong (#56493A, 7.9:1) token for regular text, keeping the original as --muted-large for >=24px-only use. All dimensions/sizes verified with `sips`/`ls -lh`/`file`. → done, handing off to whoever wires _layouts/_includes (favicon <link> tags, OG/social image references).
- 2026-09-07 [T91] Redesigned the marketing site: dropped `theme: minima` (root cause of "horrible" — blog chrome + a bug where T72's style.scss never actually shipped, since minima's own default.html only linked its own compiled /assets/main.css, not /assets/css/style.css). Replaced with purpose-built docs/_layouts/{default,home,page,article}.html + docs/_includes/{header,footer,favicon-mark}.html and a full rewrite of docs/assets/css/style.scss (same T72 tokens, kept — plus one new token, --on-accent, after finding the old --panel-as-button-text choice fails contrast in dark mode: 1.98:1). Home page restructured into hero/screenshot/feature-grid/box-grid/cta-band/learn-grid via wrapper divs in pages/index.md (kramdown markdown="1" blocks, all flush-left — indented HTML inside a markdown="1" block gets misparsed as an indented code block, found and fixed live). faq.md converted to a zero-JS <details><summary><h2>/<summary> accordion (11 questions). compare.md's table wrapped in a scroll container + its disclaimer in a .notice callout. usage.md got a "Jump to a section" nav built from its own existing H2 text/anchors (verified against the actual generated ids post-build, not guessed). Every page's prose verified byte-identical against git HEAD via a multiset line-diff (only additions were structural: divs, `{: .lead}` IALs, and the new usage.md TOC reusing existing heading text) — nothing rewritten, nothing removed. Fixed two more contrast bugs found via WCAG relative-luminance math while building: .eyebrow/.footer-grid h2 were using --muted-large (large-text-only, 4.36:1) at 12.8px, which doesn't qualify as "large text" — moved to --muted-strong (7.9:1); .install-card's terminal background used var(--ink), which flips light in dark mode — fixed to a literal dark hex so the terminal stays dark in both modes. Full regression suite re-run clean post-fix: 7/7 pages build at their permalinks, JSON-LD valid on all (SoftwareApplication home-only), exactly one twitter:card per page, sitemap.xml/robots.txt intact, 212/212 internal hrefs + usage.md's numbered anchors resolve, excluded docs (API/architecture/features/competitive-analysis/review/superpowers) still absent — plus dropping minima also fixed T88's orphaned feed.xml (no longer generated). No `frontend-design` skill exists in this repo/environment; used `ui-ux-pro-max` only (--design-system + targeted style/product/typography/ux queries), noted where its stock matches (restaurant/AI/neumorphism pairings) didn't fit and fell back to first-principles + the existing brand tokens. → status:test, handing back to spawner (senior-dev/architect) for reviewer/tester pass.
- 2026-09-07 [T92] Re-themed off brown, built from the logo's own PAGE colors (purple #C6BEDC, peach #E7CEB0), per user verdict "too brown, want it lighter." Root finding that shaped the whole palette: peach/nest/paper/text-lines are all clustered in the same ~30-33° warm hue — only purple (H=256°) is a genuinely different hue — so any darkened peach variant still reads as brown; darkening peach for text would just reintroduce the exact problem. Design decision: purple carries every text-bearing/interactive role via one derived --accent (#6E58A8, 5.13:1 on paper), peach + light-purple are surfaces only (exact logo hexes, unmodified — hero backdrop wash, new .shot-frame component, feature-card tint, box-grid stripe), nest recedes to one decorative rotation slot (box-grid stripe, never text/button/link). Also re-hued ink/muted (old #2A231C/#56493A/#7C7061 were themselves warm-brown — a second, uncredited source of "brown" beyond --accent) to a cool near-black purple-family ink (#222027, 14.26:1). New tokens: --border-strong (3:1 UI-border for .btn-secondary's default state, was --line at ~1.4:1 decorative-only) and --shot-frame (soft lilac→peach gradient card framing the 3 real DARK-UI screenshots so they read as deliberate on the now much-lighter page, not accidental — same convention reused for hero + the 2 previously-unplaced features.md screenshots, which now live under "Pin your favorites" and "Expand anywhere"). Dark mode fully re-derived (not inverted): cool near-black purple-family paper #18161D, light-purple accent #9681CF (5.41:1), dark lilac/peach surface washes verified against light ink/muted on top (11-13:1 headroom), not just assumed safe. Every ratio computed with the real WCAG relative-luminance formula (python colorsys + a contrast() function) then cross-verified by sampling actual rendered pixels off a headless-Chrome screenshot (CDP Emulation.setEmulatedMedia to force light/dark, since --blink-settings=preferredColorScheme didn't take) — rendered #222027 ink matched the computed token exactly. DB corroboration (ui-ux-pro-max --domain color, "pastel lavender peach light palette productivity app"): top hit pairs a deep-violet primary on pale-lavender bg, same shape as this palette — no stock 3-hue preset exists, so exact hexes are first-principles derivation, corroborated in shape only. Regenerated docs/assets/og-image.png (1200x630, 90.8K) and github-social-preview.png (1280x640, 93.4K, both smaller than the T72 originals, well under GitHub's 1MB cap) — same unchanged app-icon SVG tile and identical copy, only the surrounding gradient/text colors moved to the new tokens; old brown-eyebrow version would have visibly clashed with the new purple-led site. Regression suite clean post-change: clean `jekyll build`, 7/7 pages, JSON-LD SoftwareApplication home-only, exactly 1 twitter:card/page (og-image.png confirmed as twitter:image via absolute_url), sitemap.xml/robots.txt intact, 212/212 internal hrefs resolve (0 broken), seo-jsonld.html/og-tags.html/_config.yml exclude+plugins+defaults untouched (git diff confirms), zero Linux/GTK/Wayland strings. Prose diff-checked: only my 2 edits this pass (features.md's two new `<div class="shot-frame">` image blocks, index.md's hero image wrapped in the same div) were exact-match python string replacements against content read verbatim from disk — zero prose lines touched, only new lines added. → status:test, handing back to spawner for reviewer/tester pass.
- 2026-09-07 [T94] Two home-page changes. (1) Moved "Get it" from its old spot (after "How it compares") to directly under the hero screenshot, per request. Relocating the old bordered `.cta-band` panel straight under `.shot-frame`'s own bordered gradient card read as two heavy framed boxes stacked, so instead of just moving the markup, converted it to a full-bleed `.cta-strip` (same construction as `.hero` itself: background on the `<section>`, `.shell` inside) — a soft peach-tinted wash that continues the hero's own lilac→paper→peach gradient logic, one un-boxed terminal card as the sole visual anchor, subtle bottom hairline. `.install-card`/`.terminal-bar` markup itself is untouched (still bare-reused on download.md/usage.md); old now-dead `.cta-band` CSS rule removed (0 refs left, confirmed via grep + compiled CSS). (2) "What's in the box" promoted from a plain 5-item bullet list to an icon/card grid: 5 hand-authored, consistent 24x24/1.75px-stroke/round-cap SVG glyphs (Heroicons/Lucide visual language — DB's own `no-emoji-icons` + `icon-style-consistent` guidelines named those two libraries explicitly, queried via ui-ux-pro-max --domain ux/style first) inside circular badges reusing the section's EXISTING T92 rotation (purple/peach/nest) moved from the old left-border stripe onto the badge fill — not a new color rule. Literal path-reuse from assets/clipnest-icons/clipnest-color.svg wasn't honestly possible for 5 distinct concepts (history/search/pin/tag/eye): that file is one hand-traced VTracer illustration (hundreds of bezier points fitted to one composition), not a modular icon kit; confirmed by reading it directly before deciding. What IS carried from the master: the "Full clipboard history" glyph is a card-with-3-lines (no clip/tab) rather than the banned generic-clipboard-with-clip silhouette, and those lines echo the master's own "text lines" layer shape family (stacked rounded bars). Icons are `aria-hidden="true"`, decorative only (cards already have text labels). Copy: only the one permitted exception used — Snippets bullet's "— see above." replaced with "Save reusable text under a short Tag, then expand it by keyword in any app with ⌥⌘E." (verified against features.md/usage.md's own established ⌥⌘E mechanics, not invented). The other 4 bullets were split into <h3> title (from the existing bold lead-in) + <p> description using the exact original substring verbatim (just the joining "— " dropped, a structural change, zero words changed) — proved via a before/after word-multiset diff: only removed words were the 5 em-dashes + "see"/"above", only added words were the new snippet sentence, nothing else moved. Raw HTML (no markdown="1", mirroring the proven `.terminal-bar` pattern already in this exact file) used for the icon-card `<li>`s to avoid kramdown ambiguity around nesting `<svg>`/`<span>` inside a markdown="1" block — confirmed correct by inspecting compiled `_site/index.html` (no misparse, no escaped/code-blocked HTML). Contrast computed (real WCAG relative-luminance math): --accent icon-stroke on the 3 badge washes = 5.09/5.28/4.75:1; --ink on the new cta-strip peach wash = 13.47:1; --accent (Download-page link) on cta-strip wash = 4.85:1 — all pass. Visually verified via headless-Chrome screenshot (full page + cropped) — section order, spacing, icon rendering, badge rotation all confirmed by eye, not just assumed from markup. Regression suite clean: `jekyll build` clean, 6/6 pages, sitemap 6/6, JSON-LD valid (SoftwareApplication home-only), exactly 1 twitter:card/page, 0 `/compare/` refs, 0 Linux/GTK/Wayland strings, link/anchor checker 0 broken anchors (the "broken internal links" it flagged are a script limitation — favicon/css asset hrefs it doesn't glob, confirmed all exist on disk). Did not touch seo-jsonld.html/og-tags.html/_config.yml. → status:test, handing back to spawner for reviewer/tester pass.
- 2026-09-07 [T95] Research-first craft pass on "How it compares" + the shared rhythm/alignment system it exposed. Phase 1 (curl + headless-Chrome render, no scraping/copying): maccy.app has ZERO comparison table anywhere on its page (Why?/How?/testimonials/CTA, nothing vs-competitor); zed.dev/compare and rectangleapp.com/comparison both live on dedicated SUB-pages, never embedded in the homepage scroll — corroborates but does not override this site's own prior T93 decision to keep a small in-page table, which was out of this task's scope to relitigate. rectangleapp.com/comparison is the one real 4-column feature-matrix `<table>` found (not a bento grid) and is the direct model used: table capped to a comfortable measure (not stretched full-width), deliberate column proportions instead of browser auto-layout, zebra striping, and the site's own product's column visually called out (they underline their header; this pass used a bg wash + bold accent + inset top bar). cleanshot.com's own section intros ("Always one drag away" etc.) are left-flush against the same edge as the grid/tiles below them, not centered — the direct precedent for the alignment fix chosen over the alternative (centering) below. Measured the actual bug on the live page before touching CSS: heading block left edge x=312 vs table left edge x=64 at 1280px viewport (a real ~248px seam, not a rounding error).
  Fixes, mapped to the 5 faults: (1) alignment — `.section-head` dropped its `margin:0 auto` (auto-centering); text stays left-aligned but the block now starts flush at the same edge as the content below, everywhere the shared class is used (Why Clipnest? + How it compares, only 2 real usages). Centering was tried and rejected: 2-5 line paragraphs read worse centered (ragged left edge, real scanability cost — not just taste) and it's not what the named reference site does for this exact job. (2) column widths — new `.compare-table` modifier class on the existing div (structural only, zero copy change) gets `table-layout:fixed` + explicit 40/20/20/20 column percentages, replacing the browser's auto-layout that had handed ~54% to Feature. (3) Clipnest emphasis — bg wash + bold accent header + inset accent top-bar on col 2 only, reusing the EXACT already-verified `.box-grid-icon` color-mix recipe (5.09:1) instead of a new pair. (4) ticks — centered, 1.15em, 700 weight, --accent, reusing already-documented accent-on-paper/panel ratios (5.13/5.70). (5) dead space above the heading — root-caused to more than raw padding: box-grid's 5th card was orphaned alone in a 4-column grid (deterministic given --shell-max, computed the exact math) with 3 empty column-widths beside it, which is what made the gap read as unstructured void rather than intentional rhythm; centered that lone card under columns 2-3 via a single `@media (min-width:1080px)` rule at the one breakpoint where the grid is provably always 4-column. Left `.section` padding (var(--space-8), shared by every section sitewide) untouched — tightening it in one place only would itself be the "second visual convention" CLAUDE.md forbids.
  Verify: `cd docs && rm -rf _site .jekyll-cache && bundle exec jekyll build` clean; local server (deleting _site did NOT visibly kill it this time, but flagged for restart per instructions regardless — served fresh output immediately post-build). 6/6 pages, sitemap 6/6, SoftwareApplication JSON-LD home-only, both JSON-LD blocks valid JSON, exactly 1 twitter:card/page, 0 `/compare/` refs, 0 broken internal hrefs (0/N across 6 pages), 0 Linux/GTK/Wayland strings (only doc-comment hits confirming their absence), `.install-card{position:relative}`/`.copy-btn{position:absolute}` untouched, copy button visually verified rendering correctly on home + download (usage confirmed present via grep, shares identical untouched CSS). Contrast (real WCAG relative-luminance math, python): accent-on-lilac-wash 5.08:1, accent-on-panel 5.70:1, accent-on-paper 5.13:1, ink-on-panel(zebra) 15.84:1, ink-on-paper 14.26:1, muted-strong-on-paper(sources caption) 6.13:1 — all reused/already-verified pairs, no new untested combination introduced. Screenshot-verified before/after at 1280px: box-grid orphan card now centered not stranded; "Why Clipnest?" and "How it compares" headings now flush with their grids/table below; Clipnest column visibly distinct; ticks legible. Prose byte-identical — only markup change was one class attribute (`table-scroll` -> `table-scroll compare-table`) on the pre-existing div; every other edit was CSS only. → status:test, returning to spawner for reviewer/tester pass.
- 2026-09-07 [T96] Craft pass extended from the home page (T94/T95) to the 5 inner pages, which had never gotten it. Consulted ui-ux-pro-max (--domain ux/style) first: "Feature-Rich Showcase" preset corroborates icon-container badges at 48px (matches this pass's reused --space-7 .box-grid-icon size exactly) and "icons consistent style"; deliberately deviated from its "alternating background colors" checklist item (see judgement call below). New shared components in style.scss: `.content-panel`/`.content-panel-head`/`.panel-icon` (extends the home page's .feature-card/.box-grid-icon card+badge language to inner-page H2 sections — same --panel/--line/--radius recipe, same lilac/peach/nest 3-way badge rotation reused verbatim, NOT re-tinted per-panel background on purpose, restraint call below) and `.detail-list` (accent-dot list replacing the UA default-bullet "**Bold lead-in** — description" pattern that read as a wall on features.md/download.md/usage.md). Also one footer-gap fix: `.content-page`'s bottom padding (--space-9) and `.site-footer`'s own margin-top (--space-9) were stacking on every inner page (~240px measured dead void, worst on faq.md whose last element is a hard-edged .faq-item border) — scoped fix via `main:has(> .content-page) + .site-footer { margin-top: var(--space-7) }` so ONLY inner pages tighten; home's own footer rhythm (main's child is a <section>, never .content-page) is untouched.
  Per page: features.md — wrapped all 9 H2 sections in .content-panel with a topic icon each (5 reused verbatim from box-grid: history/search/tag/eye; 4 hand-authored new in the same 24x24/1.75px-stroke/round-cap family: paste-arrow, gear, keyboard, lightning-bolt/"Featherweight", shield/"Private by design" — avoided the banned generic-clipboard-with-clip silhouette per standing brand rule); the 4 bullet-list sections (Clipboard history, Finding things fast, Pasting, Settings) got `{: .detail-list}`. privacy.md — same .content-panel treatment on all 5 sections (2 new icons: monitor/"100% local", key/"password managers", sliders/"You're in control"; shield reused from features; eye reused for OCR); the OCR trade-off paragraph (middle of 3 — the specific sentence naming the password/token risk) wrapped in the EXISTING but previously-unused `.notice` component (was defined for the deleted compare.md, orphaned since T93 — now doing real work). download.md — Requirements list got `.detail-list`; the whole "Signing, honestly" section (all 3 paragraphs — one continuous trust narrative, unlike privacy's OCR section which mixes trust-critical and incidental paragraphs) wrapped in `.notice`; deliberately did NOT wrap Install/Update/Uninstall/Build-from-source in .content-panel — they're already visually anchored by `.install-card` and wrapping risked exactly the "two heavy framed boxes stacked" anti-pattern T94 already found and fixed once. usage.md — lighter touch only (see judgement call): `.detail-list` on the Requirements (#2) and Privacy & exclusions (#7) lists; `.notice` on the OCR trade-off closing paragraph (#5, same sentence pattern as privacy.md) and on the core trust paragraph of "First launch — signing, honestly" (#3, first paragraph only — the rest of that subsection is task instructions, not a trust headline). faq.md — zero markdown changes; the footer-gap CSS fix alone closes its "only real fault."
  Bug found + fixed mid-build: kramdown treats `<span>` as SPAN-level HTML, so nesting `<svg>` markup inside a `<span class="panel-icon" markdown="1">`-parent's icon wrapper caused kramdown to misparse it (icon rendered giant + a literal stray "</span>" leaked into the page text) — switched the icon wrapper from `<span>` to `<div>` (BLOCK-level, passed through as opaque raw HTML by kramdown when nested inside a markdown="1" ancestor without its own markdown attribute), verified via compiled _site output showing correct nesting, then re-verified visually via headless-Chrome screenshot.
  Judgement calls: (1) bands vs single-ground — ALL 5 inner pages stay single-ground (flat --paper, no alternating full-bleed bands). Inner pages are prose-first/reference content on a narrow 46rem .content-page column, a fundamentally different layout primitive than home's full-bleed .shell sections; forcing bands into that column would either break the deliberately controlled reading measure or render as a confusing weak effect (no wider than the text). Structure comes from the .content-panel card system instead, which gets the same "grouped, not flat" outcome without sacrificing the reading column or adding a second full-bleed-band convention pages were never designed for. (2) cards vs lists on /features/ — SECTION-level cards, not per-bullet cards: the 4 lists' individual descriptions run 1-3 sentences (e.g. Hover previews, History) and would read cramped stuffed into small grid cards (that trade-off is explicit in the brief), so the list items stay lists with `.detail-list` typography (accent dot + real gap, dropping the UA disc+tight-margin default); the GROUPING/scanability fix instead comes from wrapping each H2 topic in a .content-panel with an icon landmark — same goal (Nielsen #6 recognition, break up the wall) via a size-appropriate mechanism. (3) [bonus, not asked but decided deliberately] features.md/privacy.md get the full .content-panel+icon treatment; download.md/usage.md get only .detail-list + .notice. Reasoning: those two are short explainer pages the audit named MOST directly for having zero structure ("no cards, no grouping, no icons" / "flat prose"); download.md/usage.md are already anchored by other strong components (.install-card terminal cards, tables, usage.md's own on-page nav) that a second full card system on top would visually double up on (the exact "two heavy boxes stacked" failure mode T94 already hit once with .cta-band under .shot-frame) — and usage.md at 300+ lines is a reference guide, not a showcase page, so 11 more hand-authored icons would add scroll-weight without adding scanability (Nielsen #8, restraint over decoration). Also declined a per-panel background-tint rotation (feature-card-style) even on the pages that DID get .content-panel: 9 tinted panels on features.md would read as a distracting rainbow, so background stays one plain --panel tone and only the icon BADGE carries the lilac/peach/nest rotation — same restrained split .box-grid already uses.
  Screenshots (headless Chrome, 1280px, full page): features.md reads as a clean sequence of 9 bordered topic panels with a coloured icon badge + heading each, bullet lists inside have visible accent-dot rhythm instead of a cramped disc wall, the 2 existing shot-frames nest inside their panels without a "double box" clash (subtle hairline panel border vs. the shot-frame's own vibrant gradient card read as clearly different weights) — confirmed comfortable to read, not busy. privacy.md: the OCR trade-off paragraph now visually jumps out via the left-accent-border .notice inside its own panel — directly fixes the audit's top complaint. download.md: Signing-honestly's 3 paragraphs now sit in one clear trust callout; Install/Update/Uninstall keep their existing terminal-card rhythm untouched. usage.md: Requirements/Privacy-exclusions lists tidied, both trade-off paragraphs now flagged, rest of the (already well-organized) reference content left alone. faq.md: measured gap before/after — last accordion to footer went from ~242px to ~150px (screenshot-confirmed, not just computed), a real, visible tightening.
  Verify: `cd docs && rm -rf _site .jekyll-cache && bundle exec jekyll build` clean (flagging the rm -rf _site per the standing instruction — confirmed via curl that the detached server on :4000 survived, all 6 pages re-served 200 immediately after rebuild). 6/6 pages built, sitemap.xml 6/6, 0 `/compare/` hrefs, JSON-LD valid on all 6 + SoftwareApplication home-only, exactly 1 twitter:card/page, 175/175 internal hrefs + anchors resolve (0 broken, script bug from an earlier run — double-slash root-path key — found and fixed before trusting the 0), copy button verified rendering correctly (screenshot) on `/`, `/download/`, `/usage/`, `.install-card{position:relative}`/`.copy-btn{position:absolute}` untouched (grep-confirmed same line count of each), 0 Linux/GTK/Wayland strings (only the pre-existing seo-jsonld.html doc-comment naming their absence), `_config.yml`/seo-jsonld.html/og-tags.html diff-confirmed untouched by me this pass (og-tags.html and seo-jsonld.html show zero diff at all vs HEAD; _config.yml's existing diff predates this task). Prose byte-identical: `git diff` on all 4 touched markdown files shows the ONLY removed line in each is the pre-existing T91 `layout: default` -> `layout: article` front-matter change (the one documented allowed exception) — zero prose lines removed or altered by any agent across this file's whole history on this branch, mine included; every other changed line is a pure addition (div wrappers, IALs). Contrast (real WCAG relative-luminance math, python, recomputed fresh not just quoted): ink-on-panel (.content-panel/.notice body text) 15.84:1, accent-on-panel (.detail-list dot, decorative rhythm not sole conveyor of meaning) 5.7:1, accent icon-stroke on the 3 .panel-icon badge washes 5.09/5.28/4.75:1 — all exact re-derivations of already-verified T94 numbers (same color-mix() recipe reused verbatim, zero new pairs introduced this pass). → status:test, returning to spawner for reviewer/tester pass.
