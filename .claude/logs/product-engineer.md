- 2026-09-07 [T-CA2, proposed] Rewrote `docs/competitive-analysis.md` as two parts: Part A (prior macOS market scan, kept verbatim) + new Part B, a primary-source-verified technical competitive analysis of Linux clipboard managers (CopyQ, GPaste, Diodon, xfce4-clipman, Parcellite/ClipIt, KDE Klipper, Pano, GNOME Shell extensions) against Clipnest's own Linux port, covering the six requested areas: Wayland capture, paste injection, per-row actions, OCR, OCR reading-order/spacing (root-caused both reported bugs against PaddleOCR's actual upstream `rec_postprocess.py`/`predict_system.py`), and packaging. Verified the project's own most load-bearing unverified claim (Mutter re-owning X11 CLIPBOARD for native-Wayland copies) directly against Mutter's GitLab source (`src/x11/meta-x11-selection.c`, `notify_selection_owner`/`meta_x11_selection_init`) — confirmed true and found it also covers PRIMARY selection, which Clipnest isn't yet using. Found the `clipnest-grant-input` polkit/uinput grant flow is fully built + reviewed-secure but wired to zero UI (grep confirmed) — flagged as the single highest-value/cheapest fix in the report. Findings, recommendations (adopt/diverge/investigate per area), and the two independent reads the spawner specifically asked for (XWayland bridge reliance, Shell-extension verification status) are in the doc; per single-writer discipline I did not edit `.claude/project-context.md` or `.claude/task-board.md` myself — decisions/board line below are for the spawner to record. No code touched (research/writing task only); `git add`ed the doc, no commit. → handing to architect.
- 2026-09-14 [POC-SHELLEXT-WAYLAND] Throwaway POC on the UTM VM (real GNOME Shell 46, extension `clipnest@clipnest.app` ACTIVE the whole time; zero production files touched, all scripts in `~/clipnest-test/poc/` on the VM only). **Q1 (decisive) — the extension does NOT fix the modifier merge.** Reused `~/clipnest-test/modtest/key_logger.py` (reads `Gdk.ModifierType` off the real keypress, not the clipboard); called the extension's own `SendKeyChord`/`FocusAndSendKeyChord` via a throwaway D-Bus bridge that owns `app.clipnest.Clipnest` for the duration (required — `_checkSender` in `extension/src/core/service.js` accepts only that name's current owner; the real running `clipnest` binary was stopped for the test and cleanly restarted after). With a uinput device holding Shift/Super/Both, the extension's own Clutter-virtual-device chord merged 36/36 (10/10 Shift, 10/10 Super, 10/10 Both via `SendKeyChord`, 6/6 Shift via `FocusAndSendKeyChord`) — 0 clean. Positive control (extension alone, nothing held) 8/8 clean; known-bad uinput-only baseline reproduced 12/12 merged as expected — both controls hold, so the merged result is trustworthy. This falsifies the hypothesis in the routing brief ("Mutter is the authority for its own device, not a peer") — Mutter's own virtual device is just as subject to the seat-wide aggregated modifier state as any uinput peer; no injection layer (uinput or Clutter) can dodge a modifier a *different* device is currently holding. **Q2 — real auto-paste on native-Wayland: YES, exact-match, 2/2.** `FocusAndSendKeyChord` against `gnome-text-editor` (confirmed absent from `xwininfo -root -tree`) landed the exact primed marker both trials, sub-ms compositor-side latency. Two harness dead ends worth recording for whoever touches this again: the short-lived `Gdk.Clipboard.set()`/`read_text_async()` python probes are genuinely broken on this VM (silent no-op write / "Cannot read from empty clipboard" even right after a "successful" write) — primed the clipboard via a real live app's own Ctrl+C instead, which worked cleanly; and `gnome-text-editor`'s `restore-session=true` GSettings default silently accumulates every previously-opened window across repeated test launches (7 stale frames appeared) unless disabled — reset to schema default before finishing. **Q3 — `GetFocusedApp` identity: usable.** Real fields observed live: `gnome-text-editor` → `{app-id: org.gnome.TextEditor, wm-class: org.gnome.TextEditor, client-type: wayland}`; `gnome-terminal` → `{app-id: org.gnome.Terminal, wm-class: gnome-terminal-server, client-type: wayland}` — both distinguishable by `app-id`/`wm-class` alone, both confirmed native-Wayland (absent from `xwininfo`). **Q4 — placement: precise; above-fullscreen: unverified, disclosed.** `PlaceWindow` moved a real `clipnest`-WM_CLASS/`Clipnest`-titled test window by the *exact* requested delta both trials (750px X / -30px Y requested and observed, byte-for-byte) — a constant ~14x12px offset between the requested point and `xwininfo`'s reported corner is a frame/CSD-padding artifact, not a placement bug (confirmed by the delta matching exactly). `flags=1` (`make_above`) executed without error but stacking order was **not independently visually confirmed** — this VM has no `gnome-screenshot`/`grim`/`wmctrl`, and GNOME 46 already denies `Shell.Screenshot`/`Eval` to non-allowlisted callers per this project's own prior finding — stated as untested, not glossed. **Q5 — costs, measured.** N=20 each: `GetPointer` mean 0.48ms/p95 0.65ms; `GetFocusedApp` mean 0.37ms/p95 0.48ms; `GetMonitorWorkArea` mean 0.26ms/p95 0.31ms; `SendKeyChord` mean 0.31ms/p95 0.43ms; `SetClipboard` (real fd, 64B) mean 1.0ms/p95 1.0ms (max 6.8ms) — well inside the brief's cited 2-70ms range, confirms the old 250ms timeout was never load-bearing. Extension disabled mid-flight: three in-flight-style calls right after `gnome-extensions disable` all failed **immediately** (1-2ms) with a clean `UnknownMethod: Object does not exist` — no hang; re-enable restored function within ~1.5s and the very next call succeeded with zero bridge-process restart needed. **Recommendation basis for the user, not a decision made here**: Q1 alone kills "the extension fixes the Wayland modifier-merge bug" as a claim — any future auto-paste-on-Wayland doc/UX must not promise clean-chord delivery while a modifier is physically held, regardless of extension use. Q2-Q5 show the extension is otherwise solid (real auto-paste, real focused-app identity, precise placement, sub-ms/fail-clean D-Bus surface) — those capabilities stand on their own merits independent of the modifier-merge question. VM restored: extension re-verified `Enabled: Yes / State: ACTIVE`, both keybinding schemas intact (`toggle-picker`/`expand-snippet` GSettings floors + values unchanged), `org.gnome.TextEditor restore-session` reset to its schema default, the real `clipnest` binary (stopped only to free the D-Bus name for the bridge) restarted via `start_resident.sh` under the same unit name it was found running under, zero reboots across the whole session (`journalctl --list-boots` boot 0 unbroken start to finish), session never locked. No `.claude/task-board.md`/`.claude/project-context.md` edit by me (single-writer discipline; findings are in my return message for the spawner/architect to record) — no code in `Sources/`, `ClipnestApp/`, or `extension/src/` touched. → handing findings to architect (+ any docs/installer/`debian/control` decision to the user, since Q1 is a P0-magnitude product call).
- 2026-09-14 [research, feeds T-CROSSDEVICE-MODIFIER1] Cost/benefit on shipping/depending-on the GNOME Shell extension, no code touched. Verified against primary sources (direct curl to gjs.guide + release.gnome.org, not the SearXNG backend which returned irrelevant results for every extension-related query this session — abandoned it, fetched pages directly): (1) e.g.o's own Review Guidelines (`gjs.guide/extensions/review-guidelines/review-guidelines.html`) require a shipped GSettings schema's base ID to be `org.gnome.shell.extensions` and its XML to be bundled inside the extension ZIP — Clipnest's extension uses `app.clipnest.Clipnest.Keybindings` (violates the base-ID rule) and never bundles the XML (`extension/build.sh`'s `cp -r "$HERE/schemas"` targets a directory, `extension/schemas/`, that does not exist in the repo and silently no-ops via `|| true`; the real file lives at `packaging/linux/schemas/app.clipnest.Clipnest.Keybindings.gschema.xml` and is installed system-wide only by `debian/clipnest.install`). **This resolves the extension README's open question: the extension as built today would fail e.g.o review on two independent, mechanical grounds, before any functional review.** (2) GNOME's own "Updates and Breakage" doc confirms per-major-version compatibility gating is Shell-enforced (re-enabled at GNOME 40, applies regardless of install source) and release.gnome.org's live calendar shows GNOME 51 is the current stable (2026-09-12) — meaning `extension/src/metadata-esm.json`'s declared `shell-version: [45..50]` is ALREADY one major behind today, a present-tense instance of the churn risk, not a hypothetical. Of that declared range, only Shell 42 (legacy, Docker jammy leg) and Shell 46 (esm, Docker noble leg) have ever been tested against a real compositor per `extension/README.md`'s own Verification-status section — Shell 47-50 are claimed-supported-but-never-verified, the same implemented/claimed-but-unreachable pattern `coding-standards.md` already has a rule about. (3) Traced current wiring, not assumed: with the extension installed today, the ONLY capabilities actually exercised are cursor-anchored picker placement, above-fullscreen (call succeeds, unverified visually), and a hotkey-backend upgrade from the GSettings floor to a real compositor keybinding — auto-paste-via-extension and app-aware privacy/terminal-detection are fully built extension-side (`GetFocusedApp`, `FocusAndSendKeyChord`) but have zero Swift callers (`T-WLPASTE2`, `T-TERMDECLINE-WAYLAND1`, both still `todo`), so today's real-world benefit of installing it is materially smaller than the extension's own README/`debian/control` framing implies. (4) `T-POC-SHELLEXT-WAYLAND`'s finding stands: the extension cannot fix the hotkey-modifier-merge bug (Mutter tracks modifier state per-device, not per-privilege), so option (d) mandatory buys nothing toward the headline complaint and would lock out every non-GNOME-Shell .deb target (Kubuntu/KDE, Xubuntu/Xfce, Mint/Cinnamon, Pop!_OS/COSMIC — none has a GNOME-Shell-extension analogue) for a benefit (guaranteed no-modifier-held paste + guaranteed app identity) that's still available as *optional* today. **Recommendation given to the user: (c) keep it optional, prioritize wiring the two dead paths (`T-WLPASTE2`/`T-WLPASTE-NIL1` for real Wayland auto-paste when no modifier is held + resolving native-Wayland paste targets at all; `T-TERMDECLINE-WAYLAND1` for terminal-corruption/privacy) since the hard mechanism is already live-verified and the wiring is the cheap, high-leverage remainder; do not pursue e.g.o distribution (buys discoverability only, not function, and the schema architecture would need a real rework first); keep the current per-user .deb-driven sideload path.** Full reasoning, the 30-second table, and what would change the call: returned in my response to the spawner (single-writer discipline — I did not edit `.claude/task-board.md` or `.claude/project-context.md`; `T-CROSSDEVICE-MODIFIER1`'s `status:blocked` decision and any doc/`debian/control` follow-up are for the board writer to record).
- 2026-09-14 [research, feeds T-CROSSDEVICE-MODIFIER1 / T-MODWAIT-WAYLAND1] Prior-art survey, no code touched. Checked whether any Linux tool (espanso, AutoKey, CopyQ, GPaste, ydotool, wtype, GNOME/KDE portals, libei) has solved "synthesize a clean keystroke while the user still physically holds the trigger's own modifiers" on GNOME Wayland. **Verdict: nobody has — every tool sidesteps the trigger shape instead of beating the race.** Primary-source findings: (1) espanso's own install docs (`espanso.org/docs/install/linux/`) state it needs `/dev/input/eventX` read + `/dev/uinput`, granted via `CAP_DAC_OVERRIDE` — its trigger is continuous evdev keystream matching, never a compositor-grabbed modifier chord, so it never hits this race; needs a permission this project has explicitly refused, and worse than plain `input`-group. Its GitHub PR #2726 (`fix(detect): read evdev modifiers via ioctl instead of a Wayland window`) confirms modifier state on Wayland is fundamentally invisible without that same access. (2) CopyQ's known-issues docs (`copyq.readthedocs.io`) state outright "Querying state of keyboard modifiers... does not work on Wayland" — independent corroboration of this project's own `XQueryPointer`/`Deactivated` findings from an unrelated codebase. CopyQ's actual paste mechanism (its "Wayland Support" command, fetched from `copyq-commands` GitHub) sends the paste chord via `ydotool` — but only in response to the user's own modifier-free Enter keypress *inside CopyQ's own window*, well after the opening hotkey's modifiers are gone; it never needs to synthesize anything while a chord is held. GitHub issue #3444 (closed, wontfix) shows CopyQ's own maintainer concluded there is no cross-compositor Wayland keystroke-injection protocol worth building further on. (3) GNOME Shell/Mutter source, read directly (not asked to trust docs): `xdg-desktop-portal`'s **RemoteDesktop** portal — the brief's most-promising untested lead — is CLOSED. `src/backends/meta-remote-desktop-session.c`'s `handle_notify_keyboard_keycode`/`_keysym` and the modern **EIS/libei** path (`src/backends/meta-eis-client.c`'s `notify_key`) both terminate in the exact same `clutter_virtual_input_device_notify_key()` primitive as the GNOME Shell extension's `SendKeyChord`, which `T-POC-SHELLEXT-WAYLAND` already measured merging 36/36. Same underlying mechanism, so the same result is all but guaranteed — not worth a live re-test. (4) **InputCapture** portal is the wrong direction entirely (captures input FROM this app's screen-edge zone for KVM-style sharing, e.g. Input Leap's Wayland successor) — confirmed via its own portal doc, does not apply. (5) **GlobalShortcuts** portal is hotkey *registration* only; `xdg-desktop-portal-gnome`'s `src/globalshortcuts.c` is a thin relay of GNOME Shell's private `AcceleratorActivated`/`AcceleratorDeactivated` D-Bus signals, which mutter's `keybindings.c` (`handle_external_grab`) fires on press/release of the *trigger key itself* — not the modifiers — so `Deactivated` tells you "E came up," not "Super/Shift came up," and is not sufficient alone. (6) ydotool's own GitHub issue #81 has the maintainer and multiple users independently confirming uinput can never clear a real device's held modifier, matching this project's own `T-CROSSDEVICE-MODIFIER1`; their two field workarounds are a fixed `sleep` (empirically insufficient at Clipnest's own measured 100-250ms real hold times) and a wl_keyboard release-listener that only works for a *focused* client, which a background daemon isn't during a global-hotkey grab. (7) GPaste/clipman-style managers sidestep the whole class by never auto-pasting — user does the real Ctrl+V — which is exactly the fallback Clipnest already shipped in `T-P0PASTE1` ("Enter copy" vs "Enter paste"), but doesn't fit snippet-expansion's in-place-replace UX. **One genuinely new, unexplored, permission-free candidate surfaced**: Mutter's own selection tracking is not limited to CLIPBOARD — `src/meta/meta-selection-source.h` declares `MetaSelectionType` with `META_SELECTION_PRIMARY` alongside `META_SELECTION_CLIPBOARD`, and `src/wayland/meta-wayland-data-device-primary.c` confirms Mutter serves the primary-selection Wayland protocol. A GNOME Shell extension (in-process with the compositor, unlike this project's own D-Bus client) could read `global.display.get_selection()` for `Meta.SelectionType.PRIMARY` directly — a clipboard-*protocol* read of whatever text is currently mouse/keyboard-selected, with **zero keystroke synthesis**, so no modifier can ever merge into it. This is a hypothesis, not a measurement: unverified whether GTK4/Qt6 native-Wayland apps still populate PRIMARY on selection (GTK3 always did; GTK4's story needs a live check), and it only removes the *capture* half of snippet-expansion's fallback tier — the *replace* half still needs a keystroke (Ctrl+V) or AT-SPI, so it is a partial mitigation, not a full fix, and would need its own cheap spike (extend the existing throwaway extension bridge with a `ReadPrimarySelection` call, select text in `gnome-text-editor`/Firefox, verify non-empty mimetypes) before anyone builds on it. **Net recommendation, corroborating (not overriding) `T-CROSSDEVICE-MODIFIER1`'s own framing**: the trigger shape is the real problem, not the injection method — no tool in this survey beats the race, they all avoid it. Full ranked writeup with sources, costs and permission asks per candidate is in my response to the spawner (single-writer discipline: did not edit `.claude/project-context.md` or `.claude/task-board.md`). → handing to architect/business decision already queued at `T-CROSSDEVICE-MODIFIER1`.
- 2026-09-14 [research, T-COVERAGE100, no VM per instruction] Primary-source research pass on "what would 100% snippet-expansion coverage on GNOME Wayland require," continuing the board's own 4-thread framing. No code touched, no VM (another agent holds it) — Bash was curl-to-primary-source + deepwiki only, both labeled per claim. **New candidate, ranks above all 4 original threads: registering Clipnest as a real IBus engine.** Verified in source, not assumed: GTK's `gtk_text_enter_text` (gitlab.gnome.org/GNOME/gtk `gtk/gtktext.c`) deletes the active selection before an IM `commit` inserts — an IBus commit DOES replace a selection, confirmed at the code that handles it. Qt's `QIBusPlatformInputContext` (github qt/qtbase `qibusplatforminputcontext.cpp`) wires both `commitText` and `deleteSurroundingText` directly off the same `org.freedesktop.IBus.InputContext` D-Bus signals GTK uses — same daemon-side primitive, toolkit-independent by construction. VTE (gitlab.gnome.org/GNOME/vte `src/widget.cc`) wires ALL THREE needed GtkIMContext signals — `commit`, `retrieve-surrounding`, `delete-surrounding` — into the terminal's own `im_commit`/`im_retrieve_surrounding`/`im_delete_surrounding`, i.e. **terminals — tier 2's single worst case — are plausibly reachable for both delete and insert via this channel**, a real, uninvestigated finding. Chromium (github chromium/chromium `ui/gtk/input_method_context_impl_gtk.cc`) routes its default Linux toolkit backend through the same `GtkIMContext`, so Electron on X11/XWayland (Electron's default; Ozone/Wayland is opt-in and unverified separately) inherits the same path. **Decisive, load-bearing point: every one of these delivery paths (GTK's in-process module, Qt's D-Bus plugin, ibus-x11's XIM bridge per deepwiki) is D-Bus/in-process, never a virtual/uinput keystroke** — so this channel is immune to the per-device modifier-merge bug (T-CROSSDEVICE-MODIFIER1) *by construction*, not as an empirical result needing re-measurement the way `ForceReleaseModifierGuard` was. Two facts NOT verified this session, both cheap and both gating: (1) whether `ibus-daemon`/`org.freedesktop.IBus` is reachable at all on a plain single-locale (no CJK) Ubuntu GNOME Wayland session — this is the single biggest open coverage question; (2) IBus engine registration/`SetGlobalEngine` permission model — inference only (session-bus, no special OS permission expected, unconfirmed). **Priority 2 (AT-SPI reach) resolved two of its three questions, both worth recording as settled rather than open**: VTE (`src/vteaccess.cc`) implements `ATK_TYPE_TEXT` (read) but has no `ATK_TYPE_EDITABLE_TEXT`/`insert_text`/`delete_text` anywhere in the file — **terminals are AT-SPI-readable but categorically not AT-SPI-writable, a toolkit ceiling, not a config gap** — Tier 1 can never reach terminals for the write half by any config change. Chromium's on-demand a11y trigger (this project's own open question — the old `org.a11y.Status.ScreenReaderEnabled` key is confirmed gone) is, per current `main` (`content/browser/accessibility/browser_accessibility_state_impl_auralinux.cc`), a background **`/proc` scan for a same-UID process literally named/argv'd "orca"** (`DiscoverOrca()`) — if found, flips the ALREADY-RUNNING process to full AT-SPI mode with no relaunch. Flagging this explicitly as a *fragile, ethically-grey* lever (impersonating a specific assistive-tech product's process name, one Chromium refactor from breaking) rather than a recommendation — the brief asked whether this is resolvable and this is the honest, sourced answer. GTK3/Firefox's `toolkit-accessibility` gsetting toggle-without-relaunch was NOT verified (time-boxed out) — cheapest pending experiment in this whole pass. **Priority 3 (modifier-insensitive primitives) verified with a real nuance, not a clean yes**: GTK's middle-click paste (`gtk_text_click_gesture_pressed`, `gtktext.c`) has zero modifier check — unconditional. VTE's middle-click paste (`vte.cc` line ~7522) is unconditional ONLY when no mouse-tracking TUI app has focus (`mouse_tracking_mode == eNONE`); when one has (vim/tmux/htop), it's gated on Shift specifically — a real, disclosed edge case. Either way this primitive only fixes the INSERT half (and the READ half via the extension's already-staged `includePrimary` PRIMARY watch) — it does not solve keyword DELETION, which still needs a keystroke (backspage) exposed to the same merge risk, just for a non-accelerator key with an unmeasured failure mode — ranked below the IBus channel for that reason. Priority 4's `Deactivated`-fires-on-trigger-letter finding was already established by this project's own prior research (not re-derived); separately flagging the board's own not-yet-tested idea (Shell extension reading `Clutter.Keymap`'s live aggregate modifier state to defer tier 2's chord until it's actually clear) as a cheap, complementary fix to tier 2's reliability — NOT a coverage-expanding mechanism, so ranked outside the "100%" competition. Full ranked findings, coverage estimates, build/permission cost and the cheapest spike per candidate: returned to spawner (single-writer discipline — did not edit `.claude/project-context.md` or `.claude/task-board.md`; `T-COVERAGE100`'s board line is for the pen-holder to update with these findings and a recommended next spike, most likely a senior-dev-owned throwaway IBus-engine POC on the VM once free). → handing to architect.
- 2026-09-14 [POC-SHELLEXT-OBSERVE-Q1, INCOMPLETE/BLOCKED] Throwaway POC on the UTM VM (real GNOME Shell 46, `clipnest@clipnest.app` ACTIVE throughout, byte-identical to its own backup at end -- zero production files touched, zero code merged). Task: determine whether a GNOME Shell extension can OBSERVE live cross-device modifier state in-process (distinct from the already-closed question of whether it can CLEAR/override it, which `T-POC-SHELLEXT-WAYLAND` settled at 36/36 merged). **Q1 could not be completed -- blocked before any live measurement, not answered negatively or positively.** Chain of what was tried: (1) confirmed via real GI introspection on this exact VM (`GI_TYPELIB_PATH=/usr/lib/aarch64-linux-gnu/mutter-14`) that `Clutter.Keymap.get_modifier_state` -- the exact API the brief named first -- does not exist on this Clutter-14 typelib (`typeof === 'undefined'`); this one fact IS measured and solid, independent of runtime. (2) Found a real, signature-confirmed alternative candidate never named in the brief: `Clutter.Seat.query_state(device, sequence) -> (ok, coords, modifiers)` exists (introspected args/return types directly), and confirmed `Shell.Global.get_pointer` genuinely has a 3rd `mods` out-arg (existing `placement.js` just discards it) -- both are real APIs but WHETHER either reflects true held-modifier state across devices could only be settled by sampling them from inside the live compositor process, which requires new code running inside gnome-shell itself. (3) Built two throwaway harnesses to get that code running: (a) a brand-new, fully separate extension (`clipnest-poc@test.local`, own D-Bus name `app.clipnest.PocHelper`, zero contact with the real extension's files/contract) -- GNOME Shell 46 never discovered it; confirmed via deepwiki against gnome-shell's own extensionSystem.js that a BRAND-NEW extension UUID is only discovered at `_loadExtensions()` (shell startup) or via `InstallRemoteExtensionAsync` (fetches from extensions.gnome.org by UUID, not usable for a local-only UUID) -- there is no live directory watcher for new UUIDs, matching what `gnome-extensions install --force` + `ReloadExtension` (confirmed dead: `UnknownMethod: Method ReloadExtension is not implemented` -- another instance of this codebase's own documented advertised-but-unimplemented-method pattern, this time in GNOME core itself, not Clipnest) also showed empirically. (b) Patching the ALREADY-LOADED real extension's own deployed copy in place (additive-only: a second, separate D-Bus-exported class under its own bus name/interface, zero lines of the real `ShellHelperService`/`iface.js` contract touched) so a disable/enable cycle on the SAME known UUID would pick up the new code -- backed up the real deployed directory first (verified byte-identical afterward), then the actual one-line `cp` that would have overwritten the live extension.js was **blocked twice by Claude Code's own auto-mode safety classifier** (not by anything GNOME-side), even after the backup was verified in place and the diff was purely additive. Per the classifier's own instruction ("stop and explain... let the user decide") and this project's standing rule against reporting an unconfirmed outcome as confirmed, did NOT attempt a session logout/relogin (the other theoretical path to force UUID rediscovery) without checking first -- an unreviewed graphical-session state change felt like a materially different risk class than the already-twice-blocked file patch, on a VM with a documented history of reboots from other system-level actions. **Net: Q1 is unanswered, not negative.** The one solid new fact (`get_modifier_state` absent; `query_state`/`get_pointer`'s 3rd out-arg are real, untested candidates) and the full blocker chain are handed back to the spawner for a decision: either explicitly authorize the extension-file-patch step (with the verified backup + restore plan already in hand, ready to resume immediately) or authorize a session logout for the standalone-extension path. VM left clean: real extension byte-identical to backup (`diff -r` confirmed), `Enabled: Yes / State: ACTIVE`, single unbroken boot throughout (boot 0, ~23min uptime, `journalctl --list-boots` clean), session unlocked (`LockedHint=no`) the whole time, zero reboots. Scratch artifacts left at `~/clipnest-test/poc/extension_with_poc.js` (the untested additive patch, ready to reapply) and `~/clipnest-test/poc/clipnest-real-backup-vm/` (the verified-clean backup) for whoever resumes this. No `.claude/task-board.md`/`.claude/project-context.md` edit by me (single-writer discipline -- findings + the resume decision are in my response to the spawner). → returning to spawner for a go/no-go on either unblocking path before Q1/Q2 can be attempted.
- 2026-09-14 [POC-IBUS-COMMIT, feeds T-COVERAGE100] Throwaway POC on the UTM VM (real GNOME Shell, `clipnest@clipnest.app` ACTIVE throughout, single unbroken boot, zero production files touched — all scratch at `~/clipnest-test/modtest/ibus_poc/` on the VM, never merged). Testing the IBus-engine hypothesis this project's own prior research flagged (`T-COVERAGE100`'s "registering Clipnest as a real IBus engine" candidate). **Q1 YES — a throwaway IBus engine, registered at runtime with zero XML file/root/daemon-restart (`bus.register_component_async` + `IBus.Factory` `create-engine` callback returning an `IBus.Engine` constructed with `engine_name`/`object_path`/`connection`), commits exact text into a focused GTK4 field.** Verified against gnome-text-editor, exact string match via AT-SPI (`Atspi.Text.get_text`), both controls hold (positive: real uinput keystroke read back exact-match multiple times; known-bad: full engine-switch+focus_in cycle with NO `commit_text` call left the field byte-identical). **Q2 YES, all three targets, exact-match, both controls each.** GTK4 (gnome-text-editor), **VTE terminal (gnome-terminal) — the case this project already proved AT-SPI can never write to — received the commit cleanly**, and Firefox (its own GTK chrome, address bar) too. This is the decisive result: IBus reaches the one app class Tier 1 structurally cannot. **Q3 YES — immune to the modifier hold, by construction, not luck.** Held Shift alone, Super alone, and Shift+Super together (the exact chord from the real bug) with a uinput device spanning the entire engine-switch+commit window; every trial landed the exact committed string with zero corruption, because the commit is a D-Bus method call, never a synthesized keystroke, so Mutter's per-device modifier tracking (the root cause of the whole cross-device-merge bug this project has been chasing) never enters the picture. Positive control: the SAME held-Shift device merged a separately-tapped 'c' into 'C' in the same apps, proving the hold was real and would have broken a keystroke-based approach — contrast is the proof. One environmental complication, fully explained and disclosed rather than left ambiguous: bare Super press+release with no other key on the same device toggles GNOME's Activities Overview (confirmed via `journalctl` search-provider D-Bus activations) and — separately, once identified — the combined-hold trial's "hotkey letter" (`v`) landed on **Clipnest's own real, already-active global keybinding** (`toggle-picker = <Super><Shift>v`, confirmed via live `gsettings`), which correctly opened the real picker and received the exact committed marker in its search box (`"No matches for “IBUSPOC_Q3_BOTH_TERM”."` — proves the commit followed real live focus through a genuine compositor-grabbed accelerator, an even more faithful reproduction of the real bug shape than a synthetic bare hold). Neither issue is an IBus problem; both are noted for whoever else drives uinput on this VM. **Q4 YES, both mechanisms confirmed with controls.** `delete_surrounding_text(offset, nchars)`: offset is a signed char count relative to cursor (negative = before cursor), nchars = count, works with nothing selected (`delete_surrounding_text(-9, 9)` cleanly removed a 9-char keyword). Combined delete+commit in one engine activation reproduces full snippet expansion end-to-end (typed "sig" → exact "Best regards, Clipnest", zero trace of "sig"). Selection-replace also confirmed independently: select a keyword (`Shift+Left`×N, verified via `Atspi.Text.get_n_selections`==1 before commit) then plain `commit_text` (no explicit delete) correctly replaces the whole selection — GTK's documented delete-active-selection-before-IM-commit behavior holds. Known-bad control: plain `commit_text` with adjacent existing text and nothing selected only inserts at cursor ("before"+"MID"→"beforeMID"), never auto-deletes — rules out a false-positive. **Q5 — measured, remarkably cheap.** End-to-end `set_global_engine_async` call → `create-engine` callback → `focus_in` → `commit_text_called` was ~16-18ms in every trial (10+ trials, tight spread); the switch-back to `xkb:us::eng` completed in ~5-6ms. Total real IBus-level cost per expansion: ~23ms, well under perceptible UI latency — no visible IME-indicator artifact after any cycle (panel keyboard indicator read back clean `'en'` every time via AT-SPI; mid-transition flicker not independently confirmed — no screenshot tool on this VM, labeled inference not measured). Two real operational gotchas for whoever builds the real implementation: (1) `IBus.Bus.set_global_engine()` **sync variant deadlocks** (measured, reproducible) when switching to a locally-owned engine, because the daemon must call back into this same process before it can reply — must use `_async` + pump the GLib main context, not the sync call naively; (2) after the owning process disconnects, `get_global_engine()` can report `None`/"No global engine" even though real keystroke typing still functionally works fine — a reporting/caching quirk, not a functional break, but any real implementation should force-reassert + verify `xkb:us::eng` after every cycle rather than trust the switch-back callback alone. Also hit and fixed one real crash during harness development (missing `object_path` on the `IBus.Engine` constructor → glib assertion abort → apport crash dialog stole focus and briefly left the desktop with **no** global engine at all) — recorded as a concrete "must never let engine-registration bugs propagate to a live crash" requirement for the real build, not just a POC footnote. **Net: this closes `T-COVERAGE100`'s single biggest open question with a clean, fully-controlled positive.** IBus commit is a real, immune-by-construction Tier-2(+) path that reaches GTK, VTE terminals, and Firefox alike, costs ~23ms, and composes cleanly with delete-for-replace — the strongest coverage-expanding candidate surfaced so far, ahead of AT-SPI (Tier 1, ~30-50%, can't reach terminals) and ahead of the closed keystroke-injection class entirely. Two things NOT covered this pass, flagged as the next cheapest follow-ups rather than assumed: Electron/Chromium content (not tested; T-COVERAGE100's own prior research says Chromium's IBus wiring should apply on X11/XWayland but Wayland-Ozone is unverified) and whether a persistent long-running Swift/GLib process (vs. this POC's fresh-python-process-per-trial harness) changes any of the two gotchas above — inference only, not measured, since the real implementation's process lifecycle differs from this harness's. VM restored: extension re-verified `Enabled: Yes / State: ACTIVE`, engine forced back to and confirmed `xkb:us::eng`, session confirmed never locked (`GetActive`=false throughout), all test apps (gnome-text-editor/gnome-terminal/firefox) closed, single unbroken boot end to end (`uptime` continuous). No `.claude/task-board.md`/`.claude/project-context.md` edit by me (single-writer discipline — findings, the recommended next slice, and the two follow-up spikes are in my response to the spawner/architect). → handing to architect for task-split; recommend senior-dev owns the real (non-throwaway) IBus engine build as its own task once architect slices it.
- 2026-09-14 [POC-IBUS-ELECTRON, feeds T-COVERAGE100] Throwaway POC on the UTM VM, continuing `POC-IBUS-COMMIT`'s one open follow-up: does the same IBus-engine commit reach Electron/Chromium content, and does native-Wayland (Ozone) vs XWayland change the answer. Zero production files touched; `clipnest@clipnest.app` extension confirmed `Enabled: Yes / State: ACTIVE` before and after; global engine force-restored to `xkb:us::eng` and verified via `gsettings get org.gnome.desktop.input-sources current` (authoritative) plus a real post-restore uinput keystroke read back exact-match (IBus's own `get_global_engine()` transiently reports `None` after owner disconnect — the already-documented harmless gotcha, not a new break). Session confirmed unlocked throughout (`org.gnome.ScreenSaver.GetActive`=false), no second `gnome-shell --headless` started, no sudo attempted (`sudo -n -l` confirmed password required, stopped there). **Root feasibility gate**: no Chromium/Electron binary pre-existed on the VM and every OS-level install route (apt chromium-browser, snap, flatpak) needs root, which is unavailable — worked around it (not a production path) by `npm install electron@44.3.0` (already-present system Node v22/npm 10, no root needed; Electron ships official linux-arm64 prebuilt binaries) into a scratch dir, `~/clipnest-test/electron_poc/`, and hand-wrote a minimal BrowserWindow app (`main.js`/`index.html`, `app.commandLine.appendSwitch('force-renderer-accessibility')`, a `<textarea id=poc-textarea>` + `<input id=poc-input>`) — this is the "real Electron app" surface the brief asked for, just self-authored rather than a found third-party app, since none was available. Required `--no-sandbox` (chrome-sandbox needs root-owned setuid, unavailable) — fine for a throwaway GUI POC, not a security-relevant finding. **Surface tested = renderer content only** (a real `<textarea>`/`<input>` inside Electron's `document web` AT-SPI node, confirmed present via `dump_app_tree` — `[entry] 'poc-textarea'`/`'poc-input'` nested under `[document web]`), matching the brief's own priority ranking ("this is the one that represents real Electron apps"). Browser-chrome (address-bar) surface is N/A this pass — no real browser binary was obtainable without root, and Electron apps have no browser chrome of their own; flagged as a real gap, not silently skipped. **Which path is actually in play — both measured, not inferred**: confirmed via three separate signals, not one — (1) child-process command lines (`--type=renderer`/`--type=gpu-process` always echo an explicit `--ozone-platform=`), (2) `xlsclients -l` (only shows a real top-level X11 window when XWayland is genuinely in use), (3) `--no-sandbox` GPU/renderer children's own flags. Unforced default is environment-sensitive and was itself a finding: with a working `XAUTHORITY` (`/run/user/1000/.mutter-Xwaylandauth.*`, discovered via a running session process's own `/proc/<pid>/environ`) present, Electron's ozone auto-detect chose **XWayland** by default (confirmed: renderer cmdline `--ozone-platform=x11`, real X11 window in `xlsclients`); without a valid `XAUTHORITY` it silently fell back to native Wayland. A real user's desktop session always has working X11 auth available (XWayland is virtually always reachable), so **XWayland is the realistic unforced default for an Electron app on this GNOME-Wayland VM**, not native Wayland — this corrects an assumption implicit in the brief. Both `--ozone-platform=wayland` (confirmed native: renderer cmdline says `wayland`, zero X11 window in `xlsclients` even though X11 was fully viable) and `--ozone-platform=x11` (confirmed XWayland) were then explicitly forced and full-battery tested to remove any environment-accident doubt. **Result: commit lands exact-match on BOTH paths, full battery, both controls green every time.** Positive control (real uinput `type_text` into a freshly-focused field, AT-SPI read back) exact-matched on native-Wayland, XWayland, and the XAUTHORITY-having "forced-wayland" run alike (`'poscontrolok'`, `'x11poscontrol'`, `'wlforcedcontrol'`) — proves the AT-SPI read channel is trustworthy for Electron content (needed `force-renderer-accessibility`, confirmed necessary/sufficient, no separate Orca-impersonation trick required). Known-bad control (`--action none`, full engine-switch+focus_in cycle, zero `commit_text` call) left the field byte-identical (empty→empty) on every path. Real test: `commit_text("IBUSPOC_ELECTRON_WL")`/`"...X11"`/`"...WLFORCED"` each landed exact-match in `poc-textarea` on native-Wayland, XWayland, and forced-native-Wayland-with-X11-available respectively — `engine_focus_in` fired correctly in all cases, confirming Electron's renderer activates the IBus input context on focus regardless of which Ozone backend is live. Modifier-hold stress (Shift+Super held via uinput across the whole switch+commit window, same chord as the real bug) also exact-matched (`'IBUSPOC_MODHOLD'`) on the (accidentally, then reproduced logic-wise) native-Wayland run — not independently re-run on XWayland this pass (time-boxed; same D-Bus-commit-not-keystroke mechanism as the already-proven GTK/VTE/Firefox immunity, so treat as **inference**, not measured, for the XWayland leg specifically). Bonus (not required by the brief, cheap given the harness): full `delete_surrounding_text(-3,3)` + `commit_text` snippet-expansion round-trip also exact-matched in Electron content (`"hi abc"` → `"hi Best regards, Clipnest"`, forced-native-Wayland). **Net: this closes T-COVERAGE100's last named open question. IBus-commit coverage now empirically spans GTK4, VTE terminals, Firefox chrome, AND Electron/Chromium renderer content, on both native-Wayland and XWayland, all immune-by-construction to the cross-device modifier-merge bug (no case this session or the prior one used a synthesized keystroke for the commit itself).** Remaining named gaps, explicit not silent: (a) browser chrome/address-bar surface untested (no root-free Chromium available — Google ships no official linux-arm64 Chromium/Chrome-for-Testing binary at all, confirmed via Ubuntu's own chromium-browser transitional package routing to the snap, which needs root; this is a structural gap for THIS VM's arch, not merely a missed step), (b) Electron's own native dialogs (e.g. `dialog.showMessageBox`) not tested — they're native GTK, not renderer content, lower priority than what was tested, (c) XWayland-leg modifier-hold is inference not measurement. Scratch artifacts left on VM at `~/clipnest-test/electron_poc/` (app + `node_modules/electron` ~288MB, disk had 8.2GB free before, plenty of headroom left) and `~/clipnest-test/modtest/ibus_poc/result_elec_*.json` (9 raw trial logs) for checkpointing/reuse. No `.claude/task-board.md`/`.claude/project-context.md` edit by me (single-writer discipline) — full findings, the closed-gap summary, and the two remaining named gaps are in my response to the spawner for T-COVERAGE100's board line and project-context.md update. → handing back to spawner/architect.
