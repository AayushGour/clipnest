- 2026-08-10 [T42] added convert-to-snippet as a third always-visible row button in ItemRow.swift (pin, convert, delete order), reusing existing onSaveAsSnippet closure + supportsSaveAsSnippet gating and the context menu's "text.badge.plus" icon; swift test 128/14 unchanged, xcodebuild BUILD SUCCEEDED, swift format lint clean → senior-dev
- 2026-08-10 [Paster image/file paste] extended PasteContent with .image(Data)/.file(URL), added PasteboardWriting.writeData(_:forType:) + NSPasteboard conformance, Paster.paste now normalizes image bytes to TIFF via NSImage decode (throws PasteError.invalidImageData on bad bytes, never force-unwraps), .file writes .fileURL string; added 3 PasterTests cases (image success, invalid image throws before any write, file success) reusing ImageFixtures; swift test 131/14 passing, swift format lint clean → senior-dev
- 2026-08-10 [⌘⌫ delete fix] PickerPanel now installs a local NSEvent keyDown monitor (deleteKeyCode 51 + Cmd modifier) that swallows the event and fires new `onCommandDelete` closure before the search field's field editor can consume it, removed in deinit (token marked nonisolated(unsafe), mirroring BlobStore.fileManager's pattern, to satisfy Swift 6 Sendable check in nonisolated deinit); AppEnvironment wires panel.onCommandDelete = { viewModel?.deleteHighlighted() } next to onWillShow/onDidHide. Did not touch PickerView.swift or PickerViewModel.swift. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean; no Core unit test possible (AppKit event-monitor plumbing, real key events forbidden in ClipnestCoreTests per coding-standards.md) — runtime-only verification needed → senior-dev
- 2026-08-10 [rich per-kind row previews] ItemRow leading slot is now kind-specific: new ItemIconThumbnail (private, async .task(id:)-loaded, off-main-thread via Task.detached) renders .image thumbnails via BlobStore.read(blobPath:)+NSImage and .file icons via NSWorkspace.shared.icon(forFile:), backed by new shared ItemThumbnailCache (NSCache keyed by blobPath/fileReference); .text/.richText/.link icons unchanged; .link title line now gets .foregroundStyle(.accentColor)+.underline(true) via an outer modifier on HighlightedText (unedited) since only its highlighted-match ranges hardcode .primary. Added `blobStore: BlobStore` to PickerViewModel (new init param, real-default) and threaded it AppEnvironment's one shared BlobStore instance -> PickerViewModel -> PickerView's ItemRow(...) call site -> ItemRow. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (real thumbnail render + graceful fallback on missing blob/file) need human/manual verification, not exercised by swift test.
- 2026-08-10 [type filter chips] added new TypeFilterChips.swift (compact 5-chip row: All/Text/Image/File/Link, nil-first `options` constant, mirrors TabSwitcher's pill style at smaller caption2/tighter padding, exhaustive `title(for:)` with unreachable-.richText comment) and wired it into PickerView.swift as a new `typeFilterChips` @ViewBuilder (hidden on .snippets tab) inserted between searchField and the tabBar Divider, bound directly to `$viewModel.query.kindFilter` — no PickerViewModel.swift changes needed since `didSet{selectFirstResult()}` on `query` already covers kind-filter changes too. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (chips visibly filter by kind; selection persists across tab switches, resets on reopen) need human/manual verification, not exercised by swift test.
- 2026-08-10 [select-to-paste image/file] PickerViewModel.swift: generalized `pasteAndDismiss(_ text: String)` → `pasteAndDismiss(_ content: PasteContent)`, added new `private func pasteContent(for item: ClipItem) -> PasteContent?` mapping .text/.link→.text(previewText), .image→.image(bytes via blobStore.read(blobPath:), nil if blobPath missing or read fails), .file→.file(URL from fileReference, nil if missing/unparseable), .richText→nil; `select(_:)` now calls `pasteContent(for:)` instead of hardcoding the .text/.link guard; `pasteSnippet(_:)` now calls `pasteAndDismiss(.text(snippet.body))`. Did not touch presentSaveAsSnippetForm(from:)/saveHighlightedAsSnippet() (unrelated .text/.link-only restriction, confirmed by reading, left as-is). xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (selecting an image row pastes the actual image e.g. into Preview/Mail, selecting a file row pastes the actual file e.g. into Finder) need human/manual verification, not exercised by swift test.
- 2026-08-11 [T50] added SnippetStore.query(text:offset:limit:) + InMemorySnippetStore/SwiftDataSnippetStore impls (normalizedText field kept in sync in update()), 17 new mirrored tests, all 168 tests green, lint clean, → senior-dev
- 2026-08-11 [T52] PickerView.swift updated for windowed PickerViewModel API: viewModel.items→rows, viewModel.snippets→snippetRows (both call sites), added ScrollResettingList.onReachEnd closure wired to .onAppear on the last ForEach row → viewModel.loadMoreIfNeeded() at both list/snippetsList call sites, reworded stale typeFilterChips doc comment (removed dead SnippetSearchFilter/PickerViewModel.snippets references). xcodegen generate OK, xcodebuild BUILD SUCCEEDED, swift test 160/12 passing, swift format lint --strict clean on PickerView.swift (2 pre-existing errors in PickerViewModel.swift, out of scope, not introduced by this change) → senior-dev
