- 2026-08-10 [T42] added convert-to-snippet as a third always-visible row button in ItemRow.swift (pin, convert, delete order), reusing existing onSaveAsSnippet closure + supportsSaveAsSnippet gating and the context menu's "text.badge.plus" icon; swift test 128/14 unchanged, xcodebuild BUILD SUCCEEDED, swift format lint clean → senior-dev
- 2026-08-10 [Paster image/file paste] extended PasteContent with .image(Data)/.file(URL), added PasteboardWriting.writeData(_:forType:) + NSPasteboard conformance, Paster.paste now normalizes image bytes to TIFF via NSImage decode (throws PasteError.invalidImageData on bad bytes, never force-unwraps), .file writes .fileURL string; added 3 PasterTests cases (image success, invalid image throws before any write, file success) reusing ImageFixtures; swift test 131/14 passing, swift format lint clean → senior-dev
- 2026-08-10 [⌘⌫ delete fix] PickerPanel now installs a local NSEvent keyDown monitor (deleteKeyCode 51 + Cmd modifier) that swallows the event and fires new `onCommandDelete` closure before the search field's field editor can consume it, removed in deinit (token marked nonisolated(unsafe), mirroring BlobStore.fileManager's pattern, to satisfy Swift 6 Sendable check in nonisolated deinit); AppEnvironment wires panel.onCommandDelete = { viewModel?.deleteHighlighted() } next to onWillShow/onDidHide. Did not touch PickerView.swift or PickerViewModel.swift. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean; no Core unit test possible (AppKit event-monitor plumbing, real key events forbidden in ClipnestCoreTests per coding-standards.md) — runtime-only verification needed → senior-dev
- 2026-08-10 [rich per-kind row previews] ItemRow leading slot is now kind-specific: new ItemIconThumbnail (private, async .task(id:)-loaded, off-main-thread via Task.detached) renders .image thumbnails via BlobStore.read(blobPath:)+NSImage and .file icons via NSWorkspace.shared.icon(forFile:), backed by new shared ItemThumbnailCache (NSCache keyed by blobPath/fileReference); .text/.richText/.link icons unchanged; .link title line now gets .foregroundStyle(.accentColor)+.underline(true) via an outer modifier on HighlightedText (unedited) since only its highlighted-match ranges hardcode .primary. Added `blobStore: BlobStore` to PickerViewModel (new init param, real-default) and threaded it AppEnvironment's one shared BlobStore instance -> PickerViewModel -> PickerView's ItemRow(...) call site -> ItemRow. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (real thumbnail render + graceful fallback on missing blob/file) need human/manual verification, not exercised by swift test.
- 2026-08-10 [type filter chips] added new TypeFilterChips.swift (compact 5-chip row: All/Text/Image/File/Link, nil-first `options` constant, mirrors TabSwitcher's pill style at smaller caption2/tighter padding, exhaustive `title(for:)` with unreachable-.richText comment) and wired it into PickerView.swift as a new `typeFilterChips` @ViewBuilder (hidden on .snippets tab) inserted between searchField and the tabBar Divider, bound directly to `$viewModel.query.kindFilter` — no PickerViewModel.swift changes needed since `didSet{selectFirstResult()}` on `query` already covers kind-filter changes too. xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (chips visibly filter by kind; selection persists across tab switches, resets on reopen) need human/manual verification, not exercised by swift test.
- 2026-08-10 [select-to-paste image/file] PickerViewModel.swift: generalized `pasteAndDismiss(_ text: String)` → `pasteAndDismiss(_ content: PasteContent)`, added new `private func pasteContent(for item: ClipItem) -> PasteContent?` mapping .text/.link→.text(previewText), .image→.image(bytes via blobStore.read(blobPath:), nil if blobPath missing or read fails), .file→.file(URL from fileReference, nil if missing/unparseable), .richText→nil; `select(_:)` now calls `pasteContent(for:)` instead of hardcoding the .text/.link guard; `pasteSnippet(_:)` now calls `pasteAndDismiss(.text(snippet.body))`. Did not touch presentSaveAsSnippetForm(from:)/saveHighlightedAsSnippet() (unrelated .text/.link-only restriction, confirmed by reading, left as-is). xcodebuild BUILD SUCCEEDED, swift format lint --strict clean, swift test 131/14 passing (untouched Core, safety check only) → senior-dev. Runtime-only checks (selecting an image row pastes the actual image e.g. into Preview/Mail, selecting a file row pastes the actual file e.g. into Finder) need human/manual verification, not exercised by swift test.
- 2026-08-11 [T50] added SnippetStore.query(text:offset:limit:) + InMemorySnippetStore/SwiftDataSnippetStore impls (normalizedText field kept in sync in update()), 17 new mirrored tests, all 168 tests green, lint clean, → senior-dev
- 2026-08-11 [T52] PickerView.swift updated for windowed PickerViewModel API: viewModel.items→rows, viewModel.snippets→snippetRows (both call sites), added ScrollResettingList.onReachEnd closure wired to .onAppear on the last ForEach row → viewModel.loadMoreIfNeeded() at both list/snippetsList call sites, reworded stale typeFilterChips doc comment (removed dead SnippetSearchFilter/PickerViewModel.snippets references). xcodegen generate OK, xcodebuild BUILD SUCCEEDED, swift test 160/12 passing, swift format lint --strict clean on PickerView.swift (2 pre-existing errors in PickerViewModel.swift, out of scope, not introduced by this change) → senior-dev
- 2026-09-04 [T-SET2] Made picker footer's ⌥⏎ hint contextual ("⌥⏎ OCR text" only when the highlighted row has recognized text, else "⌥⏎ plain") instead of the always-spelled-out "⌥⏎ plain/OCR text" that truncated `esc close`; extracted the shared `kind == .image && !(ocrText ?? "").isEmpty` predicate to `ClipItem.hasRecognizedText` (Sources/ClipnestCore/Model/ClipItemOCR.swift) so ItemRow.hasRecognizedText and the new logic share one definition (DRY), added `PickerViewModel.highlightedItemHasRecognizedText: Bool` (minimal exposed surface, `highlightedItem` itself stays private), and extracted the pure string builder to ShortcutHints.text(for:hasRecognizedText:) (ClipnestApp/Sources/UI/Picker/ShortcutHints.swift) so it's unit-testable, mirroring WindowPlacement.swift's pattern. swift test 224/16 passing (baseline 220 + 4 new ClipItemOCRTests), xcodebuild 122/14 passing (9 ShortcutHintsTests + 4 PickerViewModel tests added), swift format lint --strict clean on all touched files → senior-dev. Flagged to senior: measured real SwiftUI-layout string widths against the panel's actual source-verified width (PickerPanel.defaultSize = 560pt, not the 452pt cited) — the contextual fix comfortably fits the default History/Pinned no-OCR case (490pt vs 503pt avail) but still overflows when an OCR row is highlighted (510pt) or on the Snippets tab regardless of OCR state (519-540pt vs 503pt avail); Snippets tab's baseline group was already over budget before this task. Did not implement further shortening — out of this task's scope, said so rather than silently shipping.
- 2026-09-04 [T-SET2 round 2] Confirmed panel width is 560pt (senior verified PickerPanel.defaultSize, their 452pt figure traced to a mid-fade CGWindowListCopyWindowInfo entry misread); dropped `esc close` from ShortcutHints.text(...) as the next lever per senior's direction — re-measured all 4 tab/OCR states via NSHostingView(.caption2).fittingSize against the real 493pt(with update-dot)/503pt(no dot) budget: History/Pinned no-OCR 435pt(+58/+68 margin), History/Pinned OCR 455pt(+38/+48), Snippets no-OCR 464pt(+29/+39), Snippets OCR 484pt(+9/+19, worst case) — every state fits with positive margin, so the `⌥⌘E replace→expand` fallback wasn't needed and wasn't applied. Updated ShortcutHintsTests (dropped `· esc close` from exact-match strings, added neverContainsEscClose regression test) and the doc comments in ShortcutHints.swift/PickerView.swift with the full measured budget so nobody re-adds it blind. swift test 224/16 green (unchanged), xcodebuild 123/14 green (only ShortcutHints (10) + highlightedItemHasRecognizedText (4) explicitly re-verified: 14/14), lint clean on all touched files (pre-existing HotkeyManagerTests.swift:25 LineLength ignored per brief) → senior-dev
- 2026-09-04 [T-SET4] Fixed ⌘S popping save-as-snippet for a highlighted `.image` row: promoted `supportsSaveAsSnippet` (`kind == .text || .link`) out of ItemRow into `ClipItem` (Sources/ClipnestCore/Model/ClipItemOCR.swift, alongside `hasRecognizedText`); `presentSaveAsSnippetForm(from:)` now guards on it, so `saveHighlightedAsSnippet()` (which calls that) is a true no-op for non-supporting kinds. Re-audited every footer hint against the real per-kind/per-tab code and found two corrections to the brief's hypothesis: (1) `.text`/`.link` should ALSO omit ⌥⏎ — `pasteContent(for:plainText:)`'s plainText branch returns the exact same `.text(previewText)` those kinds already paste via ⏎ (confirmed by the existing `PickerViewModelPasteContentTests` "ignores plainText — no richer form to strip" cases), same "advertising it is a lie" reasoning the brief already applied to `.file`/OCR-less-`.image`; ⌥⏎ now genuinely differs from ⏎ ONLY for `.richText` and OCR-bearing `.image`. (2) confirmed `⌘S`/`⌘P` are History/Pinned-only and `⌥⌘E replace` is actually a global system hotkey (HotkeyManager/Carbon, works everywhere, not gated by tab) — showing it only on the Snippets-tab hint group is a reasonable contextual choice, not a bug, left as-is. Replaced `PickerViewModel.highlightedItemHasRecognizedText: Bool` with `highlightedItemCapabilities: HighlightedItemCapabilities` (new value type in ShortcutHints.swift: `altEnterHint: .plain/.ocrText/nil` + `supportsSaveAsSnippet: Bool`, nil-item on Snippets tab or nothing highlighted) and widened `ShortcutHints.text(for:capabilities:)` accordingly — still pure/testable. Re-measured widths (NSHostingView(.caption2).fittingSize): worst case is now Snippets-tab at 408pt vs 493pt avail (+85pt margin, was +9pt) — every hint change only ever removes text so margin only grew. 29 new/changed tests (HighlightedItemCapabilities per-kind matrix, ShortcutHints tab×capability matrix incl. exact-string + ⌘S-absence checks, PickerViewModel capability + saveHighlightedAsSnippet no-op/positive-case tests) all pass; swift test 224/16 green (unchanged baseline); xcodebuild 138/16 green (0 failures); swift format lint --strict clean on Sources/Tests/ClipnestApp/Sources + ClipnestApp/Tests (only pre-existing HotkeyManagerTests.swift:25 LineLength remains, per brief) → senior-dev. Did not touch ClipnestApp.swift/SettingsActivator.swift (concurrent agent's files).
- 2026-09-04 [T-REV1] Replaced VisionTextRecognizer's off-main NSImage decode with ImageIO (CGImageSourceCreateWithData + CGImageSourceCreateImageAtIndex(source, 0, nil)) inside a new private static decodedCGImage(from:), matching Paster.normalizedToTIFF/PasteboardReader.imagePixelDimensions's precedent and API choice exactly (frame index 0 for multi-rep/animated sources); dropped the now-unused `import AppKit`, added `import ImageIO`. No concurrency/policy change — same recognitionQueue dispatch, same maxDownscaledDimension/maxByteSize/maxPixelDimension constants, same two early-return-to-nil continuation paths preserved. Doc comment on decodedCGImage records the ImageIO-over-NSImage rationale (now stated in all 3 places). Verified byte-identical recognized output before/after against the real fixture blob f8ec2a4c907d6d1756a4ce4b299298fcb1abc2a5ee46939aeb70b51ab3d51816 (read-only copy in scratchpad, never wrote to the real blob store/db) via a disposable test (deleted before handoff): SHA-256 bef22c51dca60f7a96d4467bbf9a77002208fde821f757ed1cf15b40f8d15f67, length 1368, both runs identical. swift test: 239/17 suites green. xcodebuild app tests: 144/18 suites, TEST SUCCEEDED. swift format lint --recursive --strict Sources Tests ClipnestApp/Sources: clean (exit 0, no output). → senior-dev
