- 2026-08-06 [T1] Clipnest design intake: wrote coding-standards.md (Swift 6/SwiftUI, macOS 14+, swift-format, Swift Testing, typed-throws, ClipnestCore/ClipnestApp layout, KeyboardShortcuts-only dependency policy, privacy musts), project-context.md (goal/users/10 user stories w/ AC/constraints/assumptions/out-of-scope/architecture/team/6 decisions), plan (docs/superpowers/plans/2026-08-06-clipnest-plan.md, 38 tasks T2–T39 across scaffold + 5 phases w/ exact project.yml+Package.swift+entitlements for T2), and task-board.md (size:L, all dev tasks status:todo, T1/T1a/T1b marked done — ux-designer/product-engineer skipped, no gap). → dev mode, T2 ready to start.
- 2026-08-06 [D6/T6] senior-dev escalation: SwiftData @Model macro unbuildable under Command Line Tools (SwiftDataMacros plugin ships only in Xcode.app; one-unit ClipnestCore build → blocks all swift test). Ruled Option B+ (refined): ClipItem/Snippet become Sendable value types, SwiftData confined behind ClipStore/SnippetStore protocols, interim concrete = InMemoryClipStore/InMemorySnippetStore (pure in-memory, permanent test store); real SwiftDataClipStore deferred to new T40 (blocked on Xcode, additive Store-layer-only swap). Unblocks T3+T6+T7 this session. Renamed SwiftDataClipStore→InMemoryClipStore in board T6/T22; updated coding-standards Persistence+Naming+layout. Handed exact D6 text to senior-dev to write into project-context.md (single-writer). standards:refreshed.
- 2026-09-03 [T-DOC1] README.md refresh against shipped code (no other files touched). Verified via code brain + direct reads: Settings window (5 tabs, ClipnestApp/Sources/UI/Settings/*.swift) is fully shipped — gave it a headline Features bullet + a new "## Settings" section. Corrected: Roadmap — checked off Settings window + configurable retention (both fully wired: HistorySettingsView -> SettingsStore.retentionCap -> ClipStore.enforceRetention; AppsSettingsView -> SettingsStore.userExcludedBundleIDs -> PrivacyFilter; ShortcutsSettingsView rebinds both KeyboardShortcuts.Name hotkeys); replaced the old "Notarized .dmg + auto-updates (Sparkle)" line with two honest unchecked items (in-app auto-update still manual-click-through-Terminal; notarized/Developer-ID .dmg still opt-in, not default) plus a new checked item for the shipped background update-availability check (UpdateChecker.swift:1-33, 24h curl poll vs GitHub Releases API, never downloads/installs — confirmed no Sparkle/auto-download anywhere). Privacy section: removed the false "no network calls at all" claim, replaced with an honest carve-out for the one daily curl call (UpdateChecker.swift:257-275) — confirmed zero `URLSession` in the repo (grep), everything else is genuinely local. Release section: rewritten — release.yml (.github/workflows/release.yml) only signs with a Developer ID if secrets are present, else ships fully unsigned; scripts/release-cert.sh mints a one-time stable self-signed cert so Accessibility grants survive updates; scripts/release-local.sh (new since last README pass, uses that cert, deliberately skips notarization) is the maintainer's local alternative to CI. install.sh/update.sh use curl (never sets quarantine) + a published .dmg.sha256 checksum instead of relying on Gatekeeper/notarization. Build-from-source: replaced the stale "re-grant Accessibility after every rebuild" caveat with the actual fix that now exists (scripts/dev-cert.sh + scripts/dev-install.sh, a stable local dev signing identity). Project-structure tree: scripts/ line and UI/ subtree updated to list all 10 scripts and the new Settings/ folder. Verified unchanged/still-accurate and left alone: keyboard-shortcut table (byte-for-byte matches PickerView.handle(_:) at PickerView.swift:158-203, incl. ⌘⌫ via PickerPanel's NSEvent monitor), Snippets section (Tag doubles as title+keyword, confirmed in SnippetFormView.swift:14-20), hover-preview claims (ItemPreview.swift: 40%-screen-width image, chunked text, file name/size/path), ItemKind five cases (no color type), Snippet has no variables/placeholders, SwiftDataClipStore.query matches only normalizedText (no source-app search), production store is genuinely SwiftDataClipStore not InMemoryClipStore (AppEnvironment.swift:59-101), Requirements/Xcode-26-for-swift-test caveat (still literally true per release.yml's own comment), "one dependency: KeyboardShortcuts" claim. Flagged to spawner, not fixed here (out of scope: README.md only): docs/usage.md:206 ("a Settings window ... is planned but not shipped" — false) and docs/architecture.md:554,556 (both call retention/excluded-apps unbuilt, referencing "the README roadmap" — both are wired end-to-end) are stale in the same way the README was; docs/features.md doesn't mention Settings at all. Recommend routing a docs/usage.md + docs/architecture.md + docs/features.md refresh as a follow-up task (senior-dev per CLAUDE.md's doc-ownership split, since these are the API/usage-reference docs, not the architect's overview/getting-started slice). Ceiling: review (not done) per spawn instructions — did not touch task-board.md or project-context.md.
- 2026-09-03 [T-DOC1 review fix] Coordinator caught two errors on review, both fixed in README.md (no other files touched): (1) Settings→History bullet claimed "Everything (default)" — actually SettingsStore.swift:29-30/90-91 default to `.items` with `defaultItemCount = 1000`; rewrote to state the real default (1,000 items) explicitly and also added the verified 30-day default for the day-based mode (`defaultDays = 30`, same file) for symmetry. (2) Dropped the unverified "⌘," Settings shortcut from all three places I'd put it (Features bullet, ## Settings intro, Usage step 6) — investigated per the coordinator's branch (a): grepped ClipnestApp/Sources for `CommandGroup`/`CommandMenu`/`NSMenu`/comma `keyboardShortcut`/`keyEquivalent` — zero hits; ClipnestApp.swift's `body` declares only `MenuBarExtra` + `Settings` scenes, no `CommandGroup` customizing the app menu; AppDelegate.swift/HotkeyManager.swift show only two real global hotkeys registered (`.togglePicker`, `.expandSnippet`, both via the KeyboardShortcuts library) — no third binding for Settings; the MenuBarExtra dropdown's "Settings…" is a plain `Button`, no `.keyboardShortcut`. The app is `.accessory` activation policy with no window scene, so there's no reliable main-menu path either. Found no evidence of a working ⌘, mechanism anywhere in the code — took branch (b): removed the claim, kept "menu-bar icon → Settings…" as the documented way in. Also re-verified other numeric/default claims already in the file against source per the coordinator's broader ask: 40%-of-screen-width image preview (`ItemPreviewController.swift:72`, `visible.width * 0.4`, confirmed exact), 24h/once-a-day update check (`UpdateChecker.checkInterval = 24 * 60 * 60`), 5 Settings tabs (`SettingsView.swift`), macOS 14.0 deployment target (`ClipnestApp/project.yml:26,84` and `Package.swift:6`) — all confirmed accurate, no further changes needed. Ceiling still: review (not done) — did not touch task-board.md or project-context.md.
- 2026-09-03 [T-OCR4] Documented the shipped (reviewer-passed, tester still in flight) OCR feature in README.md (no other files touched). Verified every claim against source before writing: toggle "Recognize text in copied images" lives in Settings → History, default OFF (`ClipnestApp/Sources/UI/Settings/HistorySettingsView.swift:63`, `SettingsStore.swift:40,95-98,120-121` — `isTextRecognitionEnabled` defaults `false`, the one Bool in that file that does). Recognition is Apple Vision on-device, `VNRecognizeTextRequest` at `.recognitionLevel = .fast` with `usesLanguageCorrection = false` (`Sources/ClipnestCore/OCR/VisionTextRecognizer.swift:84-85`); quoted the exact tunables from that file's `static let`s: downscale to `maxDownscaledDimension = 1_600`pt long edge (line 32), skip over `maxByteSize = 50_000_000` bytes (line 38) or `maxPixelDimension = 20_000`px (line 44) — cited only numbers I read directly from the constants, not the routing summary. Confirmed recognized text feeds the existing search index, not a separate mode, via `ClipItemRecord.computeNormalizedText(previewText:ocrText:)` (`SwiftDataClipStore.swift:554-556`, `"\(previewText)\n\(ocrText)".lowercased()`) and its two call sites (`init(_ item:)` line 532, `setRecognizedText` line 329). Confirmed the ⌥Return overload exactly as briefed in `PickerViewModel+Paste.pasteContent(for:plainText:)` (`PickerViewModel+Paste.swift:76-91`): `.image` with non-empty `ocrText` returns `.text(ocrText)` under `plainText`, falls through to normal image-paste handling (pastes the image) when there's no recognized text; `.text/.link/.richText` unaffected. Confirmed `text.viewfinder` badge + "Copy Recognized Text" context-menu item, both gated on `hasRecognizedText` (`ItemRow.swift:92-95,111-118,153-155,186-206`), and the preview popover's "Recognized Text" section rendered below the image via the existing chunked `TextPreview` (`ItemPreview.swift:37-42,71-91`). Confirmed the shortcut-hint string literally reads `"⌥⏎ plain/OCR text"` (`PickerView.swift:358`). Confirmed the privacy ordering the coordinator asked me to state: `ClipboardMonitor.checkNow()` gates on `privacyFilter.shouldCapture(...)` (`ClipboardMonitor.swift:243-252`) and returns early on rejection *before* the item is ever read or stored; OCR is only scheduled after a successful `store.insertOrBumpDuplicate` (lines 296-313) — so concealed/transient copies and excluded-app content never reach the recognizer, they're filtered out of capture entirely first. Added: a Features bullet, a `## Settings` History-tab sentence, a full Privacy-section paragraph (on-device, no upload/model-download, becomes searchable text including anything sensitive a screenshot showed, off-by-default, never sees PrivacyFilter-rejected content), a keyboard-shortcut-table caption + footnote sentence on the ⌥Return overload (visibility per the "silently-overloaded shortcut is a support problem" ask), a "How it's built" bullet with the exact constants, and an `OCR/` line in the `Sources/ClipnestCore/` project-structure tree. Self-caught and fixed before returning: first draft added a checked Roadmap line for OCR — coordinator explicitly said not to touch the Roadmap for this ("shipped, not planned"), removed it on the second pass. Flagged to spawner, not edited here (out of scope): `.claude/coding-standards.md`'s system-framework allow-list (line 75) has no `Vision` entry — `Vision` is a genuinely new import this feature needs (`CoreGraphics` was already on the list for `CGEvent`, so only `Vision` is actually missing) — route to whoever owns that file for a one-line addition. Ceiling: review (not done) per spawn instructions — did not touch code, tests, task-board.md, or project-context.md.
- 2026-09-04 [T-DOC3] README.md re-refresh for the 4 changes since T-DOC1/T-OCR4 (no other files touched). Verified every claim against source before writing, not the task brief's summary: (1) ⌘, now genuinely opens Settings — confirmed the previously-absent mechanism now exists: `PickerPanel`'s local `NSEvent.addLocalMonitorForEvents` (`PickerPanel.swift:181-195`, keycode 43) intercepts ⌘, ahead of `NSApp.mainMenu`'s auto-generated Settings command (which SwiftUI's `Settings` scene wires to ⌘, automatically and which `-[NSApplication sendEvent:]` would otherwise match before any `.onKeyPress` sees it), calling `onCommandComma` → wired in `AppEnvironment.swift:239` to `viewModel.openSettingsFromPicker()` (`PickerViewModel.swift:995-998`, dismisses picker then calls `openSettings()`) → `SettingsFocusCoordinator.focusAfterOpening()` (`SettingsFocusCoordinator.swift`, shared with the menu-bar "Settings…" path) does the deferred-RunLoop find-window + `SettingsActivator`-mediated raise. Separately, SwiftUI's own Settings-scene ⌘, command still fires normally whenever the Settings window itself is already key (no interception needed there — `PickerView.swift:126-144` wires the same `openSettings`/`focusAfterOpening` pair as the environment action for that case too). Confirmed scope is genuinely two Clipnest-window cases only, not global: the interception lives on `PickerPanel`'s own window (`event.window === self` guard, line 183), and the SwiftUI menu command only matches while a Clipnest window is key — pressing ⌘, from another app hits that app's own Settings/Preferences binding, untouched. Documented precisely: table row + footer sentence say "picker or Settings window focused — not global." (2) Confirmed `esc close` is gone from `ShortcutHints.text(for:capabilities:)` (`ShortcutHints.swift:165-168,195` — deliberately omitted for the 560pt panel-width budget, doc comment explains the pt-by-pt measurement) but `.onKeyPress(.escape)` still dismisses (`PickerView.swift:196-198`) — left the README's "Close | Esc" row alone, said nothing about the footer hint (out of scope, internal UI detail). (3) Traced `pasteContent(for:plainText:)` kind-by-kind (`PickerViewModel+Paste.swift:118-135`): `.text`/`.link` return the identical `.text(item.previewText)` whether or not `plainText` is set (no richer stored form — confirmed via `ShortcutHints.swift`'s own T-SET4 doc comment tracing the same code), so ⌥Return is observably a no-op vs Return there; only `.richText` (strips RTF→plain) and `.image` with non-empty `ocrText` (pastes that text instead of the image) genuinely differ; `.file` and OCR-less `.image` fall through to identical normal-paste behavior. Rewrote every ⌥Return claim (Features bullet, table cell, footer paragraph) to state the narrower true scope instead of the previous "strips rich text / OCR image" framing, which implied plain text also had something to strip. (4) Confirmed `ClipItem.supportsSaveAsSnippet` (`Sources/ClipnestCore/Model/ClipItemOCR.swift:39-41`) is `kind == .text || kind == .link` only, shared by the row UI, the ⌘S key handler, and the footer hint (single DRY definition per that property's own doc comment) — rewrote the Snippets-bullet and table/footer ⌘S claims to state "text/link items only," removing the prior unqualified "any history item" wording. (5) Confirmed the Dock-icon caveat directly in `SettingsActivator.swift`'s doc comment and `activate(observing:)`: `.accessory`-policy Clipnest cannot be brought frontmost without a Dock icon (macOS 26 window-server constraint, independently corroborated by a cited external source in that file's comment), so opening Settings flips to `.regular` (Dock icon appears) and flips back to `.accessory` on the Settings window's `willCloseNotification` (no artificial delay in code — restoration is tied to window-close, not a timer) — worded the README caveat as "briefly shows one, disappears again once you close Settings" rather than asserting a specific delay duration I couldn't verify from source. Added this caveat to the Features bullet, Usage step 1, and (fullest wording) the `## Settings` section intro rather than repeating the full explanation three times. Docs cross-check per the coordinator's ask: read `docs/usage.md:190-232` and `docs/features.md:760-786` — both already correctly describe ⌘,'s "picker or Settings-focused, not global" scope and the AppKit-interception rationale (features.md is the implementation doc, usage.md the end-user guide; my README wording is consistent with both, no fix needed there). Flagging two doc gaps found but NOT fixed (out of scope — README.md only): `docs/usage.md:11` and `:231-232` both still claim "no Dock icon" / "Yes [menu-bar-only]" unconditionally, with no mention of the transient Settings-open Dock icon — now stale against `SettingsActivator`'s shipped behavior, same class of gap T-DOC1 flagged for that file before. Also `docs/usage.md` has no OCR/⌥Return-for-images mention anywhere (pre-existing omission, not introduced by recent changes) — its shortcut table/prose only describes ⌥Return stripping rich formatting, never the image-OCR case features.md documents. Recommend both as a docs/usage.md follow-up task, same doc-ownership routing as T-DOC1's flag (senior-dev, per CLAUDE.md's usage-reference-doc ownership split). Ceiling: review (not done) per spawn instructions — did not touch task-board.md or project-context.md.
