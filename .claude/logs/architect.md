- 2026-09-14 [T-IBUS-TIER2, design only] Designed the IBus-commit tier for Linux snippet expansion (P0, replaces/demotes clipboard tier 2). Tier order: AT-SPI (1, unchanged) → new `LinuxIBusSelectionReplacer` (2, keystroke-free read-via-PRIMARY-selection-through-ShellHelperClient + write-via-IBus-commit) → `LinuxClipboardSelectionReplacer` (demoted to 3, unchanged mechanism/decline logic). New composer `LinuxTieredSelectionReplacer` (Linux-only, conforms to existing `SelectionReplacing`) falls through to clipboard tier only on IBus `.noSelection`; treats `.noMatch`/`.replaced`/new `.committedUnconfirmed` as terminal (no double-insert risk). Confirmed via deepwiki (ibus/ibus) that no no-switch commit path exists — global-engine switch is structurally required — so crash-safety is the core risk: designed persist-before-switch/clear-after-confirmed-restore/startup-reconciliation state machine (pure, fake-testable) plus SIGTERM/SIGINT handling (new infra, self-pipe→GLib idle, none exists in this codebase today) as defense-in-depth against everything except SIGKILL, for which the real safety net is (a) IBus's own daemon-side disconnect detection being kernel-driven/instant, not app-driven, and (b) startup reconciliation self-healing the next launch — flagged both as needing live-VM confirmation, not assumed. Found and corrected a mismatch between the POC's Python/libibus "sync set_global_engine deadlocks, must pump GLib" gotcha and this codebase's architecture: since Clipnest never uses libibus (raw hand-rolled D-Bus throughout, like AT-SPI/ShellHelperClient), the real fix is this codebase's own established two-connection pattern (one for inbound Factory/Engine dispatch, one for outbound Bus calls), not a GLib main-context pump — flagged as a new theory needing VM confirmation, not a re-confirmation of the POC. Full design, transport decision (private IBus bus via env var + socket-file fallback, reusing `DBusConnection`/`DBusAddress` with zero new transport code — deliberately not ibus-portal, which exists for sandboxed clients Clipnest isn't), 5-task build split (T-IBUS-ADDR junior-dev, T-IBUS-WIRE/T-IBUS-CLIENT/T-IBUS-CRASHWIRE/T-IBUS-REPLACER senior-dev) with per-task AC/deps, decisions, and VM-only items returned to spawner in this turn's message — did not touch task-board.md or project-context.md (single-writer rule, spawned worker).
