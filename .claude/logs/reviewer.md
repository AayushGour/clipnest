- 2026-08-06 [T8] Reviewed Phase A (T3-T7, T2 SPM portion): independently re-ran swift build (incl. -strict-concurrency=complete), swift test (34/34 pass), swift format lint (clean); read every changed file; grepped for networking/force-unwraps/logging. Mandatory security pass clears: concealed/transient guard verified truly first-and-unconditional, customExcludedBundleIDs verified union-only (can't weaken built-ins). 1 Important (ClipboardMonitor.swift:151 try? swallows ClipStore errors silently, inert today, flag for T40), 3 Minor nits. Findings in .superpowers/sdd/2026-08-06-clipnest-plan/phaseA-review.md. Verdict: APPROVE → tester (T9).
- 2026-08-06 [T14/T15/T11-core/T19/T20/T22/T32-core] Reviewed Phase B/C core (Paste+Search+Storage): independently re-ran swift build, swift format lint --strict (both clean), swift test via CLT workaround (84/84 pass, matches report); read every changed Sources+Tests file in full; grepped for networking/force-unwraps/logging/chmod. Mandatory security pass clears: no networking imports, concealed/transient gate runs before classification (unbypassable by new image/file path), no content/bytes logged, no force-unwraps. Verified D6/D7/D8 all implemented correctly; naming-duplication ruling: the two FrontmostApp protocols are justified (different payload/timing/consumers), the two Workspace-backed adapter structs are a trivial real duplicate (Minor). Retention (T32-core) fully correct: pinned never deleted, oldest-first trim, blob cleanup, nil-cap no-op — all tested. 1 Important (PasteboardReader's Classification.fileURL never persisted onto ClipItem — T20's "re-offer file on paste" requirement unmet, will block T21 unless tracked), 5 Minor. Findings in .superpowers/sdd/2026-08-06-clipnest-plan/phaseBC-core-review.md. Verdict: APPROVE → tester; Important #1 tracked as required before T21.
- 2026-08-10 [T47/D23] Independent review of PickerViewModel.swift's search-hang perf fix (4 uncached computed properties -> single @Published items via debounced Task.detached filter, latest-wins via cancellable filterTask). Re-ran everything myself, not taken on faith: swift test (131/131 pass), swift format lint --strict (both targets clean), xcodegen + xcodebuild clean build (BUILD SUCCEEDED, verified Swift 6 language mode genuinely active for ClipnestApp via SWIFT_VERSION 6.0, grepped compiler output around PickerViewModel.swift for warnings — none). Confirmed Sendable boundary by reading SearchFilter/SearchQuery/ClipItem source directly (all Sendable), read PickerView.swift in full to verify the integration seam (items read as plain stored @Published, transparent to the computed->stored swap), confirmed PickerViewModel is a single app-lifetime instance via AppEnvironment.swift (no lifecycle surprises). code-review-graph has no Swift coverage for this repo (stale/Python-only) — compensated with full manual trace of every scheduleFilterUpdate call site and cancellation interleaving. Hand-traced picker-open race, delete-during-search, rapid typing, tab-switch-mid-filter: latest-request-wins protocol holds in all of them (cancel is synchronous-to-task, isCancelled re-checked right before every @MainActor mutation, so only the most-recently-scheduled call can ever publish). Found 2 Important (not Critical): (1) highlightedItem/selectedItemID can lag the just-typed query by up to the 120ms debounce, so Return/⌘P/⌘S/⌘⌫ can act on a stale row if fired inside that window — a real wrong-paste/wrong-delete risk on exactly the paths this task asked me to verify, new regression vs. pre-fix (which was synchronous, always current); (2) "non-debounced" paths (tab switch/live-refresh/post-mutation reload) now always route through Task.detached too, so they're no longer truly synchronous — can flash the previous tab's stale items for tens of ms in the same large-paste scenario D23 benchmarked (30-70ms filter pass), doc comments overstate "exactly as instant as before." Plus 2 Minor (filterTask not nil'd on cancel unlike refreshTask; Snippets tab's `snippets` still has the identical uncached-computed-property pattern just fixed for History/Pinned, out of scope but same latent bug one tab over) and 1 nit (query's didSet has no equality guard unlike activeTab's). Security pass clears (no auth/secrets/PII/network surface touched, no new logging). Findings in .superpowers/sdd/2026-08-06-clipnest-plan/perf-review.md. Verdict: APPROVE-WITH-FIXES — core cancellation/Sendable design is sound and this is a definite net improvement, but Important #1 is blocking-worthy (destructive delete + wrong-paste risk) so this goes back to senior-dev for a flush-pending-filter-before-commit-action fix before tester, not forward to tester as-is.
- 2026-08-10 [T38] Independent mandatory security pass + correctness review of Phase E release pipeline (scripts/{build,sign,notarize,package_dmg}.sh, ClipnestApp/project.yml Release block, README Release section, .gitignore build/, ClipnestApp.entitlements). Did my own pass, not senior-dev's summary: grepped all touched files for Team-ID (10-char alnum), Apple-ID/email, password/secret/apikey/token, and app-specific-password (xxxx-xxxx-xxxx-xxxx) shapes — zero real hits; only matches are placeholders (<your-apple-id>/<TEAMID>/<app-specific-password>) in comments+README and the `-p codesigning` policy flag (not a password). Credentials verified arg-or-env only, never literal default, both fail-loud when empty: sign.sh IDENTITY="${1:-${CODESIGN_IDENTITY:-}}", notarize.sh PROFILE="${1:-${NOTARY_PROFILE:-}}"; project.yml uses ${DEVELOPMENT_TEAM}/${CODE_SIGN_IDENTITY} XcodeGen env expansions. Entitlements confirmed the empty/minimal <dict></dict> from T2 — Phase E added no entitlement; non-sandboxed + ENABLE_HARDENED_RUNTIME:YES + sign.sh --options runtime is the correct+consistent Developer ID/notarization config. .gitignore build/ verified via git check-ignore to cover the generated ExportOptions.plist (the one file that would embed the Team ID) + all signed .app/.zip/.dmg/.xcarchive; git ls-files/status confirm zero build artifacts tracked; nothing that SHOULD be tracked lives under build/. Only network I/O is `xcrun notarytool submit --wait` (notarize.sh) — explicit, user-initiated (needs the script run + a profile name), fail-loud, never silent/auto (the http DTD URL in build.sh's plist heredoc is the standard PropertyList DOCTYPE string, not a fetch). Correctness: all 4 scripts have set -euo pipefail, fail() with non-zero exit + clear stderr, guarded rm -rf "${VAR:?}", consistent quoting; shellcheck clean on all 4; chain composes on the shared build/Clipnest.app path (build->sign->notarize->package_dmg, each checks its input + fails loud); matches plan T35/T36/T37 must-dos verbatim. code-review-graph N/A here (shell/yaml/plist not in the tree-sitter Swift graph; verified the script-chain seam by direct path tracing instead). SECURITY VERDICT: CLEAR. 2 non-blocking nits only (sign.sh --deep is Apple-deprecated for signing but the plan mandates it verbatim + harmless for a single-bundle app; build.sh -exportArchive path can re-sign, mildly redundant with sign.sh but gated/documented/fail-loud). Verdict: APPROVE -> tester (T39).
- 2026-08-13 [ad-hoc] Independent adversarial line-level review of the full Clipnest tree (correctness/concurrency/security/process angle, complementing the existing docs/review/implementation-review.md maintainability pass). Re-ran everything myself: swift build + swift test (176/176 pass), xcodegen generate + xcodebuild App build (BUILD SUCCEEDED, zero warnings), swift format lint --strict on both Sources+Tests and ClipnestApp/Sources. Core lint FAILS (Paster.swift:65 @retroactive @unchecked Sendable trips AvoidRetroactiveConformances — confirmed no .swift-format override exists, confirmed // swift-format-ignore: AvoidRetroactiveConformances clears it in isolation) — CI never runs this lint step at all, so it's been silently red since 61c69e9. SPECIFICALLY assessed the SwiftData test-container churn per the review brief: confirmed via git log that the temp-file-URL switch (35e5a37) + explicit-Schema commit (6b6f2ce) predate the actual fix (bc548c0's CI bump to macos-26, whose own comment says "Xcode 26 doesn't have the bug") — so the store comments' "fixes Xcode 16.2 bundle-name crash" framing is now stale, and empirically reproduced the leftover cost: swift test runs during this review left 558 stray ClipnestClipTest-*/ClipnestSnippetTest-* store files (~21MB) in $TMPDIR with zero cleanup path (cleaned up after, not left for the repo). SwiftDataClipStoreTests.swift's own "in-memory"/"never a real on-disk store file" doc comments (lines 10-11, 41-45) are now factually false as a result. Security pass CLEAR: no networking, concealed/transient gate unbypassable (re-verified), password-manager list additive-only, no clipboard content in any logger call site read, no secrets in release.yml/clipnest.rb, ClipboardSelectionReplacer's suppress/restore defer ordering verified correct (no capture leak). 2 High (F1 lint-gate red + uncaught by CI; F2 SwiftData test-container stray-file leak from a now-obsolete workaround), 3 Medium (SelectedTextAccessing.swift:50 as! force-cast — confirmed the only production force-cast in the tree; no CI lint/App-build gate at all; Casks/clipnest.rb sha256 :no_check bootstrap caveat, self-healing but unmonitored), 3 Low. No Critical, no security bypass, no data-race/Sendable soundness bug found in the reviewed range. Full report with file:line citations + fixes written to docs/review/code-review.md. This was an ad-hoc deep-review request, not tied to a specific task-board entry — no board line to update; standards followed (no production code edited, findings only).
- 2026-08-21 [full-codebase-audit] Independent full-codebase review (74 files, ~10k LOC), not tied to a task-board item — requested standalone audit. Verified mechanically: swift test 183/183, xcodebuild ClipnestApp build clean (0 warnings), ClipnestAppTests 95/95, swift-format lint --strict clean on both targets. Read every Core Clipboard/Store/Paste/Model file + every App System/Picker file + install/update/sign/notarize/package scripts. Privacy musts verified enforced in code (concealed/transient unconditional, no networking APIs, no clipboard content in any logger call) — security pass: PASS with one flagged deliberate trade-off (install/update strips quarantine flag, no checksum verification — M-2). 2 HIGH: (1) Paster.swift:120-148 posts synthesized paste globally without verifying target app still frontmost — real focus-race data-leak risk; (2) best-supported-but-unconfirmed hypothesis for the open Terminal-drag-selection bug is the CGEvent modifier-flag pattern in Paster.swift + ClipboardSelectionReplacer.swift (duplicated, untestable-by-design, fires on every paste/snippet-expand) — ranked above the leaked-preview-panel hypothesis, which I traced and largely refuted (ItemPreviewController's hide() paths are redundant/reliable; an ordered-out NSWindow can't intercept events). 4 MEDIUM (DRY duplication of the CGEvent code, install/update Gatekeeper bypass, untested pinnedAt-nil sort-order edge case between the two ClipStore impls, PickerViewModel size/complexity), 2 LOW. Full findings with file:line + repro scenarios + suggested fixes: /Users/aayushgour/.claude/jobs/774e190f/tmp/codebase-review.md. Verdict: net-positive, well-engineered codebase; no blocking defect found, 2 HIGH items recommended before next release.
