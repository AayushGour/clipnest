- 2026-08-06 [T8] Reviewed Phase A (T3-T7, T2 SPM portion): independently re-ran swift build (incl. -strict-concurrency=complete), swift test (34/34 pass), swift format lint (clean); read every changed file; grepped for networking/force-unwraps/logging. Mandatory security pass clears: concealed/transient guard verified truly first-and-unconditional, customExcludedBundleIDs verified union-only (can't weaken built-ins). 1 Important (ClipboardMonitor.swift:151 try? swallows ClipStore errors silently, inert today, flag for T40), 3 Minor nits. Findings in .superpowers/sdd/2026-08-06-clipnest-plan/phaseA-review.md. Verdict: APPROVE → tester (T9).
- 2026-08-06 [T14/T15/T11-core/T19/T20/T22/T32-core] Reviewed Phase B/C core (Paste+Search+Storage): independently re-ran swift build, swift format lint --strict (both clean), swift test via CLT workaround (84/84 pass, matches report); read every changed Sources+Tests file in full; grepped for networking/force-unwraps/logging/chmod. Mandatory security pass clears: no networking imports, concealed/transient gate runs before classification (unbypassable by new image/file path), no content/bytes logged, no force-unwraps. Verified D6/D7/D8 all implemented correctly; naming-duplication ruling: the two FrontmostApp protocols are justified (different payload/timing/consumers), the two Workspace-backed adapter structs are a trivial real duplicate (Minor). Retention (T32-core) fully correct: pinned never deleted, oldest-first trim, blob cleanup, nil-cap no-op — all tested. 1 Important (PasteboardReader's Classification.fileURL never persisted onto ClipItem — T20's "re-offer file on paste" requirement unmet, will block T21 unless tracked), 5 Minor. Findings in .superpowers/sdd/2026-08-06-clipnest-plan/phaseBC-core-review.md. Verdict: APPROVE → tester; Important #1 tracked as required before T21.
- 2026-08-10 [T47/D23] Independent review of PickerViewModel.swift's search-hang perf fix (4 uncached computed properties -> single @Published items via debounced Task.detached filter, latest-wins via cancellable filterTask). Re-ran everything myself, not taken on faith: swift test (131/131 pass), swift format lint --strict (both targets clean), xcodegen + xcodebuild clean build (BUILD SUCCEEDED, verified Swift 6 language mode genuinely active for ClipnestApp via SWIFT_VERSION 6.0, grepped compiler output around PickerViewModel.swift for warnings — none). Confirmed Sendable boundary by reading SearchFilter/SearchQuery/ClipItem source directly (all Sendable), read PickerView.swift in full to verify the integration seam (items read as plain stored @Published, transparent to the computed->stored swap), confirmed PickerViewModel is a single app-lifetime instance via AppEnvironment.swift (no lifecycle surprises). code-review-graph has no Swift coverage for this repo (stale/Python-only) — compensated with full manual trace of every scheduleFilterUpdate call site and cancellation interleaving. Hand-traced picker-open race, delete-during-search, rapid typing, tab-switch-mid-filter: latest-request-wins protocol holds in all of them (cancel is synchronous-to-task, isCancelled re-checked right before every @MainActor mutation, so only the most-recently-scheduled call can ever publish). Found 2 Important (not Critical): (1) highlightedItem/selectedItemID can lag the just-typed query by up to the 120ms debounce, so Return/⌘P/⌘S/⌘⌫ can act on a stale row if fired inside that window — a real wrong-paste/wrong-delete risk on exactly the paths this task asked me to verify, new regression vs. pre-fix (which was synchronous, always current); (2) "non-debounced" paths (tab switch/live-refresh/post-mutation reload) now always route through Task.detached too, so they're no longer truly synchronous — can flash the previous tab's stale items for tens of ms in the same large-paste scenario D23 benchmarked (30-70ms filter pass), doc comments overstate "exactly as instant as before." Plus 2 Minor (filterTask not nil'd on cancel unlike refreshTask; Snippets tab's `snippets` still has the identical uncached-computed-property pattern just fixed for History/Pinned, out of scope but same latent bug one tab over) and 1 nit (query's didSet has no equality guard unlike activeTab's). Security pass clears (no auth/secrets/PII/network surface touched, no new logging). Findings in .superpowers/sdd/2026-08-06-clipnest-plan/perf-review.md. Verdict: APPROVE-WITH-FIXES — core cancellation/Sendable design is sound and this is a definite net improvement, but Important #1 is blocking-worthy (destructive delete + wrong-paste risk) so this goes back to senior-dev for a flush-pending-filter-before-commit-action fix before tester, not forward to tester as-is.
- 2026-08-10 [T38] Independent mandatory security pass + correctness review of Phase E release pipeline (scripts/{build,sign,notarize,package_dmg}.sh, ClipnestApp/project.yml Release block, README Release section, .gitignore build/, ClipnestApp.entitlements). Did my own pass, not senior-dev's summary: grepped all touched files for Team-ID (10-char alnum), Apple-ID/email, password/secret/apikey/token, and app-specific-password (xxxx-xxxx-xxxx-xxxx) shapes — zero real hits; only matches are placeholders (<your-apple-id>/<TEAMID>/<app-specific-password>) in comments+README and the `-p codesigning` policy flag (not a password). Credentials verified arg-or-env only, never literal default, both fail-loud when empty: sign.sh IDENTITY="${1:-${CODESIGN_IDENTITY:-}}", notarize.sh PROFILE="${1:-${NOTARY_PROFILE:-}}"; project.yml uses ${DEVELOPMENT_TEAM}/${CODE_SIGN_IDENTITY} XcodeGen env expansions. Entitlements confirmed the empty/minimal <dict></dict> from T2 — Phase E added no entitlement; non-sandboxed + ENABLE_HARDENED_RUNTIME:YES + sign.sh --options runtime is the correct+consistent Developer ID/notarization config. .gitignore build/ verified via git check-ignore to cover the generated ExportOptions.plist (the one file that would embed the Team ID) + all signed .app/.zip/.dmg/.xcarchive; git ls-files/status confirm zero build artifacts tracked; nothing that SHOULD be tracked lives under build/. Only network I/O is `xcrun notarytool submit --wait` (notarize.sh) — explicit, user-initiated (needs the script run + a profile name), fail-loud, never silent/auto (the http DTD URL in build.sh's plist heredoc is the standard PropertyList DOCTYPE string, not a fetch). Correctness: all 4 scripts have set -euo pipefail, fail() with non-zero exit + clear stderr, guarded rm -rf "${VAR:?}", consistent quoting; shellcheck clean on all 4; chain composes on the shared build/Clipnest.app path (build->sign->notarize->package_dmg, each checks its input + fails loud); matches plan T35/T36/T37 must-dos verbatim. code-review-graph N/A here (shell/yaml/plist not in the tree-sitter Swift graph; verified the script-chain seam by direct path tracing instead). SECURITY VERDICT: CLEAR. 2 non-blocking nits only (sign.sh --deep is Apple-deprecated for signing but the plan mandates it verbatim + harmless for a single-bundle app; build.sh -exportArchive path can re-sign, mildly redundant with sign.sh but gated/documented/fail-loud). Verdict: APPROVE -> tester (T39).
- 2026-08-13 [ad-hoc] Independent adversarial line-level review of the full Clipnest tree (correctness/concurrency/security/process angle, complementing the existing docs/review/implementation-review.md maintainability pass). Re-ran everything myself: swift build + swift test (176/176 pass), xcodegen generate + xcodebuild App build (BUILD SUCCEEDED, zero warnings), swift format lint --strict on both Sources+Tests and ClipnestApp/Sources. Core lint FAILS (Paster.swift:65 @retroactive @unchecked Sendable trips AvoidRetroactiveConformances — confirmed no .swift-format override exists, confirmed // swift-format-ignore: AvoidRetroactiveConformances clears it in isolation) — CI never runs this lint step at all, so it's been silently red since 61c69e9. SPECIFICALLY assessed the SwiftData test-container churn per the review brief: confirmed via git log that the temp-file-URL switch (35e5a37) + explicit-Schema commit (6b6f2ce) predate the actual fix (bc548c0's CI bump to macos-26, whose own comment says "Xcode 26 doesn't have the bug") — so the store comments' "fixes Xcode 16.2 bundle-name crash" framing is now stale, and empirically reproduced the leftover cost: swift test runs during this review left 558 stray ClipnestClipTest-*/ClipnestSnippetTest-* store files (~21MB) in $TMPDIR with zero cleanup path (cleaned up after, not left for the repo). SwiftDataClipStoreTests.swift's own "in-memory"/"never a real on-disk store file" doc comments (lines 10-11, 41-45) are now factually false as a result. Security pass CLEAR: no networking, concealed/transient gate unbypassable (re-verified), password-manager list additive-only, no clipboard content in any logger call site read, no secrets in release.yml/clipnest.rb, ClipboardSelectionReplacer's suppress/restore defer ordering verified correct (no capture leak). 2 High (F1 lint-gate red + uncaught by CI; F2 SwiftData test-container stray-file leak from a now-obsolete workaround), 3 Medium (SelectedTextAccessing.swift:50 as! force-cast — confirmed the only production force-cast in the tree; no CI lint/App-build gate at all; Casks/clipnest.rb sha256 :no_check bootstrap caveat, self-healing but unmonitored), 3 Low. No Critical, no security bypass, no data-race/Sendable soundness bug found in the reviewed range. Full report with file:line citations + fixes written to docs/review/code-review.md. This was an ad-hoc deep-review request, not tied to a specific task-board entry — no board line to update; standards followed (no production code edited, findings only).
- 2026-08-21 [full-codebase-audit] Independent full-codebase review (74 files, ~10k LOC), not tied to a task-board item — requested standalone audit. Verified mechanically: swift test 183/183, xcodebuild ClipnestApp build clean (0 warnings), ClipnestAppTests 95/95, swift-format lint --strict clean on both targets. Read every Core Clipboard/Store/Paste/Model file + every App System/Picker file + install/update/sign/notarize/package scripts. Privacy musts verified enforced in code (concealed/transient unconditional, no networking APIs, no clipboard content in any logger call) — security pass: PASS with one flagged deliberate trade-off (install/update strips quarantine flag, no checksum verification — M-2). 2 HIGH: (1) Paster.swift:120-148 posts synthesized paste globally without verifying target app still frontmost — real focus-race data-leak risk; (2) best-supported-but-unconfirmed hypothesis for the open Terminal-drag-selection bug is the CGEvent modifier-flag pattern in Paster.swift + ClipboardSelectionReplacer.swift (duplicated, untestable-by-design, fires on every paste/snippet-expand) — ranked above the leaked-preview-panel hypothesis, which I traced and largely refuted (ItemPreviewController's hide() paths are redundant/reliable; an ordered-out NSWindow can't intercept events). 4 MEDIUM (DRY duplication of the CGEvent code, install/update Gatekeeper bypass, untested pinnedAt-nil sort-order edge case between the two ClipStore impls, PickerViewModel size/complexity), 2 LOW. Full findings with file:line + repro scenarios + suggested fixes: /Users/aayushgour/.claude/jobs/774e190f/tmp/codebase-review.md. Verdict: net-positive, well-engineered codebase; no blocking defect found, 2 HIGH items recommended before next release.
- 2026-09-03 [T-OCR3] Reviewed T-OCR1/T-OCR2 (on-device OCR, staged diff, 23 files). Verdict: PASS. Migration safety: `ocrText: String?` is genuinely additive-safe (mirrors `pinnedAt` precedent); the pre-`normalizedText` legacy-row migration test was extended in place and independently verified green (`swift test` → 205/205, incl. the extended migration test) — a migrated row gets `ocrText == nil` (correct terminal state, not just non-crashing) and `setRecognizedText` works normally afterward. `computeNormalizedText` is confirmed the single derivation point (grepped for stray `previewText.lowercased()` — only the two legitimate InMemory/backfill-input sites remain). Concurrency: `checkNow()` never awaits `scheduleTextRecognition` (proved by a gated-fake non-blocking test I re-ran); detached task uses `weak self` + local `let store`/`recognizer` captures, all Sendable (`ClipStore: Sendable`, `TextRecognizing: Sendable`, `ClipItem: Sendable`); a delete/clearHistory racing the OCR write surfaces `ClipStoreError.notFound` (both store impls), logged and swallowed — no resurrection. Security: PrivacyFilter is untouched and structurally upstream — OCR only fires after `insertOrBumpDuplicate` succeeds, so concealed/transient/excluded-app content can never reach the recognizer; setting defaults false (`UserDefaults.object(forKey:) as? Bool ?? false`); Settings copy explicitly says recognized text becomes searchable incl. sensitive on-screen content; no clipboard/OCR content logged anywhere (grepped every `logger.*` call touched). Verdict: mandatory security pass CLEARS. Independently ran `swift build`, `swift test` (205/205), `swift format lint --recursive --strict` (Sources+Tests, ClipnestApp/Sources — both clean), `xcodegen generate` + `xcodebuild build` (BUILD SUCCEEDED) myself rather than trusting the report. Non-blocking follow-ups noted (not filed as blockers): (1) two rapid duplicate copies of the same image before the first Vision pass completes can schedule OCR twice concurrently (idempotent result, wasted work only — untested edge case); (2) existing images captured before the setting was ever turned on are never retroactively OCR'd (opt-in-at-capture-time is a reasonable v1 scope, but worth confirming intentional with product); (3) coding-standards.md's system-framework list doesn't yet mention `Vision`/`CoreGraphics` — docs-only nit for architect. → tester.
- 2026-09-03 [T-PERF2] Reviewed T-PERF1 (main-thread offload: PasteboardReader.pullRawPayload/classify split, ClipboardMonitor.checkNow off-main classify, Paster .image decode via ImageIO+Task.detached, PickerViewModel+Paste async pasteContent), staged+working diff, concurrency/offload files only. Independently re-verified: swift format lint --strict on every touched file (clean), swift test --filter PasteboardReaderTests (16/16), PasterTests (14/14), ClipboardMonitorTests (30/30) — reran myself, didn't take senior-dev's 215/215 on faith. Read every changed line of PasteboardReader.swift/ClipboardMonitor.swift/Paster.swift/PickerViewModel+Paste.swift plus their test diffs; grepped full staged diff for `@unchecked Sendable` — confirmed zero new ones in production (NSPasteboard's @retroactive @unchecked Sendable in Paster.swift pre-existed, verified via `git show HEAD:...`), test-only ones (Counter, MutableBox) mirror the existing pattern. PrivacyFilter gate ordering unchanged (still runs before pullRawPayload). ClipboardMonitor state machine (lastChangeCount/ignoredChangeCount/isPaused) ordering unchanged — only classify's CPU work moved to Task.detached. Self-paste-suppression invariant (item 1): verified rigorously by hand-tracing — decode-before-write was already the ordering pre-T-PERF1 (just synchronous, not suspended), so the write→suppressOwnPasteboardWrite race window (~40ms, dominated by synthesisDelay) is provably unchanged; author's argument holds. Dismiss-ordering change (item 2): verified via diff that pasteContent already resolved before dismiss() in the OLD code too (pasteAndDismiss dismissed before Paster.paste's synthesisDelay, never before content resolution) — so the only real delta is sync-blocking-main -> async-off-main for the blob read, which the author's own doc correctly calls "a few ms later, strictly safer, never less safe"; concur, not a regression. New tests judged: PasterTests.imageDecodeDoesNotBlockTheCallingActor is a genuine non-vacuous proof (MainActor counter races the real paste() call, would fail pre-T-PERF1). PasteboardReaderTests.classifyRunsOffTheMainThread only proves classify() CAN run off-main via an explicit Task.detached the test itself constructs — it does NOT prove checkNow() actually calls it that way, so a future "simplification" reverting checkNow() to inline classify would compile and pass every existing test; flagged as non-blocking follow-up (add a checkNow()-level MainActor-counter test mirroring the Paster one). BLOCKING: Paster.swift:209-222 — paste()'s own doc comment was NOT updated and is now false for the .image case ("the pasteboard write itself still happens synchronously, before any suspension point" — there IS a suspension, `await normalizeTask.value`, before the write in that branch; "async solely to allow synthesisDelay" also omits the new second reason). The correct invariant (write completes before paste() returns, not before the first suspension point) IS correctly written elsewhere in this same diff (PickerViewModel+Paste.swift's performPaste doc) but not fixed at its primary/authoritative source. This was task item 1's explicit, highest-priority ask ("verify the doc comment was updated to match reality") and it wasn't — cheap, surgical fix (rewrite ~5 lines of comment, no logic change). Security pass: local-only preserved, no new networking/logging of clipboard content, no new imports outside the approved system-framework list (ImageIO, UniformTypeIdentifiers, AppKit — all system). Verdict: REJECT (single blocking doc-fix, everything else — design/functionality/tests/Sendable/PrivacyFilter ordering/state-machine ordering — is sound and net-positive) → back to senior-dev to fix Paster.swift's paste() doc comment, then straight to tester (no other changes needed).
- 2026-09-03 [T-HANG3] Reviewed T-HANG1 (VisionTextRecognizer serial `recognitionQueue`) + T-HANG2 (Paster.paste onPasteboardWrite callback). Verdict: PASS. Independently reran everything, didn't take senior-dev's numbers on faith: swift format lint --strict on all 6 touched files (clean), swift test full suite (220/220, matches claim), swift test --filter "VisionTextRecognizer|Paster" (21/21, no CheckedContinuation misuse/leak warnings in output), xcodegen generate + xcodebuild ClipnestApp build (BUILD SUCCEEDED, confirms the Paster/PickerViewModel+Paste seam actually compiles under the new onPasteboardWrite signature). Cap-of-1 justification (item 1, the highest-risk claim): VERIFIED, not just trusted — read the raw `sample_paste_hang.txt` capture myself. It shows 9 distinct threads each on its OWN concurrent `com.apple.VNRecognizeTextRequestRequestRevision3` dispatch queue, but every one of those threads is blocked in `_dispatch_semaphore_wait_slow` inside `-[VNRequest performInContext:error:]_block_invoke` — i.e. genuinely idle, not computing — while exactly ONE thread (on the single serial `com.apple.VN.detectorSyncTasksQueue.VNCRImageReaderDetector`) is doing the real work, itself blocked on ANE hardware via another semaphore. This directly confirms Apple already serializes the real recognition compute internally regardless of how many `perform()` calls are outstanding — a cap of 1 loses no real throughput, matching the code comment's claim exactly. Static (not per-instance) queue is correct given that global internal bottleneck; no testability regression (behavior is verified via the observable heartbeat-starvation symptom, not internal queue substitution). Continuation-resume (item 3): traced every early return in the dispatched closure (decode failure, pixel-ceiling, and both branches of performVisionRequest's completion handler/catch) — exactly one resume per path, confirmed by code reading + empirical absence of any continuation-misuse trap across the full test run. T-HANG2 (item 4): onPasteboardWrite fires post-switch, before synthesisDelay, structurally for every content kind (single call site); does not fire when .image throws before any write (test + code confirm); no deadlock (@MainActor closure awaited from a nonisolated context is a standard actor-hop, not a blocking wait); Paster.paste's doc comment accurately states the new contract (the earlier T-PERF2 doc-staleness finding is fixed in this diff). Harness "6 of 6"/"9 of 9" metric (explicitly asked to confirm): verified by reading ConcurrencyTracker.swift directly — enter() fires before the call is even dispatched to recognitionQueue, so a queued-but-not-executing call is indistinguishable from an executing one in that metric; confirmed harness-metric limitation, not evidence the cap is broken. checkNow() still never awaits scheduleTextRecognition (unchanged fire-and-forget) — no main-actor blocking reintroduced. Security pass CLEAR: no new imports outside the approved system-framework list, no networking, all logger.error calls log only String(describing: error)/item IDs, never content; no force-unwrap/try!/as! introduced. Standards: DRY/magic-numbers/naming all followed; the doc comments explicitly pre-empt the "next person simplifies this back to Task.detached" risk the task worried about. Non-blocking follow-ups (not filed as blockers): (1) unbounded FIFO backlog's memory exposure under a real 100-image burst isn't quantified/load-tested — same characteristic existed pre-fix (Task.detached queuing on the cooperative pool also retained imageData while blocked), so not a new regression, but worth a follow-up stress run at higher N; (2) no explicit Task-cancellation plumbing exists for a queued-but-not-yet-run recognition when its item is deleted/history cleared — but OCRConcurrencyScenario's real end-to-end run already proves this is safe today (deletedMidFlightSurvivedAsGhost=false both qualities, ClipStoreError.notFound is thrown+swallowed), just not via cancellation; (3) no unit test directly pins VisionTextRecognizer.maxConcurrentRecognitions==1 — low value since the doc comment itself says the constant doesn't drive behavior, and the real regression surface (starvation) is already covered by manyConcurrentRecognitionsDoNotStarveTheCooperativePool. → tester.
- 2026-09-04 [holistic-review WIP part1] Resumed after infra timeout. Progress so far (items 1-3 of the 7-item brief), full verdict pending:
  ITEM 1 (coherence): CONFIRMED single-sourced. `ClipItemOCR.swift` (Sources/ClipnestCore/Model/ClipItemOCR.swift:20,39) is the sole definition of `hasRecognizedText`/`supportsSaveAsSnippet`; grepped every call site (ItemRow.swift, ShortcutHints.swift, PickerViewModel.swift) — all reference the Core property, zero re-inlined copies. `normalizedText` derivation: single point `ClipItemRecord.computeNormalizedText(previewText:ocrText:)` (SwiftDataClipStore.swift:572-575), used by convenience init, the pre-existing migration-crash-fix backfill, AND the new `setRecognizedText` — grepped for stray `.lowercased()` normalizedText computation, found none outside this function + InMemoryClipStore's parallel (but not identical — flagging, see below) filter logic. Settings-open path: CONFIRMED single coordinator — `SettingsFocusCoordinator.focusAfterOpening()` (SettingsFocusCoordinator.swift) is a `static let activator = SettingsActivator.live()` shared singleton, called from both `MenuBarContent` (menu-bar "Settings...") and `PickerView`'s new ⌘, path (via `PickerPanel`'s local NSEvent monitor intercepting ⌘, ahead of AppKit's auto-generated Settings command — root-caused and documented, not guessed). No second/competing path found.
  ITEM 3 (data safety) — SOUND, verified by direct read of SwiftDataClipStore.swift in full (601 lines): `ocrText` on `ClipItemRecord` is an *additive optional* field (var ocrText: String?, no default needed — SwiftData lightweight-migration defaults new optionals to nil automatically, same precedent as the existing `pinnedAt` field). `normalizedText` itself is non-optional+defaulted (`= ""`) which is what lightweight migration requires for a new non-optional attribute (this is pre-existing from the prior migration-crash fix, not new today) — `backfillNormalizedText(in:)` (line 189-216) runs on every `init` but is a no-op after the first launch (predicate matches nothing once repaired), and — critically — its backfill computation now correctly calls `ClipItemRecord.computeNormalizedText(previewText:record.previewText, ocrText:record.ocrText)` (line 206-207), NOT a stale `.lowercased()`-only call, so a store that migrates in fresh today gets correctly-recomputed normalizedText that already accounts for ocrText (trivially nil for all pre-OCR rows, so behaviorally identical to before OCR existed for every existing row — no risk to the user's real 263-row store). This matches the swiftdata-migration-safe pattern (optional/defaulted new attrs + backfill) exactly. Real senior-dev log entry (grepped .claude/logs/senior-dev.md) states this was tested against a cross-suite Core-Data-entity-name race caught pre-handoff and fixed by reusing the existing serialized test suite rather than adding a second test-local @Model type — good catch, judged as correctly resolved.
  ITEM 2 (concurrency) — partial, core hot paths read in full: VisionTextRecognizer.swift (P0 hang fix) — dedicated SERIAL DispatchQueue (`recognitionQueue`, qos .utility) wraps the ENTIRE decode+downscale+VNImageRequestHandler.perform pipeline, confirmed no cooperative-pool thread is ever blocked (perform() runs sync on the private queue, called from inside recognitionQueue.async, continuation resumed exactly once per path — traced all 4 early-return branches). maxConcurrentRecognitions=1 justification is well-reasoned (Apple's own VNDetector already serializes internally). ClipboardMonitor.checkNow()/scheduleTextRecognition (ClipboardMonitor.swift:244-404): confirmed checkNow() never awaits scheduleTextRecognition (fire-and-forget Task.detached, [weak self] used correctly, no retain cycle). Paster.paste .image case (Paster.swift:266-329): confirmed off-main ImageIO decode/re-encode via Task.detached BEFORE the pasteboard write, then onPasteboardWrite fires synchronously right after the write (changeCount read is NOT deferred across a suspension point — evaluated as a plain argument expression right after the write) — this closes the T-HANG2 self-paste race down to a single @MainActor-hop window (residual 1/700, disclosed, see item 6). PickerViewModel+Paste.swift fully read: select()/pasteSnippet()/performPaste() all use [weak self] in Task closures correctly, dismiss-before-paste ordering preserved and explained. FINDING (nit, non-blocking): VisionTextRecognizer.swift:145 constructs `NSImage(data: imageData)` off the main actor (inside recognitionQueue.async, called from ClipboardMonitor's Task.detached path) to get a CGImage — this is the exact same "NSImage not documented thread-safe for every operation" hazard that Paster.swift's own new doc comment (this same session, normalizedToTIFF) and PasteboardReader.swift's own new doc comment (imagePixelDimensions) explicitly called out and deliberately avoided by switching to ImageIO. VisionTextRecognizer is new code from today and did not get the same treatment — it's on a private serial queue so no concurrent-access risk exists in practice (stress-tested extensively, zero reported failures), but it is a real inconsistency in an otherwise careful "avoid NSImage off-main" rule applied elsewhere in this exact diff. Recommend follow-up: switch to CGImageSourceCreateWithData (ImageIO) for consistency, not blocking.
  Continuing to items 4 (security — mandatory trigger), 5 (ship-as-unit incl. tools/), 6 (debt triage), 7 (test quality) next.
- 2026-09-04 [holistic-review WIP part2] ITEM 4 (mandatory security/privacy pass) — CLEARS. `PrivacyFilter.swift` is NOT in today's diff at all (confirmed via `git status --short`) — the concealed/transient/excluded-app gate is byte-for-byte unchanged, and structurally precedes OCR: `ClipboardMonitor.checkNow()` only calls `scheduleTextRecognition` after `store.insertOrBumpDuplicate(item)` succeeds (ClipboardMonitor.swift:325-348), which itself only runs after `privacyFilter.shouldCapture(...)` passed (line 258-267) — concealed/transient/excluded content is rejected before a `ClipItem` even exists, so it structurally can never reach the recognizer. `OCRBackfillCoordinator` only operates on `fetchImagesNeedingRecognition()`'s result set — already-stored items that, by construction, already passed the same gate at capture time; no new exposure surface. Grepped full staged diff for networking APIs (URLSession/Network/Socket) — zero hits, local-only preserved. Grepped every `logger.*`/`Logger(` call site in touched OCR/store/monitor files — all log metadata only (error case names, `String(describing: error)`, item UUIDs), never `previewText`/`ocrText`/blob bytes. OCR default confirmed OFF: `SettingsStore.swift:143` — `defaults.object(forKey:) as? Bool ?? false`. Privacy trade-off explicitly disclosed to the user in `HistorySettingsView.swift:116` ("...sensitive shown on screen at the time"). No secrets/keys found in any touched file (grepped for password/token/apikey/secret patterns — zero hits in new code). SECURITY VERDICT: CLEAR.
- 2026-09-04 [holistic-review WIP part3] ITEM 5 (unsafe/unwise to ship as a unit, incl. tools/): The +24,133 line total is dominated by `.claude/logs/stress-artifacts/` (14,811 lines/22 files, raw `sample`/`swift test`/`xcodebuild test` captures used as P0-hang evidence) — NOT application code. Sampled these for content leakage: `sample`/stack-trace captures contain only process/thread/file-path metadata (paths already show `/Users/USER/...`, appears deliberately redacted), `swift test` output only test names/pass-fail, `run*.txt`/`t-hang3-*.txt` only generic CoreText/font I/O error descriptions — grepped for recognized-text/previewText patterns, zero hits. These artifacts are evidence logs, arguably reasonable to keep for audit trail but bulky; non-blocking suggestion: consider whether 14.8k lines of raw sample captures belong permanently in git history vs. being summarized + archived elsewhere — a nit, not a blocker, since they're pure text, no secrets found, and .claude/logs/ is already the established evidence convention this project uses.
  `tools/` (glyph-classifier + stress-harness) contributes only ~3,419 of the +24,133 lines (source only). VERIFIED (c) — does NOT link into shipping targets: grepped root `Package.swift` and `ClipnestApp/project.yml`, zero references to `tools/` from either; `tools/glyph-classifier/Package.swift` explicitly documents itself as a standalone, never-referenced-by-root SPM package specifically so `GlyphTrainer`'s `import CreateML` can never end up in the app binary or in `swift test`/`swift build`'s dependency graph — confirmed no path dependency exists in either direction. `tools/stress-harness/Package.swift` has a one-directional local path dependency ON `ClipnestCore` (exercises real production types) — correct direction, harness depends on app, not vice versa. VERIFIED (b) — gitignore coverage: `.gitignore` diff adds `tools/glyph-classifier/{.build,.swiftpm,dataset,dataset_smoke,output,real_eval_crops}/` — confirmed via `git status --short tools/` that ONLY `.swift`/`Package.swift` source files are staged, zero `.png`/`.mlmodel`/dataset artifacts tracked (`git status --short --ignored` confirms `output/` and `real_eval_crops/` are correctly ignored, not staged). Noted: `real_eval_crops/` holds crops harvested from the user's REAL clipboard-history screenshots (disclosed in the gitignore's own comment) — correctly excluded, but flagging that this directory exists un-encrypted on local disk as a residual local-only privacy consideration (not a repo/commit issue).
  VERIFIED (a) — belongs in repo: `stress-harness` is high-value (directly responsible for finding+confirming the P0 164s+ hang and quantifying the self-paste race), reuses real production types, well-isolated — clear keep. `glyph-classifier` is a FAILED, deliberately-not-integrated spike (would corrupt ~1/3 of real characters per the brief) — well-isolated and correctly never linked, but it is ~1,453 lines + a full CreateML dependency chain for a feature with zero forward path today. Judgment: not blocking (properly isolated, doesn't touch the shipping build, doesn't fail any gate), but worth an architect call on whether a failed spike belongs permanently in `main` vs. being archived/summarized in a doc + parked on a branch — flagging as a follow-up decision, not a defect.
  Nothing found in this item that is unsafe to ship. The two "unwise" candidates are both non-blocking (artifact log bulk, spike-in-main hygiene).
- 2026-09-04 [holistic-review WIP part4] ITEM 6 (debt triage — judge, don't rediscover):
  - `PasterTests.imageDecodeDoesNotBlockTheCallingActor` (Tests/ClipnestCoreTests/PasterTests.swift:236-267): CONFIRMED structurally vacuous. Counter task and `paste()` both run cooperatively on MainActor; `#expect(counter.value > 0)` only proves SOME suspension occurred, not that the image decode specifically ran off-main (SE-0338 reentrancy makes this untestable this way). Matches disclosed status — noise for ship decision (the actual off-main behavior IS real, per direct code read of `Paster.swift`'s `Task.detached(priority: .utility)` wrapping `normalizedToTIFF`; this is a test-quality gap, not a functional bug). Non-blocking, worth a follow-up test using a real timing/thread-check assertion instead.
  - 1/700 self-paste race: CONFIRMED residual and correctly disclosed (Paster.swift's doc comment explains the exact remaining window: the `@MainActor` hop inside `onPasteboardWrite` could theoretically still lose a race against `ClipboardMonitor`'s Timer-driven poll under extreme load). Real, quantified, disclosed, non-blocking — matches this project's existing bar for documented residual risk (see reviewer.md entries #3, #9 for precedent of similar acceptable residual races).
  - No cancellation for queued OCR jobs: NUANCED — `OCRBackfillCoordinator.run()` (the user-triggered Settings backfill) DOES have real cancellation (checks `Task.isCancelled` between items, never mid-item, tested). The debt is specifically about `ClipboardMonitor.scheduleTextRecognition` (capture-time OCR, ClipboardMonitor.swift:380-404) — a fire-and-forget `Task.detached` with no cancellation handle stored; if the item is deleted mid-flight, `setRecognizedText` throws `.notFound`, logged and swallowed (safe, not a correctness bug — verified this is structurally proven, not just claimed, in the T-HANG3 review's `OCRConcurrencyScenario` notes cited in reviewer.md entry #9). This is real, minor, wasted-work-only debt — correctly low priority, not blocking.
  - `PickerView.handle`'s bare `.return` (no modifier guard, ⌘⏎ falls through to plain-⏎ behavior): CONFIRMED PRE-EXISTING — `git status --short` shows `PickerView.swift` IS touched today, but the `handle(_:)` switch statement itself (lines 194-238) is untouched by this diff's actual changes (verified: tester's own log entry independently confirmed this via `git diff --cached` before this review even started — .claude/logs/tester.md's AC(5) entry). No new regression from today's work; legitimate pre-existing quirk, correctly out of scope for this diff.
  - `HotkeyManagerTests.swift:25` LineLength lint failure: CONFIRMED pre-existing (file not touched in today's `git status --short`), confirmed the violating line is real (100-char `optionCommandV` declaration). Cosmetic, correctly non-blocking, already tracked.
  VERDICT on debt list: all 5 items are correctly triaged by the team already — none should block this commit. Only the OCR-cancellation item and the vacuous test are worth follow-up tickets; the other 3 are accurately "known and acceptable."

  ITEM 7 (test quality, sampled not exhaustive): `VisionTextRecognizerTests.manyConcurrentRecognitionsDoNotStarveTheCooperativePool` (Tests/ClipnestCoreTests/VisionTextRecognizerTests.swift:104-171) is a genuinely excellent regression test — measures real cooperative-pool starvation via a sleep-paced heartbeat/max-gap tracker, doc comment explicitly states it was watched RED against the unfixed code and explains why an earlier concurrency-counter version was deleted as a poor proxy (queued-but-not-executing looked identical to executing). This is real, load-bearing test engineering, not box-checking. `ClipItemOCRTests.swift` (40 lines): tight, non-vacuous, covers nil/empty/wrong-kind boundary cases for `hasRecognizedText`. `SettingsActivatorTests.swift`: proper dependency-injected spy asserting exact call sequences/counts (`policyChanges == [.regular]`, `activateCount == 1`) — not vacuous, genuinely locks down the idempotency/re-activate-on-second-click behavior the type's doc comment claims. `OCRBackfillCoordinatorTests.swift`: exercises real `InMemoryClipStore` + temp-dir `BlobStore` + fake recognizer, asserts exact progress sequences, deleted-mid-run resilience, cancellation-between-items. Overall: the sampled new tests genuinely constrain behavior, with the one already-known vacuous exception (`PasterTests.imageDecodeDoesNotBlockTheCallingActor`). No new vacuous/tautological test found beyond the one already disclosed.
- 2026-09-04 [holistic-review FINAL] Verdict: SHIP-WITH-CAVEATS. Independently re-ran (not taken on faith): `swift format lint --recursive --strict Sources Tests` (clean, exit 0), `swift format lint --recursive --strict ClipnestApp/Sources` (clean, exit 0), `swift test` (239/239, 17 suites, matches claim — corrupt-store CoreData error spam in output is expected/from the deliberate corrupt-store recovery tests, not a failure), `xcodegen generate` + `xcodebuild -project ClipnestApp/ClipnestApp.xcodeproj -scheme ClipnestApp -destination 'platform=macOS' build` → BUILD SUCCEEDED. Did not re-run the full `xcodebuild test` (144 App tests) given time budget — accepted on the strength of per-task reviewer entries #7/#8/#9 above (each independently reran it at the time) plus this session's own full read of every integration seam (Settings coordinator, OCR wiring, paste path). Full cross-cutting findings + answers to items 1-7 delivered to spawning agent in final response (not duplicated here — see WIP part1-4 entries above for the detailed evidence trail). Summary: design is coherent (single-sourced capability predicates/normalizedText/Settings-open path, confirmed not just claimed), the P0 hang fix is sound and independently verified via direct read of the sample capture reasoning, data migration is safe, security pass clears, tools/ is correctly isolated from shipping targets with correct gitignore coverage. One new (not previously flagged) non-blocking finding: VisionTextRecognizer.swift:145 uses `NSImage(data:)` off-main, the same hazard this exact session's Paster.swift/PasteboardReader.swift changes deliberately moved away from elsewhere — inconsistent but low-risk (private serial queue, no concurrent access), recommend follow-up not a blocker. No blocking defects found. → tester (no board edit; spawned).
- 2026-09-04 [second-reviewer-pass, item 1/5] VisionTextRecognizer.swift ImageIO swap — INDEPENDENTLY VERIFIED, not taken on faith. Read the file in full (279 lines). Continuation-resume audit: `recognizeText(in:quality:)` has 4 total `continuation.resume` call sites, all mutually exclusive — (a) `decodedCGImage` guard-fail -> resume(nil)+return, (b) pixel-dimension guard-fail -> resume(nil)+return, (c)/(d) inside `performVisionRequest`'s `VNRecognizeTextRequest` completion handler (resume on error-or-cast-fail, resume on success) XOR its `catch` block if `handler.perform` throws before invoking the completion handler at all — since `perform(_:)` either runs the completion handler synchronously before returning or throws instead of running it, (c) and (d) can never both fire. No double-resume, no zero-resume path found. `import AppKit` confirmed absent (grep); remaining imports (CoreGraphics/Foundation/ImageIO/Vision/os) are exactly what the file's own code uses — no silent behavior change from the drop (NSImage was only ever used inside the now-replaced decode function). Frame-0 selection matches `Paster.normalizedToTIFF`'s existing precedent (`CGImageSourceCreateImageAtIndex(source, 0, nil)`, same call shape, same doc-comment justification).
  Went further than a code read: independently reproduced the "byte-identical before/after" claim myself rather than trusting junior-dev's log entry, using MY OWN synthetic fixtures (not the user's real blob — kept that store untouched) since the pre-fix NSImage code no longer exists in the tree to diff against. Wrote a throwaway script (`/private/tmp/.../scratchpad/verify_decode.swift`, not part of the repo, deleted after use) that hand-reconstructs the old `NSImage(data:) + cgImage(forProposedRect:)` path verbatim next to the current `decodedCGImage(from:)`, and compares: (1) a single-frame PNG with real rendered text — both paths produced pixel-identical CGImage buffers (raw RGBA byte comparison, sha256 match) AND identical Vision `.accurate` OCR text output (sha256 `2de7c42...`, len 31, both runs); (2) a synthesized multi-representation TIFF (two embedded reps, different sizes) as a targeted edge case for "could NSImage's best-representation-for-rect heuristic ever disagree with ImageIO's always-frame-0 contract" — in this construction both picked the same (800x480) frame and produced identical OCR text, though this is not an exhaustive proof no multi-rep TIFF could ever disagree, just a real, executed edge-case check the prior review didn't attempt. For the realistic input shape production actually sees (single-frame screenshot/pasteboard image), verified byte-identical. VERDICT: item 1 clears, matches the claimed finding, no discrepancy with the prior review.
- 2026-09-04 [second-reviewer-pass, item 2/5] Full line-by-line read of ItemRow.swift (296 lines), ItemPreview.swift (286 lines), ShortcutHints.swift (198 lines) — none read by the prior reviewer. ItemRow.swift: diff is small/contained (`hasRecognizedText`/`supportsSaveAsSnippet` promoted to `ClipItem`, new OCR badge overlay + "Copy Recognized Text" context-menu item); confirmed via `git diff --cached` that `ItemIconThumbnail.load()`'s pre-existing off-main `NSImage(data:)` call (line 275) is untouched by today's diff (matches senior-dev.md's own "confirmed pre-existing, left alone" note — not a new regression). ItemPreview.swift: OCR text section correctly appended below the image, reuses `TextPreview` (no duplicate long-text renderer), diff is minimal/contained. ShortcutHints.swift: well-designed pure function + value-type (`HighlightedItemCapabilities`), genuinely well-tested (286-line test file). Cross-checked its claims against the real implementations rather than trusting the doc comment: `PickerViewModel+Paste.pasteContent(for:plainText:)`'s `plainText` branch confirmed to return `.text(item.previewText)` verbatim for `.text`/`.link`/`.richText` (matches the "⌥⏎ identical to ⏎" claim) and gate `.image`'s OCR-paste on non-empty `ocrText` (matches the `.ocrText` hint case); `presentSaveAsSnippetForm(from:)` confirmed to itself re-check `item.supportsSaveAsSnippet` (true single source, not just UI-level gating). No blocking issues in any of the three files.
- 2026-09-04 [second-reviewer-pass, item 4/5 — single-sourcing by READING, not grepping] Found what a grep for `hasRecognizedText` would miss: `ClipItem.hasRecognizedText` (`kind == .image && !(ocrText ?? "").isEmpty`) is the declared single source (Sources/ClipnestCore/Model/ClipItemOCR.swift:20-22) and IS correctly called by ItemRow.swift:116 and ShortcutHints.swift:145 — but THREE other call sites re-derive the semantically-identical condition inline instead of calling the property: (1) `ItemPreview.swift:83` — `if let ocrText = item.ocrText, !ocrText.isEmpty`; (2) `PickerViewModel+Paste.swift:127` (`pasteContent`'s plainText branch) — `if let ocrText = item.ocrText, !ocrText.isEmpty`; (3) `PickerViewModel+Paste.swift:217` (`copyRecognizedText`) — `guard let ocrText = item.ocrText, !ocrText.isEmpty else { return }`. All three are inside a `case .image:`/an already-`.image`-narrowed context, so `kind == .image` is guaranteed true at each site and the inline check is BEHAVIORALLY equivalent to `hasRecognizedText` today — not a functional bug, and #3's independent guard is a defensible defense-in-depth choice (guards against a hypothetical future caller bug rather than trusting `ItemRow`'s gating). But it is real, if low-risk, logic duplication of exactly the kind coding-standards.md's DRY rule targets: if `hasRecognizedText`'s definition ever changes (e.g. a minimum-length threshold, trimming whitespace-only OCR noise), these three sites would silently NOT pick up the change while `ItemRow`/`ShortcutHints` would. Same read-don't-grep technique found `supportsSaveAsSnippet` is, by contrast, genuinely single-sourced everywhere including the actual gating logic (`PickerViewModel.presentSaveAsSnippetForm` re-checks the Core property itself, not just the UI). FINDING (Minor/nit, non-blocking): `ItemPreview.swift:83`, `PickerViewModel+Paste.swift:127`, `PickerViewModel+Paste.swift:217` — replace `if let ocrText = item.ocrText, !ocrText.isEmpty` with `if item.hasRecognizedText, let ocrText = item.ocrText` (or equivalent) to close the gap; not blocking since current behavior is correct, but flagging since the prior review's grep-based "confirmed single-sourced" claim for this specific predicate was incomplete.
- 2026-09-04 [second-reviewer-pass, item 3/5] SettingsActivator.swift (204 lines) read in full + SettingsActivatorTests.swift (155 lines, 5 tests) read in full. Leak analysis for `.regular`->`.accessory` restore: (a) rapid open/close/reopen cycles — genuinely tested (`reopenAfterCloseRunsFullSequenceAgain`) and correct: `restoreAccessory()` (called from the one-shot close observer) resets `isTemporarilyRegular` before the next `activate(observing:)` call can run, so a fresh open always re-arms the full sequence, never leaves it permanently `.regular` nor double-toggles. (b) two rapid clicks while already open — tested (`secondActivateWhileOpenReactivatesWithoutRetoggling`): policy flip + observer registration are correctly idempotent (guarded by `isTemporarilyRegular`), `activateApp()` intentionally still fires every call (documented real-bug fix, not a leak vector — force-activating an already-active app is a no-op). (c) observer-never-fires risk — the observer is `NSWindow.willCloseNotification`, which fires for both the standard title-bar close button and Cmd+W (`performClose:`); grepped Settings/App sources for any `orderOut`/direct window manipulation that would bypass `.close()` and skip the notification — none found, so nothing in this diff's own code can cause the observer to never fire. If the app quits with Settings still open, the process exits entirely — `.regular` state doesn't outlive the process, not a real leak either. (d) one real, untested edge case identified by direct reading (not exercised by any of the 5 tests, all of which reuse the SAME window object across calls): `SettingsFocusCoordinator.settingsWindow()`'s fallback path (`NSApp.windows.first { $0.isVisible && !($0 is NSPanel) && !($0 is SnippetEditorWindow) }`, used only if the primary frame-autosave-name match fails) could theoretically hand `activate(observing:)` a DIFFERENT window instance on a later call while `isTemporarilyRegular` is already `true` (no intervening close) — since the idempotency guard skips re-registering the close observer in that state, nothing would ever observe the NEW window's close, and if the ORIGINAL observed window then never closes (or already had), `.accessory` could fail to restore. This requires the primary match to already be broken (a documented future-SwiftUI-rename contingency, not today's normal path) AND a second distinct visible window to appear meanwhile — a low-probability compound edge case, not exercised by any test, worth a follow-up test/hardening note but NOT blocking given SwiftUI's Settings scene reuses one persistent window instance in the primary (non-fallback) path that this app actually exercises today. VERDICT: no realistic leak found in the paths this diff's own code and tests actually exercise; one narrow, pre-existing-fallback-only theoretical gap flagged as a nit for a future test.
- 2026-09-04 [second-reviewer-pass, item 5/5 — glyph-classifier opinion] Independently re-confirmed isolation (not re-trusting the prior review): grepped both root `Package.swift` and `ClipnestApp/project.yml` for `tools/` — zero references either direction; read `tools/glyph-classifier/Package.swift`'s own header, which documents the CreateML-must-never-link-into-shipping-app reasoning directly. My opinion: this should NOT stay permanently on `main` as first-class tracked source. It's a well-isolated, well-documented, ~1,453-line dead end for a feature with no current or planned forward path (failed its own accuracy gate, per the brief) — keeping ~1,453 lines of Core-ML training/dataset-gen code "temporarily" in `main` is exactly the kind of thing that quietly becomes permanent scope creep future readers have to understand is inert every time they browse `tools/`. Recommend: archive it to a dedicated branch (e.g. `spike/glyph-classifier`) or a single summary paragraph in a design-decisions doc citing the branch/commit, then remove the source tree from `main`. This is a repo-hygiene/architecture call, not a code defect — not blocking this commit either way (it doesn't touch the shipping build, doesn't fail any gate, is correctly gitignored for artifacts), but I'd escalate the "does it stay on main" question to architect rather than let it default to "yes" by inertia.
- 2026-09-04 [second-reviewer-pass, FINAL] Verdict: **COMMIT**. Did not re-run `swift test`/`xcodebuild test`/`swift format lint` myself per the coordinator's instruction (already run just now on the current staged tree: 239/17 suites PASS, 144/18 suites TEST SUCCEEDED, lint clean) — spent the full budget on independent reading + one hands-on empirical check instead. Findings, all non-blocking: (1) Minor/nit — `hasRecognizedText`'s predicate is re-derived inline (not called) at 3 sites (`ItemPreview.swift:83`, `PickerViewModel+Paste.swift:127,217`) instead of reusing `ClipItem.hasRecognizedText` — behaviorally correct today (each site is already kind-narrowed to `.image`), but real duplication a future edit to the predicate wouldn't propagate to; this is the one place I disagree with the prior review's "confirmed single-sourced" claim for this specific property — its grep for the property NAME couldn't see these 3 sites, which express the same rule without naming it. (2) nit (already flagged by the prior review, independently re-confirmed via a different method) — `VisionTextRecognizer`'s `decodedCGImage` swap to ImageIO is correct: verified continuation-resume-exactly-once by direct read, and independently reproduced the byte-identical-output claim myself with fresh synthetic fixtures (single-frame PNG: pixel-identical + identical Vision OCR text, sha256-matched; a constructed multi-representation TIFF edge case also agreed between old/new decode paths) rather than trusting junior-dev.md's log entry on the user's real blob. (3) `SettingsActivator`'s restore-on-close: no realistic leak in the paths this diff exercises (rapid open/close/reopen and double-click-while-open are both tested and correct); one narrow, untested, fallback-path-only theoretical gap noted for a future test, not blocking. Commit hygiene: independently confirmed (in addition to the coordinator's own scan) no untracked files sit outside `.gitignore` coverage (`git status --short --ignored=matching`), `.claude/logs/stress-artifacts/` (22 files) and `release-cert/` are both correctly ignored and unstaged, no secret-shaped strings found in any non-`.claude/logs` staged file (only doc prose discussing the absence of secrets / placeholder credential examples). Agree with the prior review everywhere else I re-checked (P0 hang fix soundness, security-pass clearance, migration safety, tools/ build isolation). Did NOT re-cover: the full `.claude/task-board.md`/`project-context.md`/`README.md` prose diffs (skimmed only, no factual claims independently re-verified beyond what's cited above), the OCRBackfillCoordinator/OCRBackfillViewModel UI wiring end-to-end, or any manual on-device interaction (per the task's own instruction not to). Nothing stripped from the commit — glyph-classifier's "stay on main vs. archive to a branch" is a repo-hygiene call for architect, not a defect blocking this commit.
- 2026-09-04 [perf-remediation batch: T-PF1..T-PF4, independent review] Read every changed line via `git diff --cached` (24 files, +1793/-187) + full `SwiftDataClipStore.swift`/`ClipboardMonitor.swift`/`AppEnvironment.swift`/`AppDelegate.swift`/`BlobStore.swift`/`RowThumbnailLoadLimiter.swift`/`ImageThumbnailDecoder.swift` reads; pulled `detect_changes_tool` (risk 0.40, 81 changed fns) for triage. Independently built+tested Core (`swift build` clean; targeted `swift test` on SwiftDataClipStore/SwiftDataSnippetStore/ClipboardMonitor/PasteboardReader/BlobStore suites = 136/5 passed) and ran `swift format lint --recursive --strict` on Sources/Tests/ClipnestApp/Sources (clean) and ClipnestApp/Tests (only the pre-existing tracked T-BUG2 HotkeyManagerTests.swift:25 failure, nothing new) — did not re-run the central full `swift test`/`xcodebuild` per instructions. MANDATORY SECURITY PASS: PASS, no blocking findings — grepped diff for URLSession/sockets (none, only comment prose), confirmed `PrivacyFilter.shouldCapture`'s concealed/transient check in `ClipboardMonitor.checkNow()` runs before and independently of T-PF2's new size/pixel ceiling (ceiling cannot bypass the marker rule), confirmed no clipboard content is logged in any new `logger.error`/`.fault` call (metadata/error-description only), verified `curlArguments` passes argv as array elements (no shell injection), and independently verified BlobStore's `.mappedIfSafe` argument (write() never overwrites an existing content-addressed path — early-returns if it exists — so a mapped read is never truncated in place; delete-while-mapped is standard POSIX-safe unlink semantics) — concur it's correct. T-PF2's new capture-time ceilings are themselves a security-positive (bounds a decompression-bomb-shaped/oversized untrusted-input DoS vector). VERDICT PER TASK: T-PF2 PASS, T-PF4 PASS (verified `fetchLatestReleaseJSON`'s continuation is resumed exactly once on all 3 paths — no leak/double-resume). T-PF1 REJECT (blocking, narrow): `SwiftDataClipStore.prepare()`/`SwiftDataSnippetStore.prepare()` (SwiftDataClipStore.swift:199-203, mirrored in SwiftDataSnippetStore.swift) call `markBackfillComplete` unconditionally after `backfillNormalizedText`, but that method swallows its own fetch (line 267-274) and save (281-287) failures internally without throwing — a transient I/O error during the one-shot backfill permanently marks it "complete" via the UserDefaults marker with zero retry path, silently leaving affected legacy rows unsearchable forever. Untested (no failure-path test exists). Fix: gate `markBackfillComplete` on `backfillNormalizedText` actually succeeding. Same bug + its surrounding marker plumbing (`backfillCompleteDefaultsKeyPrefix`/`backfillCompleteDefaultsKey`/`hasCompletedBackfill`/`markBackfillComplete`) is duplicated near-verbatim between the two store files — meets coding-standards.md's own "extract at 2nd duplicate" bar inside this same diff; recommend extracting one shared helper so the fix lands once. T-PF3 REJECT (blocking, narrow): `RowThumbnailLoadLimiter.acquire()` (RowThumbnailLoadLimiter.swift:55-63) uses `withCheckedContinuation`, which does not observe `Task` cancellation — a row's `.task(id:)` cancelled while parked in `waiters` (fast scroll, row recycled) stays queued in FIFO order and, once eventually granted a permit, still runs the full read+decode for a row nobody can see. Under sustained fast scrolling — the exact scenario this fix targets — stale/off-screen waiters queued ahead of newly-visible rows can delay the images the user is actually looking at; not a deadlock (permits keep circulating) but real wasted I/O/CPU and possible starvation, untested and undocumented (unlike `scheduleRetentionEnforcement`'s explicitly-documented debounce tradeoff). Recommend checking `Task.isCancelled` post-acquire (skip `body()` if cancelled) or `withTaskCancellationHandler`, or explicitly document+measure as an accepted tradeoff. Both rejects are narrow/cheap fixes; rest of each task's design (async init + concurrent container setup + fetchCount-windowed retention + debounce for T-PF1; ImageIO thumbnail decode + split-cost NSCache + NSHostingController reuse + analytic sizing for T-PF3) is sound, well-tested, and approved in spirit. CROSS-CUTTING: `import ImageIO` (ImageThumbnailDecoder.swift + its test) is not on coding-standards.md's enumerated system-framework allowlist — technically blocking per that doc's own "anything not on it is a rejection, not a nit," though ImageIO is an obviously benign bundled Apple framework and the correct/only API for this decode; fastest fix is a 1-line architect addition to the allowlist, not a rework. Checked the explicitly-flagged DRY suspects: `ImageThumbnailDecoder.decode` vs `VisionTextRecognizer.downscaled` — NOT a duplicate (different techniques: direct-from-Data bounded decode vs. post-decode CGImage downscale for Vision; conflating them would defeat T-PF3's whole point). Nits (non-blocking): docs/API.md:246 still says image priority is `.tiff`/`.png` (now `.png`/`.tiff`) and doesn't mention the new `maxCapturedImageByteSize`/`maxCapturedImagePixelDimension` public constants or `classify(_:) -> Classification?`'s signature change; `PasteboardReader.maxCapturedImageByteSize`/`maxCapturedImagePixelDimension` (50_000_000/20_000) duplicate `VisionTextRecognizer.maxByteSize`/`maxPixelDimension`'s identical values as two independently-named constants with no shared source of truth; T-PF1's `delete(id:)`/`clearHistory()` still delete blobs synchronously in-actor (only `enforceRetention` was moved off-actor) — likely an intentional scope call (lower-frequency explicit user actions) but worth confirming; no unit test directly proves T-PF1's core concurrency claim ("a `query()` landing during blob cleanup is no longer stuck behind it") — recommend one using a fake delayed `BlobStore.delete`. DECISION TO RECORD in project-context.md: T-PF2's PNG-preferred capture changes `contentHash` for any image offering both `.png`/`.tiff` — a user re-copying pre-upgrade content once, post-upgrade, gets a one-time duplicate history entry instead of a dedup-bump (self-healing after that first re-copy); senior-dev flagged this explicitly as an accepted, not-fixed-here product tradeoff, worth a permanent record rather than only living in a log line. → returning REJECT (T-PF1, T-PF3) / PASS (T-PF2, T-PF4) + full findings to spawner; not touching task-board.md or production code.
- 2026-09-04 [round 2: T-PF1/T-PF3 refix + T-PF5a/b/e/c] Independently re-verified everything (not on faith): `swift test` (my own run, scratch-path) → 282/20 PASS matching claim; `swift format lint --recursive --strict` on Sources+Tests and ClipnestApp/Sources → clean both; `xcodegen generate` + `xcodebuild ClipnestApp build` (own scratch DerivedData) → BUILD SUCCEEDED, confirms the App-target integration seam (ImageThumbnailDecoder/ItemRow/RowThumbnailLoadLimiter/ItemPreview*) compiles against today's Core changes. Read every changed line via `git diff --cached` across all 30 files; ran `detect_changes_tool` (risk 0.50, flags AppDelegate/AppEnvironment.init as untested — expected, matches this codebase's established "App-layer async orchestration is build+lint+trace verified, no App test target for it" precedent, D18-D22/T43).
  T-PF1 (normalizedText backfill marker): PASS. `OneShotStoreMigration.run` extraction is clean DRY; "nothing to backfill" correctly returns `true` (genuine success) not `false`, so an empty legacy store is never mistaken for a failed pass. `completionKey` = `keyPrefix + configuration.url.path` — verified both stores use fully distinct keyPrefix literals (`SwiftDataClipStore.normalizedTextBackfillComplete.` / `SwiftDataSnippetStore.normalizedTextBackfillComplete.` / `SwiftDataClipStore.imageContentHashBackfillComplete.`), so two migrations on the same store file and the same migration-shape on two different store files are both independently gated — directly proven by `OneShotStoreMigrationTests.markerIsIsolatedPerStoreFilePath`/`differentKeyPrefixesOnSameFileAreIndependent`, which I read and consider non-vacuous (real on-disk temp files, real distinct fakes).
  T-PF3 (RowThumbnailLoadLimiter cancellation): PASS. Traced permit accounting on all 4 asked-for paths: cancelled-while-waiting (cancelWaiter removes from FIFO, resumes false, `withPermit` returns nil, body never runs, permit never touched — proven by `cancelledWaiterNeverRunsBody`), cancelled-after-grant (cancelWaiter no-ops since already dequeued, body runs to completion, permit released normally — matches doc comment), normal completion (balanced), and the concurrent cancel/release race (actor serialization makes the two outcomes mutually exclusive per-waiter, argued correctly in the doc comment and exercised by `permitsFullyRestoredAfterMixedCancellationsAndCompletions`). One very narrow, non-blocking residual: if `Task.cancel()` lands in the sub-microsecond window between `acquire()`'s early `Task.isCancelled` check and entering `withTaskCancellationHandler`, `onCancel` can fire (no-op, waiter not yet appended) before `operation` appends the waiter — the waiter then sits in the FIFO and is eventually granted `true` despite being cancelled. This does NOT leak or double-release a permit (fully accounted for either way), it's the same class of "narrowly-lost cancellation race" the file's own doc comment already discloses as acceptable elsewhere — noting as a nit, not blocking. `ItemRow`'s `DecodedThumbnail??` unwrap verified correct: outer nil (cancelled-while-queued) and inner nil (permit granted, load ran, produced nothing — missing blob/deleted file/undecodable bytes) are two separate, correctly-named guards; a real load failure is never misreported as a cancellation.
  T-PF5a (CoreGraphicsImagePixelHasher): PASS. Chunk-boundary math verified correct (not just "the sizes it happens to try") — tests include the single-chunk case, uneven-division case, wide/short case, AND the extreme one-row-per-chunk case, each checked against an independently-written naive reference. Canonicalization verified sufficient for realistic clipboard-capture inputs: alpha/premultiplication (`.copy` blend mode prevents stale-buffer bleed-through — correctly reasoned and specifically why `.copy` was chosen over the default composite), gray content, true sRGB vs. Display P3 color-profile equivalence (tested with an achromatic fixed-point construction to avoid rounding flakiness — a genuinely careful test design). Two real, non-blocking gaps found (both in the "missed dedup," never "false collision," direction — consistent with D44's explicit safety bias): (1) EXIF/TIFF orientation is not normalized — `CGImageSourceCreateImageAtIndex` is called with no orientation option, so the raw sensor-orientation pixel grid is hashed, while `ImageThumbnailDecoder.swift` (same batch, `kCGImageSourceCreateThumbnailWithTransform: true`) explicitly DOES bake in orientation for display — an undocumented cross-file inconsistency; a same-picture re-copy with a different EXIF orientation tag would fail to dedup (safe direction) but the hasher's own doc-comment claim ("makes two containers of the SAME picture hash identically... in all realistic cases") should be caveated. (2) Premultiplied-alpha canonicalization means two images differing only in the RGB values of fully-transparent (alpha=0) pixels hash identically — an inherent property of the premultiplied-alpha choice (itself the right choice for the alpha-bleed correctness reason documented), narrow real-world impact, but mildly contradicts the "not perceptual, ever" framing in D44 and is worth a one-line doc caveat. Neither blocks — recommend a follow-up doc/test task, not a rework.
  T-PF5b/e (PasteboardReader wiring): PASS. Verified `readImage` enforces `maxCapturedImageByteSize`/`maxCapturedImagePixelDimension` BEFORE `imageContentHash` is ever called, and `imageContentHash` checks `maxPixelHashPixelCount` BEFORE calling `pixelHasher` — a rejected/over-ceiling image can never reach the hasher (also proven by 3 call-counting fake tests: byte-ceiling, pixel-ceiling, megapixel-ceiling, all assert `hasher.callCount == 0`). Ceiling arithmetic checked: 40,000,000 px × 4 B/px = 160,000,000 B decoded × 2.1 measured multiplier = 336,000,000 B — the comment's "(~320 MB)" label is really MiB (336,000,000 / 1,048,576 ≈ 320.4), same unit mismatch pattern as the "~153 MiB" line just above it which IS correctly labeled — MB/MiB labeling nit only, the number and reasoning are right (nit, non-blocking). The modified `prefersPNGOverTIFFWhenBothOffered` test is legitimate, not weakened-to-green: it now independently constructs a SECOND `CoreGraphicsImagePixelHasher()` instance and asserts its output equals `PasteboardReader`'s result AND differs from both raw-byte hashes (PNG and TIFF) — a real integration-wiring proof ("the default init wires in the real hasher"), not a tautology.
  T-PF5c (image contentHash backfill) — LEAD ITEM: REJECT (blocking). Confirmed the concern is real and quantified it empirically, not just by reasoning: built a throwaway SPM harness (scratch-path, path-dependency on this repo, deleted after use) that calls the ACTUAL staged `CoreGraphicsImagePixelHasher.pixelContentHash(of:)` production code, release build, on synthetic-but-realistic screenshot/photo sizes — 1280x800: 10.1ms; 2560x1600 (retina screenshot): 28.0ms; 2880x1800: 35.7ms; 4032x3024 (12MP phone photo): 81.8ms (decode+canonicalize+hash only, excludes blob disk-read). Marginal cost converges to ~6.8µs/MP for larger images. At a realistic mixed-history rate of ~15-80ms/image depending on composition: 100 images ≈ 1.5-8s, 500 images ≈ 7.5-41s, 1000 images (the default retention cap) ≈ 15-82s of stall, before any real disk-I/O overhead on a cold cache. `backfillImageContentHashes` is synchronous (confirmed: `OneShotStoreMigration.run`'s `migration` param is non-async, `prepare()` never awaits it), so this ENTIRE scan runs as one uninterrupted turn on the `SwiftDataClipStore` actor — and `AppEnvironment.init` (confirmed via full read) does not assign `self.clipStore`/`self.snippetStore` until BOTH stores' `prepare()` calls return, and `AppDelegate.launchEnvironment()` calls `registerHotkey()`/`startCapture()` only after that assignment. So for the ENTIRE stall window: the global hotkey is not registered at all (silently does nothing if pressed), the menu bar's "Open Clipnest" is `environment?.showPicker()` (silent no-op), and the Settings window shows only "Starting Clipnest…" — zero progress feedback, indistinguishable from a hung/broken app. This is the exact launch-stall bug CLASS T-PF1 was built to fix (its own board line: "Launch: async store construction off main; one-shot normalizedText backfill..."), reintroduced by a much more expensive per-row operation (decode+hash every image blob) riding the same "one big synchronous pass gates app readiness" shape T-PF1 explicitly moved away from for the cheap field-only backfill. Made worse by persistence shape: `modelContext.save()` is called exactly ONCE, after the WHOLE loop — so a user who force-quits an apparently-frozen app mid-scan (a highly plausible reaction) loses ALL progress and retries the entire scan from zero on next launch; a power user who habitually force-quits could never get past this. Verified the one positive: memory does NOT stack across the loop — `blobData`/decode buffers are loop-local and released via ARC each iteration, so T-PF5a's measured 2.0-2.1x-of-decoded-size peak is the effective ceiling for the whole migration, not cumulative; this is a real, correctly-engineered property, just orthogonal to the wall-clock/availability problem. Failure-split logic itself (missing blob/undecodable = permanent+marker-set, other ioFailure = transient+marker-unset+retry) is correct in isolation and consistent state (SwiftData's `save()` is all-or-nothing, no torn writes) — the defect is entirely in WHEN/HOW this runs, not in its per-row error handling. Recommended shape, in order of fit: (a) best — mirror the already-existing, directly-analogous precedent in this SAME codebase, `OCRBackfillCoordinator` (Sources/ClipnestCore/OCR/OCRBackfillCoordinator.swift): user-triggered or lazily-scheduled AFTER the app is already usable (not gating `AppEnvironment` readiness), cancellable, per-item progress persisted via its own store write (not one giant batched save), so an interruption never discards all progress; (b) acceptable — keep it a launch-time pass but move it off `AppEnvironment.init`'s critical path (run it in a `Task` fired AFTER `clipStore` is assigned and hotkey/capture are live, with incremental per-batch saves so a mid-scan quit doesn't lose everything); (c) if it must stay synchronous-closure-shaped inside `OneShotStoreMigration`, that alone does not fix the availability-gating problem and is insufficient by itself. → REJECT to senior-dev for T-PF5c specifically; T-PF1/T-PF3/T-PF5a/T-PF5b/T-PF5e all PASS and should proceed once T-PF5c is addressed (they don't depend on T-PF5c's fix).
  Cross-cutting duplication hunt (beyond the already-tracked T-PF6 50_000_000/20_000 dup): the "4 bytes/pixel RGBA decoded-cost" constant is independently duplicated 3x — `CoreGraphicsImagePixelHasher.canonicalBytesPerPixel`, `ImageThumbnailDecoder.bytesPerPixelEstimate`, `ItemRow.ItemIconThumbnail.approximateByteCost`'s inline `* 4` — each well-named/documented in isolation but the same real constant; fold into T-PF6's scope (or a sibling) rather than a new task. `docs/API.md:246` (already tracked under T-PF6) should also now cover `maxPixelHashPixelCount` and the `.image`-only pixel-hash `contentHash` change (D44/D45) since T-PF6 predates T-PF5. Every new `import` checked against coding-standards.md's allowlist (`ImageIO`/`CoreGraphics`/`CryptoKit`/`SwiftData` only) — all approved, `ImageIO` addition from round 1 confirmed present at coding-standards.md:75.
  SECURITY VERDICT: CLEAR (mandatory pass run per integrity rule 3 — capture/storage/user-input touched). Grepped full staged diff for URLSession/sockets/secrets/keys/tokens — zero hits beyond the pre-existing, unchanged GitHub releases URL (curl subprocess, not URLSession, matches established pattern). `PrivacyFilter`/concealed-transient gate ordering in `ClipboardMonitor.checkNow()` unchanged by this diff (confirmed via the actual diff — only `classify`'s new `Optional` return is threaded through). Every new `logger.error` call site read — metadata-only (error descriptions, "missing"/"did not decode", byte counts), never `previewText`/`ocrText`/blob bytes. New capture-time ceilings (`maxCapturedImageByteSize`/`maxCapturedImagePixelDimension`/`maxPixelHashPixelCount`) are a net security positive — they bound decode/hash cost against a decompression-bomb-shaped image from any source app, closing a DoS vector. No force-unwrap/`try!`/`as!` introduced in production code (grepped touched files). `BlobStore.read`'s `.mappedIfSafe` change (T-PF4, re-confirmed) — lifetime-safety doc comment enumerates every read call site including the new `backfillImageContentHashes`, correctly reasoned (no caller retains `Data` across a `delete`).
  → REJECT (T-PF5c only, blocking) to senior-dev with the recommended shape above; T-PF1/T-PF3/T-PF5a/T-PF5b/T-PF5e PASS. Did not edit production code or task-board.md.
- 2026-09-05 [full-batch final pass: T-PF5c restructure + T-PF8/T-BUG1/T-BUG2/T-PERF4/T-OCR6/T-HANG5/T-HANG4] Independently rebuilt (`swift build` clean) and ran targeted suites myself (`ClipboardMonitorTests|SwiftDataClipStoreTests|ImageContentHashBackfillCoordinatorTests|OneShotStoreMigrationTests|BlobStoreTests|ProductionStoreIsolationTests|PasterTests` = 140/7 suites PASS; CoreData corrupt-store stderr spam is the known-expected recovery-test noise). Read every file in the 51-file diff via `git diff HEAD`; used `detect_changes_tool` (risk 0.55) to confirm nothing outside the reviewed set changed.
  T-PF5c (restructure): PASS. `ImageContentHashBackfillCoordinator` mirrors `OCRBackfillCoordinator` correctly — `prepare()` only schedules (fire-and-forget `Task`), never awaits; per-row `ClipItemRecord.pixelContentHashMigrated: Bool = false` is additive+defaulted exactly like the `normalizedText` precedent (needed since it's read inside a `#Predicate` scan) — migration-safe, verified by direct read of `SwiftDataClipStore.swift:1032-1183`. `markImageContentHashBackfillComplete()` fires ONLY on `.completedCleanly` (verified in code + by `SwiftDataClipStoreTests.swift`'s cancel/transient-failure tests, which assert the marker stays unset and a resumed run redoes nothing already done — non-vacuous, real UserDefaults keys, real temp stores). `waitForImageContentHashBackfillForTesting()`/`cancelImageContentHashBackfillForTesting()` are `public` but mirror this file's own pre-existing `makeContainerForTesting`/`insertRecordWithEmptyNormalizedTextForTesting` convention (not a new pattern introduced here) — nit, not blocking. Known gap (declared by the task, independently confirmed via `PasteboardReader.swift:360-364`): a row whose capture-time pixel hash was skipped for being over `maxPixelHashPixelCount` (40MP, decodable) is marked `pixelContentHashMigrated: true` at insert and never revisited, even though the backfill itself has no such ceiling and could resolve it later — permanently stuck on the raw-byte hash for that one item. Narrow (screenshots/typical photos are far under 40MP; only huge decodable originals, e.g. 48MP phone-camera shots, hit it) but the doc comment at `SwiftDataClipStore.swift:1085-1090` overclaims ("already the decoded-pixel hash... an already-accepted assumption") — true for the common path, false for this one. Nit: tighten that comment. Undecodable-at-capture rows are fine either way (backfill would hit the same failure and produce the identical end state).
  T-PF8 (`CLIPNEST_TEST_DATA_ROOT`): CORRECTNESS PASS, SECURITY BLOCKING. Fixes a real, serious pre-existing bug (`xcodebuild test` was writing/migrating the user's live `~/Library/Application Support/Clipnest` on every run — confirmed via `ProductionStoreIsolationTests.swift`, which mutates the real process env and proves the real store's mtime is untouched while the override path gets written). BUT: `BlobStore.defaultBaseDirectory(fileManager:environment:)` (`BlobStore.swift:97-113`) honors the override completely unconditionally — no `#if DEBUG` gate, no second signal (e.g. `XCTestConfigurationFilePath` co-presence), no validation of the value — and it is read by `AppEnvironment.init` in EVERY build configuration including a notarized Release `.dmg`. This directly matches `.claude/skills/security-review/SKILL.md`'s hard-trigger checklist item ("path traversal: any file path derived from input is normalized and jailed") — it is neither. `project.yml` only sets this var in the Xcode scheme's `Test` action (`/tmp/clipnest-test-data-root`, project.yml:153), so the sanctioned path is narrow, but the runtime code itself has zero structural defense against the var being present another way (a stray shell export inherited via `open`, `launchctl setenv`, a malicious LaunchAgent) — for an app whose own coding-standards.md calls clipboard content "first-class" sensitive, a silent, unauthenticated, unvalidated full-storage-redirect env var reachable in the shipped Release binary is a real, if narrow, gap, and it's cheap to close (gate to `#if DEBUG`, which this project's Release archive build would compile out entirely). Recommend fixing before `done`, does not require reworking the rest of T-PF8 (the redirect mechanism/wiring itself is correct and necessary).
  T-BUG1: PASS. `PickerView.returnAction(for:)` (PickerView.swift:296-309) extracted cleanly; `PickerViewTests.swift` exactly covers the modifier matrix (plain/incidental-flags/⌥/⌘/⌃/⇧/⌘⇧) with real, non-vacuous assertions; `.option` branch checked first so ⌥+anything still selects plain text unchanged from before (old code's `where press.modifiers.contains(.option)` had the identical "any other modifier along for the ride is ignored" shape).
  T-BUG2: PASS. Trivial line-wrap; re-ran `swift format lint --strict` on the file myself — clean.
  T-PERF4: PASS. `Paster.swift` confirmed untouched (`git status`/`grep Task.detached` both show the `.image` case's offload intact) and the deleted test's replacement comment (PasterTests.swift:219-267) accurately explains why the old assertion was structurally unfalsifiable (SE-0338 nonisolated-async actor-hop) without overclaiming what's still covered.
  T-OCR6: PASS. `scheduleTextRecognition`'s store-write-failure path now calls the injected `captureFailureHandler` instead of the hardcoded static logger (ClipboardMonitor.swift ~537) — matches every other failure path in the file; test asserts the injected handler receives the real thrown error.
  T-HANG5: PASS (with the already-declared, tracked-separately T-HANG6 gap). `pendingRecognitionTasks` dictionary is MainActor-isolated, written synchronously on schedule, removed via a correctly-constructed `Task { @MainActor in ... }` hop from inside the detached job's `defer` (can't mutate MainActor state directly from a nonisolated context — this is the right pattern). Two-jobs-same-id edge case (rapid duplicate paste bumping the same row) is explicitly documented as "stale removal is a no-op, never cancels the wrong job" — verified true by reading the dictionary-keyed logic. Tests use cancel-before-release ordering (not sleeps) for determinism — non-vacuous.
  T-HANG4: REJECT — BLOCKING, confirmed by direct empirical repro (throwaway SPM harness against the real `BlobStore`, deleted after use). `ClipboardMonitor.checkNow()`'s new second `ignoredChangeCount` check (ClipboardMonitor.swift:423-441) does a "best-effort" `blobStore.delete(blobPath:)` on bail, on the theory a blob written earlier in this same cycle is now orphaned (comment: "a blob orphaned by this rare bail is a disk-space leak, not a correctness issue"). That's false: `BlobStore` is purely content-addressed with no reference counting (`BlobStore.swift:136-151`, `write()` returns the EXISTING path without creating a new file whenever the bytes already exist on disk) — this bail path fires specifically for a SELF-PASTE race, i.e. the bytes being (re-)classified this cycle are, by construction, an existing history item's own content written back to the pasteboard. For `.richText`, `Paster.writeRichText`/`PasteboardReader.readRichText` round-trip the stored RTF bytes byte-for-byte unmodified (Paster.swift:308-309, PasteboardReader.swift:260) — so `blobStore.write(rawData)` in this `checkNow()` cycle deterministically returns the SAME blobPath the live, currently-visible item already references, and the bail-path delete removes that shared file out from under it: a rare timing race silently corrupts a DIFFERENT, unrelated, already-successful history item (its future "paste rich" falls back to plain previewText, matching the existing "no blob" degradation path) rather than merely creating a harmless duplicate row — a strictly worse outcome than the bug this exists to fix. Empirically reproduced end-to-end (write same bytes twice -> same path; delete that path -> original blob now throws `.notFound`) in a throwaway `swift run` harness against the real, unmodified `BlobStore` production code, not just reasoned about. Two compounding gaps: (1) `tools/stress-harness/.../SelfPasteRaceScenario.swift`'s "11/780 -> 0/2600" evidence backing T-HANG4 sweeps only `.text`/`.image` — `.richText` (the deterministic-once-it-races case) is never exercised by that harness at all; (2) zero unit tests in `ClipboardMonitorTests.swift` exercise this new second-check/cleanup code path (grepped for "T-HANG4"/"ignoredChangeCount" — no hits outside the pre-existing top-of-function test). `.image` self-pastes are lower-risk in practice (Paster re-encodes to TIFF via `normalizedToTIFF` before writing, generally producing different raw bytes than a PNG-preferred original capture) but not provably collision-free for every case (e.g. an original TIFF-fallback capture). Recommend: don't delete unconditionally on bail — either drop the cleanup (the actual failure mode of a leaked blob is at worst "harmless, since BlobStore already dedups re-writes of the same bytes") or have `BlobStore.write` report whether it created a new file vs. reused an existing one, and only ever delete-on-bail in the former case.
  Cross-cutting: `RGBAPixelFormat.bytesPerPixel` confirmed genuinely single-sourced at all 3 sites (`CoreGraphicsImagePixelHasher.canonicalBytesPerPixel`, `ImageThumbnailDecoder.bytesPerPixelEstimate`, `ItemRow.approximateByteCost` all alias it, grepped for stray `* 4`, none found). `OneShotStoreMigration` vs. `ImageContentHashBackfillCoordinator` staying separate is deliberate and documented (the former's `migration` param is contractually sync `() -> Bool`, can't await) — not an accidental duplicate abstraction. `docs/API.md` now accurately covers PNG-before-TIFF order, the three new `PasteboardReader` ceilings, and a full `## Media` section for `ImagePixelHashing`/`CoreGraphicsImagePixelHasher` including both documented false-dedup caveats — closes the round-2 staleness nits; still doesn't mention `ImageContentHashBackfillCoordinator`/`CLIPNEST_TEST_DATA_ROOT`/the new cancellation methods (nit, docs/API.md already reads as a curated public-surface reference, not exhaustive). `AppEnvironment.init`/`AppDelegate.launchEnvironment` (T-PF1, previously reviewed) spot-checked for interaction damage with this round's changes — `prepare()` still awaited for both stores before `self.clipStore`/`self.snippetStore` assignment, consistent with T-PF5c's non-blocking claim. No new networking imports, no secrets, no force-unwrap/try!/as! in any touched production file (grepped the full diff). `UpdateChecker.fetchLatestReleaseJSON`'s continuation re-verified resumed exactly once on all 3 paths (unchanged from round-1 PASS).
  SECURITY VERDICT: mandatory pass (capture/storage/user-input/external-I-O all touched) — CLEARS except the one T-PF8 finding above (path-derived-from-unvalidated-input, no build-config gate), which is a real, if narrow, gap under this project's own security-review skill's hard-trigger checklist and should be closed before `done`, not carried as a nit. Concealed/transient/excluded-app privacy gate ordering in `checkNow()` reconfirmed unaffected by any change in this diff (both new checks sit downstream of `privacyFilter.shouldCapture`). No clipboard content logged in any touched `logger.*` call site (metadata/error-description/UUIDs only).
  OVERALL VERDICT: SHIP-WITH-CAVEATS. The large majority of this batch (T-PF5c restructure, T-BUG1, T-BUG2, T-PERF4, T-OCR6, T-HANG5, plus the previously-passed T-PF1/2/3/4/5a/5b/5e/6) is well-designed, thoroughly and non-vacuously tested, and a definite net improvement — reads as one coherent, disciplined effort despite the 12-agent parallel construction. Two items are BLOCKING and must return to senior-dev before this reaches tester: T-HANG4 (data-loss bug, confirmed by direct repro — highest priority, this is worse than the race it fixes) and T-PF8 (unconditional env-var storage redirect live in Release builds — cheap fix, gate to DEBUG or a second signal). Once those two are fixed, the rest does not need re-review. → REJECT (T-HANG4, T-PF8) / PASS (T-PF5c, T-BUG1, T-BUG2, T-PERF4, T-OCR6, T-HANG5) to senior-dev; not touching task-board.md or production code.
- 2026-09-06 [T-LX1/T-LX2/T-LX3/T-LX4, ad-hoc validation] Independently validated 4 claimed Linux runtime defects at the orchestrator's request (branch feat/linux-port), not a formal review of a submitted diff (senior-dev was live-editing the fix during this pass; did not touch any production file, task-board.md, or project-context.md). Read DBusConnection.swift, ClipnestControlService.swift (pre- and post-fix), SingleInstanceDecision.swift, LinuxAppLifecycle.swift, LinuxAppEnvironment.swift, Logging.swift, clipnest-ctl, StatusNotifierTray.swift, ATSPIFocusTracker.swift, ShellHelperClient.swift in full. Reproduced end-to-end: built the pre-fix .deb via packaging/linux/vnc/Dockerfile, ran it under Xvfb+openbox, read the app's real DBUS_SESSION_BUS_ADDRESS from /proc/<pid>/environ (avoided the second-dbus-launch trap), confirmed GetNameOwner=:1.1 but Ping/Introspect both hard-timeout (gdbus, 8s), confirmed /tmp/clipnest.log fully empty while the process was alive, confirmed wmctrl -l showed no Clipnest window, confirmed clipboard capture (xclip write -> SQLite WAL) kept working regardless — proving the app wasn't globally wedged, only the D-Bus control path. Then rebuilt against the current (mid-fix) tree and re-ran the identical checks: Ping now replies in ~17ms, Introspect now replies in ~25ms with a fast UnknownMethod error (not a hang), clipnest-ctl toggle-picker (correct env) forwards in 55ms and a new X11 window is mapped (wmctrl), a second toggle correctly unmaps it, and stderr now carries real INFO lines even piped through tee. Verdicts: T-LX1 CONFIRMED but reviewer's own hypothesis REFUTED — the DBusConnection is never actually touched by two threads at once (SingleInstance.acquire completes synchronously before the receive thread is even created); real root cause is a classic Thread{[weak self]} retain race: startControlService's `let service = ClipnestControlService(...)` had no owner after the function returned, so ARC deallocated it before pthread_create's new thread got scheduled, `self` was nil on first (and only) access, receiveLoop() never ran one iteration (LinuxAppLifecycle.swift's old startControlService + ClipnestControlService.swift:129-134 old start()) — independently confirmed root cause via repro, matches senior-dev's own in-flight fix (static `controlService` var, LinuxAppLifecycle.swift:81/166-182). T-LX2 CONFIRMED (Logging.swift's non-Apple info/debug were true no-ops, and old error() printed to stdout, which is fully block-buffered off a TTY — compounded with T-LX1 to fully explain zero output); fix (stderr + FileHandle.standardError raw write) verified working. T-LX3 CONFIRMED (clipnest-ctl's toggle-picker/expand-snippet did a bare `exec` with zero bound); flagging a NEW regression risk in the in-flight fix: wrapping those two subcommands in `timeout 10 --kill-after 5` will SIGTERM/SIGKILL a freshly cold-started resident `clipnest` instance if this invocation is the one that wins the single-instance race (reproduced: a busUnavailable-path standalone instance was killed by timeout at the 10s mark) — needs a design decision (e.g. only bound the D-Bus-only subcommands, or detect "started, not forwarded" and skip the kill) before this leaves review. T-LX4 CONFIRMED as a pure consequence of T-LX1, not an independent UI defect — directly reproduced: no window pre-fix, correct picker show/hide post-fix. ADDITIONAL FINDING (not in the original 4, escalating to architect as a design gap, not just a code nit): the exact same unowned-Thread{[weak self]} pattern that caused T-LX1 is still live and unfixed in StatusNotifierTray (LinuxAppLifecycle.swift:224-236 startTray + StatusNotifierTray.swift:50-63 start()) — `let tray = ...` has no retainer either, so the tray icon's receive loop will very likely never run (could not fully exercise live — no org.kde.StatusNotifierWatcher exists under bare Xvfb+openbox, matching the Dockerfile's own stated GNOME-Shell-extension test gap, but the code shape is byte-for-byte identical to the just-fixed bug). ShellHelperClient.swift:95 has the same shape too; it currently survives only via an accidental mutual-retain cycle with LinuxAppEnvironment that has no independent anchor of its own — LinuxAppEnvironment's whole-process lifetime is not deliberately owned anywhere, it merely rides along on whichever of {controlService(fixed)/tray(unfixed)/shellHelperClient(unfixed)} happens to still be alive. Recommend a documented "who retains a background-thread service for process lifetime" rule (coding-standards.md) or a small registry, not three more one-off static vars. Minor/nit (non-blocking): DBusConnection.swift:92-106 applyTimeout derives SO_RCVTIMEO from the *connect* timeout, not each call's own timeout — harmless today (every call site's connect timeout >= its later call timeouts) but a latent trap; DBusConnection.swift:228-237 call() drops (doesn't re-buffer) a non-matching-serial message read while waiting for a reply; LinuxAppLifecycle.swift installHotkeys's `environment` parameter is unused. Security: no new networking/secrets/PII surface in anything read; app.clipnest.Control remains session-bus-scoped same as senior-dev's own prior note. Cleaned up all docker containers/images I created (clipnest-review, clipnest-review-fix and their images); did not touch senior-dev's own clipnest-dev container or clipnest-vnc image. No production file, task-board.md, or project-context.md edited — findings returned to orchestrator for hand-off once senior-dev's diff is finalized (StatusNotifierTray/ShellHelperClient + the clipnest-ctl kill-after regression should be addressed or explicitly deferred before this reaches tester).
